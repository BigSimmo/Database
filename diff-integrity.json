{
  "maxRemovedFraction": 0.25,
  "minRemovedCases": 3,
  "perFileMaxRemovedFraction": 0.5,
  "perFileMinRemovedCases": 3,
  "approvedReductions": [
    {
      "path": "tests/ward-pull-vocabulary.dom.test.tsx",
      "before": 16,
      "after": 15,
      "reason": "Deliberate retirement of the one case rendering <WardModeWorkspace mode=\"exceptions\" />, a mode MERGE 01 turned into a redirect. Its two halves went different ways and both were checked before the case was dropped. The vocabulary rule itself (\"Bed pull expired\", never \"Bed hold expired\") was carried onto the live DelaysScreen in 43c56d6c5, with a control proving the mutation fails that one test by name; re-asserting it here would be a second copy of a live pin. The other half asserted the action label \"Reconfirm or release bed pull\", and DelaysScreen offers no per-item action at all by design - its own footer reads \"Nothing on this screen decides anything\" - so there is no control whose wording could be wrong, and pinning the absence of one would fight the next redesign. Whether that lost capability should return is an owner question, flagged to Ward Lead rather than settled by choosing an assertion. NOTE: 1 case of 16 is below this file's own perFileMinRemovedCases floor of 3, so check:diff-integrity would never have demanded this entry - it is recorded because a retirement that leaves no trace is indistinguishable from a deletion nobody noticed, which is the failure this array exists to prevent.",
      "approvedOn": "2026-09-05"
    },
    {
      "path": "tests/ward-capacity-view.dom.test.tsx",
      "before": 13,
      "after": 6,
      "reason": "13 -> 6. Six cases resolved on the first pass and one more after the owner approved building four of the five open questions on 2026-09-05, at which point the honest resolution stopped being \"retire or park\" and became \"build it, then re-point\". RETIRED, subject moved to ward-screen.tsx at /ward/[unitId] and proved guarded there by mutation: the Confirmed/Expected breakdown replacing the old Potential lump (Confirmed chip reading the Expected field turns 3 ward-screen cases red); the reconciliation identity and its floor (Held chip pointed at capacity.blocked - the wrong-label-at-a-sibling-cell defect the case named as its own reason for existing - turns 2 red); a leave bed never folded into Ready and its fixture assumption (Ready as available+leaveUsable turns 2 red, and rph-adult-secure carries the fixture's one usable leave bed and is among the units rendered, so the mutation was live not inert). RETIRED because the property is now guarded on the live capacity screen instead: the two zero-as-words cases, since capacity-screen.tsx now renders a zero ready count as the word and ward-capacity-screen.dom.test.tsx asserts that claim for every row including both directions on the absence - re-pointing them here would put a second guard over one fact, and two guards over one fact drift apart. RE-POINTED rather than retired: \"a expected release must never soften Available now\" moved to ward-bed-release.dom.test.tsx, which was a live hole - rendering Ready as available minus expectedToday left 714 tests green across all 41 files that could catch it while two rendered wards genuinely changed from Ready 2 to Ready 1. The six that remain in the file are four live cases against CapacityScreen (Mental Health Act authorisation on the network view, the coordinator's refresh control, the excluded-beyond-horizon count, and the split fixture assumption) and TWO STILL POINTED AT THE DEAD MODE ON PURPOSE, so the reachability guard keeps reporting this file: a ward's sex mix and specialling headroom on a network view, which nothing reachable shows and which overlaps Ward Lead's unbuilt sex-mix ruling and the unsettled question of whether Unit.sexMix or derivedSexMix is the honest source; and the six-figure headline that never shows a sum, on which the owner made no ruling and none was read into his silence. Source hashes d86f1549 (ward-screen.tsx), 9e27492d (capacity-derivations.ts) and 3e379533 (capacity-screen.tsx) before and after every mutation cited. NOTE ON THE PAIR ITSELF: 13 -> 6 is the CUMULATIVE reduction across two commits, which is what a reviewer needs to see; the gate computing any single diff sees 13 -> 7 then 7 -> 6 and matches neither, and both are under the 50% per-file ceiling so it consults no approval at all. This entry is a record, not a gate condition, and is written that way deliberately.",
      "approvedOn": "2026-09-05"
    }
  ]
}
