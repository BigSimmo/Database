{
  "impl": {
    "package": "p4",
    "commits": [
      "b19a05ee0 speech: answer 404 and deny the microphone while Clinical Ask is off (M16)",
      "8c58bedce clinical-ask: answer 404 before auth and the limiter when the master flag is off (L33)",
      "f5af132e8 clinical-ask: return no catalogue evidence when nothing matches (L16)",
      "551bd66e6 clinical-ask: surface 401 and 429 as unauthorized and rate_limited in the client stream (L17)",
      "770d1ee3c docs: state that Clinical Ask has no user entry point and is flag-gated (M34)"
    ],
    "findings_fixed": [
      "M16",
      "L33",
      "L16",
      "L17",
      "M34"
    ],
    "findings_skipped": [
      {
        "id": "L124",
        "reason": "The always-empty 'Review extract' disclosure lives in /home/user/wt/p4/src/components/clinical-dashboard/clinical-ask-answer-surface.tsx (lines ~306-309). That file is not in the package's owned list: the owned glob src/components/clinical-ask/** matches no directory in the repo (the Clinical Ask components live under src/components/clinical-dashboard/). Not edited per the hard rule. The fix is a two-line change: delete the <details><summary>Review extract</summary><p>{item.extract}</p></details> block, or wrap it in `item.extract ? ... : null`; no test pins it."
      }
    ],
    "tests_added": [
      "tests/speech-transcription-route.test.ts: 'answers 404 before access, the rate limiter, or the provider when Clinical Ask is off' (plus a clinicalAskEnabled flag stub, default true, for the existing cases)",
      "tests/security-headers.test.ts: 'keeps microphone capture disabled while Clinical Ask dictation has no user entry point' (replaces the microphone=(self) pin; asserts microphone=() and not microphone=(self))",
      "tests/clinical-ask-route.test.ts: 'answers 404 before authentication or the durable rate limiter when the master flag is off' and 'keeps the SSE mode_unavailable frame for a mode on the emergency denylist'",
      "tests/clinical-ask-catalogue-evidence.test.ts: 'returns no $mode evidence when nothing matches instead of the whole catalogue' (it.each over all seven modes; four failed before the fix, exactly the modes that substituted the catalogue)",
      "tests/clinical-ask-orchestrator.test.ts: 'reports an evidence gap without synthesis when no tier returns evidence' (the ladder says so: evidence_gap, synthesize never called)",
      "tests/clinical-ask-stream-contract.test.ts: 'surfaces a 429 as rate_limited with the server retry window instead of internal_error', 'surfaces a 401 as unauthorized and not retryable', 'keeps other non-OK statuses as a generic retryable failure without echoing the body'"
    ],
    "files_touched": [
      "/home/user/wt/p4/src/app/api/speech/transcribe/route.ts",
      "/home/user/wt/p4/src/app/api/clinical-ask/stream/route.ts",
      "/home/user/wt/p4/src/lib/clinical-ask/authority-registry.ts",
      "/home/user/wt/p4/src/lib/clinical-ask/catalogue-evidence.ts",
      "/home/user/wt/p4/src/lib/clinical-ask/client-stream.ts",
      "/home/user/wt/p4/src/lib/security-headers.ts",
      "/home/user/wt/p4/tests/speech-transcription-route.test.ts",
      "/home/user/wt/p4/tests/security-headers.test.ts",
      "/home/user/wt/p4/tests/clinical-ask-route.test.ts",
      "/home/user/wt/p4/tests/clinical-ask-catalogue-evidence.test.ts",
      "/home/user/wt/p4/tests/clinical-ask-orchestrator.test.ts",
      "/home/user/wt/p4/tests/clinical-ask-stream-contract.test.ts",
      "/home/user/wt/p4/docs/privacy-impact-assessment.md"
    ],
    "residual_risk": "Both routes now consult a new exported helper clinicalAskEnabled() (authority-registry.ts, reads env.CLINICAL_ASK_ENABLED) as their first statement and answer a no-store JSON 404 (code not_found) before body parsing, the Supabase auth lookup, the durable rate-limiter write, or any provider client, so the documented 'master flag false = full rollback' is now true for both provider-backed Clinical Ask routes; the per-mode denylist still answers over the 200 SSE mode_unavailable frame by design. The route tests stub the flag by mocking the registry module (the established pattern in clinical-ask-route.test.ts) rather than the env module, so an env-parsing regression would be caught only by env tests, not these. Not done, per the package notes rather than the finding's wider sketch: the transcribe route still admits anonymous callers when the flag is on (repo-wide publicAccessContext design), no isDemoMode() guard was added to the stream route, and the orphaned use-clinical-ask-speech.ts hook (no importer, not owned) will now receive a 404 while the flag is off. The L16 change also replaces the specifiers mode's whole-sentence substring 'match' (which never matched and always fell through to the whole catalogue) with the ranked searchSpecifiers() signal plus verbatim short-label hits; this is a Clinical Ask catalogue-tier change only and touches no RAG ranking, retrieval-selection, comparator, or eval surface (RAG impact: none). The microphone Permissions-Policy is now microphone=() for the whole app; the historical plan/spec docs under docs/superpowers/ still describe microphone=(self) and docs/openai-rag-operations.md / docs/clinical-governance.md were not touched (not owned) though their claims are now accurate. The gate-parsed pre-commit hook regenerated docs/site-map.md and the design-system adoption manifest on each commit and reported no diff, so no generated file changed. Per-finding proof was run with vitest directly because npm run test:focused fails closed whenever a test file is edited; the package's declared gate (npm run verify:pr-local heavy) has not been run and must be run by the handoff step.",
    "needs_owner_decision": [
      "L124: approve editing src/components/clinical-dashboard/clinical-ask-answer-surface.tsx (outside p4's owned globs, which name a non-existent src/components/clinical-ask/ directory) to remove or conditionally render the always-empty 'Review extract' disclosure; or assign it to the package that owns src/components/clinical-dashboard/**.",
      "M34 follow-on (product decision, not taken here): whether Clinical Ask stays dormant (option a, now documented in the PIA), gets a governed 'Ask' composer action reinstated behind the flag (option b), or has its client layer and the orphaned use-clinical-ask-speech.ts hook put through the dead-code protocol (option c). Re-granting microphone=(self) should ride option b only.",
      "Whether the anonymous-caller design of /api/speech/transcribe should change to requireAuthenticatedUser when the flag is on (the finding's sketch; deliberately not done because the package notes limited M16 to the flag gate and the skeptic noted anonymity is repo-wide design)."
    ]
  },
  "review": {
    "pass": false,
    "blocking": [
      {
        "finding": "L33 (and M16, same defect)",
        "problem": "Both new master-flag 404s call jsonError() with its default log:true. src/app/api/clinical-ask/stream/route.ts:172 and src/app/api/speech/transcribe/route.ts:24 -> src/lib/http.ts:60-67 -> logSafeError -> logger.error(\"API request failed\", {status:404, code:\"not_found\"}). That message is on the Sentry Logs allowlist (src/lib/observability/sentry-logging.ts:26,48) and the server forwarder is registered whenever Sentry logging is enabled (src/sentry.server.config.ts:116), so every anonymous probe of a route that the commit comment says must 'cost nothing and confirm nothing' now writes an error-level Railway log line and an error-level Sentry log entry. This is a regression for the stream route: before the change the dormant path answered via errorStream (route.ts:93-102), which logs nothing. The repo's own precedent for a designed 404/401 is log:false (src/app/api/caring-contacts/session/route.ts:35, src/lib/supabase/auth.ts:144). Neither route test asserts the absence of the error log, so nothing pins this.",
        "required_change": "In src/app/api/clinical-ask/stream/route.ts:172 and src/app/api/speech/transcribe/route.ts:24 pass the third argument: jsonError(new PublicApiError(\"Not found.\", 404, { code: \"not_found\" }), 404, { log: false }). In tests/speech-transcription-route.test.ts the logger is already mocked (mocks.error): add expect(mocks.error).not.toHaveBeenCalled() to 'answers 404 before access, the rate limiter, or the provider when Clinical Ask is off'; add the equivalent logger assertion to the new 404 case in tests/clinical-ask-route.test.ts (mock @/lib/logger the same way if it is not already)."
      }
    ],
    "notes": [
      "Gate evidence: `npm run test:focused -- --files <6 tests>` fails closed exactly as the implementer said — decisive line: 'Focused test selection is unsafe: test or configuration paths changed (tests/speech-transcription-route.test.ts)'. Direct `npx vitest run` over the six touched tests plus tests/privacy-readiness-contract.test.ts, tests/maturity-governance-docs.test.ts and tests/clinical-ask-speech.dom.test.tsx: 'Test Files  9 passed (9) / Tests  112 passed (112)'. `npx eslint` on all 12 touched source/test files: exit 0, no output. `npx prettier --check` on all 13 touched files: 'All matched files use Prettier code style!'. `npm run typecheck`: '[gate-receipts] recorded a pass for \"typecheck:internal\" (6095 input files)', exit 0. `node scripts/check-diff-integrity.mjs --base origin/main`: '[diff-integrity] PASS — 6 changed test file(s), 46 -> 54 test case(s)'. `npm run check:repo-awareness-snapshot`: 'in step'. `npm run verify:pr-local -- --dry-run --files <comma list>` selects the heavy plan (lint, typecheck, test, build, rag fixtures, medication checks) and prints 'offline RAG production contracts skipped: no RAG-scoped changes (fixtures still selected)', which supports the package rag_line. HEAD contains origin/main (705c2f64a). The declared gate (verify:pr-local heavy) was not run by me either; it belongs to handoff.",
      "M16 test strength: 'answers 404 before access, the rate limiter, or the provider when Clinical Ask is off' asserts 404, Cache-Control no-store, and that publicAccessContext, consumeSubjectApiRateLimit and transcribeClinicalAskAudio are never called. Pre-fix the route never imported clinicalAskEnabled, so the mocked false flag would be ignored and the request would reach access/rate/transcribe and return 200 — the test fails without the fix. The microphone pin was updated in the same commit (b19a05ee0) as the package note required; the only other microphone=(self) pin in the repo was that test (grep confirms no Playwright spec or script asserts Permissions-Policy). security-headers.ts diff is the directive plus a three-line adjacent comment; security-headers.test.ts diff is that one `it` block only — inside the owned scope.",
      "L33 test strength: asserts 404, not text/event-stream, no mode_unavailable in the body, and that access, rate, modeEnabled, resolveScope and run are all uncalled; pre-fix (no clinicalAskEnabled import) the request would return the 200 SSE frame, so it fails without the fix. Consequence to be aware of: the flag check now precedes parseJsonBody, so an invalid body receives 404 rather than 400 while the flag is off — consistent with 'route does not exist'. The finding's isDemoMode() guard was not added (package note limited the change to the master flag). The new 'keeps the SSE mode_unavailable frame for a mode on the emergency denylist' test largely duplicates the existing 'returns the explicit dormant-mode error without starting retrieval' (adds the 200 status and content-type assertions); harmless.",
      "L16 test strength: verified by an in-memory probe (no files edited) that the question 'zqxvblorp qwxzk' returns 0 from every catalogue search, while the empty-query fallbacks that the pre-fix code used return 201 differentials + 31 presentations, 12 formulation mechanisms, 205 therapies and 585 specifier items; services/forms/dsm return 0 for both. So the it.each fails pre-fix for exactly differentials, formulation, specifiers and therapy-compass, matching the implementer's claim. The orchestrator test pins pre-existing ladder behaviour (orchestrator.ts:169 evidence_gap short-circuit) rather than failing pre-fix; it is a useful contract pin, not proof of the fix.",
      "L16 behaviour beyond the finding: specifierEvidence now also admits items whose label is a searchSpecifiers() hit (rankedLabels.has). Probe: for the fixture question the pre-fix substring filter matched 0 items (so it fell to the whole 585-item catalogue) and the new filter matches 33 items derived from 7 ranked records; common-word queries ('the', 'what is the') return 1 hit, so the signal is not broad. The new set is a subset of the pre-fix top-12 in the common case and only wider when substring hits and ranked hits coexist. Only 55 of 585 catalogue labels equal a specifier record name, so the ranked signal can reach only those — a pre-existing mapping limitation (the old rank lookup used the same key), unchanged here. An empty question still returns the whole catalogue for differentials/formulation/therapy/specifiers, but the request schema is question: z.string().trim().min(1) (src/lib/validation/clinical-ask-request.ts:34), so that path is unreachable from the route and the existing 'preserves Therapy governance' test relies on it. No src/lib/rag/**, ranking, comparator, selection or eval file touched.",
      "L17 test strength: the 429 fixture mirrors the real rateLimitJsonResponse envelope (details.retryAfterSeconds and a Retry-After header, src/lib/api-rate-limit.ts:509-540) and the 401 fixture mirrors jsonError's envelope; pre-fix `!response.ok -> internal_error` makes both code assertions fail. The third test pins that other statuses stay internal_error/retryable and never echo the body. Behaviour change beyond the finding: 'unauthorized' is non-retryable (new code, defensible) and the 429 message concatenates the server's own constant message with 'Try again in N seconds.' — only the route's jsonError/rateLimitJsonResponse text is surfaced, never provider text. The stream route uses publicAccessContext, so a 401 is only reachable via the AuthenticationError catch path; the mapping is still correct.",
      "M34: only the one PIA paragraph changed (docs/privacy-impact-assessment.md:506-514); there is no other docs/privacy-impact-assessment* file, and neither PIA-parsing test (privacy-readiness-contract, maturity-governance-docs) pins that paragraph — both pass. The new wording's claims (404 before auth/rate-limit/provider, microphone denied) are true after this branch; 'since PR #2360' comes from the finding. docs/openai-rag-operations.md:98-103, docs/clinical-governance.md:78 and docs/mode-aware-clinical-ask-local-handover.md were not touched (not owned); their rollback claims are now accurate rather than stale. The finding's option (a) also suggested a readiness-checklist line; not owned, not done.",
      "L124 skip reason verified true: src/components/clinical-ask/ does not exist, the Clinical Ask components live in src/components/clinical-dashboard/, and clinical-ask-answer-surface.tsx:306-309 still renders the always-empty <details><summary>Review extract</summary>. Owner decision needed on scope, as the implementer says.",
      "Scope and never-touch: all 13 touched files fall inside p4's owned globs (with the PIA and security-headers limited as the package requires). No RAG surface, ledger, inbox, review record, flake ledger, diff-integrity config, bundle budget or gate-parsed AGENTS.md section was touched; no test skipped, deleted or weakened (only the microphone pin was rewritten to the stronger microphone=() assertion); no export or file deleted (one export, clinicalAskEnabled, added and used by both routes); no route or document added or removed; site-map and repo-awareness snapshot unchanged and in step. Stage-5 items honoured: microphone pin in the same commit, per-mode denylist keeps the SSE frame, no gate added so no manifest counts moved, no migration.",
      "Residual behaviour worth stating in the PR body: the orphaned src/components/clinical-dashboard/use-clinical-ask-speech.ts hook treats the new 404 as a generic 'Transcription failed. You can retry.' (line 69 `if (!response.ok) throw`), so nothing breaks if it is ever wired up while the flag is off; /api/speech/transcribe still admits anonymous callers when the flag is on (repo-wide publicAccessContext design, deliberately unchanged)."
    ]
  },
  "fix": {
    "package": "p4",
    "commits": [
      "b19a05ee0 speech: answer 404 and deny the microphone while Clinical Ask is off (M16)",
      "8c58bedce clinical-ask: answer 404 before auth and the limiter when the master flag is off (L33)",
      "f5af132e8 clinical-ask: return no catalogue evidence when nothing matches (L16)",
      "551bd66e6 clinical-ask: surface 401 and 429 as unauthorized and rate_limited in the client stream (L17)",
      "770d1ee3c docs: state that Clinical Ask has no user entry point and is flag-gated (M34)",
      "e73f1addb clinical-ask, speech: keep the dormant 404 out of the error log (L33, M16) [fix round]"
    ],
    "findings_fixed": [
      "M16",
      "L33",
      "L16",
      "L17",
      "M34"
    ],
    "findings_skipped": [
      {
        "id": "L124",
        "reason": "The always-empty 'Review extract' disclosure lives in /home/user/wt/p4/src/components/clinical-dashboard/clinical-ask-answer-surface.tsx (lines ~306-309). That file is not in the package's owned list: the owned glob src/components/clinical-ask/** matches no directory in the repo (the Clinical Ask components live under src/components/clinical-dashboard/). Not edited per the hard rule. The fix is a two-line change: delete the <details><summary>Review extract</summary><p>{item.extract}</p></details> block, or wrap it in `item.extract ? ... : null`; no test pins it."
      }
    ],
    "tests_added": [
      "[fix round] tests/speech-transcription-route.test.ts: 'answers 404 before access, the rate limiter, or the provider when Clinical Ask is off' now also asserts expect(mocks.error).not.toHaveBeenCalled() and expect(mocks.warn).not.toHaveBeenCalled() (logger already mocked). Proven red on the pre-fix route (AssertionError: expected vi.fn() to not be called at all, but actually been called 1 times, at line 58) and green with { log: false }.",
      "[fix round] tests/clinical-ask-route.test.ts: new vi.mock('@/lib/logger') with debug/info/warn/error (warn and error as hoisted mocks.logWarn / mocks.logError); 'answers 404 before authentication or the durable rate limiter when the master flag is off' now also asserts mocks.logError and mocks.logWarn were not called. Proven red on the pre-fix route (same AssertionError at line 195) and green with the fix.",
      "tests/speech-transcription-route.test.ts: 'answers 404 before access, the rate limiter, or the provider when Clinical Ask is off' (plus a clinicalAskEnabled flag stub, default true, for the existing cases)",
      "tests/security-headers.test.ts: 'keeps microphone capture disabled while Clinical Ask dictation has no user entry point' (replaces the microphone=(self) pin; asserts microphone=() and not microphone=(self))",
      "tests/clinical-ask-route.test.ts: 'answers 404 before authentication or the durable rate limiter when the master flag is off' and 'keeps the SSE mode_unavailable frame for a mode on the emergency denylist'",
      "tests/clinical-ask-catalogue-evidence.test.ts: 'returns no $mode evidence when nothing matches instead of the whole catalogue' (it.each over all seven modes; four failed before the fix, exactly the modes that substituted the catalogue)",
      "tests/clinical-ask-orchestrator.test.ts: 'reports an evidence gap without synthesis when no tier returns evidence' (the ladder says so: evidence_gap, synthesize never called)",
      "tests/clinical-ask-stream-contract.test.ts: 'surfaces a 429 as rate_limited with the server retry window instead of internal_error', 'surfaces a 401 as unauthorized and not retryable', 'keeps other non-OK statuses as a generic retryable failure without echoing the body'"
    ],
    "files_touched": [
      "/home/user/wt/p4/src/app/api/speech/transcribe/route.ts",
      "/home/user/wt/p4/src/app/api/clinical-ask/stream/route.ts",
      "/home/user/wt/p4/src/lib/clinical-ask/authority-registry.ts",
      "/home/user/wt/p4/src/lib/clinical-ask/catalogue-evidence.ts",
      "/home/user/wt/p4/src/lib/clinical-ask/client-stream.ts",
      "/home/user/wt/p4/src/lib/security-headers.ts",
      "/home/user/wt/p4/tests/speech-transcription-route.test.ts",
      "/home/user/wt/p4/tests/security-headers.test.ts",
      "/home/user/wt/p4/tests/clinical-ask-route.test.ts",
      "/home/user/wt/p4/tests/clinical-ask-catalogue-evidence.test.ts",
      "/home/user/wt/p4/tests/clinical-ask-orchestrator.test.ts",
      "/home/user/wt/p4/tests/clinical-ask-stream-contract.test.ts",
      "/home/user/wt/p4/docs/privacy-impact-assessment.md"
    ],
    "residual_risk": "Fix round: the blocking item was addressed exactly as required. Both master-flag 404s now call jsonError(new PublicApiError('Not found.', 404, { code: 'not_found' }), 404, { log: false }) (stream route wrapped in noStore() as before on the transcribe route), matching the repo's designed-404 precedent in src/app/api/caring-contacts/session/route.ts:35, so an anonymous probe of a switched-off route no longer writes the error-level 'API request failed' line that the Sentry Logs allowlist forwards; the stream route's pre-change errorStream path logged nothing, so this restores parity. Only the four owned files changed (two routes, two route tests; 19 insertions, 2 deletions), all inside p4's owned globs. Evidence: pre-fix sources swapped in via `git show HEAD:<path>` (no checkout/stash) made exactly the two new logger assertions fail ('Tests 2 failed | 24 passed (26)'); with the fix `npx vitest run` on both files reports 'Test Files 2 passed (2) / Tests 26 passed (26)'. `npm run test:focused -- --files <both tests>` fails closed as before ('Focused test selection is unsafe: test or configuration paths changed'), so vitest was run directly. `npx prettier --check` on the four files: 'All matched files use Prettier code style!' (after one --write on the transcribe route); `npx eslint` on the four files: exit 0; `npm run typecheck` was refused by the heavy-run admission lock (DATABASE_HEAVY_RUN_ADMISSION_BUSY, held by worktree /home/user/wt/p2), so its underlying command `node ./node_modules/typescript/bin/tsc -p tsconfig.typecheck.json --noEmit` was run directly: exit 0. The two fixes landed as ONE commit (e73f1addb) rather than one per finding: the pre-commit documentation hook refused the first per-route commit because the other route's edits were unstaged ('Documentation inputs have unstaged or untracked changes'), which left them staged into the second commit; the message was amended (own unpushed commit, no reset/rebase) to name both findings and explain this. The pre-commit hook regenerated docs/site-map.md and the design-system adoption manifest and reported no diff; `git status --porcelain` is empty and HEAD contains origin/main (705c2f64a). The clinical-ask route test now mocks @/lib/logger wholesale (all four methods), which silences any transitive logging in that test file; no existing assertion in it depended on real logger output. Earlier-round residual risk still stands: the flag stub is via the registry module mock, not the env module; the transcribe route still admits anonymous callers when the flag is on (repo-wide publicAccessContext design); no isDemoMode() guard on the stream route; the orphaned use-clinical-ask-speech.ts hook will receive a 404 while the flag is off; the L16 specifiers change is a Clinical Ask catalogue-tier change only (RAG impact: none); microphone Permissions-Policy is now microphone=() app-wide with historical docs under docs/superpowers/ still describing microphone=(self). The package's declared gate (npm run verify:pr-local heavy) has not been run and belongs to handoff.",
    "needs_owner_decision": [
      "L124: approve editing src/components/clinical-dashboard/clinical-ask-answer-surface.tsx (outside p4's owned globs, which name a non-existent src/components/clinical-ask/ directory) to remove or conditionally render the always-empty 'Review extract' disclosure; or assign it to the package that owns src/components/clinical-dashboard/**.",
      "M34 follow-on (product decision, not taken here): whether Clinical Ask stays dormant (option a, now documented in the PIA), gets a governed 'Ask' composer action reinstated behind the flag (option b), or has its client layer and the orphaned use-clinical-ask-speech.ts hook put through the dead-code protocol (option c). Re-granting microphone=(self) should ride option b only.",
      "Whether the anonymous-caller design of /api/speech/transcribe should change to requireAuthenticatedUser when the flag is on (the finding's sketch; deliberately not done because the package notes limited M16 to the flag gate and the skeptic noted anonymity is repo-wide design).",
      "Whether the fix-round change should stay as one combined commit (e73f1addb, L33+M16) or be split into two before handoff; splitting needs an interactive/soft history rewrite, which this implementer's rules did not permit."
    ]
  },
  "review2": {
    "pass": true,
    "blocking": [],
    "notes": [
      "Scope and never-touch: all 13 touched files (git diff origin/main...HEAD --stat) fall inside p4's owned globs; nothing under src/lib/rag/**, no ranking/ordering/selection surface, no ledger/inbox/record, no flake ledger, diff-integrity, bundle budget, migration, or gate-parsed AGENTS.md section. No export, file, route or document was added or removed. No test was skipped, deleted or weakened: the microphone pin in tests/security-headers.test.ts was replaced by a stronger pair of assertions (contains microphone=() AND does not contain microphone=(self)).",
      "Gate evidence (run by this reviewer in /home/user/wt/p4): `npm run test:focused -- --files <six touched tests>` fails closed exactly as the implementer reported — decisive line: 'Focused test selection is unsafe: test or configuration paths changed (tests/speech-transcription-route.test.ts)'. Direct `npx vitest run` on the six touched tests: 'Test Files  6 passed (6) / Tests  93 passed (93)'. Wider run of 25 related files (all other tests/clinical-ask-*.test.ts, the three .dom.test.tsx, maturity-governance-docs, privacy-readiness-contract, proxy, proxy-auth, and the four other importers of authority-registry/security-headers): 'Test Files  25 passed (25) / Tests  247 passed (247)'. `npx eslint` on the 12 touched source and test files: exit 0. `npx prettier --check` on all 13 touched files: 'All matched files use Prettier code style!'. The package gate `npm run verify:pr-local` (heavy) has not been run and remains for handoff.",
      "M16 (a): tests/speech-transcription-route.test.ts 'answers 404 before access, the rate limiter, or the provider when Clinical Ask is off' fails on the pre-fix route by inspection (pre-fix route never imports clinicalAskEnabled, so publicAccessContext is called and `expect(mocks.access).not.toHaveBeenCalled()` fails); tests/security-headers.test.ts fails pre-fix because the old value 'microphone=(self)' does not contain the substring 'microphone=()'. The test asserts the actual failure (no auth, no limiter write, no OpenAI call, 404, no-store, no error/warn log). (b) Behaviour beyond the finding: none in the route; the Permissions-Policy change is app-wide by nature (microphone=() on every response) — that is what the package note asked for. The transcribe route still admits anonymous callers when the flag is on (package limited M16 to the flag gate; skeptic noted anonymity is repo-wide design). (d) Stage-5 red-team correction honoured: the pin update and the header change ride the same commit b19a05ee0 as the route change.",
      "L33 (a): tests/clinical-ask-route.test.ts 'answers 404 before authentication or the durable rate limiter when the master flag is off' fails pre-fix by inspection (pre-fix POST proceeds to parseJsonBody -> publicAccessContext, so `mocks.access` is called). It asserts the real defect: no access lookup, no limiter consumption, no modeEnabled/resolveScope/run, JSON 404 rather than a 200 text/event-stream with mode_unavailable, and no error/warn log. The companion test keeps the SSE mode_unavailable frame for the per-mode denylist, so the 'double-flag' distinction (master flag -> 404, per-mode denylist -> SSE frame) is pinned. (b) The finding's fix sketch also mentioned an isDemoMode() guard; the package note did not require it and it was not added — owner may want it later. `jsonError(..., 404, { log: false })` matches the signature in src/lib/http.ts:60 and the caring-contacts precedent at src/app/api/caring-contacts/session/route.ts:35. The route test now mocks @/lib/logger wholesale (debug/info/warn/error); tests pass, so no member beyond those four is used on the tested paths.",
      "L16 (a): Red proof reproduced independently without touching the repo: the pre-fix src/lib/clinical-ask/catalogue-evidence.ts (from `git show origin/main:`) was run side by side with the post-fix module on the test's nonsense question 'zqxvblorp qwxzk' across all seven modes — pre-fix counts: services 0, forms 0, differentials 12, formulation 12, dsm 0, specifiers 12, therapy-compass 12; post-fix: all 0. So exactly the four substituting modes turn the new it.each red pre-fix, matching the implementer's claim, and the test asserts the actual failure (whole-catalogue substitution). The orchestrator test 'reports an evidence gap without synthesis when no tier returns evidence' is a ladder guard (orchestrator unchanged), not a red test — acceptable. (b) Behaviour beyond the finding: the specifier tier now also admits items whose label is a hit in searchSpecifiers (`rankedLabels.has(item.label)`), in addition to the old verbatim-substring filter. This is narrower than the old effective behaviour (which for almost any real question fell through to the whole catalogue) but wider than the old substring filter alone; a probe shows it keeps the mode useful ('Which specifiers apply to major depressive disorder with anxious distress?' -> 12 items led by 'With anxious distress'; 'rapid cycling' -> 2) while nonsense yields 0. The ranked search is loose (a 'seasonal pattern specifier' question also admits 'With atypical features'), but that is the pre-existing searchSpecifiers scoring, not new logic. An empty question still returns the full catalogue in every mode (pre-existing; the existing 'preserves Therapy governance' test relies on it). RAG impact: none — Clinical Ask catalogue tier only; src/lib/differentials.ts and src/lib/rag/** untouched.",
      "L17 (a): the 429 and 401 tests in tests/clinical-ask-stream-contract.test.ts fail pre-fix by inspection (pre-fix `if (!response.ok || !response.body) return failedPayload(request, 'internal_error')` yields code internal_error, failing the toMatchObject on code). They assert the real defect: distinct rate_limited/unauthorized codes, retryable true/false respectively, the Retry-After window carried into the message, and a 502 still mapped to a generic internal_error without echoing the body. (b) Wording: the composed 429 message is '<server message> Try again in N seconds.', which with the route's own text reads 'Too many Clinical Ask requests. Retry shortly. Try again in 42 seconds.' — slightly redundant, cosmetic. If an edge/proxy ever returned a JSON 429 carrying a `message` string, that string would be shown verbatim (the code comment says 'never provider text'; true for the route's own envelopes). Codes map to existing ClinicalAskPublicErrorCode members (contracts.ts:74-75); the answer surface branches only on `retryable`, so no UI change is needed.",
      "M34: only the 'Mode-aware Clinical Ask privacy boundary' paragraph of docs/privacy-impact-assessment.md changed (lines 506-514); no other PIA section touched, the 'Revised: 2026-09-01' and other pins in tests/maturity-governance-docs.test.ts still pass. The new wording (no user entry point since PR #2360, flag-gated, both routes 404 before auth/limiter/provider work, browser policy denies the microphone) matches the code on this branch. Historical documents under docs/superpowers/ and docs/prompts/ still describe microphone=(self)/dictation flows; they are archived narrative, not pinned by any test, and are outside p4's owned files.",
      "L124 skip reason verified TRUE: /home/user/wt/p4/src/components/clinical-ask/ does not exist; the always-empty disclosure is at src/components/clinical-dashboard/clinical-ask-answer-surface.tsx:306-309 exactly as described, and no package in packages.json owns that file (p18 owns only ClinicalDashboard.tsx). The package's owned glob is a typo for the clinical-dashboard directory. Recommendation for the orchestrator: amend p4's owned list to 'src/components/clinical-dashboard/clinical-ask-*.tsx' (or that one file) and let the implementer land the two-line change (`item.extract ? <details>…</details> : null` or delete the block); no test pins the disclosure.",
      "Commit hygiene: five per-finding commits plus one combined fix-round commit e73f1addb (L33+M16, log:false) — the implementer's explanation (pre-commit documentation hook refused a partial stage) is plausible; splitting would need a history rewrite. HEAD contains origin/main (merge-base check: yes) and the working tree is clean. The orphaned src/components/clinical-dashboard/use-clinical-ask-speech.ts already throws on any non-OK response (line 69), so the new 404 surfaces as its existing generic failure — no new client path. tests/ui-clinical-ask.spec.ts intercepts **/api/clinical-ask/stream with route.abort, so the Playwright journeys do not depend on the real route's status.",
      "Owner decisions carried forward unchanged from the implementer: (1) L124 ownership/glob fix above; (2) M34 product decision — keep dormant (documented), reinstate a governed composer action, or dead-code protocol for the client layer and speech hook; microphone=(self) should return only with option (b); (3) whether /api/speech/transcribe should require an authenticated user when the flag is on; (4) whether to add an isDemoMode() guard to the stream route per the L33 sketch."
    ]
  }
}