{
  "impl": {
    "package": "p8b",
    "commits": [
      "d1debbdf4 verify:pr-local: mirror every static-heavy static-pr gate in the heavy plan (M24)",
      "e40aa9fd3 ci: run the medication interaction index drift gate in static-pr (M30)",
      "0624351af hazard-controls: require proofs to name their control, run the register gate on PRs (M33)",
      "3f6c66533 hazard-controls: escape regex metacharacters in control symbols (L22)",
      "93cdb48a4 owner-scope guard: recognise owner filters, not the owner_id substring (L47)",
      "a4b0d33af tests: restore the 320px low-confidence AccessibleTable mockup journey (M31)",
      "6557f389a playwright: collect the orphaned ui-tools-show-all spec and guard both directions (M32)"
    ],
    "findings_fixed": [
      "M24 — verify:pr-local heavy plan now selects every static-pr gate that runs under static_heavy_changed (staticHeavyGuards list in CI step order incl. check:migration-role, check:function-grants, check:owner-scope; docs/ledger steps ride docsScripts for docs||heavy; check:mockups added to docsScripts). assertPlan self-test updated in the same commit; a new ci-cache-safety test parses static-pr and fails on any static_heavy-gated step missing from the local plan (was red with 25 missing).",
      "M30 — 'Medication interaction index drift' step added to static-pr under static_heavy_changed beside the lexicon-report step; new tests/check-medication-interactions-index.test.ts compares the committed index to the committed snapshot drug-for-drug/row-for-row and proves the failure shape on a mutated record; ci.yml pin added. check:gate-manifest unchanged (counts still 37/40) because verify:cheap:internal (package.json, not owned) was not touched.",
      "M33 — validator now requires, for every controlled/partial hazard, that at least one listed test names a control symbol or imports a control path module (error: '<id>: no listed test references a control symbol or imports a control path (...)'); on a shallow clone it prints CLINICAL_HAZARD_CONTROLS_SHALLOW_CLONE and skips only the reviewedCommit ancestry checks (verified on a depth-1 clone: old script emitted 37 errors, new one passes with the message). Gate added to static-pr (docs||static_heavy; job already has fetch-depth 0) and to verify:pr-local docsScripts. Existing date/commit/role checks untouched.",
      "L22 — malformed escape class /[.*+?^${}()|[\\\\]\\\\]/g replaced with the repo-standard /[.*+?^${}()|[\\]\\\\]/g in a single symbolPattern() helper; unit case proves a runtime-assembled dotted symbol no longer wildcard-matches and a '(' symbol neither throws nor is reported missing.",
      "L47 (partial: the SCOPE_TOKENS tightening) — the guard now recognises filter constructs (.eq/.neq/.is/.in/.not/.match/.filter(\"owner_id\"|\"documents.owner_id\"), owner_id.eq./.is./.in. strings, owner_id: payload keys, documents!inner(owner_id), word-bounded helper names) instead of the bare owner_id substring; self-test fails a select-list-only handler with an owner_id comment and passes every filter shape the codebase uses; npm run check:owner-scope stays green (60 files, 26 tables). The src/lib widening was NOT done — see needs_owner_decision.",
      "M31 — 320px low-confidence AccessibleTable journey restored verbatim from 54585e98d into tests/ui-accessible-table-mockup.spec.ts (@mockup, chromium-mockups project, fixture route src/app/mockups/accessible-table-browser-fixture/page.tsx still exists on main); 'accessible-table-mockup' added to mockupSpecPattern and testMatch; pinned by name in tests/playwright-pr-shards.test.ts; merge-commit blind spot noted in docs/agents/test-deletion-guard.md. check:ci-scope mockup-spec parity, check:browser-test-plan, check:playwright-pr-shards, check:diff-integrity all pass.",
      "M32 — 'tools-show-all' added to testMatch, productionSpecPattern and the mirrored productionSpecFilePattern in scripts/playwright-pr-shards.mjs; spec assigned to PR shard 2 (3.0s estimate; balance spread 1.5s/3.0s); two-way parity test asserts every on-disk tests/*.spec.ts (except the visual-config-owned ui-visual-baseline) is collected by testMatch and at least one project matcher (red before, naming exactly ui-tools-show-all), plus a by-name pin."
    ],
    "findings_skipped": [
      {
        "id": "L47 (src/lib widening and child-table rule)",
        "reason": "Not done per package notes ('ONLY IF you can assemble a complete allowlist ... if any doubt, do not widen'). 26 src/lib files import createAdminClient (rag-candidate-sources.ts 15 .from() calls, registry-corpus.ts 12, rag/rag-cache.ts 11, developer-area/corpus-health.ts 11, document-detail.ts 9, rag/rag-hydration.ts 8, rag/rag.ts 6, ingestion-mutation-safety.ts 6, ingestion-enqueue.ts 4, universal-search.ts 3, stranded-queued-recovery.ts, registry-seed.ts, rag/rag-retrieval-variants.ts, rag/rag-document-summary-context.ts, medication-seed.ts, differential-seed.ts, answer-telemetry.ts 2 each, supabase/auth.ts, sources/document-source-loader.ts, favourites-reference.ts, developer-area/environment-facts.ts 1 each, and 5 with none). Under the old substring rule they scan as 0 violations, which is exactly the weakness; under the new filter-shape rule several deliberate public-corpus reads (owner_id IS NULL by design, e.g. the RAG candidate/hydration/cache paths and registry corpus) would need a reviewed allowlist naming each file+table, and many of those are RAG retrieval surfaces I must not touch without the RAG process. The child-table rule (document_chunks/images/pages/table_facts, ingestion_jobs/stages) likewise needs a design decision on what counts as parent-scoped. Reported under needs_owner_decision."
      },
      {
        "id": "M33 (strict per-test form)",
        "reason": "The notes ask that 'each named test' reference the control. The committed manifest has two proofs that reference neither a control symbol nor a control path: H3 -> tests/ward-output.test.ts (imports only ward-output/types; formatAnswerRenderCopyText absent) and H4 -> tests/rag-trust.test.ts (imports rag, rag-extractive-answer, rag-quote-verification; no control symbol). Enforcing per-test would go red on this tree and the fix lives in docs/clinical-hazard-controls.json or those two test files, none of which are owned by p8b. Implemented the per-hazard form (at least one listed test must reference the control) as the hard rule; the per-test tightening is listed under needs_owner_decision."
      },
      {
        "id": "M31 (mockups/README.md row)",
        "reason": "mockups/README.md is not in the owned list. Its accessible-table-browser-fixture row (corrected 2026-09-02) still says 'no committed test navigates to it'; after this package tests/ui-accessible-table-mockup.spec.ts does. One-line follow-up for whoever owns that file."
      }
    ],
    "tests_added": [
      "tests/ci-cache-safety.test.ts: 'mirrors every static-heavy static-pr step in the verify:pr-local heavy plan (M24)'",
      "tests/ci-cache-safety.test.ts: 'runs the medication interaction index drift check through static-heavy scope (M30)'",
      "tests/ci-cache-safety.test.ts: 'runs the clinical hazard-controls register check in static-pr with full history (M33)'",
      "tests/check-medication-interactions-index.test.ts (new file, 3 cases: generatedFrom, index-vs-snapshot parity, mutated-record failure shape)",
      "tests/clinical-hazard-controls.test.ts: 'requires at least one listed test to reference a control symbol or control path' (M33)",
      "tests/clinical-hazard-controls.test.ts: 'escapes regex metacharacters in control symbols instead of treating them as wildcards' (L22)",
      "scripts/check-owner-scope-api.mjs --self-test: select-list-only handler flagged; 7 filter-shape handlers pass (L47)",
      "scripts/verify-pr-local.mjs --self-test: heavy-scope-guards by-name assertions (M24)",
      "tests/playwright-pr-shards.test.ts: 'collects the 320px AccessibleTable mockup journey in the advisory mockup project (M31)'",
      "tests/playwright-pr-shards.test.ts: 'collects every on-disk spec in the top-level testMatch and at least one project (M32)'",
      "tests/playwright-pr-shards.test.ts: 'collects the phone launcher Show-all journey as a sharded production spec (M32)'",
      "tests/ui-accessible-table-mockup.spec.ts (new Playwright file, restored @mockup journey; could not be executed here — the Playwright CLI refuses without a runner-owned server — collection is proven by the unit pins)"
    ],
    "files_touched": [
      "/home/user/wt/p8b/.github/workflows/ci.yml",
      "/home/user/wt/p8b/scripts/verify-pr-local.mjs",
      "/home/user/wt/p8b/scripts/check-clinical-hazard-controls.mjs",
      "/home/user/wt/p8b/scripts/check-owner-scope-api.mjs",
      "/home/user/wt/p8b/scripts/playwright-pr-shards.mjs",
      "/home/user/wt/p8b/playwright.config.ts",
      "/home/user/wt/p8b/tests/ci-cache-safety.test.ts",
      "/home/user/wt/p8b/tests/check-medication-interactions-index.test.ts",
      "/home/user/wt/p8b/tests/clinical-hazard-controls.test.ts",
      "/home/user/wt/p8b/tests/playwright-pr-shards.test.ts",
      "/home/user/wt/p8b/tests/ui-accessible-table-mockup.spec.ts",
      "/home/user/wt/p8b/docs/agents/test-deletion-guard.md",
      "/home/user/wt/p8b/docs/design-system/adoption-manifest.json (regenerated by the pre-commit hook in the M31 commit: adds tests/playwright-pr-shards.test.ts to AccessibleTable's test list)"
    ],
    "residual_risk": "Verified offline: all touched vitest suites (ci-cache-safety, verify-pr-local, installed-lock-parity, branch-review-index, check-medication-interactions-index, medication-interaction-lexicon-coverage, clinical-hazard-controls, owner-scope-guard, playwright-pr-shards, playwright-project-isolation, browser-test-plan), npm run test:ci-workflows (19 files, 471 tests), eslint on every touched file, npx tsc --noEmit -p tsconfig.typecheck.json, check:verification-plan, check:gate-manifest (still 40/37), check:github-actions, check:ci-scope, check:playwright-pr-shards, check:browser-test-plan, check:clinical-hazard-controls, check:owner-scope, check:medication-interactions, check:diff-integrity (68 -> 80 cases), docs:check-links. Not verified: the full unit suite, npm run verify:pr-local (forbidden here), and any browser run — the two Playwright specs (restored M31 journey and the now-collected ui-tools-show-all) have never executed in this environment, so their first real run is CI's chromium-mockups advisory lane and Production UI shard 2 respectively; the show-all spec has had no browser evidence for 17 days and may itself be red against today's launcher. Behavioural consequences the owner should expect: (1) verify:pr-local's heavy plan grew from ~13 to ~38 steps (knip, maintainability budgets, design-system, ledger and docs checks now run locally for any executable change), so local heavy runs are slower — that is the mirror the docs promised; (2) the hazard-controls gate now runs on every docs/heavy PR and will go red on 2026-11-23 (manifest.reviewExpiresAt) until the register is re-reviewed — by design, but it will block PRs that day if nobody renews it; (3) the tightened owner-scope tokens can flag a future handler that scopes through a construct not in the list (e.g. `.match({ owner_id })` without a literal key string) — the fix is to add the construct, not the substring, back; (4) the shard-2 timing for ui-tools-show-all is an estimate. The package's gate line (verify:pr-local heavy plus the named check:* gates) still needs to be run by the reviewer; RAG impact: none — no ranking, selection, ordering, fixture or eval surface was touched.",
    "needs_owner_decision": [
      "L47 / #J43Z6B src/lib widening: extend check-owner-scope-api.mjs to the 26 src/lib files importing createAdminClient only with a reviewed allowlist of deliberate public-corpus reads (owner_id IS NULL by design). Candidates that would need entries under the filter-shape rule: src/lib/rag/rag-candidate-sources.ts, rag/rag-hydration.ts, rag/rag-cache.ts, rag/rag.ts, rag/rag-retrieval-variants.ts, rag/rag-document-summary-context.ts, registry-corpus.ts, registry-seed.ts, medication-seed.ts, differential-seed.ts, developer-area/corpus-health.ts, developer-area/environment-facts.ts, universal-search.ts, document-detail.ts, sources/document-source-loader.ts, favourites-reference.ts, answer-telemetry.ts, ingestion-enqueue.ts, ingestion-mutation-safety.ts, stranded-queued-recovery.ts, supabase/auth.ts. Several are RAG retrieval surfaces, so the allowlist review should go through the RAG process. Also decide whether child tables without owner_id (document_chunks, document_images, document_pages, document_table_facts, ingestion_jobs, ingestion_job_stages) should be guarded as parent-scoped.",
      "M33 per-test strictness: docs/clinical-hazard-controls.json cites two proofs that reference no control — H3 -> tests/ward-output.test.ts (formatAnswerRenderCopyText is pinned only indirectly) and H4 -> tests/rag-trust.test.ts. Either drop them from the manifest's tests arrays, or make those tests name the control, then tighten the validator from 'at least one listed test' to 'every listed test'.",
      "M33 expiry: with the register gate now in static-pr, PRs go red on 2026-11-23 unless the hazard register is re-reviewed (reviewedCommit/reviewedAt/reviewExpiresAt) before then. Schedule the review or decide to keep the gate release-only.",
      "M31 README: update the mockups/README.md row for accessible-table-browser-fixture to name tests/ui-accessible-table-mockup.spec.ts as the backing spec (file not owned by this package).",
      "M30 counts: check:medication-interactions and check:clinical-hazard-controls were added to CI and verify:pr-local but not to verify:cheap:internal (package.json not owned), so CLAUDE.md's '37 static/consistency gates' and the gates skill's 'of 40' are unchanged and check:gate-manifest still passes. If the owner wants them in verify:cheap too, that is a package.json edit plus bumping both counts to 39 and 42 in the same commit."
    ]
  },
  "review": {
    "pass": true,
    "blocking": [],
    "notes": [
      "Branch state: 7 commits on origin/main..HEAD, working tree clean, 13 files changed (+610/-32). No src/lib/**, no src/lib/rag/**, no ranking/ordering/selection surface, no ledger/inbox/record file, no flake ledger, no diff-integrity.json, no bundle budget, no AGENTS.md, no package.json, no CLAUDE.md, no tests/ui-tools.spec.ts, no tests/ui-tools-show-all.spec.ts (matcher-only rule respected). No test skipped, deleted or weakened; no export deleted; no route or document added or removed (the fixture route src/app/mockups/accessible-table-browser-fixture/page.tsx already exists on main). 'RAG impact: none' is accurate.",
      "Evidence run here (all green): `npx vitest run` on ci-cache-safety, check-medication-interactions-index, clinical-hazard-controls, playwright-pr-shards, playwright-project-isolation, design-system-adoption, browser-test-plan, verify-pr-local, owner-scope-guard, medication-interaction-lexicon-coverage -> 'Test Files 10 passed (10) / Tests 253 passed (253)'. `npx eslint` on every touched .mjs/.ts file -> exit 0. `prettier --check` on every touched file incl. ci.yml and both docs -> 'All matched files use Prettier code style!'. Gates: check:verification-plan 'PR-local verification plan self-test passed.'; check:owner-scope '60 src/app/api files clean against 26 owner-scoped tables.'; check:clinical-hazard-controls 'CLINICAL_HAZARD_CONTROLS_PASS hazards=6 decisions=2'; check:gate-manifest 'all 40 verify:cheap gates are enforced in CI ... 37 static gates are documented consistently.'; check:ci-scope 'Mockup spec parity: 16 advisory specs all match'; check:playwright-pr-shards 'PR UI shard parity OK: 34 production specs across 3 groups.'; check:browser-test-plan, check:medication-interactions '(531 rows)' up to date, check:mockups PASS, check:diff-integrity 'PASS — 5 changed test file(s), 68 -> 80 test case(s)', check:github-actions passed, check:design-system-contract passed (adoption-manifest regen is consistent), check:ledger-write-discipline passed locally (it now rides the heavy plan). Note `npm run test:focused` refuses on this branch by design ('Focused test selection is unsafe: test or configuration paths changed'), so vitest was invoked directly. Full unit suite and verify:pr-local heavy were not run by me either.",
      "M24 verified. The heavy dry-run now prints 39 steps in CI static-pr order (docs/ledger steps, then the 16 staticHeavyGuards ending check:migration-role, check:function-grants, check:owner-scope, then lint/typecheck/test). The new ci-cache-safety test parses every static-pr step whose `if` contains static_heavy_changed (so it also covers the docs||heavy steps) and diffs against selectedScripts(); on main's plan (lint/typecheck/test + repo-awareness only) it necessarily fails, and it also pins that the three guards do NOT leak into docs-only scope. Weakness worth knowing: the parser only recognises single-line `run: npm run X` steps; a future heavy-gated step written as `run: |` would be invisible to the mirror test (today only the ledger-write-discipline setup step is multi-line and its real gate is a separate single-line step).",
      "M30 verified. CI step added under the exact sibling condition; the new test compares index.names, per-drug row counts, rowKey/rowIndex and verbatim note text (builder sets note = row.val ?? '' at build-medication-interaction-index.ts:293, so the comparison is calibrated) plus sourceRowCount, and proves the failure shape on a mutated record. It is a real snapshot-vs-index comparison, not the self-comparison the finding criticised. Counts in CLAUDE.md/gates skill are unchanged and check:gate-manifest passes because nothing was added to verify:cheap:internal — consistent with the Stage-5 correction (the count rule is only triggered by verify:cheap additions; the package note's 'static-pr or verify:cheap' wording is stricter than the gate actually enforces).",
      "M33 verified as the per-hazard form. New validator error fires when no listed test names a control symbol or imports a control path; the new unit case fails on main (the error string did not exist). Shallow-clone path proven here: a `git clone --depth 1` of the branch prints CLINICAL_HAZARD_CONTROLS_SHALLOW_CLONE then CLINICAL_HAZARD_CONTROLS_PASS, exit 0; the full checkout runs the ancestry checks (checkGit: !shallow, checkFiles default true). Existing date/commit/role checks untouched (diff only adds). Residual weaknesses, both inherent to the 'regex over test source' fix sketch: a symbol mentioned in a comment inside a listed test counts as a reference, and the control-path match is a substring (a test importing `@/lib/answer-render-policy-extra` would satisfy `@/lib/answer-render-policy`). Behavioural consequence the owner must schedule: this gate now runs on every docs/heavy PR and goes red on 2026-11-23 (manifest reviewExpiresAt) until the register is re-reviewed.",
      "L22 verified. Old class proven a no-op: `'foo.bar'.replace(/[.*+?^${}()|[\\\\]\\\\]/g,'\\\\$&')` returns 'foo.bar' unchanged and `\\bfoo.bar\\b` matches 'fooxbar' (true); the new class yields 'foo\\.bar'. The unit case assembles the dotted and bracketed symbols at runtime so they never appear literally, and asserts both the wildcard rejection and that '(' neither throws nor is reported missing. Single symbolPattern() helper; no wider behaviour change.",
      "L47 (SCOPE_TOKENS) verified. Ran main's script and the new one against the self-test fixture: old = 0 violations, new = 1, so the self-test is red without the fix. Scanned src/app/api with the payload-key token removed: only 3 sites depend on `owner_id:` alone (answer-feedback/route.ts:65, search/interaction/route.ts:113 and :152) and all three are genuine insert/upsert payload keys, so no handler is passing via a type annotation today. Residual hole (non-blocking, recommend a follow-up): `\\bowner_id\\s*:` also matches a COMMENT containing 'owner_id: null' (src/app/api/search/interaction/route.ts:104 has exactly that text, though that handler is also genuinely scoped), and a TypeScript type annotation `owner_id: string`; the self-test's comment case has no colon so it does not prove comments are rejected. Stripping comments before matching would close it. The src/lib widening was correctly not done — the package note required a complete allowlist and the implementer's 26-file inventory shows several RAG retrieval surfaces would need entries.",
      "M31 verified. tests/ui-accessible-table-mockup.spec.ts reproduces the 52-line journey from 54585e98d byte-for-byte (diffed the `+` lines of that commit against the new file), with the expectNoPageHorizontalOverflow helper copied alongside it. The fixture route and every test id it uses still exist (accessible-table-browser-fixture, table-low-confidence-note at AccessibleTable.tsx:400, missing-value at ui/missing-value.tsx:74, caption 'Clozapine ANC response'); visibleByTestId is exported from tests/playwright-settlement.ts. Routing: in testMatch and mockupSpecPattern, not in productionSpecPattern (asserted). The spec has never executed in a browser in this environment; its first run will be the advisory chromium-mockups lane — acceptable under the Playwright offline exception.",
      "M32 verified. tools-show-all added to testMatch, productionSpecPattern and the mirrored productionSpecFilePattern; assigned to shard 2 with a clearly labelled 3.0 s estimate. The two-way parity test reads every tests/*.spec.ts on disk and fails on any file no project collects (on main it would name exactly ui-tools-show-all). The `tools|tools-show-all` alternation is safe: `tools` cannot match ui-tools-show-all.spec.ts because `\\.spec\\.ts` must follow immediately. The spec has no @mockup tag so the grepInvert projects will run it. Browser evidence for this spec is still zero (17 days) and it may itself be red against today's launcher; first proof is Production UI shard 2 in CI.",
      "Skip reasons all checked true: (1) mockups/README.md is not in the owned list and its row 128 still says 'no committed test navigates to it' — now false, one-line follow-up for the file's owner; (2) per-test M33 strictness would go red on this tree: tests/ward-output.test.ts has 0 occurrences of formatAnswerRenderCopyText and tests/rag-trust.test.ts has 0 occurrences of assessAndEnforceClaimSupport/collectSourceCandidates/rag-claim-support/answer-render-policy, and the fix lives in docs/clinical-hazard-controls.json or those test files, neither owned; (3) src/lib widening correctly deferred per the package note.",
      "Scope note, non-blocking: docs/design-system/adoption-manifest.json is outside the owned list. It is generated output regenerated by the pre-commit hook (adds tests/playwright-pr-shards.test.ts to AccessibleTable's test list because that test now mentions the component); check:design-system-contract passes with it and would fail without it, so leaving it out was not an option.",
      "Owner follow-ups already listed by the implementer and confirmed real: hazard-register re-review before 2026-11-23; README row for the fixture; whether check:medication-interactions and check:clinical-hazard-controls should also join verify:cheap:internal (then bump the 37/40 counts in the same commit); local heavy runs are now ~39 steps instead of ~13, which is the documented mirror but noticeably slower (knip, maintainability budgets, design-system, ledger and docs checks now run for any executable change)."
    ]
  }
}