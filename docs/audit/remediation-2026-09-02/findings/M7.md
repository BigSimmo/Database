### M7 · /privacy page states 'There is no shared corpus across accounts' while migrations promoted every indexed document to a public, anonymously searchable corpus

`src/lib/privacy-page-content.tsx:135 (pinned by tests/privacy-ui.test.ts); supabase/migrations/20260706120000_promote_remaining_indexed_documents_public.sql:1-12; src/app/api/search/route.ts:1009-1012; src/lib/owner-scope.ts:10-16` · privacy/clinical · **CONFIRMED** · safe_cleanup=false · lane L21 (L21-F2)

> **Correction (Stage 5, red team):** Delete 'pinned by tests/privacy-ui.test.ts': no test references 'shared corpus' or 'another signed-in user'; tests/privacy-ui.test.ts:49,131 pin only the heading 'Who can access your data'. Amend §10.2 ('three tests … asserting a recorded defect' → two) and §14 item 20 ('test pins the text' → re-run tests/privacy-page.dom.test.tsx, which renders the content).

- **Defect:** The 'Who can access your data' section says: 'Every document, log row, and saved item is stamped with the account that owns it, and database row-level security restricts reads to that owner. There is no shared corpus across accounts: another signed-in user cannot search, retrieve, or cite your uploads.' Migration 20260706120000 (comment: 'Promote all remaining indexed owned documents to the public corpus (owner_id IS NULL) regardless of clinical_validation_status, so anonymous callers can access the full indexed corpus') set owner_id = null on the operator's uploads; resolveRetrievalAccessScope always returns includePublic:true; /api/search accepts unauthenticated callers as `publicOnly` (route.ts:1012, publicAccessContext) — so those uploads are searchable, retrievable and citable by every account and by anonymous visitors. The PIA (line 99: 'access.ownerId (or undefined for anon/public)') and source-governance.ts:76 ('all indexed documents are anonymously searchable') both acknowledge this; the transparency page does not.
- **Trigger:** Any reader of /privacy (the page intended as the APP 1/APP 5 notice, readiness item PRIV-LEGAL-APP1-APP5-NOTICE, status pending) and any document uploaded before 2026-07-06 or later promoted.
- **Impact:** The public privacy notice misdescribes who can reach the clinician's uploaded guideline documents (and the labels/summaries promoted with them). No patient data is exposed by this — the corpus is reference material — but a notice that is provably wrong on access scope undermines the pending privacy-adviser approval and the page's own promise that every statement 'can be checked against the repository'.
- **Proof:** not executed — traced. privacy-page-content.tsx:135 quoted above. 20260706120000 migration lines 1-2 and `set owner_id = null` at :10; 20260705220000 also nulls document_labels/document_summaries owner_id (:25-30). owner-scope.ts:11 `return ownerId ? { ownerId, includePublic: true } : { includePublic: true }`. search/route.ts:1012 `const publicOnly = !access.authenticated && !isLocalNoAuthMode();` then proceeds to buildScopedSearchPayload rather than returning 401.
- **Fix sketch:** Reword the paragraph to describe the two tiers that exist: documents the operator has published to the shared public corpus (owner_id null, searchable by anyone including signed-out users) versus owner-scoped uploads (RLS + owner_id). Update the matching assertion in tests/privacy-ui.test.ts and the 'Who can access' gist; cross-reference PIA §3. Consider also stating on the page that sign-in is not required to search the public corpus.
- **Caution:** The public-corpus retrieval model is a deliberate product decision (tenancy review); this finding is about the notice, not about re-scoping retrieval.
- **Skeptic (CONFIRMED):** The sentence is contradicted by committed migrations and the retrieval scope code; later publication migrations (20260717131000, 20260722190000) keep a governed path to owner_id null, so the shared public corpus is an ongoing design, not history. Reference documents only, no patient data, so not High; but a false access-scope statement on the privacy notice is privacy-relevant drift, so Medium rather than Low. Fix is copy-only and touches no protected surface.
