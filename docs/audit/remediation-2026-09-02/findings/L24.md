**L24 · Dormant `ingestion-worker` Edge Function plus live `invoke_ingestion_worker` RPC can stamp never-extracted documents as `indexed`** — `supabase/functions/ingestion-worker/index.ts:46,195,243,305; supabase/config.toml:4-5; supabase/schema.sql:3464-3506 (invoke_ingestion_worker); supabase/schema.` · data-integrity · CONFIRMED · safe_cleanup=false · L05-F2

The recovered `ingestion-worker` Edge Function is still deployable (`[functions.ingestion-worker] verify_jwt = true` in config.toml) and `public.invoke_ingestion_worker(integer)` is a live SECURITY DEFINER RPC (service_role-executable) that POSTs to it with a Vault-stored service JWT. The function claims real rows from the same `claim_ingestion_jobs` queue the container worker uses (index.ts:305) but performs no extraction: it reads existing chunks, builds a heuristic summary, and embeds title/summary with `Supabase.ai.Session("gte-small")` (384 dimensions) into … **Impact:** A newly uploaded guideline can show as indexed with nothing retrievable, and the container worker's retry budget for that document is consumed by a path that cannot succeed. This is the same silent-corruption shape the strict completion gate exists to prevent, reachable through an endpoint the repo … **Fix:** Retire it: remove `supabase/functions/ingestion-worker/` and its config.toml block, and ship a migration (inside an approved merge window) dropping `invoke_ingestion_worker(integer)` — the step the 3-PR plan already names. If retirement must wait, make the function return 410 before touching the … **Skeptic:** Chain traced with quoted code: a 384-dim insert into vector(1536) cannot succeed, every claimed job lands in the catch, and fail_or_retry sets documents.status='indexed' unconditionally. Downgraded to Low per the re-grade rule: the trigger needs a service-role/operator action (or a live cron the repository does not define), the function is not …
