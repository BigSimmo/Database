**L47 · check:owner-scope passes any handler that merely mentions `owner_id` (e.g. in a select list) and ignores child tables without an owner_id column** — `scripts/check-owner-scope-api.mjs:32-42 (SCOPE_TOKENS), :171-176 (substring test); docs/audit/tenancy-defense-in-depth-review.md:284` · process · CONFIRMED · safe_cleanup=false · L04-F3 · corroborates docs/audit/tenancy-defense-in-depth-review.md §6 item 2 (the guard is the stated fix for the 'future handler drops the owner filter' class; this shows the fix is weaker than claimed)

The gate treats a handler as scoped if the literal substring `owner_id` appears anywhere in its body. A projection such as `.select("id,owner_id,title")`, a comment, or an unrelated object key satisfies it. Separately, only tables with an `owner_id` column are checked (26 tables); `document_images`, `document_chunks`, `document_pages`, `document_table_facts`' parents, `ingestion_jobs` and `ingestion_job_stages` are outside the guard entirely, yet they carry clinical text/images and are only safe when the handler first resolves the parent document under `withOwnerReadScope` (as … **Impact:** The repository's single tenancy layer (service-role client, RLS bypassed — tenancy review §1) has a CI guard that reports '60 files clean' while missing the two most likely regression shapes; a cross-owner read of storage paths, chunk text or image rows would merge green. **Fix:** Tighten SCOPE_TOKENS to filter constructs (`.eq("owner_id"`, `.is("owner_id"`, `owner_id.eq.`, `owner_id:` in insert/update payloads, `documents.owner_id`) instead of the bare substring; add a second rule that any … **Skeptic:** Reproduced. The gate really does pass the two most likely regression shapes. Downgraded to Low per the re-grade rule: the trigger is a future repository write (privileged actor), AGENTS.md does not name this gap, and the measured live corpus has zero owner-scoped documents, so a regression today would expose no other owner's data. Still worth …
