**L54 · Railway watch patterns omit a script both Dockerfiles copy and run during `npm ci`** — `railway.app.json:6-26 and railway.worker.json:6-27 (watchPatterns) vs Dockerfile:31,76 and Dockerfile.worker:37,54 (`COPY scripts/check-installed-lock-parity.mj` · process · CONFIRMED · safe_cleanup=false · L16-F8

`scripts/check-installed-lock-parity.mjs` executes inside every image build (postinstall `--write-stamp`) yet is not a watch pattern for either service, unlike its siblings check-node-engine.cjs and install-git-hooks.mjs. A change to it alone does not trigger a Railway rebuild, so the deployed image can be built from a script version main no longer has until an unrelated push rebuilds it. **Impact:** Silent deploy skip for an image-build-time guard; low because the next any-source push rebuilds. Same class as the tests/stubs/server-only.ts entry Railway already tracks. **Fix:** Add `"/scripts/check-installed-lock-parity.mjs"` to both watchPatterns arrays; `tests/container-ci-contract.test.ts` could assert every `COPY scripts/*` in a Dockerfile appears in the matching railway.*.json. **Skeptic:** Omission confirmed. Impact is smaller than the finder's 'deployed image built from a script version main no longer has': the script only runs at image-build time, and the next rebuild uses that push's tree, so the lone-commit case merely defers a rebuild that changes no runtime behaviour. Kept as a Low consistency gap (siblings are watched, the …
