**L82 · Routes panel labels every /mockups page 'These do not exist in production', but four /mockups subtrees are served in production behind the administrator gate** — `src/app/mockups/development/routes/page.tsx:125; scripts/generate-repo-awareness-snapshot.ts:55; scripts/ci-change-scope.mjs:290` · docs-drift · CONFIRMED · safe_cleanup=true · L10-F6

The generator classifies any route starting with `/mockups` as `area: "mockup"` and the page prints 'These do not exist in production. They are exempt from the button-wiring and reachability checks, and from nothing else.' The developer hub itself, Caring Contacts, Care Plan and Ward Flow (`DEVELOPER_GATED_PATH_PREFIXES`, headers.ts:26-31) are production routes reachable by an administrator. `scripts/ci-change-scope.mjs:290` repeats the stale claim '(already excluded; 404 in production)' about `src/app/mockups/development/`. **Impact:** Misstates the production surface on the panel whose job is to state it; the ci-change-scope comment justifies a perf-exclusion with a false premise (the exclusion is still reasonable because the hub is admin-only). **Fix:** Reword line 125 to 'Design-scratch pages are unavailable in production except the developer-gated subtrees (development, caring-contacts, care-plan, ward-flow), which require an administrator sign-in'; optionally add a third `RouteArea` ('developer') derived from `DEVELOPER_GATED_PATH_PREFIXES` so … **Skeptic:** The panel's blanket statement is false for the four gated subtrees, including the page displaying it. Copy/comment drift only; the ci-change-scope exclusion remains justified on other grounds: Low. Adding a third RouteArea would change the committed snapshot shape and the gate that compares `routes` — fine, but must regenerate the snapshot in the …
