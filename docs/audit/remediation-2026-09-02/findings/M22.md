### M22 · Lexicon review sheet still leads with "Status: reviewed 2026-08-22 — All 37 catalogue terms reviewed" although the lexicon and catalogue changed on 2026-08-28 and the sheet's own mappings moved

`docs/medication-interaction-lexicon-review.md:3 and :154-160; scripts/build-medication-lexicon-report.ts:97-99 (status line rendered from the carried-forward sign-off date), :279-281 (sign-off block appended verbatim), :268-270 (check mode compares only `beforeSignOff`)` · process · **CONFIRMED** · safe_cleanup=false · lane L03c (L03c-F3)

> **Correction (Stage 5, red team):** Downgrade to Low/recommendation: the status line at docs/medication-interaction-lexicon-review.md:3 already says 'Any lexicon change made since is NOT covered by it: re-check every term the change touches', and the Outcome row is historically true for 2026-08-22. The real gap is the missing content hash in the sign-off.

- **Defect:** The generator preserves whatever sign-off block exists and re-renders the "reviewed <date>" status line from it, and its --check mode deliberately ignores the block, so nothing detects that the sign-off predates the mappings it claims to cover. The sheet's body text disclaims later changes, but the leading status line and the Outcome row ("All 37 catalogue terms reviewed") are what a reader sees first.
- **Trigger:** Commit 0e70217cf (#2448, 2026-08-28) added CV medications to data/medications-snapshot.json (328→330) and added `sourceDenySlugs` to the statins/fibrates terms in src/lib/medication-interaction-lexicon.ts. The regenerated sheet in that same commit changed the resolved lists: `acei` now resolves to Perindopril + Ramipril (was Perindopril only), `statins` to 3 drugs (+Simvastatin), `antihypertensives` to 18, `fibrates` rows 2→1 — while the Sign-off block still says 2026-08-22 and `check:medication-lexicon-report` passed (gate log: "up to date (37 catalogue terms)").
- **Impact:** The sheet is the clinical-facing artefact that tells the owner which drug-class mappings a qualified reviewer accepted (#1YPV51 exists because an earlier closure claimed a review that had not happened). A clinician reading "reviewed 2026-08-22" will assume Ramipril's ACEi resolution and Simvastatin's statin resolution were part of that review; they were not.
- **Proof:** git show 0e70217cf -- docs/medication-interaction-lexicon-review.md shows the +/- rows quoted above with no change to the Sign-off table; git log --since=2026-08-22 on the lexicon lists f3d1a3cce (the sign-off itself, 2026-08-23) and 0e70217cf (2026-08-28). readSignOff (lines 293-305) extracts only the date; no content hash is bound. Not executed — traced.
- **Fix sketch:** Record a sha256 of the rendered catalogue-term table in the Sign-off block when a review is recorded; have --check fail (or the status line render "reviewed 2026-08-22 — N terms changed since") when the current table hash differs. Until then, the owner should re-attest the acei/statins/antihypertensives/fibrates rows.
- **Skeptic (CONFIRMED):** Generator behaviour and the post-sign-off mapping changes are exactly as described; the sheet's disclaimer sentence on line 3 partially mitigates but the status line and Outcome row still lead. No gate binds sign-off to content. Medium: clinical-facing governance artefact, but no number a clinician acts on directly. Fix sketch (hash in sign-off block, fail check) does not touch protected surfaces; it would change the `check:medication-lexicon-report` behaviour, which is in verify:pr-local and CI, so the owner must re-attest before the stricter check ships.
