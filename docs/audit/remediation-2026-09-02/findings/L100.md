**L100 · docs/site-map.md shows the generator fallback text for nine product routes, including all five new Sources catalogue routes** — `docs/site-map.md:44, 49, 55-60, 65; scripts/generate-site-map.ts:374-377` · docs-drift · CONFIRMED · safe_cleanup=true · L18-F9

`routeLine()` emits 'Route discovered from app directory' when the description map has no entry. 168 site-map lines carry that fallback; nine are product routes — /forms/search, /formulation/search, /services/search, /specifiers/search, /sources, /sources/[sourceId], /sources/method, /sources/publishers, /sources/topics — so the generated map that CLAUDE.md names as the route reference describes the whole Sources mode (landed 2026-09-02) with placeholder text, while the mockup section is placeholder throughout. **Impact:** The route reference cannot tell a reader what the Sources catalogue routes do; sitemap:check passes because the placeholder is the generator's own output. **Fix:** Add descriptions for the nine routes to the generator's description map and run `npm run sitemap:update`; consider making the generator warn when a non-mockup route lacks a description. **Skeptic:** Reproduced, and the finder under-counted: the fallback also covers a block of API routes, so the description map is further behind than stated. Generated reference doc with placeholder text for the newest mode — Low docs-drift. Fix (add descriptions, `npm run sitemap:update`) touches the generator's description map only; sitemap:check re-validates.
