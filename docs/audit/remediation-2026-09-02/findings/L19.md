**L19 · cleanClinicalTableText strips in-cell page pointers and leaves empty brackets in clinical cells** — `src/components/AccessibleTable.tsx:35-43` · correctness · CONFIRMED · safe_cleanup=false · L13-F2

metadataCellPattern removes `p. N`, `page N`, `source N`, `citation N` wherever they occur inside a cell, including legitimate cross-references in guideline tables, and leaves the surrounding punctuation behind. **Impact:** The clinician loses the page pointer that would let them verify the row against the source, and sees artefacts like '()' — mildly misleading, and contrary to the product's verify-against-source posture. **Fix:** Only scrub cells in columns whose header is metadata, or restrict the pattern to whole-cell matches; strip empty '()' / '[]' after scrubbing. **Skeptic:** Reproduced. The scrub is deliberate (it strips provenance noise from answer tables) so this is a design trade-off rather than a plain bug, but the empty-bracket artefacts and the loss of in-cell page pointers are real and contradict the verify-against-source posture. Low: the clinical value itself is preserved; only the pointer is lost. Fix …
