**L108 · WebVitalsReporter claims to tree-shake to a no-op in production but always registers six web-vitals observers in the root layout** — `src/components/web-vitals-reporter.tsx:12-14, :36-45; src/app/layout.tsx:164; node_modules/next/dist/client/web-vitals.js:13-24` · performance · CONFIRMED · safe_cleanup=false · L22-F3

The doc comment says the `NEXT_PUBLIC_WEB_VITALS_DEBUG` flag 'is a static boolean and the whole component tree-shakes to a no-op when unset'. Only the callback body and the JSX return are gated; `useReportWebVitals(cb)` is invoked unconditionally, and Next's implementation runs `onCLS/onFID/onLCP/onINP/onFCP/onTTFB` inside a `useEffect` regardless of the callback. The component is mounted in the root layout for every route. **Impact:** `next/dist/compiled/web-vitals` (7,914 bytes raw) ships in the shared client bundle and six PerformanceObservers plus the FID/INP event listeners run on every page for no consumer. Small but permanent, and the comment misleads anyone deciding whether the reporter costs anything. Semgrep also … **Fix:** Gate at module boundary instead of inside the hook: `export const WebVitalsReporter = DEBUG_ENABLED ? WebVitalsReporterImpl : () => null;` (or conditionally render in layout.tsx). Then the comment becomes true and the compiled web-vitals module drops out of the production graph. **Skeptic:** The comment is false by Next's own implementation: the effect subscribes six observers regardless of the callback body, and the compiled web-vitals module stays in the import graph because the hook is referenced. Not measured (no build in evidence dir), so the byte figure is the finder's; the behavioural claim is fully traced. Low: a few KB and …
