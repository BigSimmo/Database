**L61 · PR-handoff hook tells the model to stop at PR creation by default, while AGENTS.md, docs/agents/pull-request-workflow.md and the handoff skill say a 30-minute babysit is the default** — `.claude/hooks/pr-handoff-stop.sh:333 vs docs/agents/pull-request-workflow.md:72-75 and .claude/skills/handoff/SKILL.md:88-96` · process · CONFIRMED · safe_cleanup=false · L20-F2

PR #2220 (2026-08-21, 'Publish bare PRs without readiness delay') rewrote the PostToolUse context injected after every PR creation to: 'The pull request is open: hand over its URL and stop. Do not read checks, wait, sync from main, or push follow-up fixes unless the user expressly asks to babysit' — and `tests/pr-handoff-stop.test.ts:134-148` pins that wording ('makes handoff the default and reserves babysitting for an explicit request'). But the hook's own header (lines 3-16), AGENTS.md 'Babysit the pull request, then stop', `docs/agents/pull-request-workflow.md:72-75` ('the session gets a … **Impact:** Non-deterministic behaviour at the exact hand-off moment: some sessions walk away from a PR whose required check goes red 90 s later (the case the budget was designed for), others babysit and are told by the hook they should not have. The committed test locks the hook side, so a future edit to the … **Fix:** Decide one default. Either (a) restore the budget wording in the hook context ('you may follow this PR for N minutes; stop when CI settles') and update the test expectation, or (b) make AGENTS.md/pull-request-workflow.md/handoff step 8 say babysitting is opt-in. Keep the bare-publication route as … **Skeptic:** Contradiction confirmed verbatim across four instruction sources, with the test pinning the hook side. Downgraded to Low: the enforcing behaviour (deny follow tools after the budget) is identical under both narratives, so the only effect is whether a session babysits by default — a process inconsistency with no data, clinical or gate consequence. …
