**L41 · Provisioning scripts fetch and execute unverified downloads (Node tarball, gh tarball, third-party plugin `npm install`) without checksum pinning** — `.claude/hooks/session-start.sh:47-48; scripts/setup-claude-cloud.sh:217-218,176,192-196` · security · CONFIRMED · safe_cleanup=false · L20-F7

`session-start.sh` pipes `curl -fsSL https://nodejs.org/dist/v24.19.0/...tar.xz | tar -xJ` with no SHASUMS256 check; `setup-claude-cloud.sh` does the same for `gh_2.67.0_linux_amd64.tar.gz`, runs `claude plugin marketplace add`/`claude plugin install` from `obra/superpowers-marketplace` (a personal GitHub repo) and then `npm install --omit=dev` inside each cached plugin directory, executing any lifecycle scripts those plugins' dependencies declare. All of this runs automatically at SessionStart in every cloud container (`.claude/settings.json` hooks). **Impact:** Arbitrary code in the container that holds the checkout and the platform's injected GitHub credential proxy. Bounded by the cloud container's isolation and by the repo's demo-mode posture (no provider secrets in cloud), so Low. **Fix:** Verify the Node and gh tarballs against a pinned SHA-256 before extracting; pin plugin marketplaces to a commit SHA if the CLI supports it; consider `npm ci --ignore-scripts` for plugin dependency repair. **Skeptic:** Lines quoted exactly as claimed; no checksum verification exists for the Node or gh tarballs, and plugin dependency repair runs `npm install` with lifecycle scripts enabled. Low as the finder graded: TLS to nodejs.org/github.com via the platform proxy, cloud containers hold no provider secrets by the repo's demo-mode posture, and exploitation …
