**L25 · pathway_versions and retention_state are outside the transactional audit guard, so a direct SQL change to which pathway is published, or a hand-inserted retention clearance, commits with no audit row** — `caring-contacts/supabase/migrations/0001_caring_contacts_foundation.sql:306-308; 0003_caring_contacts_workspace.sql:513-516; 0006_caring_contacts_plan_assurance` · data-integrity · CONFIRMED · safe_cleanup=false · L07-F6

`attach_audit_guard` is applied to 14 tables (referrals, plans, contacts, contact_dispatches, cultural_identity_reports, service_stops, service_state, service_restart_approvals, pathway_version_approvals, plan_assignments, plan_reassignments, notification_preferences, training_records, plan_assurances). `pathway_versions` (state: draft/review/published/retired, which decides what patients receive) and `retention_state` (the de-identification record) are not in any list, so the deferred constraint trigger that refuses 'a change that writes no audit event' does not fire for them. The store … **Impact:** The schema's own promise ('EVERY CHANGE CARRIES ITS AUDIT EVENT IN THE SAME TRANSACTION', 0001:19-21) is not true for the publication/retirement of a message pathway or for a clearance record; a silent publish or retire would be invisible to the access trail. Synthetic data only today. **Fix:** Add `select caring_contacts.attach_audit_guard(array['pathway_versions','retention_state'])` in a 0009 migration with a matching migrations-test case, or record the omission as a ruling with its reason. **Skeptic:** The finder mis-states the pathway_versions state vocabulary (actual: draft/inReview/approved/retired, not 'published'), but the defect stands: the schema's headline promise is per-table and two clinically meaningful tables — which pathway version is approved/retired, and the de-identification clearance record — carry no `require_audit` trigger, so …
