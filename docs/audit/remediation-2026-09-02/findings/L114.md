**L114 · src/components/calculators/index.ts publishes six mockup components that no production route imports (production mockups come from calculator-mockups/)** — `src/components/calculators/index.ts:1-7; src/components/calculators/bedside-sheet.tsx:134,211; clinical-console.tsx:86; directory-grid.tsx:182; guided-flow.tsx:` · quality/simplification · CONFIRMED · safe_cleanup=false · L03b-F3

The production calculators barrel exports `CalculatorsBedsideSheetMockup`, `CalculatorsClinicalConsoleMockup`, `CalculatorsDirectoryGridMockup`, `CalculatorsGuidedFlowMockup`, `CalculatorsPopupSheetMockup` and `CalculatorsSearchDetailMockup`. Every /mockups/calculators-* route imports the parallel copies in `src/components/calculator-mockups/` instead, and `tests/calculator-mockup-boundary.test.ts` forbids production code from importing calculator-mockups — so the production tree carries a second, drifting copy of the mockups (including its own `plannedCalculators` list) that nothing renders. … **Impact:** Dead-looking clinical UI code carrying quarantined-instrument rendering paths inside the production module; bundle attribution and the 4-copy fixture drift make the safety review surface larger than it needs to be. Triage candidate only — no deletion proposed here. **Fix:** Run `npm run check:dead-code-candidate` on the six exports; either drop them from the barrel (and the unreferenced files) after the refusal-list check, or move them under calculator-mockups/ so the production module contains only CalculatorsSearchPage and its helpers. **Skeptic:** Factual and reproduced from the knip evidence; no ledger row mentions the calculators barrel. Low quality item. Fix must go through docs/agents/dead-code-deletion.md (`check:dead-code-candidate`) and cannot delete search-detail.tsx or guided-flow.tsx wholesale (production ScorePanel import; tests/contextual-back-navigation-contract.test.ts names …
