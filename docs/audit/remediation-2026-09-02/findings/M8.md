### M8 · A source whose clinical_validation_status is 'unknown' (partially recorded governance metadata) is cited with no 'not locally validated' caveat, contrary to the governance rule that unknown is treated as unverified

`src/lib/source-governance.ts:151 (`if (source.clinical_validation_status === "unverified")`); src/lib/source-metadata.ts:98-109 (normalizeOptionalSourceMetadata → "unknown" when the field is absent but another governance field is recorded); src/lib/rag/rag-candidate-sources.ts:663,770,971; src/lib/clinical-review-queue.ts:85-86; docs/clinical-governance.md:19 ('Treat unknown source metadata as unverified, not current')` · privacy/clinical · **PLAUSIBLE** · safe_cleanup=false · lane L21 (L21-F3)

> **Correction (Stage 5, red team):** Line references are source-governance.ts:157 (not :151), clinical-review-queue.ts:87 (not :86), docs/clinical-governance.md:15 (not :19). The trigger paragraph is misleading: therapy-source-governance.ts:28 sets clinical_validation_status explicitly and registry-corpus.ts:125 / sources/catalogue-core.ts:119 pass one through, so none of the named writers produces the absent-field shape; with no live row named, PLAUSIBLE + unestablished precondition argues Low until a provider-backed read finds one.

- **Defect:** Warnings are emitted only for the literal value "unverified". Candidate sources are built with normalizeOptionalSourceMetadata, which returns clinical_validation_status "unknown" whenever the field is absent but document_status or extraction_quality is recorded (pinned by tests/source-metadata.test.ts:37-41: `{document_status:"current"}` → validation "unknown"). For such a document sourceGovernanceWarnings emits neither UNVERIFIED nor any other validation warning, so buildWarnings (answer-render-policy.ts:461) and the frontend badge list carry no caveat. The same asymmetry exists in clinical-review-queue.ts:86 (`validation === "unverified"`), so the document also never enters the review queue for that reason. The trust cap is partly preserved: rag-claim-support.ts:1138-1141 maps unknown→"unverified" so high-risk claims are capped to medium trust — but routine claims on that source render at full trust with no notice, and `document_status: "unknown"` produces no warning at all (source-governance.ts:130-146 only handles outdated/review_due).
- **Trigger:** A document whose metadata records document_status/extraction_quality/review_date but not clinical_validation_status — e.g. the bulk metadata route (src/app/api/documents/bulk/route.ts:286-300) sets extraction_quality, review_date, publication_date and publisher fields via setMetadataValue without a validation default; registry/therapy adapters set document_status "unknown" (therapy-source-governance.ts:26). The ordinary upload path (upload/route.ts:261) does write "unverified", so prevalence in live data is unverified here (medium confidence).
- **Impact:** A clinician can receive a source-backed answer citing a document that nobody has locally validated, with the 'not locally validated' caveat missing from the answer warnings and the source badges — the exact caveat source-governance.ts:74-79 says is 'clinically material' since the public-corpus promotion.
- **Proof:** not executed — traced. source-governance.ts:151 compares to "unverified" only; source-metadata.ts:5 lists "unknown" as a known value so normalizeSourceMetadata (:68-73) keeps it rather than defaulting to "unverified"; source-metadata.test.ts:37-41 asserts `{ document_status: "current" }` → `clinical_validation_status: "unknown"`. No test in tests/source-governance.test.ts covers `clinical_validation_status: "unknown"` (grep: only document_status "unknown" at :170 with validation "locally_reviewed").
- **Fix sketch:** In sourceGovernanceWarnings treat `clinical_validation_status !== "locally_reviewed" && !== "approved"` (i.e. unverified OR unknown) as the UNVERIFIED caveat, mirror it in clinical-review-queue.ts:86, and add an explicit `document_status: "unknown"` info/warning line ('Review status unknown') to the warnings channel. Add source-governance tests for the unknown case. This is a clinical-caveat change: run the focused governance tests and note it under the PR governance preflight; it does not touch retrieval ordering.
- **Caution:** Do not repurpose the new caveat to weight retrieval (RAG ranking protection).
- **Skeptic (PLAUSIBLE):** The code asymmetry is reproduced: an `unknown` validation status yields no caveat. The unestablished precondition is whether any live document actually carries governance fields without clinical_validation_status — every writer inspected sets "unverified" or a reviewed value, so prevalence may be zero (the finder's own confidence is medium). Kept Medium because if such a row exists the missing caveat is exactly the clinically material one; would be High only with evidence of affected live documents (provider-backed, not checked). Fix touches sourceGovernanceWarnings only — not retrieval ordering — and must not feed the RAG ranking surfaces.
