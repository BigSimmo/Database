**L115 · Useless ternary in the Colour coding reference heading (both branches identical)** — `src/components/reference/colour-coding-reference-content.tsx:101` · quality/simplification · CONFIRMED · safe_cleanup=true · L03b-F6

`isGuide ? "text-2xl sm:text-3xl" : "text-2xl sm:text-3xl"` — the guide/non-guide branch selects the same class string, so the conditional documents an intent (different heading scale in the Guide Centre) that the code does not implement. **Impact:** Cosmetic; misleading to the next editor. Flagged by semgrep `useless-ternary` (rule severity ERROR) in the Semgrep results file (session evidence; the first scan attempt produced none because the proxy blocked semgrep.dev). **Fix:** Replace the ternary with the literal class string (or supply the intended guide-mode size if one was meant). **Skeptic:** Line says exactly what is claimed; cosmetic. Replacing with the literal string cannot break anything (both branches identical).
