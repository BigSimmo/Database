**L20 · `@types/node` 26.x is typechecked against a Node 24 runtime, and Dependabot's dev group will keep it there** — `package.json devDependencies `@types/node: ^26.4.0`; engines.node `>=24.15.0 <25`; Dockerfile:21 / Dockerfile.worker:30 `node:24-bookworm-slim`; .github/dependa` · correctness · CONFIRMED · safe_cleanup=false · L17-F3 · previously-reported (docs/framework-dependency-modernization-checklist.md 2026-07-18, P2 'Runtime/types' row, still unchecked)

Types are two Node majors ahead of every runtime contract (engines, .nvmrc 24, .node-version 24, both Dockerfiles, setup-node-cached uses .nvmrc, codex-run-pr-operator pins 24.15.0). `tsc` will accept a Node 26-only API (or a signature change) that throws on the Railway Node 24 image. The bump came from Dependabot #549 (2026-07-13, 24.13.3 → 26.1.1) and the dependabot.yml `ignore` list holds only typescript and eslint majors, so nothing stops the next major. **Impact:** Silent type-level blind spot in a repo that leans on typecheck as its cheapest gate; a runtime TypeError in the worker or an API route would surface only in Playwright/production. **Fix:** Pin `@types/node` to the 24.x line (`^24.x` matching the runtime floor) and add a dependabot.yml `ignore` entry for `@types/node` semver-major, with a one-line comment tying it to engines.node; tick the checklist row when done. **Skeptic:** The mismatch is a fact in the manifest and the checklist row is still unchecked; the harm needs a contributor to use a Node-26-only API, so it is a latent blind spot rather than a live defect — Low. The checklist is a doc, not the ledger, so not a DUPLICATE. Fix (pin ^24 + dependabot ignore) is safe; tests/check-runtime.test.ts pins the engine …
