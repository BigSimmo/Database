**L64 · The RAG_TEXT_WEAK_OR_RELAXATION=true branch has no test in either state at the integration point** — `src/lib/rag/rag-candidate-sources.ts:280` · test-coverage · CONFIRMED · safe_cleanup=false · L02-F8

The only tests touching the feature exercise the pure predicate shouldRelaxWeakTextMatches (tests/retrieval-query-variants.test.ts:1219-1259). No test sets RAG_TEXT_WEAK_OR_RELAXATION=true (grep of tests/ for the env name hits only env-name inventories: rag-generation-fingerprint, rag-adversarial-fixtures, client-secret-surface, answer-evidence-preview), so the enabled path — merge/append behaviour after strict-AND retrieval — is unexercised. Semantic rerank, by contrast, has tests/semantic-rerank.test.ts with the `enabled` argument. **Impact:** Silent rot of an experiment flag; acceptable while default-off, but the flag is documented as re-enableable. **Fix:** Either add a stubbed-env unit test of the enabled merge path, or mark the flag deprecated in env.ts and behaviour-map. **Skeptic:** Verified: the enabled branch (OR-relaxed append after weak strict-AND, telemetry 'weak_augment') has no unit test in either state at the integration point; only the predicate is covered. Default-off and documented as re-enable-only-behind-a-golden-run, so Low. Adding a stubbed-env unit test does not change retrieval behaviour, but any edit in …
