**L101 · The branch-review archival policy states a size with no method, contrary to its own rule two lines later** — `docs/branch-review-archival-policy.md:346-349` · docs-drift · CONFIRMED · safe_cleanup=true · L19-F7

Lines 346-347 say the records directory totals `2.4 MB`; measured apparent size is 0.36 MB for 580 files (2.4 MB is the allocated-block figure, 4 KiB × ~580). Line 349 then insists 'Quote the method with the number … a figure without its method invites a later reader to correct it'. The src/ figure on the same line (`23 MB` apparent) also no longer holds (17.4 MB apparent today; docs/ is 25.4 MB). **Impact:** Misleading by an order of magnitude on the one number the section is about; low consequence. **Fix:** Change line 347 to `totalling 0.36 MB by apparent size (2.4 MB allocated)` and refresh the src/docs figures with the method. **Skeptic:** The 2.4 MB figure is the allocated-block size (4 KiB blocks × ~580 small files); apparent size is 0.38 MB (finder said 0.36 — same order, likely a few records added since). The line two below insists on quoting the method. Pure documentation drift, Low. Fix is a one-line wording change with no gate impact (check-stale-docs skips this directory).
