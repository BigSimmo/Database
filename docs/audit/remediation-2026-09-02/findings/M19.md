### M19 · Production npm audit is red (browserslist high); the blocking CI audit will fail the next lockfile-changing PR and the weekly scheduled full run

`.github/workflows/ci.yml:465-474 (safety job: `npm audit --omit=dev --audit-level=high` when lockfile_changed); package-lock.json:6770-6772 (browserslist 4.28.2); scripts/ci-change-scope.mjs:7-16 (full-run sentinel includes package-lock.json)` · security · **CONFIRMED** · safe_cleanup=false · lane L16 (L16-F1)

> **Correction (Stage 5, red team):** Merge into one finding. The audit step is ci.yml:468-477. A red scheduled run is the signal notify-ci-failure.yml:17 delivers, so 'loses the weekly main signal' overstates the harm.

- **Defect:** The orchestrator's `npm audit` evidence (evidence/npm-audit-prod.json, 2026-09-02) reports 1 high in the production tree: browserslist <=4.28.6 (GHSA-c83g-rgw3-j3cx unbounded memory growth; GHSA-73wf-gq98-2v4g prototype write via untrusted browserslist-stats.json), `fixAvailable: true`, no major bump needed. The lock resolves browserslist 4.28.2 through `webpack` and `@babel/helper-compilation-targets`, both recorded without `dev: true`, so `--omit=dev` still sees it.
- **Trigger:** Any PR that touches package-lock.json or .npmrc (Dependabot's Monday npm groups included) sets lockfile_changed=true and runs the blocking audit; the Sunday `schedule` run of CI resolves to the full-run sentinel, which also sets lockfile_changed=true.
- **Impact:** Dependency PRs and the weekly main verification go red on an advisory that is not runtime-exploitable here (browserslist runs only at build time on repo-controlled config), so the owner either loses the weekly main signal or learns to ignore a red `safety` job — the exact habit `docs/process-hardening.md` warns against. Runtime exposure to clinicians: none traced.
- **Proof:** evidence/npm-audit.log: `prod exit=1` and `npm-audit-prod.json {"high":1,...}`; node -e over npm-audit-prod.json: `browserslist high isDirect=false range<=4.28.6 fixAvailable=true`; awk over package-lock.json: `webpack no dev flag (prod)`, `babel-hct prod`, `browserslist prod`. CI consequence traced from ci.yml:465-474 — not executed.
- **Fix sketch:** In an ordinary deps PR: `npm update browserslist` (lands >=4.28.7), commit the lockfile, and let the `safety` job's blocking audit prove it green. No major, no behaviour change to the app.
- **Skeptic (CONFIRMED):** Trigger and impact chain hold end to end: the audit is currently red in --omit=dev mode, the blocking step runs on every lockfile-touching PR and on the scheduled full run, and retries do not help a deterministic advisory. Runtime exposure is nil (browserslist is a build-time consumer of repo-controlled config), so this is a red-gate/process problem rather than an exploitable one; Medium stands because the next Dependabot PR and the weekly main signal will go red. Fix (`npm update browserslist` within range) touches no never-touch surface; a lockfile PR would itself run the blocking audit as proof.
- **Merged at Stage 5:** M26 (L17-F1, lane L17) "The only npm-audit high (browserslist ≤4.28.6) sits in the PRODUCTION tree, so the lockfile-gated CI audit step is red until it is bumped" — `package-lock.json (node_modules/browserslist → 4.28.2, no dev flag); .github/workflows/ci.yml:466-477; scripts/ci-change-scope.mjs:414-419`; its skeptic (CONFIRMED): The advisory is observed (phase-0 audit evidence), the package is in the --omit=dev tree by lock metadata, and the CI step exits non-zero at --audit-level=high, so the chain to a red safety job on the next lockfile PR or weekly run holds end-to-end; only the actual CI run was not observed (GitHub not queried), which … Same browserslist high in the production tree and the same lockfile-gated CI audit step (ci.yml:468-477 on this branch). Stage 5 notes a red scheduled run is exactly the signal notify-ci-failure.yml delivers, so 'loses the weekly main signal' overstates the harm.
