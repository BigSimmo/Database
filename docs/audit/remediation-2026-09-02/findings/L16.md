**L16 · Catalogue tier substitutes the whole catalogue when nothing matches, so synthesis always runs on unrelated evidence** — `src/lib/clinical-ask/catalogue-evidence.ts:103-104,165 (also :60,:77,:131,:145); src/lib/differentials.ts:695,703; src/lib/clinical-ask/orchestrator.ts:169-176` · correctness · CONFIRMED · safe_cleanup=false · L12-F1

> **Correction (Stage 5, blue team):** Add both to §14 as 'must land before CLINICAL_ASK_ENABLED=true' in the same PR as M16/M17: the catalogue-fallback substitutes the whole catalogue when nothing matches, and the stream route consumes the durable limiter before the flag check; activation is one env flag.

When the question matches no catalogue record, `differentialEvidence` calls `searchDifferentialRecords("")` / `searchPresentationWorkflows("")`, which return EVERY record (`differentials.ts:695,703`: `if (!normalizeSearchText(query)) return differentialRecords`), and `specifierEvidence` falls back to `specifierCatalogItems()` unfiltered (`:165`). The first 12 records in catalogue order become 'evidence' for a question they have nothing to do with. (For services/forms the same fallback is inert because `rankCatalogRecords` returns [] on an empty query, `catalog-search.ts:153`; … **Impact:** Cost: `evidence.length === 0` (orchestrator:169) can never short-circuit for those modes, so every such question pays the strong-model synthesis call over ~12 irrelevant 2,000-char extracts, then the governance retry (a second strong call, orchestrator:185-189), and — because coverage is inevitably … **Fix:** Return [] when the mode search yields no match (drop the `matches.length ? matches : search("")` pattern and the unfiltered `specifierCatalogItems()` fallback) so the orchestrator's evidence-gap path fires before any provider call; change the catalogue test to assert an empty result for a … **Skeptic:** Reproduced offline, and the finder under-claimed: formulation also falls back to the whole catalogue. Real correctness defect on a governed surface, but the surface is dormant (CLINICAL_ASK_ENABLED off, production-readiness.ts:131 requires it explicit) and has no UI entry point (L12-F3), so a clinician cannot reach it today and enabling it is an …
