**L97 · Root design-qa.md is a machine-local Ward Flow QA handoff: it cites an absent artifacts/ directory and a Windows path, and describes the constellation view as kept although the index records it as retired** — `design-qa.md:5-7, 29 (vs docs/codebase-index.md:544)` · docs-drift · CONFIRMED · safe_cleanup=true · L18-F6

design-qa.md (added by PR #2140, 101f02b5c, 2026-08-19) references `C:\Users\joshs\.codex\generated_images\…png` and `artifacts/ward-management/*.png`; `artifacts/` is neither tracked nor present nor gitignored. Line 29 says 'Kept the full constellation as a dedicated wide command view', but codebase-index.md:544 records `src/app/mockups/ward-flow/constellation/page.tsx` as a retired server redirect to `/mockups/ward-flow/network`. Root-level files other than README.md and AGENTS.md are outside `docs:check-links` (scripts/check-docs-links.mjs:87), so no gate sees the dead references. **Impact:** A stale QA verdict ('final result: passed') sits at the repository root describing a view that no longer exists, with evidence paths nobody can open; the earlier archive of an identical file shows the pattern recurs. **Fix:** git mv design-qa.md docs/archive/design-qa-ward-flow-2026-08-19.md and add a one-line 'point-in-time, artifacts not tracked' note; or delete it if the owner does not want the record. **Skeptic:** Reproduced: a second machine-local QA handoff sits at the root with dead evidence paths and a stale 'kept' claim about a retired view. Low docs drift. Fix (move to docs/archive or delete) is safe; if moved under docs/, confirm docs:check-links treats the backtick paths as text rather than links before relying on the gate.
