**L122 · knip's `**/*mockup*` ignore hides 300 files / 76,269 lines (22.8% of src TypeScript) from the unused-export gate; no orphan mockup file found, but exports inside are unchecked** — `knip.json:11 (`"ignore": [..., "**/_mockup_", ...]`)` · quality/simplification · CONFIRMED · safe_cleanup=false · L11-F5

`find src -path '*mockup*' \( -name '*.ts' -o -name '*.tsx' \)` = 76,269 lines of 334,226 (22.8%); of that, the in-scope non-gated corpus is 74 component files (48,703 lines) under src/components plus 75 route dirs (2,391 lines). check:knip and check:knip:exports both passed (exit 0) but cannot see any of it. Importer trace of all 74 in-scope component files: every one has ≥1 src importer (a route page, an index.ts, or a sibling study), so there is NO orphan mockup component file today. What remains invisible is unused exports within those files and any future orphan. **Impact:** Dead design scratch accumulates undetected; combined with F4 it is the mechanism by which the mockups budget grows. Not a correctness or clinical issue. **Fix:** Triage candidate, not a delete: run `npx knip --include exports --ignore ''` offline once to size the hidden unused-export set, then decide whether to narrow the ignore to the four gated prototype trees only. Any removal must go through `check:dead-code-candidate` per … **Skeptic:** The ignore glob is exactly as cited and hides every mockup file from both knip gates; the finder's own trace found zero orphan files today, so this is a latent-coverage note, not a defect. Low and correctly framed as a triage candidate: narrowing the ignore will surface a large unused-export set and any removal must go through … **Merged at Stage 5:** L130 (L17-F5) — Same knip **/_mockup_ ignore; L17-F5 explains the basename-only matching that leaves 49 mockup-directory rows in the report.
