**L36 · Advisory SAST workflow runs a mutable Semgrep image tag while the blocking job pins a digest** — `.github/workflows/sast.yml:30 (`image: semgrep/semgrep:1.168.0`) vs .github/workflows/ci.yml:543 (`semgrep/semgrep@sha256:59fbed61…`)` · security · CONFIRMED · safe_cleanup=false · L16-F4

sast.yml executes a third-party container resolved by tag on every push and PR with a `contents: read` GITHUB_TOKEN on a private clinical repository. The tag can be re-pushed upstream; ci.yml's ingestion-sast job already demonstrates the immutable form. The job is `continue-on-error: true`, so its verdict never blocks — but the code still runs with repository read access. **Impact:** Arbitrary code with read access to the private source tree on every CI run; no merge-gate effect. Defence-in-depth gap only; SECURITY.md:59-60 cites this workflow as the repo's static-analysis control (see stale_docs). **Fix:** Pin sast.yml to the same `semgrep/semgrep@sha256:…` digest ci.yml uses (or resolve one for 1.168.0 with scripts/resolve-oci-image-digest.mjs) and let the Dependabot docker group bump both together. **Skeptic:** Line reads as claimed; no gate pins the advisory job's image. Trigger needs an upstream re-tag/Docker Hub compromise and the token is read-only on a private repo, so exposure is source-read only — defence-in-depth, Low. Fix (digest-pin) is safe; the existing digest in ci.yml may not correspond to 1.168.0, so resolve one for that tag rather than …
