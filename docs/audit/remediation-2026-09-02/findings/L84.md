**L84 · Generated site map claims robots.txt disallows indexing of /mockups; robots.txt allows everything and the README says the opposite** — `docs/site-map.md:1390 (source scripts/generate-site-map.ts:713); src/app/robots.ts:1-6; src/lib/crawler-policy.ts:26-31; mockups/README.md:193-196` · docs-drift · CONFIRMED · safe_cleanup=true · L11-F3

generate-site-map.ts:713 emits "production returns 404 and `robots.txt` disallows indexing". `PRIVATE_APP_ROBOTS_TXT` is `{ userAgent: '*', allow: '/' }` — deliberately permissive so crawlers can observe the per-response `X-Robots-Tag: noindex, nofollow` set in next.config.ts for `/mockups/:path*` (#4XBMMR resolution, tests/mockup-crawler-policy.test.ts). The README (:195) documents that correctly; the generated site map contradicts it. The same line also says every /mockups route 404s, omitting the four developer-gated subtrees. **Impact:** A governance statement about crawler exposure that is false; the actual control (X-Robots-Tag) is stronger than the doc implies, so no exposure — but the site map is regenerated by `npm run sitemap:update`, so fixing the doc without fixing the generator re-introduces the error. **Fix:** In scripts/generate-site-map.ts:713 replace with: "`/mockups/*` prototype routes 404 in production (developer-gated subtrees excepted); `robots.txt` allows crawling and responses carry `X-Robots-Tag: noindex, nofollow`." Then `npm run sitemap:update`. **Skeptic:** The generator string is literally false against robots.ts and contradicts the README fixed under #4XBMMR; the finder is right that fixing docs/site-map.md alone is undone by `sitemap:update`. #4XBMMR is resolved and its resolution text names only next.config.ts, mockups/README.md and the crawler test, so this is a residual, not a duplicate. Low: … **Merged at Stage 5:** L107 (L23-F3) — Same generated site-map robots/404 caveat string.
