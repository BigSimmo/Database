**L42 · Anonymous callers can drive OpenAI-backed generation with only a per-IP-hash limit and no aggregate anonymous ceiling** — `src/app/api/answer/route.ts:93-102; src/app/api/answer/stream/route.ts:314-332; src/app/api/clinical-ask/stream/route.ts:173-181; src/app/api/speech/transcribe/` · security · PLAUSIBLE · safe_cleanup=false · L23-F7

The answer, streaming answer, Clinical Ask and speech-transcription routes accept anonymous requests (they use publicAccessContext / getOptionalAuthenticatedUser, not requireAuthenticatedUser). Limits are per hashed client IP (answer 6/min, clinical_ask 4/min) and all callers with no trusted forwarding header share one `unknown-ip` bucket. Nothing enforces a total anonymous request or spend budget; SPEND_ALERT_DAILY_USD (health-response.ts:80-89) only alerts. **Impact:** Provider spend and OpenAI quota exhaustion for the clinician's own use; no data exposure (anonymous scope is the public corpus only). **Fix:** Add a global anonymous bucket (e.g. all-anonymous answer calls per hour) alongside the per-IP one, or require sign-in for generation while leaving retrieval public; document the chosen posture next to the rate-limit table. **Skeptic:** The absence of a global anonymous ceiling is verified and the per-IP design is intentional (finder acknowledges). Whether a rotating-IP caller can actually exhaust quota depends on runtime preconditions not establishable offline (Railway edge protections, OpenAI org limits, the alerting path), so PLAUSIBLE rather than CONFIRMED. Impact is …
