**L91 · CODEOWNERS lists RAG paths that no longer exist and does not name the real `src/lib/rag/` tree** — `.github/CODEOWNERS:26-28` · docs-drift · CONFIRMED · safe_cleanup=true · L16-F7

`/src/lib/rag.ts` is absent and `/src/lib/rag-*.ts` matches no file (the stack moved to `src/lib/rag/` in #994, the same move that bit ci-change-scope per #SDQSFD). The protected tree is covered only by the `*` catch-all, so the file's stated purpose — routing review on high-risk surfaces as collaborators join — would not apply to the RAG directory. **Impact:** None today (single owner); wrong review routing later. Also `/scripts/audit-source-governance.ts` is duplicated by `/scripts/*governance*.ts`. **Fix:** Replace lines 26-27 with `/src/lib/rag/ @BigSimmo` and keep `/src/lib/retrieval-*.ts`, `/src/lib/*search*.ts`. **Skeptic:** Lines read as claimed; the two RAG patterns match nothing and the directory is covered only by the catch-all. No effect while single-owner, so Low docs-drift. Fix (replace two lines with `/src/lib/rag/`) touches no protected surface.
