**L85 · mockups/README.md orientation text describes the pre-2026-08-09 bundle gate, a wrong folder count, and a shell-suppression policy the layout no longer follows** — `mockups/README.md:376; mockups/README.md:17; mockups/README.md:168-176; src/app/mockups/mockups-layout-client.tsx:11-134; src/app/mockups/layout.tsx:21; src/pro` · docs-drift · CONFIRMED · safe_cleanup=true · L11-F6

(a) README:376 says `check:bundle-budget` "totals every built chunk, mockups included, and main sits at roughly +9.4% against a 10% tolerance" — false since PR #1779 split production from mockups (bundle-budget.json has separate `production`/`mockups` buckets). (b) README:17 "84 folders" — `find src/app/mockups -mindepth 1 -maxdepth 1 -type d | wc -l` = 79. (c) README:172-176 says only Favourites/Tools mockups hide the composer and that a standalone mockup should be moved outside `/mockups`; mockups-layout-client.tsx now carries 38 route-specific suppressions and two in-layout bypasses … **Impact:** Wrong mental model of the gate (leads to needless route deletions like the breadcrumb-header removal the paragraph itself describes) and of the shell contract; no runtime effect. **Fix:** One-line edits: README:376 → 'the separate `mockups` scratch budget (25% tolerance)'; README:17 → drop the number; README:172-176 → point at mockups-layout-client.tsx as the suppression registry and note the two bypasses; layout.tsx:21 and proxy.ts:128-129 → 'the developer-gated subtrees in … **Skeptic:** Every sub-claim (a)-(e) checks out against the quoted lines. No ledger row covers mockups/README orientation drift (#4XBMMR covered only the indexing paragraph). Low: no runtime effect, no safety/privacy claim. The proposed one-line edits are safe; note the README is prose, not a gate-parsed file.
