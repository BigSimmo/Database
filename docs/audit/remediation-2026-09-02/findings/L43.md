**L43 · Three sibling administrator ingestion routes — including the mutating retry POST — have no rate limit, unlike ingestion/quality** — `src/app/api/ingestion/batches/route.ts:43-58; src/app/api/ingestion/jobs/[id]/retry/route.ts:23-31; src/app/api/jobs/route.ts:44-56 (all requireAuthenticatedUse` · security · CONFIRMED · safe_cleanup=false · CRIT-F7 · corroborates L10-F9 — extends it from one route to the whole admin ingestion family

Across all 60 API routes, consumeApiRateLimit appears in 10. Within the administrator ingestion family only /api/ingestion/quality is limited; /api/ingestion/batches, /api/ingestion/jobs, /api/ingestion/jobs/[id]/retry (POST that re-enqueues a job) and /api/jobs are not. The retry route is the only unlimited mutating admin endpoint. **Impact:** Admin-only, so exposure is bounded to a compromised or careless administrator session; but the inconsistency shows the guard is applied by hand and unchecked (no gate covers rate limiting the way check:owner-scope covers owner_id). **Fix:** Add the same consumeApiRateLimit block ingestion/quality uses to the three routes (and ingestion/jobs per L10-F9); consider a static check that every route importing requireAuthenticatedUser with administrator:true also imports api-rate-limit. **Skeptic:** The cited lines are accurate and the guard-exists search found nothing: rate limiting is applied by hand per route, no static gate or coverage test enforces it, and the sibling ingestion/quality route shows the intended pattern. The finding is a genuine extension of L10-F9 (three additional routes, one of them mutating), so not a duplicate. The …
