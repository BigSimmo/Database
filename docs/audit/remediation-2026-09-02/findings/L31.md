**L31 · The administrator gate lives only in a layout; client-side navigations between hub pages do not re-run it and the proxy does not enforce it** — `src/app/mockups/development/layout.tsx:5-7; src/proxy.ts:132-136` · security · CONFIRMED · safe_cleanup=false · L10-F2

Next.js App Router does not re-render a shared layout on soft navigation between its child routes, so after one authorised render of `/mockups/development` the layout gate is not re-evaluated for sibling page RSC fetches (`/ledger`, `/routes`, `/review-state`, ...). The proxy only marks the request with `x-developer-area` headers (proxy.ts:132-136); it does not itself check the administrator claim. A session that is signed out elsewhere or expires keeps returning sibling page payloads until a hard reload. The live-data pages (`corpus-health`, hub index count, ingestion, clinical-trust) … **Impact:** Defence-in-depth gap on repository-metadata pages only; no library or patient data path is affected because those loaders and APIs check `isAdministratorUser` per request. **Fix:** Either enforce in `src/proxy.ts` for `isDeveloperGatedPath` (it already refreshes the Supabase session; read `app_metadata.site_role` from the refreshed user and rewrite non-admins to the gate screen), or call `resolveDeveloperAccessState()` at the top of each hub page (React `cache()` dedupes the … **Skeptic:** Chain holds end to end from quoted code plus the framework's own documented layout behaviour. Residue is repository-metadata pages after a session is revoked mid-visit; corpus/ingestion data re-checks per request. Defence-in-depth only, no data path: Low. Fix_sketch (per-page `resolveDeveloperAccessState()` or proxy-side check) touches nothing …
