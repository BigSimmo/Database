**L79 · Route documents still describe the pre-sandbox world: mode-map says the role design 'has not been built yet' and lists 8 of 17 routes; ward-flow-context lists `/ward-management` paths as 'Routes today'** — `docs/ward-management-mode-map.md:6 and 41-50; docs/ward-flow-context.md:163-166; docs/ward-flow-phase-3-workspace/README.md:23` · docs-drift · CONFIRMED · safe_cleanup=true · L09-F6

mode-map.md:6 'That design has not been built yet — Phase 2 is what actually retires these modes' sits four lines above its own Phase 2 update and is now false through Phase 5 (last commit b786f2f9a 2026-08-30 left the banner). Its route table (lines 41-50) names 8 routes and says 'read this document for what is currently live', but src/app/mockups/ward-flow has 17 page.tsx (tests/ward-landmarks.test.ts:127 measures '17 … 16 renderable + 1 redirect-only'); missing are ed/[edId], ward/[unitId], transport/officer, patients/[patientId], handover, escalation, search, discharges and the … **Impact:** Navigation and route-coverage reasoning starts from a map missing half the surface; the stated 'currently live' promise is what makes it misleading rather than merely old. **Fix:** Replace mode-map line 6 with 'Phases 2-5 are built; this table is the coordinator-view subset — the role and board routes are in src/components/ward-management/ward-nav.ts (WARD_VIEWS + WARD_NAV)' and add the nine missing rows; change context.md:165 to the `/mockups/ward-flow/...` paths; README:23 … **Skeptic:** Reproduced. Nuance the finder underplays: the mode-map already opens with a 'Superseded' banner, so a reader is warned — but line 6-8 then asserts the opposite ('not been built yet … currently live'), which is the actionable contradiction. ward-flow-context.md's '/ward-management' routes 404 everywhere since the sandbox move (proxy only knows …
