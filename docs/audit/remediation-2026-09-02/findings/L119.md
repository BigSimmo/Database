**L119 · knip reports ~25 unused exports across both mockup trees (triage candidates only)** — `evidence/gate-check_knip_exports.log:27-55 — e.g. src/components/care-plan/mockups/domain.ts:168 CMHT_CONTACT_SUBJECT, routes.ts:56 withQuery (export), patient-` · quality/simplification · PLAUSIBLE · safe_cleanup=false · L08-F7

`check:knip:exports` (run with --no-exit-code, so advisory) lists 19 exports in care-plan/mockups and 8 in caring-contacts/mockups with no importer. Several are clearly intentional reference constants for tests (FICTIONAL_DATA_MARKER, DESIGNATED_FICTIONAL_MOBILE_NUMBERS) or are used internally and merely over-exported (withQuery is used by carePlanRoute in the same file). This is a triage list, not a delete list, per docs/agents/dead-code-deletion.md and #800E5M (in-flight programme scaffolding must not be treated as dead). **Impact:** Hygiene only; the export surface is wider than the usage surface, which makes 'is this still needed' harder to answer for a 1,976-line reducer and a 1,440-line fixture file. **Fix:** Run `npm run check:dead-code-candidate` per symbol; drop `export` from the internal-only ones (withQuery, section-id helpers) and leave test-reference constants exported with a comment naming their consumer. **Skeptic:** The knip output is accurate and the gate is advisory by design (--no-exit-code), so nothing is broken. The finder concedes it is a triage list; withQuery in particular is consumed through carePlanRoute in tests, so the 'over-exported' reading is correct. Not a defect, kept as Low hygiene. Any deletion (as opposed to dropping an export keyword) …
