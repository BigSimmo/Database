**L62 · Cursor `repo-auditor` skill instructs the agent to 'safely remove' dead files, contradicting the repository's triage-only dead-code rule that the Claude twin enforces** — `.cursor/skills/repo-auditor/SKILL.md:19 vs .claude/agents/repo-auditor.md:22-31 and docs/agents/dead-code-deletion.md` · process · CONFIRMED · safe_cleanup=false · L20-F3

The two surfaces are the same skill with different rules. Cursor's says 'Identify and safely remove files that are truly dead and not part of route mappings...'; Claude's (updated 2026-07-28 and again 2026-09-02) says outputs are triage, 'Do not delete or move files during a review', and lists the evidence required. AGENTS.md's 'Deleting code you believe is dead' rule (added 2026-08-21, `5db9d1817`) and `check:dead-code-candidate` apply to every agent, but the Cursor copy was last touched 2026-08-13 and never picked the rule up. `check:skills` validates `.cursor/skills/*` only for two … **Impact:** An agent can delete files that are only reachable from a route, `package.json` script, migration, worker, or test — the exact class of deletion the 2026-08-31 truncation incident (`#Y30AXB`) and the dead-code rule exist to prevent. The repo's own process claims ('rules live in one place and cannot … **Fix:** Rewrite `.cursor/skills/repo-auditor/SKILL.md` §1 to the triage wording and add the 'Safety before deletion' block; add a `requireContract('.cursor/skills/repo-auditor/SKILL.md', ['Do not delete or move files during a review'])` entry in `scripts/list-database-skills.mjs` so the two surfaces cannot … **Skeptic:** Divergence confirmed and the skill gate demonstrably does not cover it. Downgraded to Low: the actor is the owner's own Cursor session (privileged), AGENTS.md — which Cursor also loads and which outranks a skill per the external-skill-precedence rule — carries the triage-only rule, and any deletion still passes the PR gates …
