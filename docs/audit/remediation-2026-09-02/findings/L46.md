**L46 · ClinicalDashboard.tsx sits at 4100 of its 4140-line maintainability budget (40 lines of headroom)** — `src/components/ClinicalDashboard.tsx (4100 lines); scripts/check-maintainability-budgets.mjs:7` · process · CONFIRMED · safe_cleanup=false · L03b-F7

The hotspot budget gate passes with 99% of the ceiling consumed. The budget was last tightened from 4272 (2026-07-19, 38beb57e1) to 4140; the file has since regrown to within 40 lines of it, so the next small dashboard change will fail `check:maintainability-budgets` and invites a budget bump instead of an extraction. **Impact:** Either a red verify:cheap for an unrelated change or a silent widening of the budget; the file already hosts search orchestration, scope persistence, admin gating and mode routing in one module. **Fix:** Plan one extraction (e.g. the private-scope persistence block around lines 600-650 and 1830-2180, or the admin/library-health target handlers) before the next feature lands; do not raise the number. **Skeptic:** Factual headroom observation; the gate is behaving as designed. Low process. The fix sketch (extract before the next feature; do not raise the number) respects the gate's own comment.
