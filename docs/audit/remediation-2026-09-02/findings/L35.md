**L35 · scripts/design-sync.mjs installs five unpinned npm packages with --package-lock=false and executes a shell command string read from a JSON config, with no npm-script entry or doc mention of the supply-chain shape** — `scripts/design-sync.mjs:27 (DS_SYNC_PACKAGES), :106-108 (npm install --no-save --package-lock=false), :82-87 (spawnSync(buildCmd, {shell:true}))` · security · CONFIRMED · safe_cleanup=false · L15-F6

The script downloads esbuild, ts-morph, @types/react, @tailwindcss/cli and geist at whatever version the registry serves that day (no version, no lockfile), then runs `.design-sync/config.json`'s buildCmd through the shell. The config is committed, so today's command is benign, but the pattern is exactly what check:github-actions / check:installed-lock-parity exist to prevent elsewhere, and this is the only script in scripts/** that uses shell:true on a non-literal string (build-analyze.mjs's shell:true is on a fixed `npm run build`). **Impact:** Supply-chain exposure limited to the local workstation (.ds-sync is gitignored and not part of the production build), plus a registry-network call that falls outside the repo's pinned-dependency posture; not a production or patient-data path. **Fix:** Pin DS_SYNC_PACKAGES to exact versions (or read them from package.json devDependencies) and pass buildCmd as an argv array instead of a shell string; add the script to docs/scripts-index.md's design-system row with its network behaviour stated. **Skeptic:** Lines say what the finder says. No pinning or lock parity guard covers an ad-hoc `--prefix` install, and the config file is repo-controlled so the shell string is only as trusted as the tree. Local-workstation tooling with no production or patient-data path; Low. Fix (pin versions, argv array) is safe.
