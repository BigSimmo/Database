**L96 · README.md and process-hardening.md describe verify:cheap as six gates plus lint/typecheck/test, and README says static-pr 'always runs' action-pin, lint and typecheck — all three claims are stale** — `README.md:244-246, 260-261; docs/process-hardening.md:372` · docs-drift · CONFIRMED · safe_cleanup=true · L18-F5

README.md:244-246 lists `verify:cheap` as 'check:runtime + check:github-actions + sitemap:check + brand:check + check:type-scale + check:icon-scale + lint + typecheck + test'; docs/process-hardening.md:372 lists 'check:runtime, sitemap:check, lint, typecheck, and unit tests'. package.json `verify:cheap:internal` runs 36 static gates before lint/typecheck/test (check:gate-manifest: 'all 39 verify:cheap gates … 36 static gates documented consistently'), which CLAUDE.md:98 states correctly. README.md:260-261 says `static-pr` 'always runs runtime, action-pin, CI-scope, format, lint, and typecheck … **Impact:** Under-states what verify:cheap covers (so a reader may run redundant checks or misjudge runtime) and over-states CI coverage on docs/workflow-only PRs (lint/typecheck do not run there); two of the four authoritative summaries disagree with the gate manifest. **Fix:** README.md:244: '36 static/consistency gates + lint + typecheck + test (see `npm run check:gate-manifest`)'; README.md:260: 'always runs a small baseline (runtime, lock parity, CI-scope, verification-plan, diff-integrity, changed-file format) and selects the rest by change scope'; … **Skeptic:** All three claims are stale and no gate covers README or process-hardening (gate-manifest reads CLAUDE.md only, which is correct). Concerns gate descriptions but the authoritative files are right and the README understates rather than fabricates coverage; Low. Fix is prose; keep the gate counts consistent with check:gate-manifest wording.
