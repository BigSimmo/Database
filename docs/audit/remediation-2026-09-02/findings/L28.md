**L28 · API CSRF guard blocks only Sec-Fetch-Site: cross-site; same-site origins and clients without Fetch Metadata pass with no Origin check** — `src/proxy.ts:101-115` · security · CONFIRMED · safe_cleanup=false · L01-F3

State-changing /api/* requests are rejected only when `sec-fetch-site === "cross-site"`. A request with `same-site` (any sibling subdomain of the app's registrable domain) or with no Sec-Fetch-Site header at all (non-Fetch-Metadata user agents) is allowed, and there is no Origin/Host comparison anywhere (`grep headers.get("origin") src` hits only local-project-guard.ts). **Impact:** Practical CSRF risk is low because @supabase/ssr writes session cookies with `sameSite: "lax"` (node_modules/@supabase/ssr/dist/main/utils/constants.js:6), so cross-site POSTs do not carry the session; the guard is defence-in-depth only and its coverage is narrower than a reader of the 403 message … **Fix:** Additionally reject when an Origin header is present and its host differs from the request host (allowing absent Origin for non-browser clients), and add a `same-site` test case. **Skeptic:** Traced exactly as stated: only the literal `cross-site` is rejected; `same-site` and absent Fetch Metadata pass and no Origin/Host comparison exists anywhere. Practical CSRF is mitigated by SameSite=Lax session cookies, and the only same-site attacker would need a sibling subdomain of psychiatry.tools, so this is defence-in-depth narrower than …
