**L60 · Report-only sweep scripts reach the network by default** — `scripts/sweep-branch-ledger.mjs:13 and ~130-215; scripts/sweep-merged-branches.mjs:30-32` · process · CONFIRMED · safe_cleanup=false · L19-F8

sweep-branch-ledger.mjs advertises itself as REPORT ONLY but performs `git fetch` unless `--no-fetch` is passed (and refuses to report if the fetch refspec is narrow and no fetch happened); sweep-merged-branches.mjs runs `git remote prune origin` unless `--no-prune`, and in its default dry-run. Under the API-and-provider confirmation boundary a GitHub round-trip needs explicit confirmation, so the safe default is inverted for the exact tools an agent reaches for during a read-only audit. **Impact:** Unconfirmed provider access; no data risk. **Fix:** Flip to opt-in `--fetch` / `--prune`, and print the exact command the user must confirm when omitted. **Skeptic:** Both defaults are network-on as claimed; the report-only mode still fetches/prunes from origin. Low: read-only git traffic to GitHub, no data risk, and both are operator utilities rather than gates. Fix sketch (opt-in flags) would not break any gate; sweep-branch-ledger's 'refuse to report on narrow refspec without a fetch' logic (lines 206-230) …
