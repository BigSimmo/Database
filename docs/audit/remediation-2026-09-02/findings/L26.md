**L26 · npm run clean:artifacts deletes a tracked repository file: docs/archive/staging-tenancy-evidence-29795051547/ is git-tracked but matched by the script's rimraf glob and by .gitignore** — `package.json:263 (clean:artifacts); .gitignore:167; docs/archive/staging-tenancy-evidence-29795051547/staging-tenancy-evidence.json` · data-integrity · CONFIRMED · safe_cleanup=false · L15-F5

`clean:artifacts` is `npx rimraf test-output*.txt docs/archive/staging-tenancy-evidence-*`. rimraf 6.1.3 does not expand globs itself, but npm runs the line through sh, which expands `docs/archive/staging-tenancy-evidence-*` to the tracked directory; rimraf then removes it recursively. The directory's one file was committed on 2026-07-23 (#1088) and the ignore rule for that pattern was added the next day (ae54de2b1, 2026-07-24), so it is simultaneously tracked, ignored and targeted for deletion. No doc or script references the directory. **Impact:** A tracked evidence document disappears from the working tree and shows up as an unexpected deletion in the next commit; because the path is ignored, `git status` porcelain still reports it (tracked files ignore .gitignore) but re-adding it later needs `git add -f`. Recoverable from history, so Low. **Fix:** Decide whether the evidence file is an artifact (then `git rm --cached` it so ignore + clean agree) or a document (then move it out of the ignored/cleaned glob and reference it from the relevant runbook). **Skeptic:** Chain holds by trace: sh expands `docs/archive/staging-tenancy-evidence-*` to the tracked directory, rimraf removes it recursively as a literal path. Proof deliberately not executed because it would delete a tracked file (read-only mandate). Recoverable from history and only reachable by running a clean script, so Low. Fix is a git-metadata …
