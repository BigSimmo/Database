**L38 · @claude workflows gate only on `user.type != 'Bot'`; the header's 'collaborator' claim is not enforced at workflow level** — `.github/workflows/claude.yml:3 (comment), :27-38 (`if:`), :39-44 (contents/pull-requests/issues/id-token write); .github/workflows/claude-backlink.yml:29-39` · security · PLAUSIBLE · safe_cleanup=false · L16-F9

The trigger admits any non-bot account that can comment on an issue/PR; there is no `author_association` check. Today the repository is private, so commenters are members/collaborators, and claude-code-action's own default rejects actors without write permission — those two facts, not the workflow, are what keep `contents: write` + `id-token: write` from being reachable by a read-only or outside account. claude-backlink.yml additionally fires on plain issues, not just PRs. **Impact:** A comment author outside the maintainer's trust boundary could start a Claude run that reads CI results and pushes to the PR branch; prompt text in the comment and PR body is the model's instruction. Defence-in-depth only under the current private, single-maintainer configuration. **Fix:** Add `contains(fromJSON('["OWNER","MEMBER","COLLABORATOR"]'), github.event.comment.author_association)` (and the review equivalent) to both `if:` blocks, and reword claude.yml:3 to match whichever gate is enforced. **Skeptic:** Workflow-level gap is real as quoted. The neutralising control is claude-code-action's own default actor-permission check (rejects actors without write permission unless allowed_non_write_users is set); that is a third-party behaviour I cannot quote offline, hence PLAUSIBLE rather than CONFIRMED or REFUTED. Under the current private, …
