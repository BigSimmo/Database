**L40 · MCP deny rules are spelled for the `.mcp.json` server name (`mcp__supabase__*`) and do not cover connector-surfaced tools that present as `mcp__Supabase__*`** — `.claude/settings.json:34-44` · security · PLAUSIBLE · safe_cleanup=false · L20-F6

The deny list blocks `mcp__supabase__execute_sql`, `apply_migration`, `deploy_edge_function`, branch/merge/reset/pause/restore. `docs/claude-cloud.md:178-185` recommends a claude.ai connector over the `.mcp.json` entry for cloud sessions; in this very session the connector's tools are listed as `mcp__Supabase__execute_sql`, `mcp__Supabase__apply_migration`, `mcp__Supabase__merge_branch` etc. (capital S). If Claude Code matches MCP rule names case-sensitively (not verifiable offline), the write-capable tools the file means to hard-deny are only in the default `ask` state when they arrive via … **Impact:** Loss of the one hard stop on live-database mutation from an AI session; merging a migration is production deploy per AGENTS.md, so an unintended `apply_migration` reaches the clinical database directly. **Fix:** Add capitalised twins (`mcp__Supabase__execute_sql`, ...) to `permissions.deny`, or a wildcard shape if supported (`mcp__Supabase__*` for the write tools), and add a test asserting both spellings are denied. **Skeptic:** The shape mismatch is real and observable in this very session; the recommended cloud route surfaces tools under a name the deny list does not spell. Whether Claude Code's MCP rule matcher is case-sensitive (and whether the deny list is even consulted for connector-provided servers) could not be tested offline, so PLAUSIBLE. Low: in this session …
