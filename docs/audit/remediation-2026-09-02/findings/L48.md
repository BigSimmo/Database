**L48 · Two open P2/P3 rows in the outstanding-issues ledger describe defects that were fixed on main weeks ago** — `docs/outstanding-issues.md:111 (#Z61JRT) and :125 (#N8B176); src/app/api/images/signed-urls/route.ts:94-103; src/lib/documents/is-public-document.ts:58` · process · CONFIRMED · safe_cleanup=true · L04-F4 · previously-reported (2026-08-23 repo-wide audit rows #Z61JRT, #N8B176) — both now stale

#Z61JRT says the batch image endpoint calls `getPublicUrl` against a private bucket; the route now signs every image uniformly (comment at lines 94-96 explains the change; `getPublicUrl` removed by #2399 on 2026-08-27). #N8B176 says `isPublicDocument` ranks metadata aliases above `owner_id`; the helper now requires `owner_id === null && metadata.public_corpus === true` (#2323) and, additionally, has no importer outside its own module. **Impact:** Wasted re-investigation and a queue that misreports two closed privacy items as open; the queue snapshot (`70 open`) is inflated. **Fix:** Close both via `npm run issues:done` with the fixing PR numbers (#2399, #2323); record `isPublicDocument` as a dead-code triage candidate ('no importer found') rather than deleting it. **Skeptic:** Both rows describe code that no longer exists on main; the ledger is stale. Not DUPLICATE: the rows are the subject of the finding, not a prior record of it. Caveat: three unapplied inbox requests reference these IDs, so a closure may already be queued pending `issues:reconcile` — the fix should check those before adding another request. Low: …
