**L118 · src/lib/supabase/database.types.ts is 10 days stale: five tables and fifteen RPCs are missing, and code is already casting around it** — `src/lib/supabase/database.types.ts (last regenerated 2026-08-23, commit 883f1007a); src/lib/ingestion-mutation-safety.ts:186-188; docs/codebase-index.md:237` · quality/simplification · CONFIRMED · safe_cleanup=false · L06-F3

Comparing the generated types against supabase/schema.sql: tables api_rate_limit_subjects, document_title_words, indexing_v3_agent_jobs, document_corpus_access_state, document_corpus_access_snapshots have no Row type; RPCs retrieval_owner_matches, search_schema_health, schema_drift_snapshot, migration_history_versions, set_document_corpus_access_mode, match_document_chunks_text_scoped, match_document_index_units_hybrid_scoped, consume_api_subject_rate_limit, invoke_indexing_v3_agent, update_indexing_v3_agent_job_status, backfill_legacy_index_health_batch, purge_expired_rag_query_misses, … **Impact:** Type safety on the rate-limit path (api-rate-limit.ts:322 calls consume_api_subject_rate_limit untyped), the setup-status health probe and the ingestion mutation guard is lost; ingestion-mutation-safety.ts:186 drops to `args.supabase as unknown as SupabaseClient` to read indexing_v3_agent_jobs. … **Fix:** Regenerate the types from the replayed schema in the existing CI 'Migration replay' job (local `supabase gen types typescript --local` — offline, no provider) and add a freshness comparison next to the drift-manifest freshness step, or a unit test asserting every schema.sql table/function name … **Skeptic:** Verified by direct comparison; type coverage lost on the newer tables/RPCs and code already casts around it. Low. Fix (regenerate with `supabase gen types typescript --local` in the offline Migration replay job, plus a freshness check) is offline and touches no never-touch surface; a regenerated types file is a large diff and should be committed …
