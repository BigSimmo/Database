**L30 · One env flag (NEXT_PUBLIC_MOCKUPS_ENABLED=true) switches the administrator gate off in production for all four developer-gated subtrees, while the proxy needs two flags to open the rest of /mockups** — `src/components/developer-area/developer-area-gate.tsx:19-21; src/lib/env.ts:452-460; src/proxy.ts:266-284` · security · CONFIRMED · safe_cleanup=false · L10-F1

> **Correction (Stage 5, blue team):** Regrade to Medium: a single build-time flag disables the administrator gate for all four gated subtrees in production (developer-area-gate.tsx:19-21; env.ts:452-459 has no production refinement; instrumentation.ts guards LOCAL_NO_AUTH and PLAYWRIGHT_OFFLINE_MODE but not this flag; production-readiness.ts never checks it). The report itself schedules it 'this week' and proposes P2; Low contradicts §2's defence-in-depth rule.

`DeveloperAreaGate` returns `<>{children}</>` whenever `mockupsEnabled()` is true, and `mockupsEnabled()` is true in production as soon as `NEXT_PUBLIC_MOCKUPS_ENABLED === "true"`. The proxy's blanket production block (`shouldBlockProductionMockups`) deliberately requires BOTH `PLAYWRIGHT_OFFLINE_MODE=true` and `NEXT_PUBLIC_MOCKUPS_ENABLED=true`, but it lets `isDeveloperGatedPath` paths through unconditionally on the assumption that the layout gate will run. So the single public build flag, set alone, opens `/mockups/development/**`, `/mockups/caring-contacts/**`, `/mockups/care-plan/**` and … **Impact:** Without a sign-in, a visitor gets the hub index, the task ledger (70 open items with detail prose naming internal failures and PR numbers), 2,665 review records, the route/documentation inventory, the clinical-trust cockpit UI and the three synthetic-patient prototypes. Live library data stays … **Fix:** In `DeveloperAreaGate`, bypass only when `process.env.NODE_ENV !== "production"` (or mirror the proxy's two-flag rule) so the opt-in flag never disables administrator auth in a real deployment; add a `check:production-readiness` assertion that `NEXT_PUBLIC_MOCKUPS_ENABLED` is unset/false when … **Skeptic:** Lines say exactly what the finder claims and the two guards in the tree encode the disagreement instead of resolving it. Trigger requires an operator (Railway variable write) to set a build-time public flag on the production service, so per the re-grade rule this is a privileged-actor misconfiguration trap, not an exposed hole today: Low. Fix …
