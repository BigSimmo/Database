**L131 · Two pdfjs-dist majors ship in the production tree, three ZIP implementations, and `playwright-core` is imported without being declared** — `package.json dependencies (pdf-parse ^2.4.5 + pdfjs-dist ^6.2.108; jszip + exceljs); scripts/capture-chrome-parity.ts:21; tests/ui-route-coverage.spec.ts:2, tes` · quality/simplification · PLAUSIBLE · safe_cleanup=false · L17-F6

pdf-parse 2.4.5 pins `pdfjs-dist 5.4.296` (nested at node_modules/pdf-parse/node_modules/pdfjs-dist) while the app imports pdfjs-dist 6.2.108 directly — two copies of a large PDF engine in every production image, and two different parsers can disagree on the same upload. ZIP handling exists three times (jszip direct; archiver + unzipper via exceljs, both forced to newer majors by overrides). `playwright-core` is imported in five files but declared nowhere; it resolves only because `playwright` pins it exactly. Also: `@next/env` and `eslint-config-next` are exact-pinned at 16.3.3 while `next` … **Impact:** Image size and duplicated attack surface for untrusted-upload parsers; a transient next/eslint-config-next minor skew between two Dependabot PRs; a type-only import that could break when hoisting changes. **Fix:** In the document-library upgrade group: check whether pdf-parse ≥2.x can accept pdfjs-dist 6 (or move extraction to one engine); declare `playwright-core` as a devDependency at the same version `playwright` pins, or import `Route` from `playwright`; align `@sentry/node` floor with `@sentry/nextjs`; … **Skeptic:** The duplicated pdfjs-dist majors and three ZIP implementations are facts in the lock. Two of the finder's sub-claims are weaker than stated: (a) the playwright-core 'undeclared' point was already judged NOT A DEFECT in #087 and knip's dependencies gate passes, and because `playwright` pins playwright-core exactly the hoisting cannot skew between …
