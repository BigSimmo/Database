**L52 · Nine landed Ward Flow plans still carry 441 unchecked task boxes, so the dead-code gate pins 140 of 189 ward exports as 'in-flight scaffolding' indefinitely** — `docs/superpowers/plans/2026-08-19-ward-flow-phase-3-role-screens.md:1 (68 unchecked) and 8 sibling plans; scripts/check-dead-code-candidate.mjs:208-225,320-323` · process · CONFIRMED · safe_cleanup=false · L09-F1

`planContractHits` (check-dead-code-candidate.mjs:208-225) counts `- [ ]` lines in any docs/superpowers/plans|specs file naming a symbol and refuses (line 323) when open > 0. Phases 1-5 of Ward Flow have all landed (docs/ward-flow-roadmap.md, tests/ward-landmarks.test.ts measures 17 routes on main), yet the nine ward plan files hold 441 unchecked boxes (35/61/43/55/68/71/37/23/48). My inventory script found 140 of the 189 `export` declarations under src/components/ward-management are named in at least one of those plans, so any future cleanup of them is refused by the gate as scaffolding, … **Impact:** The repository's stated dead-code policy (docs/agents/dead-code-deletion.md) cannot be satisfied for Ward Flow because the plans lie about being in flight; the sandbox's 13,364 lines can only grow. Reviewers reading the plans are also sent to paths that no longer exist. **Fix:** Tick the boxes (or add a 'Status: landed <date>' line and teach `planContractHits` to treat it as complete) in the nine ward plans whose phases have shipped; correct or annotate the moved paths in the same edit. Then the gate downgrades those hits to warnings (line 325) and the knip-flagged exports … **Skeptic:** Mechanism and counts reproduce and my proof run shows a shipped Phase 2/3 symbol refused as 'in-flight'. But the pinning is the gate's documented design, and the gate runs only when a maintainer invokes it, so the real defect is stale plan bookkeeping in nine landed plans, not a gate bug — the finder's own status note concedes #800E5M built this …
