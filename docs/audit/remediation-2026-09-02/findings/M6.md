### M6 · The P1 pilot-blocking hazard record (#1S81R8) points at docs/caring-contacts/hazard-log.md, which exists in no commit reachable from this repository; the documented recovery command names a commit that does not exist

`docs/outstanding-issues.md:123 (#1S81R8); docs/caring-contacts/phase-1-handoff.md:109-114` · privacy/clinical · **CONFIRMED** · safe_cleanup=false · lane L07 (L07-F3)

> **Correction (Stage 5, red team):** Merge into one finding. The full reachable history is 6,889 commits (git rev-list --all --count), not 5,489 (HEAD first-parent only); the M6 proof used --all so the claim stands under the larger number. docs/caring-contacts has 24 entries, not 23.

- **Defect:** #1S81R8 (P1) says `docs/caring-contacts/hazard-log.md records H-00, H-04 and H-05`. The file is absent from the working tree; `git log --all --name-only | grep -i hazard-log` finds nothing across 6,881 commits. phase-1-handoff.md says the six governance documents (hazard log, evidence brief, referral feasibility, outreach drafts, message review pack, demonstration script) 'are intact in history: git checkout 32d408c2f -- docs/caring-contacts/'; `git cat-file -t 32d408c2f` → fatal: not a valid object name. H-00/H-04/H-05 are not described anywhere else under docs/ (grep for 'H-00' and 'safety officer' hits only the ledger row and two handoff mentions).
- **Trigger:** Anyone trying to read, update, or sign off the hazards that block a real-patient pilot.
- **Impact:** The repository's only P1 clinical-governance blocker for this workspace rests on a document the repository cannot produce. Sign-off, review by a lived-experience panel, and the Aboriginal health review cannot be tracked against a record nobody can open; docs/clinical-hazard-analysis.md covers PsychSift answers, not this workspace.
- **Proof:** git cat-file -t 32d408c2f → 'fatal: Not a valid object name'; find docs/caring-contacts -iname '_hazard_' → no result; git log --all --format=%h --name-only | grep -i hazard-log → empty.
- **Fix sketch:** Recover the six documents from the external handoff bundle (`D:\Repos\caring-contacts-handoff-2026-08-20\`, named in PROGRESS-LEDGER §3) and commit them, or rewrite #1S81R8 and phase-1-handoff.md:109-114 to state where the hazard log actually lives. Add the hazard log path to docs/README.md so docs:check-links guards it.
- **Skeptic (CONFIRMED):** Every factual claim reproduces offline in a full (non-shallow) clone with all origin refs: the recovery commit does not exist, the file never existed in any fetched history, and the only in-repo description of H-00/H-04/H-05 is the ledger row that points at the missing file. This is not a duplicate of #1S81R8 — that row is about the hazards themselves; this finding is that the row's evidence base is unreachable, which is new. The most likely mechanism (feature branch squash-merged, docs never committed to the branch that landed, or the commit lives only on the D:\ machine) cannot be distinguished offline, but every mechanism yields the same repository state. Re-grade: Medium — no patient data and no live pilot, so not High, but the P1 clinical-governance blocker for a suicide-aftercare workspace cannot be read, reviewed or signed off from this repository, and the handoff document actively misdirects a recoverer. Fix sketch (recover from the external bundle or rewrite the row and handoff) touches nothing load-bearing; adding the path to docs/README.md would make `docs:check-links` fail until the file exists, which is the intended effect.
- **Merged at Stage 5:** M27 (L19-F2, lane L19) "The P1 clinical-safety row #1S81R8 points at a hazard log and message-review pack that have never existed in the repository" — `docs/outstanding-issues.md (open row #1S81R8, Detail cell); docs/caring-contacts/phase-2a-sdd-archive/task-1-brief.md:128; docs/caring-contacts/phase-2b-sdd-archive/task-p-brief.md:35`; its skeptic (CONFIRMED): Every claim checks out: the P1 row's next action and the message-copy approval gate point at artefacts with no trace in the repository or its history. No OPEN ledger row or prior audit mentions hazard-log/message-review-pack (grep of docs/audit/*.md → none). Kept Medium rather than Low because the false claim concerns … Same missing hazard log and review pack behind P1 #1S81R8, seen from the Caring Contacts lane and the ledger lane.
