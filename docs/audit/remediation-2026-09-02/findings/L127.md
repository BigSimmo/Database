**L127 · Six byte-identical copies of `expectNoPageHorizontalOverflow` and six of `gotoApp` live inline in specs instead of tests/helpers** — `tests/ui-stress.spec.ts, tests/ui-document-image-status-mockup.spec.ts, tests/ui-tools-show-all.spec.ts, tests/ui-smoke.spec.ts, tests/ui-accessibility.spec.ts,` · quality/simplification · CONFIRMED · safe_cleanup=true · L14-F5

md5sum of the awk-extracted overflow helper is identical (0bae7b2e…) in all six files. A tolerance change (the `<= 2` px allowance) must be edited six times, and the copies are exactly the sort of region a whole-file rewrite tool re-emits (the #Y30AXB failure mode). **Impact:** Drift between copies produces inconsistent phone-overflow verdicts across journeys; low but repeated maintenance cost. **Fix:** Move both into tests/helpers/phone-scroll.ts (already a shared Playwright helper module) and import; no behaviour change. **Skeptic:** The overflow-helper claim reproduces exactly (six byte-identical copies). The `gotoApp` claim is overstated: only three of the six named copies are identical, the other three already diverge, which is itself mild evidence of the drift the finder warns about but means a single shared helper needs a behavioural reconciliation first. Kept at Low …
