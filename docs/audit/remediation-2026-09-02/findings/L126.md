**L126 · Offline page still shows the legacy 'KB' mark and global-error uses lang="en" not en-AU** — `public/offline.html:170 (`<div class="mark">KB</div>`); src/app/global-error.tsx:24` · quality/simplification · CONFIRMED · safe_cleanup=false · L13-F8

After the PsychSift rename the offline shell's visual mark is still the two-letter 'KB' glyph, and the last-resort error boundary renders `<html lang="en">` while every other document is `en-AU` (layout.tsx:123, offline.html:2, sw.js emergency page). **Impact:** Cosmetic brand inconsistency; screen readers switch pronunciation dictionaries on the error page. Note offline.html edits require a CACHE_VERSION bump (tests/pwa-manifest.test.ts:110-122). **Fix:** Replace the mark text (or drop it) and bump CACHE_VERSION in the same change; set lang="en-AU" in global-error.tsx. **Skeptic:** Both facts verified (mark is at line 166, not 170 — immaterial). The rename commit itself edited offline.html and left the 'KB' glyph, which is aria-hidden so screen readers are unaffected; the lang mismatch is a genuine but cosmetic a11y inconsistency on a last-resort boundary. Low. The finder's own caveat is correct: tests/pwa-manifest.test.ts …
