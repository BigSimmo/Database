**L18 · External authority matching is exact-hostname while the provider filter is domain-wide, so subdomain results are silently discarded; governance doc promises path-prefix and subdomain controls that do not exist** — `src/lib/clinical-ask/authority-registry.ts:126-130,141-145; src/lib/openai.ts:120-124; docs/clinical-governance.md:87-89` · correctness · PLAUSIBLE · safe_cleanup=false · L12-F8

`authorityForUrl` requires `authority.domain === url.hostname` after stripping only a leading `www.`; OpenAI `web_search.filters.allowed_domains` (openai.ts:123) matches subdomains, so results on e.g. `ww2.health.wa.gov.au` or `www.nice.org.uk/…` subhosts are returned by the provider, paid for, and then dropped locally. The registry has no `permitted path prefixes` field although docs/clinical-governance.md:87-88 says every registry entry names them, and no subdomain test exists (tests/clinical-ask-authority-registry.test.ts covers scheme, credentials, www/tracking only). **Impact:** Wasted web-search spend and spurious Evidence Gaps for exactly the WA authorities the registry was built for; a governance claim reviewers may rely on is not implemented. Whether WA Health serves clinical pages on a subhost was not verifiable offline. **Fix:** Decide the intended policy: either allow `*.domain` with a registry `allowedHosts` list, or keep exact hosts and pass the exact hostnames to the provider; add a subdomain test either way and correct the governance sentence (path prefixes are not implemented). **Skeptic:** Exact-host matching and the missing path-prefix field/subdomain test are confirmed from code and docs. Whether the provider actually returns subhost results for an apex `allowed_domains` entry (and whether the WA authorities publish on subhosts) is a provider-side runtime fact not verifiable offline — hence PLAUSIBLE. Dormant surface, wasted spend …
