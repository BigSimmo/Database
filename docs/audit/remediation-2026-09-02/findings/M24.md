### M24 · verify:pr-local's heavy-scope plan omits the three load-bearing database/tenancy guards and 12 other static gates that CI static-pr runs for the same scope

`scripts/verify-pr-local.mjs:34 (staticHeavyScripts = ["lint","typecheck","test"]) vs .github/workflows/ci.yml:337-440` · process · **CONFIRMED** · safe_cleanup=false · lane L15 (L15-F2) · corroborates docs/audit/gate-consolidation-audit-2026-09-02.md G1 (adds new evidence: the omission also covers check:owner-scope, check:function-grants, check:migration-role, sitemap:check, check:forms-pdf-manifest — not only 'generated-artifact drift')

> **Correction (Stage 5, red team):** For static_heavy the static-pr job also runs check:upload-limit-parity (ci.yml:226), check:gate-manifest (:250) and every docs/ledger step (docs:check-index/-inventory/-scripts/-links, check:branch-review-ledger, check:outstanding-issues, check:ledger-write-discipline), which verify-pr-local adds only on docs_changed. The gap is the three guards plus 14 others, not 12.

- **Defect:** For static_heavy scope CI's static-pr job additionally runs check:knip, check:maintainability-budgets, check:type-scale, check:icon-scale, brand:check, check:assets, sitemap:check, check:repo-awareness-snapshot, check:therapy-data-index, check:cross-mode-index, check:mha-act-sections, check:forms-pdf-manifest, check:design-system-contract, check:migration-role, check:function-grants and check:owner-scope. verify-pr-local.mjs selects none of these for that scope except check:repo-awareness-snapshot, yet CLAUDE.md:99 calls it the 'risk-routed PR mirror'. check:gate-manifest only compares verify:cheap:internal to CI, so nothing guards pr-local parity.
- **Trigger:** Any PR touching supabase/migrations/** or src/app/api/** (both classify static_heavy) verified locally with `npm run verify:pr-local`.
- **Impact:** A migration that opens a SECURITY DEFINER function to PUBLIC, an API route that reads an owner-scoped table without an owner filter, or a migration using a non-postgres role passes the recommended local gate green and is only caught after push by CI — the exact incident shapes check:function-grants / check:owner-scope / check:migration-role were built to stop. Since merging a migration deploys to the live clinical database within seconds, a late red here shortens the review window.
- **Proof:** Executed: `node scripts/verify-pr-local.mjs --dry-run --files supabase/migrations/20260901000000_x.sql,src/app/api/answer/route.ts` printed a 13-step plan: check:runtime, check:installed-lock-parity, format:changed, check:diff-integrity, lint, typecheck, test, check:repo-awareness-snapshot, build, eval:rag:offline, eval:rag:adversarial:offline, check:medication-interactions, check:medication-lexicon-report — no owner-scope, function-grants or migration-role. ci.yml:416-429 runs those three under `if: needs.changes.outputs.static_heavy_changed == 'true'`.
- **Fix sketch:** Add a `staticHeavyGuards` list (at minimum check:migration-role, check:function-grants, check:owner-scope, check:design-system-contract, check:type-scale, check:icon-scale, sitemap:check) to selectedScripts() for static_heavy scope and extend the self-test; or reword CLAUDE.md:99 / docs/testing.md:153 to state that these gates are CI-only after push.
- **Skeptic (CONFIRMED):** Reproduced offline. Not a ledger duplicate; G1 in the same-day gate audit covers the artifact-drift subset, and the finder's addition of the three tenancy/database guards (plus sitemap:check, forms-pdf-manifest) changes the substance from hygiene to security-gate parity, so it is not merely a duplicate of G1. Partial mitigation the finder omits: verify:cheap runs all three, so the gap is specific to the documented 'PR mirror' gate. Impact stays post-push-detectable (CI still catches it before merge), so not High. The fix sketch must also update the selectedScripts self-test (verify-pr-local.mjs:241-304) and check:verification-plan; it does not touch any never-touch surface.
