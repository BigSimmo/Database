**L55 · `allowScripts` policy is stale: it approves esbuild@0.28.1 while the pinned/installed esbuild is 0.28.2, and omits @sentry/cli which also has a postinstall** — `package.json:372-379 (allowScripts) vs package.json overrides/devDependencies `esbuild: "0.28.2"`` · process · CONFIRMED · safe_cleanup=true · L17-F2

Dependabot PR #2468 (2026-08-31) bumped the esbuild pin from 0.28.1 to 0.28.2 in both `overrides` and `devDependencies` but the `allowScripts` key still reads `esbuild@0.28.1`. npm 11.17.0 implements this policy (lib/utils/check-allow-scripts.js → @npmcli/arborist unreviewed-scripts.js): an entry matches by exact name@version from the lock's resolved URL, so esbuild@0.28.2 is now 'unreviewed'. In arborist rebuild.js:229-237 unreviewed nodes still run their scripts (only an explicit `false` blocks), so today this is an advisory warning during `npm ci`, but the … **Impact:** The allowlist no longer describes what actually runs, so it gives false assurance about supply-chain script review; a future strict setting breaks every install path including the Railway image builds. **Fix:** Change the key to `esbuild@0.28.2` (or the exact-version disjunction `esbuild@0.28.1 || 0.28.2` if older lock states matter), add `@sentry/cli@2.58.6`, and add a small vitest contract (like tests/check-runtime.test.ts) that every allowScripts key's version equals the lock's version for that package … **Skeptic:** Stale entry and missing @sentry/cli entry reproduce from the lock; npm's own source confirms unreviewed packages still run today, so the impact is advisory drift until strict enforcement is enabled, exactly as the finder says. Low. Fix (update keys, add a contract test) is safe.
