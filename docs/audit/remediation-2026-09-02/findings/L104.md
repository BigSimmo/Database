**L104 · observability-slos.md publishes warn/page thresholds for four SLOs that nothing in the repo measures or alerts on** — `docs/observability-slos.md:35-46 (latency p95 by route mode + /api/search), :78-85 (source-gap rate), :87-104 (unsupported rate); scripts/lib/operational-alerts` · docs-drift · PLAUSIBLE · safe_cleanup=false · L22-F2

§2 defines page/warn thresholds for answer latency p95 (fast ≤10 s, strong ≤25 s, extractive ≤6 s, /api/search ≤3 s), source-gap rate (>20 %/>30 %) and unsupported rate (>15 %/>25 %). The only emitted metrics are in `answerSloSnapshot` (hybrid RPC errors, degraded/truncation/timeout rates), `cacheMetricsSnapshot`, `answerCoalescingMetricsSnapshot` and `spendSnapshot`; the ops digest's alert codes are exactly OPS_ANSWER_SLO_UNKNOWN, OPS_HYBRID_RPC_ERROR_RATE__, OPS_DEGRADED_ANSWER_RATE__, OPS_PROJECTED_DAILY_SPEND_WARNING and OPS_EVAL_CANARY_STALE_WARNING. No code computes a latency percentile … **Impact:** A latency or unsupported-rate regression that the doc says will 'page' is invisible unless someone hand-runs the SQL; the doc's own thesis is that silent degradation is the defining failure mode. Stale governance claim rather than a code defect. **Fix:** Either add the three windowed rates to `answerSloSnapshot` (one `select metadata->>'total_latency_ms'` sample per route mode + two more count queries, mirroring the existing pattern) and wire OPS_* codes, or relabel §2's latency/source-gap/unsupported rows as 'manual SQL — not alerted' and list … **Skeptic:** The code facts are confirmed: no latency percentile, source-gap or unsupported rate is computed or alerted, and the deep probe returns four blocks where §4 says three. What could not be established is that the doc claims those thresholds are automated: §4's own wording (`The §2 reliability SQL is now also a scrape`) scopes automation to the …
