**L57 · Deleting or rewriting-and-renaming a branch-review record is caught by no gate, and no ledger row tracks that gap** — `scripts/check-ledger-write-discipline.mjs:178-183 (governedReason); scripts/check-branch-review-ledger.mjs:374-381; docs/branch-review-archival-policy.md §'Forb` · process · CONFIRMED · safe_cleanup=false · L19-F4

The review-record corpus is the review-throttle's memory (AGENTS.md 'Codex review throttling'). check-branch-review-ledger validates each existing file against its own content hash, so a record edited and renamed to the new hash passes, and a deleted record is never named. check-ledger-write-discipline governs only the two ledgers and the inbox — `docs/branch-review-records` is absent from governedReason. The only incidental net is the generated index's relative links, and the index is advisory and lags (it is regenerated only by docs:update / ledger:index). **Impact:** A branch whose review record vanished is reviewed again (cost), or worse, a record's verdict can be altered without trace; tamper-evidence is the property the corpus exists for. **Fix:** Extend check-ledger-write-discipline: `git ls-tree` of docs/branch-review-records at base vs head; fail on any removed or modified blob (additions only). Zero cost on ordinary PRs, and it closes the 'edit + rename' loophole the policy documents. Queue a ledger row so the gap is tracked until then. **Skeptic:** The gap is real and the policy document itself concedes it; the only partial net is the generated index's relative links, which are regenerated only by docs:update/ledger:index. Not ledgered as an open row. Downgraded to Low: the trigger requires repository write on a PR that a human merges, and AGENTS.md does not name this as a defence-in-depth …
