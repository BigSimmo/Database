### M13 · Live tools catalogue advertises Caring Contacts as 'ready' with an Open button, but every workspace route and API returns 404 in a real production process

`src/lib/tools-catalog.ts:358-372; src/lib/caring-contacts-server/session.ts:36-49; src/app/caring-contacts/page.tsx:77; src/lib/caring-contacts-server/handler.ts:113-115,205,313` · correctness · **CONFIRMED** · safe_cleanup=false · lane L07 (L07-F1)

> **Correction (Stage 5, red team):** Merge into one finding and note it partly corroborates open #4STSM1 (production build spec-approved, unimplemented), which already frames the 19 Aug listing and 22-23 Aug lock as a sequencing gap. M14's '3 tests' is wrong: the 'the production lock' describe has four cases (tests/caring-contacts-session.test.ts:85,89,97,109).

- **Defect:** The catalogue entry is `status: "ready"`, `actionLabel: "Open"`, `href: /caring-contacts` and is not filtered by environment. `isCaringContactsDemoEnabled()` returns false whenever NODE_ENV is production unless BOTH PLAYWRIGHT_OFFLINE_MODE and NEXT_PUBLIC_DEMO_MODE are exactly "true", and `src/instrumentation.ts` refuses to start a real production process with PLAYWRIGHT_OFFLINE_MODE. Every page then calls `notFound()` and every API handler returns `{refusal:"not-found"}` 404.
- **Trigger:** Any signed-in user on psychiatry.tools opens the Tools catalogue and presses Open on the Caring Contacts card.
- **Impact:** A suicide-prevention workspace is presented on the live site as ready and then dead-ends on a 404 with no explanation; the clinician cannot tell whether the product is broken or withheld. The layout comment (layout.tsx:6-7) records the listing decision of 19 Aug 2026, which predates the production lock decision of 22-23 Aug (PROGRESS-LEDGER §5b); the two decisions were never reconciled.
- **Proof:** not executed — traced: tools-catalog.ts has no status/environment gate for this entry (grep for isDemo/production/hidden returns only `status` literals); session.ts:36 `if (environment !== "production") return true;` then line 49 requires both flags; page.tsx:77 `if (!isCaringContactsDemoEnabled()) notFound();`. tests/caring-contacts-session.test.ts 'the production lock' (4 cases) and tests/caring-contacts-api-handler.test.ts:160 both pin the 404 — ran green (61/61).
- **Fix sketch:** Either gate the catalogue card on the same predicate (hide it, or show it as an 'unavailable — demonstration only' status with no Open action) in production, or record an explicit owner decision that the dead link is accepted. Add a unit test asserting the catalogue entry's availability matches `isCaringContactsDemoEnabled()`.
- **Skeptic (CONFIRMED):** Lines say exactly what the finder claims. Guard lens: no proxy rule, no catalogue filter, no test ties the card's `status: "ready"`/`Open` to `isCaringContactsDemoEnabled()`; the launcher renders `statusLabels[app.status]` (line 259) without an environment check. Trace: production process → NODE_ENV=production, PLAYWRIGHT_OFFLINE_MODE unset (instrumentation.ts refuses it otherwise) → predicate false → page `notFound()` and API 404 → the card's Open dead-ends. The 19 Aug listing decision and the 22-23 Aug lock decision were made in different sessions and §5b does not reconcile them. Ledger grep for a catalogue/404 row on this workspace found only #800E5M (dead-code scanning), so not a duplicate. Re-grade: unprivileged signed-in clinician sees a dead link on the live site; no data or clinical figure exposure, so Medium (user-facing correctness on a suicide-prevention surface), not High. Fix sketch (gate the card or mark it unavailable, add a contract test) touches no never-touch surface; it must keep the Playwright offline exception intact so `ui-caring-contacts-workspace` still sees the card.
- **Merged at Stage 5:** M14 (L23-F1, lane L23) "Live Tools launcher advertises a Caring Contacts card whose target is a hard 404 in every production deployment" — `src/lib/tools-catalog.ts:354-368 (href: CARING_CONTACTS_ROUTES.today = /caring-contacts); src/app/caring-contacts/page.tsx:78 (`if (!isCaringContactsDemoEnabled()) notFound();`); src/lib/caring-contac`; its skeptic (CONFIRMED): Chain holds end-to-end: production launcher card -> route that unconditionally 404s in production. Not a wrong number, so Medium (dead link on the primary clinical launcher, with an owner decision on both sides that were never reconciled). Fix (a) is safe; any catalogue-filter test would be additive. Finder cited … Same live launcher card whose target is a 404 in every production deployment. Stage 5 adds that this partly corroborates open P2 #4STSM1 (synthetic production build spec-approved, unimplemented); the new evidence is that the card is unconditional.
