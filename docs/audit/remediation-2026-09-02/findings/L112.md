**L112 · Universal-search 'view all' links target the consolidated bare paths and take a redirect hop the codebase elsewhere says in-app navigation should avoid** — `src/lib/universal-search.ts:765-792 (universalSearchViewAllHref: `/services?q…`, `/forms?q…`, `/differentials?q…`, `/specifiers?q…`, `/formulation?q…`) vs src/l` · quality/simplification · CONFIRMED · safe_cleanup=false · L03a-F6

Five of the twelve domains build their 'See all' href on the bare mode path, which the proxy 307s to `<mode>/search`; dsm and therapies already point at `/search` directly. tools-catalog.ts documents that a client transition through the compatibility redirect 'can briefly retain the Tools route shell and its phone geometry before the second navigation settles'. **Impact:** An avoidable server round-trip and a transient wrong-shell frame on phones; behaviour is otherwise correct because the consolidated redirect preserves the query. **Fix:** Return `${consolidatedModeSearchPath(mode)}?q=…&run=1` (or appModeHomeHref(mode, { query, run: true })) for the five consolidated domains. **Skeptic:** Code read confirms the inconsistency and that a helper for the direct path already exists. Behaviour is correct after the 307 (the consolidated redirect preserves the query), so this is an avoidable hop and a transient shell frame on phones. Low quality item; fix is safe but should keep the existing universal-search href tests green.
