**L103 · Retention migration is named and attested as 'staging' but ships in supabase/migrations, so it re-created the production pg_cron jobs on merge; PIA job-id evidence and the readiness attestation text no longer describe production** — `supabase/migrations/20260901033250_enable_staging_privacy_retention_schedules.sql:1-3,28-40; docs/privacy-impact-assessment.md:280-283,286-290; docs/governance/` · docs-drift · CONFIRMED · safe_cleanup=false · L21-F4

AGENTS.md states merging a migration applies it to the live database within seconds. This migration (merged 2026-09-01 in #2490) unschedules the five named jobs and calls cron.schedule for four of them, so on production the jobs now have new jobids. The PIA still presents 'jobid 11 / 12 / 13 / 16 … verified live' as current evidence, and the readiness register's external evidence describes the change as 'Staging migration … enabled pg_cron on the pinned staging project' without recording that the same statement executed on production. The file name itself invites a reviewer to treat it as … **Impact:** Retention evidence (a PIA-4 control and an APP 11 claim) points at job ids that should no longer exist; the migration's production effect is undocumented, so a future operator cannot tell whether the current production schedule came from 2026-07 or 2026-09 and whether the post-merge live-drift gate … **Fix:** In the PIA §6 replace the 2026-07 jobid block with a dated note that 20260901033250 re-created the jobs on production and staging, and cite the post-merge live-drift run; amend the readiness externalEvidenceReference to say the migration applied to both projects. Prefer environment-neutral … **Skeptic:** The documents describe a migration living in supabase/migrations as staging-only, and the PIA's jobid evidence predates a migration that unschedules and recreates those jobs; both are verified offline. Whether production actually executed it (and whether post-merge live-drift was green) is provider-backed and unverified, but either outcome leaves …
