**L49 · Worker image bakes docling models from HuggingFace at build time with no revision or hash pin** — `Dockerfile.worker:83-84; eval/docling/Dockerfile:44-45` · process · CONFIRMED · safe_cleanup=false · L05-F8

Both Python venvs are hash-locked, but `docling-tools models download --output-dir /opt/docling-models` fetches whatever the upstream model repos currently serve. An upstream model change silently changes shadow-extraction measurements (and Gate B lab comparability) between two builds of the same commit; it also makes the image build depend on HuggingFace availability. **Impact:** Shadow-only: the live index never reads docling output (verified by `runShadowExtraction`'s numbers-only zod schema and the focused test run). Reproducibility and supply-chain hygiene only. **Fix:** Pin model revisions (docling-tools supports per-model revision arguments, or vendor the artifacts) and record the resulting digest in the Gate B decision record. **Skeptic:** Build lines are as quoted: Python packages are hash-pinned, model weights are not. Impact is reproducibility of shadow measurements and a build-time HuggingFace dependency; the live index never reads docling output. Low process/supply-chain hygiene. Whether docling-tools accepts per-model revision pins was not verified offline; vendoring the …
