**L90 · docs/scripts-index.md describes merge-branch-review-ledger.mjs as the live `merge=ledger` union driver, but that driver was deliberately removed and the ledger guard no longer checks it** — `docs/scripts-index.md:156-157; .gitattributes:12-24; scripts/check-branch-review-ledger.mjs (no reference)` · docs-drift · CONFIRMED · safe_cleanup=true · L15-F4

The index says the script is 'the merge=ledger union driver from .gitattributes; check-branch-review-ledger.mjs fails if that protection is lost'. .gitattributes now carries no `merge=` attribute and its comment (lines 16-23) records that the union driver was tried in PR #1416 and removed after it duplicated the open-items table four times on 2026-07-30. `git config --get merge.ledger.driver` returns nothing; grep of check-branch-review-ledger.mjs for merge=ledger/gitattributes/merge-branch-review-ledger returns nothing. The script has no package.json entry, no importer, no test and no … **Impact:** The doc asserts a protection that does not exist; the script is a retirement-triage candidate that the doc instead presents as load-bearing. **Fix:** Rewrite lines 156-157 to say the driver was retired (PR #1416/#1430) and mark the script as a provenance-only retirement candidate; keep the file until the dead-code-deletion contract is satisfied. **Skeptic:** Every element of the doc sentence is now false and the guard it cites no longer contains that check. Docs-drift about a non-existent protection; Low (the protection's absence is itself a deliberate, ledger-recorded decision, so no safety gate is misdescribed as present in a way that hides risk). Fix (reword + retirement-triage note) is safe; …
