**L53 · The mockups bundle 'runaway detector' has been re-baselined five times in 17 days (+129%) and never recorded a decision** — `bundle-budget.json:12-15 (`mockups.gzipBytes: 627814`, `tolerancePct: 25`); scripts/check-bundle-budget.mjs:32-37` · process · CONFIRMED · safe_cleanup=false · L11-F4 · corroborates #252 (resolved 2026-08-09) — the split works as designed, but the looser ceiling it introduced has absorbed every growth since

git history of bundle-budget.json: mockups baseline 273,873 (#1779, 08-09) → 303,094 (#1926, 08-14) → 379,113 (#2095, 08-18) → 507,074 (#2274, 08-22) → 499,284 (#2293, 08-23) → 627,814 (#2390, 08-26). Each step except one exceeded the 25% tolerance and was cleared by `--update` inside the feature PR that caused it. The script header says this ceiling exists "to catch unbounded accumulation ... not to gate the next mockup", but nothing in the JSON records why each reset was accepted, and `updatedAt` (2026-08-23) does not even match the last change (08-26). **Impact:** Repo-hygiene only (mockup chunks are not user-downloaded weight); but the gate's stated purpose — detecting runaway scratch — is being silently defeated, and the growth is mostly the gated prototypes (Care Plan, Caring Contacts, Ward Flow) that now dwarf the design-study corpus this lane covers. **Fix:** Add a `mockups.rationale`/`mockups.baselineHistory` note (or a `$comment` per refresh) and have `check:pr-policy` require a one-line justification in the PR body when `bundle-budget.json` `mockups.gzipBytes` rises by more than tolerance; fix `updatedAt` on every `--update`. **Skeptic:** Numbers reproduced from git; +129% over 17 days with the last bump not even going through `--update` (updatedAt unchanged). Whether each PR body justified its reset cannot be checked offline (GitHub), so 'never recorded a decision' is verified only for the JSON itself. #252 is resolved and describes the split, not the subsequent drift, so not a …
