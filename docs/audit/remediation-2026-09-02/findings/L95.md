**L95 · AGENTS.md claims all five 'exact-text' sections stay in full and are gate-parsed, but the Codex Cloud section is a two-line pointer and nothing parses the Anti-conflict section** — `AGENTS.md:37-40 (claim); AGENTS.md:380-382 (pointer); AGENTS.md:322-350 (Anti-conflict)` · docs-drift · CONFIRMED · safe_cleanup=true · L18-F4

AGENTS.md:37-40: 'Five sections stay here in full because a committed test or script reads their exact text: … `## Anti-conflict and CI-speed operating procedure`, and `## Codex Cloud environment`. Their wording is code, not prose — moving or rewording it fails `verify:cheap`.' Reality: (1) `## Codex Cloud environment` at AGENTS.md:380-382 is a pointer to docs/agents/codex-cloud-environment.md, and its only parser, scripts/check-codex-cloud-setup.mjs:1142-1146, pins the heading of what it itself calls the 'pointer section'. (2) No test, script or workflow reads the Anti-conflict section: grep … **Impact:** Agents are told a gate protects text that no gate reads, so a silent rewording of the Anti-conflict procedure would pass verify:cheap despite the file saying otherwise; and the 'in full' claim sends readers to the wrong file for Codex Cloud rules. **Fix:** Reword AGENTS.md:37-40 to name the three sections that are actually pinned by tests, say the Codex Cloud heading is pinned as a pointer, and either add a small contract test for the Anti-conflict text or drop it from the list. **Skeptic:** Both sub-claims hold: the Codex Cloud section is a pointer whose only parser checks the heading, and nothing reads the Anti-conflict text. False claim about gate coverage, but the direction is over-protective (agents avoid editing) and no verification decision depends on it, so Low. Fix is a rewording of AGENTS.md:36-40; must not touch the three …
