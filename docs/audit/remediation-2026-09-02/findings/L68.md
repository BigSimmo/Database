**L68 · mobile-webkit and mobile-pwa-standalone Playwright projects run only on the release matrix's fail-safe branch** — `playwright.config.ts:141-156; .github/workflows/ci.yml:1447-1454` · test-coverage · CONFIRMED · safe_cleanup=false · L14-F4 · previously-reported (ledger #71NT23, closed as 'Configured mobile-webkit…')

The two iPhone-14 projects are defined but no package script or CI step names them. The release matrix's ordinary path (`ui_changed && ui-critical success`) runs only `--project=chromium-mockups --project=firefox --project=webkit`; they execute only in the `else` branch (`npm run test:e2e` with no project filter), i.e. when UI did not change or Chromium proof is missing. **Impact:** Phone PWA standalone-mode scroll/dock behaviour (the #71NT23 rationale) has cross-engine proof only on non-UI runs; docs/testing.md:421-422 describes the matrix as Chromium shards + Firefox/WebKit full suites and does not mention the mobile projects, so the gap is invisible. **Fix:** Add the two projects to the ordinary-path project list at ci.yml:1451 (or state in docs/testing.md that they are fallback-only and why), and add a unit assertion that every non-advisory project in playwright.config.ts is named by at least one CI step. **Skeptic:** Trace holds: `test:e2e` = `node scripts/run-playwright.mjs` with no project filter runs all projects, so the two mobile projects execute only on the fallback (non-UI-change) branch. #71NT23 is closed and only records that the projects were configured, so this is a distinct, not-duplicate observation. Low: cross-engine phone proof is missing on the …
