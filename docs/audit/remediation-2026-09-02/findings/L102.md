**L102 · Cursor Supabase skill's repository safety override is conditioned on a workstation path that no longer exists (`C:\Dev\Apps\Database`)** — `.cursor/skills/supabase/SKILL.md:13,136` · docs-drift · CONFIRMED · safe_cleanup=true · L20-F8

The override paragraph reads 'When this skill runs in `C:\Dev\Apps\Database`, root AGENTS.md ... take precedence ... never use `execute_sql`...'. The canonical checkout is `D:\Repos\Database` (`.claude/settings.json` autoMode environment 'Trusted repo', `plugins/clinical-kb/skills/clinical-kb-workflow/SKILL.md:3,8`, and the 2026-08-21 memory `dev-drive-project-location.md`). The only other `C:\Dev\Apps` references left in the repo are in `docs/archive/**` and old audit records. `check:skills` asserts the fragments exist (`list-database-skills.mjs:148-152`) but not the path. **Impact:** The one prose guard that keeps Cursor's Supabase MCP (see L20-F5) away from `execute_sql` reads as not applying to the actual repository location. **Fix:** Change both occurrences to 'this repository (any checkout of BigSimmo/Database)'; add the path-free wording to the `requireContract` fragments. **Skeptic:** Confirmed: the override paragraph is conditioned on a workstation path the repo no longer records as its location, and the skill gate asserts the fragments but not the condition. Low docs drift; the practical guard against execute_sql from Cursor is AGENTS.md's provider boundary, which is path-independent. Fix (path-free wording) does not affect …
