**L125 · Unreachable governance branch, unused telemetry module, and an accepted-but-unused priorTurns field that is not identifier-scanned** — `src/lib/clinical-ask/response-governance.ts:96; src/lib/clinical-ask/telemetry.ts:14-24; src/lib/clinical-ask/contracts.ts:144 + src/lib/validation/clinical-ask` · quality/simplification · CONFIRMED · safe_cleanup=false · L12-F7

(a) `if (extractClinicalValueAtoms(text).length > 0 && !hasDirectSupport) return null;` follows `if (!hasDirectSupport) return null;` so it can never be true. (b) `buildClinicalAskTelemetry`/`clinicalAskLatencyBucket` have no importer outside tests (knip export log line 201 flags `clinicalAskLatencyBucket`); the content-free telemetry the governance doc describes is never emitted. (c) `priorTurns` (up to 6 x 2,000 chars) is validated and accepted but never read by the orchestrator or synthesis, and `containsIdentifier`/`identifierInput` do not scan it — harmless today, a leak trap if someone … **Impact:** Maintenance noise; (b) means operators have no Clinical Ask outcome metrics despite docs implying them; (c) is a latent privacy trap. **Fix:** Drop line 96; either wire telemetry into the route's `finally` (content-free fields only) or record the module as a triage candidate; either remove priorTurns from the request contract or include it in the identifier scan now. **Skeptic:** All three sub-claims verified from code and the pre-gathered knip evidence. Dead branch, unwired telemetry, and a latent (not live) privacy trap: Low. Dropping line 96 or the telemetry export must respect docs/agents/dead-code-deletion.md; adding priorTurns to the identifier scan is additive and safe.
