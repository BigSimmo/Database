**L124 · Answer surface renders a 'Review extract' disclosure that is always empty because the server strips extracts by contract** — `src/components/clinical-dashboard/clinical-ask-answer-surface.tsx:306-309; src/lib/clinical-ask/response-governance.ts:65-67` · quality/simplification · CONFIRMED · safe_cleanup=true · L12-F6

`publicClinicalAskEvidence` blanks `extract` on every answered/evidence_gap payload (pinned by tests/clinical-ask-response-governance.test.ts:75-99), yet the Evidence list renders `<details><summary>Review extract</summary><p>{item.extract}</p></details>` for each item. **Impact:** A clinician opens a disclosure and finds nothing; an accessibility control with no content. Also a latent contradiction with the PIA's 'extracts excluded from browser' guarantee if a future change stops stripping. **Fix:** Remove the disclosure (or render it only when `item.extract` is non-empty, which today is never). **Skeptic:** An always-empty disclosure by contract. Cosmetic/accessibility on a dormant surface: Low. Removing it breaks no test.
