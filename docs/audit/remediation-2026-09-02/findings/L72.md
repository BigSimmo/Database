**L72 · Ingestion docs still describe a lease with no heartbeat and 45-minute double-processing; the worker has had a guarded heartbeat and lease-fenced RPCs since July** — `docs/ingestion-state-machine.md:42,67,405; docs/worker-deploy-runbook.md:128-130; docs/README.md:32` · docs-drift · CONFIRMED · safe_cleanup=true · L05-F3

`docs/ingestion-state-machine.md` is indexed by docs/README.md:32 as the current 'ingestion job lifecycle and states' reference, yet it states three times that 'There is no heartbeat — a running worker never refreshes locked_at' (lines 42, 67, 405, the R1 'root enabler'). `worker/main.ts:63-67` defines `jobLeaseHeartbeatMs` (one third of `WORKER_STALE_AFTER_MINUTES`, floored at 30 s) and `updateJobProgress` (main.ts:118-140) refreshes `locked_at` guarded by `locked_by = workerId` and `status = 'processing'`, throwing 'Ingestion lease lost' on 0 rows; the extraction phase runs a 60 s heartbeat … **Impact:** Wrong operating model for the one production worker: capacity decisions and incident diagnosis are made against a race that the code already closed, and the real failure mode (a container OOM-killed mid-job, which stops heartbeating and is reclaimed after 45 min) is not what the docs describe. **Fix:** Add a dated status banner to ingestion-state-machine.md ('snapshot as of 2026-07-07; R1/R2/R5 landed in PRs after 20260708130000 — heartbeat in worker/main.ts updateJobProgress, lease-fenced RPCs') and rewrite runbook lines 128-130 to 'a job whose worker stops heartbeating for … **Skeptic:** Docs contradict shipped code in three files (the finder cited two; deployment-architecture.md:338 is a third). Downgraded from Medium to Low per the re-grade rule: documentation drift is Low unless the false claim concerns safety, privacy or a gate — this is an operating-model claim about replica sizing. Fix is docs-only and safe; a dated status …
