**L59 · /issues and the SessionStart hook ignore the pending inbox, so 14 already-closed rows (and one duplicate pair) are presented as open work** — `scripts/issues-report.mjs:150-176 (loadRevalidatedLedger); .claude/hooks/issues-surface.sh:36-48` · process · CONFIRMED · safe_cleanup=false · L19-F6

issues-report reads origin/main's ledger only; the hook reads the worktree ledger only; neither reads docs/outstanding-issues-inbox. Today 18 `done` requests are pending (14 effective after cancels): #Z61JRT, #875H6T, #CTA8CR, #EFETZT, #AGRAKQ, #B0530F, #N8B176, #Z5P2BW, #ZM8902, #Y30AXB, #JFRCZ4, #27TWKM, #Y090R5, #9P4XAE. All of them still appear in the 70 'open' rows. #B0530F and #27TWKM are also the same defect filed twice (caring-contacts Postgres suite not in CI), and both were already stale-open: ci.yml:1135 has carried a `caring-contacts-db` job since 424db172e. #CTA8CR's fix (a … **Impact:** Sessions re-investigate closed work (the issues skill's #292 duplicate-PR failure mode) and the reported P2 count overstates open work by roughly a fifth. **Fix:** Have issues-report apply the pending batch (as check-docs-links already does) or at least emit `pending: {done: N, update: N, add: N}` and mark affected rows `closing`; print the same one-liner in the hook. Reconcile the current 42 soon — the batch plans cleanly today. **Skeptic:** Counts and mechanism reproduced. Adjacent to open #Y090R5 (generated snapshot conflicts), which is a different defect, so not a duplicate. Low: stale-open rows cost re-investigation, nothing clinical. One nuance the finder overstates: #B0530F/#27TWKM's job is conditional on db/static-heavy scope, so 'stale-open' is fair but the row's concern about …
