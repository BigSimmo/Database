**L87 · docs/pwa.md allow-list row for `/apple-icon` describes caching that can never occur** — `docs/pwa.md:96 (table row) vs public/sw.js:29,48` · docs-drift · CONFIRMED · safe_cleanup=true · L13-F7

Next's metadata-image loader emits the generated Apple icon as `href="/apple-icon?<contenthash>"` (next-metadata-image-loader.js:64 `hashQuery = '?' + contentHash`; app-icons doc line 56). sw.js rejects every request with a query (`url.search !== ""`), so the `/apple-icon` allow-list entry is inert and the doc's 'stale while revalidate' claim is false for that path. **Impact:** Harmless in the privacy direction (less is cached), but the documented cache contract and the manual checklist ('all icon URLs load…') describe behaviour reviewers cannot observe, which erodes trust in the rest of the table. **Fix:** Either drop `/apple-icon` from isPublicPwaAsset and the doc table, or note that the generated icon carries a hash query and is intentionally network-only. **Skeptic:** Traced through Next's loader: the emitted `<link rel=apple-touch-icon>` href carries `?<contenthash>`, so the worker's own query-string denylist rejects it before the allow-list can match; the doc's SWR row for `/apple-icon` describes a path the browser never requests. The row is not strictly dead (a bare `/apple-icon` GET would cache) so this is …
