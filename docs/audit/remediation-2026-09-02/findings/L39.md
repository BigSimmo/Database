**L39 · Cursor Supabase MCP entry omits the `features=docs,development` restriction that the Claude and Codex entries carry, so a Cursor session gets read-only SQL/debugging tools against the live clinical database, enforced by prose only** — `.cursor/mcp.json:3 vs .mcp.json:7 and .codex/config.toml:22-26; pinned by tests/cursor-mcp-contract.test.ts:44` · security · PLAUSIBLE · safe_cleanup=false · L20-F5

`.mcp.json` and `.codex/config.toml` pin `...&read_only=true&features=docs%2Cdevelopment` and the Codex file comments 'database and debugging groups are excluded'; `docs/agents/codex-cloud-environment.md:15-17` says 'do not enable database, SQL, row, or log tools'. `.cursor/mcp.json` uses `...?project_ref=sjrfecxgysukkwxsowpy&read_only=true` with no `features` parameter, which leaves the server's default feature groups (including `database` — `execute_sql`, `list_tables`, `get_logs`/advisors) enabled in read-only mode. `docs/agents-guide.md:67` even lists 'read SQL' as a sanctioned use for … **Impact:** Read access to `documents`, `rag_queries`, `rag_response_cache`, feedback and other owner-scoped tables holding clinical reference text and query history from a surface the repo's own policy says must be metadata-only; provider spend and audit noise on the live project. Not a write path … **Fix:** Add `&features=docs%2Cdevelopment` to `.cursor/mcp.json` (or `docs,development,debugging` if advisors are wanted) and update the URL assertion in `tests/cursor-mcp-contract.test.ts:44`; reconcile `docs/agents-guide.md:67` ('read SQL') with `docs/agents/codex-cloud-environment.md:16-17`. **Skeptic:** The configuration asymmetry is confirmed verbatim and pinned by a test. What could not be established offline is the runtime precondition: that the hosted Supabase MCP enables the `database`/`debugging` groups when `features` is omitted (my recollection of the server's defaults says yes, but it was not verified). Hence PLAUSIBLE. Downgraded to …
