**L69 · Developer-gated prefix count has drifted: code has four, comments/docs/test titles say two or three, and the header test exercises only two** — `src/lib/developer-area/headers.ts:27-32 (four prefixes); src/proxy.ts:122-126 (comment lists three); src/components/developer-area/developer-area-gate.tsx:11-13` · test-coverage · CONFIRMED · safe_cleanup=false · L23-F5

The x-developer-area header test at tests/proxy.test.ts:198-222 requests /mockups/development, /mockups/care-plan/…, a look-alike, and an ordinary mockup — it never asserts the header is set for /mockups/caring-contacts/** or /mockups/ward-flow/**. The production-boundary test at line 179 covers caring-contacts for the _block_ decision only. A regression that dropped ward-flow from DEVELOPER_GATED_PATH_PREFIXES would fail closed (404) rather than open, so this is a coverage/drift gap, not an exposure. **Impact:** Documentation and test names understate the gated surface; a reviewer counting 'two subtrees' would not look for the ward-flow prototype. **Fix:** Iterate DEVELOPER_GATED_PATH_PREFIXES in the header test (assert header set for each prefix and for `${prefix}/deep/path`), and reword the three comments and codebase-index.md:241 to name all four. **Skeptic:** Verified: no test asserts the x-developer-area header for caring-contacts or ward-flow; comments/docs count two or three of four. Fail-closed on regression (a dropped prefix -> 404), so Low coverage/drift. Fix (iterate the constant in the header test, reword four comments) is additive.
