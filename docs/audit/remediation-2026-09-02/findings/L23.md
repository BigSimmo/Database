**L23 · Eval-capture stores caller-supplied chunk ids and file names without ownership validation** — `src/app/api/eval-cases/route.ts:164-167` · data-integrity · CONFIRMED · safe_cleanup=false · L02-F10

`top_chunk_ids`, `cited_chunk_ids` (UUID-shape only) and `top_files` (free strings ≤512 chars ×20) are written to rag_query_misses verbatim; only expected_document_id / expected_chunk_id go through ownedDocumentId/ownedChunkReference. **Impact:** Promoted eval cases can reference chunks the owner cannot read or that do not exist, polluting loadCapturedRagEvalCases input for scripts/eval-retrieval.ts; admin-only so low. **Fix:** Filter the id arrays through the same owned-chunk check (batched) before insert, recording the rejected count as already done for malformed ids. **Skeptic:** Trace holds: the id arrays and file names are stored without an ownership or existence check, so a promoted eval case can reference chunks the owner cannot read. The actor must already be an administrator (requireAuthenticatedUser administrator:true) writing rows under their own owner_id, and the consumer is an offline eval script, so the impact …
