**L105 · docs/performance.md links to an absolute Windows path on one developer's machine** — `docs/performance.md:29 and :50 (`file:///C:/Users/joshs/.gemini/antigravity/worktrees/Database/list_manual_ledger_tasks/...`)` · docs-drift · CONFIRMED · safe_cleanup=true · L22-F5

Two references (the CLS reserve in `src/app/(search-app)/documents/search/page.tsx` and `docs/phone-chrome-physical-acceptance.md`) are markdown links whose href is a `file:///C:/...` path in a personal Antigravity worktree. The backticked repo paths are correct (page.tsx:10 does carry `min-h-[55dvh]`), which is why `docs:check-links` passes — it validates backticked repo paths, not link hrefs. **Impact:** Dead links in the performance reference; also leaks a local username/tool path into the repository. **Fix:** Replace both hrefs with repo-relative links (`../src/app/(search-app)/documents/search/page.tsx`, `phone-chrome-physical-acceptance.md`). Zero behaviour change. **Skeptic:** Lines read exactly as claimed; both hrefs are absolute Windows paths inside a personal Antigravity worktree and are dead on GitHub and any other machine. The gate passes because it validates backticked repo paths and does not reject `file://` hrefs — the finder's explanation matches the script. Low docs-drift (plus a minor local-username leak). …
