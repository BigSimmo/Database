**L37 · Secret Scan workflow grants `security-events: write` and `pull-requests: read` that nothing uses** — `.github/workflows/secret-scan.yml:15-18; scripts/run-gitleaks-pinned.mjs:137 (gitleaks args carry no `--report-*` / SARIF upload)` · security · CONFIRMED · safe_cleanup=true · L16-F5

The permissions block is a leftover from gitleaks-action@v3 (which uploads SARIF); the pinned script runs `gitleaks detect --source=. --log-opts=… --redact --verbose --exit-code=1` and never touches the code-scanning or pull-request APIs. Least-privilege is violated on a workflow that checks out PR heads. **Impact:** Defence-in-depth only; no functional effect today. **Fix:** Reduce to `permissions: contents: read`; `npm run check:gitleaks-pinned` self-test does not assert the permissions block, so nothing else changes. **Skeptic:** Confirmed leftover grant; nothing in the job uses code-scanning or PR APIs. Trigger requires compromise of a step inside the job on a private repo, so least-privilege only — Low. Fix (drop two lines) is safe; check:gitleaks-pinned does not assert the permissions block.
