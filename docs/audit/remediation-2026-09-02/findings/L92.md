**L92 · Codex auto-resolve high-risk path list names deployment files that do not exist and misses the ones that do** — `.github/workflows/codex-autofix-review-comments.yml:72 (`/^(?:Dockerfile|railway\.json|nixpacks\.toml)$/`)` · docs-drift · CONFIRMED · safe_cleanup=false · L16-F10

The repo has railway.app.json, railway.worker.json and Dockerfile.worker; it has no railway.json or nixpacks.toml. A PR touching only the worker Dockerfile or either Railway config is therefore classified low-risk for routing. Direction of failure is benign for safety (low-risk PRs skip the unattended repair pass rather than receive it), but the list is provably stale and `check:codex-autofix-workflow` does not cover it (docs/agents/codex-github-review.md has no railway text). **Impact:** Inconsistent routing decisions and a misleading risk classification in the workflow that decides when automation may write to a branch. **Fix:** Change the pattern to `/^(?:Dockerfile(?:\.worker)?|railway\.(?:app|worker)\.json)$/` and add a fixture line for it in scripts/check-codex-autofix-workflow.mjs. **Skeptic:** Stale pattern confirmed; Dockerfile.worker and both Railway configs fall outside the high-risk list. Direction is benign (low-risk routing withholds the unattended repair pass rather than granting it), so Low. Fix is a regex edit; since check-codex-autofix-workflow.mjs does not enforce this line it will not trip the exact-text contract, but the …
