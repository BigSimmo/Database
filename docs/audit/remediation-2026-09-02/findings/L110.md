**L110 · /api/setup-status varies its payload by operator token/Authorization but sends a cacheable private response without Vary** — `src/app/api/setup-status/route.ts:285-291 and :448-467` · quality/simplification · CONFIRMED · safe_cleanup=false · L01-F6

The route returns either the full diagnostic payload (operator token or admin bearer) or the coarse payload, under `Cache-Control: private, max-age=5, stale-while-revalidate=30` with no `Vary: Authorization, x-health-deep-token`. **Impact:** Same-user only (`private`), so no cross-user leak; the polled setup UI can briefly show the wrong detail level. Hygiene issue, not exposure. **Fix:** Add `Vary: Authorization, x-health-deep-token` to setupStatusResponse, or use `no-store` for the detailed variant. **Skeptic:** Confirmed: two payload variants keyed on x-health-deep-token/Authorization, one cacheable `private` response, no Vary. `private` keeps it to the same browser and the window is 5 s (+30 s SWR), so it is hygiene, not exposure. Low. Adding Vary or no-store for the detailed variant is safe.
