**L58 · The canonical ledger's own prose is unreachable by any sanctioned write path, so its conventions section is frozen with stale claims and mojibake** — `docs/outstanding-issues.md:15, :17, :23; scripts/check-ledger-write-discipline.mjs:143-160` · process · CONFIRMED · safe_cleanup=false · L19-F5

verifyIssueReconciliation requires head `docs/outstanding-issues.md` to equal base + the moved inbox requests byte-for-byte. Inbox requests can only add/resolve/update rows and edit queue cells; nothing reaches the prose above the tables. So the header now says (line 17) `/issues add …, /issues done <id> … mutate the tables below` and (line 23) IDs are `a monotonic #NNN … Allocate the next number above the current max`, while the real system queues immutable requests and allocates ULID-derived six-character IDs (`#Y30AXB`); line 15 carries cp437 mojibake `ΓåÆ` for `→`. Every one of these is a … **Impact:** Agents are told the wrong allocation and write model by the ledger itself; the conventions cannot be repaired without a deliberate, one-off gate-aware change. **Fix:** Add a `prose` request type (or a `--prose-patch` reconcile flag) whose application is part of the audited transaction, then use it once to rewrite lines 15-25 to describe the inbox/ULID model; alternatively move the conventions text into docs/outstanding-issues-inbox/README.md and leave a one-line … **Skeptic:** Traced: no inbox action type edits prose (ACTIONS = add|done|update|queue|cancel; each targets a table row), and the audited-transaction comparison is byte-exact, so the header's stale allocation rule and mojibake are unfixable through any sanctioned path. Low, not Medium: a session that follows the stale `#NNN` rule would be rejected by the same …
