**L89 · docs/scripts-index.md claims every top-level script is named, but 44 of the top-level .mjs/.ts scripts are absent and the gate that guards the doc only checks counts and npm-run tokens** — `docs/scripts-index.md:7-8; scripts/check-docs-script-refs.mjs:3-6; scripts/update-docs-inventory.mjs (--check)` · docs-drift · CONFIRMED · safe_cleanup=true · L15-F3

Line 7 states 'Every top-level .mjs/.ts/.cjs script is named below'. Basename and stem grep of all 252 top-level scripts against the doc finds 44 unnamed, including gate scripts this repo depends on (check-clinical-hazard-controls.mjs, check-diff-integrity.mjs, check-ledger-write-discipline.mjs, check-privacy-readiness.mjs, check-source-catalogue.ts, check-stale-docs.mjs, check-dead-code-candidate.mjs, ledger-inbox.mjs, generate-outstanding-issues-snapshot.mjs, worktree-cleanup.mjs, setup-codex-worktree.mjs, trivy-image-scan.mjs, generate-worker-python-lock.mjs, check-worker-python-lock.mjs … **Impact:** The doc's classification (live / infra / one-shot archive candidate) is the repo's own convention for retirement triage (#194 relied on it); missing entries make retirement decisions and orientation unreliable, and the 'generated facts' sentence lends the false claim borrowed authority. **Fix:** Either add the 44 names to the appropriate sections, or change line 7 to 'Most … are named below' and have update-docs-inventory.mjs --check assert the naming invariant so it cannot drift again. **Skeptic:** The false sentence and the gate gap reproduce (finder's '252' total is off; it is 242, the 44 misses are exact). Line 4 of the same doc already says 'This is orientation, not an exhaustive per-file listing', so the doc contradicts itself. Per the re-grade rule documentation drift is Low unless the false claim concerns safety, privacy or a gate; …
