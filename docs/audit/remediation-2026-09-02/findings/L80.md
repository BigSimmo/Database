**L80 · Hub index comment says the document count is scoped by row-level security; it is scoped by an application filter on the service-role client, and RLS is bypassed** — `src/app/mockups/development/page.tsx:96-100` · docs-drift · CONFIRMED · safe_cleanup=true · L10-F3

The JSDoc block above `<EnvironmentStrip>` states 'That count is owner-scoped by the database's own row-level security, not by anything asserted here'. Since commit 5fef4bfb6 (2026-09-02, 'Read the hub document count with a credential that has privileges') `resolveHubEnvironmentFacts` reads through `createAdminClient()` with `.eq("owner_id", user.id)` (environment-facts.ts:86-90), and that module's own doc (lines 44-48) says the filter 'is the whole of the owner-scoping guarantee now'. The comment was written the day before (blame: ce07c4242, 2026-09-01) and now asserts the opposite of the … **Impact:** Wrong-direction guidance on the exact line that is the access boundary for a service-role read; no runtime effect today. **Fix:** Replace the sentence with: 'That count is owner-scoped by an explicit `owner_id` filter on the server-only admin client (the service role bypasses RLS) — see `resolveHubEnvironmentFacts`.' **Skeptic:** The comment asserts the opposite of the module it points at, one commit later. Code comment on a service-role read boundary but no runtime effect and a test (tests/developer-hub-environment-facts.test.ts, named in the module doc) asserts the filter on the issued query: Low docs-drift. One-line fix, nothing load-bearing.
