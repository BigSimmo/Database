**L70 · Documentation says the ward-management Tools catalogue entry still exists; it was deleted in the same commit that wrote that sentence** — `docs/codebase-index.md:526-531 vs src/lib/tools-catalog.ts:23-39 (ToolCatalogId union has no ward id) and src/lib/category-identity.ts:164 (TOOL_ICON record has` · docs-drift · CONFIRMED · safe_cleanup=true · L03a-F3

codebase-index.md claims 'the Tools catalogue entry (`src/lib/tools-catalog.ts`, id `ward-management`) still exists and now points at the gated path … a separate, not-yet-landed task drops it (and the applications-launcher, tools-search-results, and category-identity mentions)'. The catalogue has 16 ids and none is ward-related; category-identity.ts, tools-search-results-page.tsx and the launcher no longer mention it. The only remaining `ward-management` string outside the ward directories is src/app/mockups/mockups-layout-client.tsx (developer area). **Impact:** An agent could re-open a task that is already done or, worse, re-add the entry to match the index. Misstates which prototype surfaces are reachable from clinical discovery. **Fix:** Replace lines 526-531 with: 'PR #2373 also removed the Tools catalogue entry (id `ward-management`) and its category-identity/launcher/results references, so the prototype is reachable only from the developer page.' **Skeptic:** Docs sentence and the deletion it says has not happened landed in the same commit (3ab1f3dcc). Pure documentation drift about a developer-gated prototype's discoverability; not safety/privacy/gate related, so Low. The proposed one-sentence replacement is a safe cleanup. **Merged at Stage 5:** L78 (L09-F5), L93 (L18-F1) — Same removed Ward Flow Tools-catalogue entry in docs/codebase-index.md, seen from three lanes; corrected in commit 2 (§11.1). L09-F5 also covers the bed-release count, likewise corrected.
