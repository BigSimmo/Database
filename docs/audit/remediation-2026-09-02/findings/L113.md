**L113 · Two silent fallbacks in the mode/tool registries: isSearchableAppMode is tautological, toolCatalogRecordById returns the first tool for an unknown id** — `src/lib/app-modes.ts:612-635 (every AppModeSearchKind is enumerated, so the predicate equals isAppModeId); src/lib/tools-catalog.ts:274-276 (`?? toolCatalogReco` · quality/simplification · CONFIRMED · safe_cleanup=true · L03a-F7

isSearchableAppMode lists all twelve search kinds, so it can never return false for a defined mode; tests/app-modes.test.ts:62 pins it true for every mode. toolCatalogRecordById substitutes 'PsychSift Search' for any unknown id instead of returning undefined (only src/components/tools-page-mockups/tool-fixtures.ts:147 calls it today). **Impact:** Maintenance hazard only: the function names a distinction the type no longer allows, and a typo would render the wrong tool card rather than fail. **Fix:** Either delete the kind list and delegate to isAppModeId, or make the return type honest (`modeId is AppModeId`); have toolCatalogRecordById return `ToolCatalogRecord | undefined`. **Skeptic:** Both claims verified: the type alias and the exhaustive kind list make isSearchableAppMode equivalent to isAppModeId, and toolCatalogRecordById silently substitutes the first tool for an unknown id (only a mockup fixture uses it). Maintenance hazard only, Low. Fix sketch is a safe cleanup; the tautology test at app-modes.test.ts:62 would keep …
