**L66 · Bed-release reducer: blocked→confirmed, confirmed→blocked, blocked→released and all three state-refusal branches have no asserting test** — `src/components/ward-management/ward-flow-reducer.ts:704-706 (CONFIRM refusal), 747-749 (BLOCK refusal), 782-784 (RELEASE refusal); tests/ward-bed-release-lifecy` · test-coverage · CONFIRMED · safe_cleanup=false · L09-F2 · corroborates #9GPWT3 (adds: the model the clinician has not validated is also the least-proven part of the reducer; row is not stale)

The reducer allows six legal transitions and refuses the rest with `cannot move release ${id} from ${state} to …`. Every ward test that dispatches CONFIRM/BLOCK/RELEASE targets only WR-001 (seeded `confirmed`) or WR-002 (seeded `predicted`): `grep -rnoE '"WR-0.."' tests/ward-*` shows 36 hits on those two ids and none on WR-007/WR-009 (`blocked`) or WR-008 (`released`). So predicted→confirmed, predicted→blocked and confirmed→released are proven; blocked→confirmed, confirmed→blocked, blocked→released are not; and `grep -rn 'cannot move release' tests/` returns nothing, so no refusal branch is … **Impact:** The transitions #9GPWT3 predicts are most likely wrong are exactly the ones whose current behaviour would change silently; the capacity board's 'Blocked'/'Confirmed' counts (ward-management-modes.tsx CapacityView) would drift with no red test. **Fix:** Add six cases to ward-bed-release-lifecycle.test.ts: WR-007 blocked→confirmed, WR-001 confirmed→blocked, WR-007 blocked→released, and CONFIRM on WR-008 (released), BLOCK on WR-009 already blocked, RELEASE on WR-002 (predicted) asserting the exact refusal text and unchanged state. Land them before … **Skeptic:** Reproduced: no test dispatches a state-changing event at a blocked or released release and no state-refusal string is asserted anywhere. The finder marked this 'corroborates #9GPWT3'; that row is a clinical-validation task and does not record a coverage gap, so the finder's evidence adds distinct substance (the untested transitions are exactly the …
