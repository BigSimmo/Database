**L34 · Page CSP connect-src allows three wildcard Sentry ingest origins although no browser Sentry SDK exists** — `src/lib/security-headers.ts:78; tests/security-headers.test.ts:42-46` · security · CONFIRMED · safe_cleanup=false · L13-F5

connect-src includes `https://*.ingest.sentry.io https://*.ingest.de.sentry.io https://*.ingest.us.sentry.io`, but the repository has no client Sentry init (no sentry.client.config.ts; src/instrumentation-client.ts only sets Zod jitless) and docs/error-tracking.md:3 states 'there is no client Sentry bundle path'. The allowance was added in the 29-finding sweep d745d154c (2026-08-21) and is pinned by a unit test as 'telemetry fetches'. **Impact:** A wildcard third-party egress channel to any Sentry project (attacker-controlled included) is open from the clinical origin for no product purpose — the exact 'needless widening of the exfil surface' the file's own img-src comment argues against. **Fix:** Remove the three Sentry origins from connect-src and flip the security-headers test to assert their absence; re-add only if a browser SDK is ever introduced (with the privacy review docs/error-tracking.md requires). **Skeptic:** Trace confirmed: three wildcard third-party egress origins are allowed with no client that uses them, and a unit test pins them in. Downgraded to Low: the trigger needs prior script execution past a nonce+strict-dynamic CSP, and the existing `https://*.supabase.co` wildcard is already an attacker-controllable exfil origin of the same shape, so …
