**L128 · Three copies of findOwnerIdByEmail and two confirmation helpers live side by side in scripts/, so a fix to one (e.g. the audit-L1 unknown-flag hardening) does not reach the others** — `scripts/eval-utils.ts:118, scripts/import-documents.ts:26, scripts/enrich-documents.ts:65 (findOwnerIdByEmail); scripts/cli-utils.ts:7 vs inline readline in scr` · quality/simplification · CONFIRMED · safe_cleanup=false · L15-F8

eval-utils.ts already exports findOwnerIdByEmail and is imported by purge-query-logs.ts; import-documents.ts and enrich-documents.ts each re-implement it (paginated auth.admin.listUsers scan). The duplicated copies are exactly the two scripts whose argument handling is inconsistent with the rest (F1). The lane's other duplication probes were clean: no scripts/lib file name collides with src/lib, sensitive-text.mjs (secret/JWT/URL-credential redaction for CI logs) and src/lib/privacy.ts (runtime PHI/path redaction) serve different inputs, and clinical-aliases.ts is consumed from … **Impact:** Maintenance drift only; no clinical or data effect by itself. **Fix:** Import findOwnerIdByEmail from ./eval-utils in import-documents.ts and enrich-documents.ts; have review-therapy.mjs reuse cli-utils confirm (or the reverse). **Skeptic:** Three definitions and two readline confirm implementations exist as claimed; no test or gate deduplicates them. Maintenance drift only; Low. Fix (import from eval-utils) is safe; eval-utils.ts is not an AGENTS.md RAG-protected surface (the protected list names eval-retrieval.ts and clinical-aliases.ts), though any edit near scripts/eval-*.ts …
