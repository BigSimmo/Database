**L33 · Dormant /api/clinical-ask/stream still authenticates and consumes the durable rate limiter before the master flag is checked, and answers 200 rather than 404** — `src/app/api/clinical-ask/stream/route.ts:167-187; src/lib/clinical-ask/authority-registry.ts:165-179` · security · CONFIRMED · safe_cleanup=false · L12-F2

> **Correction (Stage 5, blue team):** Add both to §14 as 'must land before CLINICAL_ASK_ENABLED=true' in the same PR as M16/M17: the catalogue-fallback substitutes the whole catalogue when nothing matches, and the stream route consumes the durable limiter before the flag check; activation is one env flag.

Order in POST is: parse body -> `createAdminClient()` -> `publicAccessContext` (Supabase auth lookup when a bearer token is present) -> `consumeSubjectApiRateLimit` (durable RPC write, bucket `clinical_ask`, fails closed 503 when the limiter is unavailable) -> ONLY THEN `clinicalAskModeEnabled(body.mode)`. The disabled outcome is an HTTP 200 `text/event-stream` carrying an `error` frame (`errorStream("mode_unavailable")`), not a 404/403. `CLINICAL_ASK_ENABLED` is read nowhere except `authority-registry.ts:167,175`, so the route has no earlier guard, and unlike /api/answer (route.ts:79) there … **Impact:** A feature the owner believes is switched off still performs a Supabase auth check and a rate-limit row write per request, exposes a probeable 200 endpoint that confirms the route exists and which modes are 'unavailable', and can emit 503s from the fail-closed limiter. Nothing reaches OpenAI … **Fix:** Move the `clinicalAskModeEnabled` check (and an `isDemoMode()` guard) to the top of POST, before `createAdminClient()`, returning `jsonError(404)` when `env.CLINICAL_ASK_ENABLED` is false; keep the SSE `mode_unavailable` frame only for the per-mode denylist case. Add a route test asserting … **Skeptic:** Trace holds: a switched-off feature still costs an auth lookup and a durable limiter write per anonymous POST, and 'mode_unavailable' over a 200 SSE is a deliberate design (test title 'returns the explicit dormant-mode error'). Nothing reaches OpenAI and the limiter bounds DB load; information disclosed is only that the route exists: Low. Fix …
