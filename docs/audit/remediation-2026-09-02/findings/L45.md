**L45 · data/specifiers-search-index.json is a derived clinical dataset with no generator, no freshness gate, and a header pointing at a script that does not exist** — `src/lib/specifiers-search-index.ts:6-10 (comment cites `scripts/build-specifiers-index`); data/specifiers-search-index.json; data/specifiers-content.json` · process · CONFIRMED · safe_cleanup=false · L03a-F4

The client-side /specifiers search and universal-search item shapes come from a pre-flattened index that the code says is 'regenerated from the source JSON by scripts/build-specifiers-index (kept in sync with data/specifiers-content.json)'. No such script exists (ls scripts | grep -i specif → nothing; no package.json script), and unlike cross-mode/therapy/MHA/forms-manifest/medication-interaction data there is no `--check` gate. tests/specifiers-content.test.ts checks counts and slug shape, not field parity. Today the two files agree on slug/label/disorder/category/group/src/def for all 585 … **Impact:** Search results (labels, review badges, source-verified counts) could silently diverge from the detail pages a clinician opens; a corrected specifier label or a status flip from 'needs verification' to 'defined' would not reach search until someone remembers the by-hand step. **Fix:** Add scripts/build-specifiers-search-index.mjs (flatten + deliberately blank `meaning` until clinician review, with the reason in the file) and a `check:specifiers-search-index -- --check` entry in verify:cheap, mirroring build-cross-mode-differentials-index.mjs; fix the comment at … **Skeptic:** The stale comment and the absence of any generator or --check gate are confirmed. Partly over-stated: the blank-`meaning` decision IS pinned (test :101-102, 'nothing generated is displayed or ranked on') and slug parity is pinned (:39-41), so what is genuinely unguarded is label/status/group parity and the regeneration path. Low process item; the …
