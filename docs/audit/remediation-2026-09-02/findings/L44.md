**L44 · Answer feedback silently impossible when RAG_QUERY_HASH_SECRET is unset outside production** — `src/lib/answer-feedback-token.ts:18-20, 47-48 and src/app/api/answer-feedback/route.ts:52-60` · process · CONFIRMED · safe_cleanup=false · L02-F11

The feedback token is signed with RAG_QUERY_HASH_SECRET, which is optional (env.ts:264) and only enforced in production (env.ts:422). Without it createAnswerFeedbackToken returns undefined, answerFeedbackMetadata omits the token, and every POST /api/answer-feedback fails with 'Answer feedback could not be verified. Run the question again.' — a message that tells a staging/dev user to retry something that can never succeed. **Impact:** Misleading UX in staging; no production impact because readiness requires the secret. **Fix:** Return a distinct 'feedback_unavailable' code when no secret is configured, and hide the feedback controls when the payload carries no feedbackToken. **Skeptic:** The finder's chain is wrong in the middle: the dashboard short-circuits before any POST, so 'every POST /api/answer-feedback fails' does not happen in the UI. The substance survives via a different line, though: without the secret the client shows 'This answer predates traceable feedback. Run the question again.' -- an instruction that can never …
