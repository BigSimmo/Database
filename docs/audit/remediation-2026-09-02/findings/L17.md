**L17 · Client stream collapses 401/429 into 'internal_error', so the rate_limited and unauthorized codes in the contract are never shown** — `src/lib/clinical-ask/client-stream.ts:32; src/lib/clinical-ask/contracts.ts:70-82` · correctness · CONFIRMED · safe_cleanup=false · L12-F5

`if (!response.ok || !response.body) return failedPayload(request, "internal_error")` treats the JSON 429 from `rateLimitJsonResponse` and the 401 from `unauthorizedResponse` the same as a network fault, discarding `retryAfterSeconds` and the server message. The contract's `rate_limited`/`unauthorized` codes exist only in the schema. **Impact:** The user sees 'Clinical Ask stream could not be read.' with a retryable flag instead of 'try again in N seconds' or 'sign in', which invites immediate retries that keep hitting the limiter. **Fix:** Branch on `response.status` 401/429 and map to the existing `unauthorized`/`rate_limited` codes, carrying the server message and Retry-After into the failed payload. **Skeptic:** Straight code trace; the 429 and 401 collapse into a generic retryable failure. Dormant feature with no UI entry point, user-experience only: Low. Fix is local to the client stream.
