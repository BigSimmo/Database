**L81 · Routes panel card and three docs say 'all 15 modes'; the app registers 16 and the panel renders 16** — `src/lib/developer-area/hub-panels.ts:134; docs/codebase-index.md:54, :510, :699; CLAUDE.md:78` · docs-drift · CONFIRMED · safe_cleanup=true · L10-F5

`HUB_PANELS` summary for `routes` is 'Every page and all 15 modes'. `src/lib/app-modes.ts` has 16 `id:` entries (none `devOnly`), `data/repo-awareness-snapshot.json` `routes.counts.modes` is 16, and the routes page heading renders `App modes · 16` from the snapshot directly under a card that says 15. docs/codebase-index.md:787 already says 'all 16 modes', so the index contradicts itself. **Impact:** Inconsistent counts on a page whose purpose is stating what exists; no runtime effect. **Fix:** Change the card summary to 'Every page and every app mode' (count-free, so it cannot drift again) and update the three doc lines to 16. **Skeptic:** Counts verified offline; the index contradicts itself. Pure text drift, no runtime effect: Low. Fix is copy only. **Merged at Stage 5:** L94 (L18-F2) — Same 15-versus-16 mode count; the documentation half was corrected in commit 2 (§11.1); src/lib/developer-area/hub-panels.ts:134 still says 15 and is a code change.
