### M18 · npm run enrich:documents mutates live document/image rows across ALL owners and calls OpenAI by default, with no --dry-run, --confirm, or project guard

`scripts/enrich-documents.ts:27-36 (defaults), :41-44 (--all-owners), :507-511 (only refusal), :327-354, :439, :483-499, :604, :651 (writes)` · security · **CONFIRMED** · safe_cleanup=false · lane L15 (L15-F1)

> **Correction (Stage 5, red team):** State the mitigations in the defect paragraph: without --include-current only documents needsEnrichmentBackfill (:546-556) flags are touched, limit 25, CONCURRENCY = 1 (:564), and a populated .env.local with a service-role key is required. Nine .update( sites, not eight. Medium rests solely on the sibling-script convention.

- **Defect:** parseArgs sets allOwners = true whenever RAG_EVAL_OWNER_EMAIL / RAG_EVAL_OWNER_ID / LOCAL_NO_AUTH_OWNER_ID are all unset, mode defaults to 'summaries-labels-images' (OpenAI-backed) and limit to 25. The only refusal (line 507) fires when no owner AND allOwners is false — which the defaults make impossible. There is no --dry-run, no --confirm/--write, no readline confirm(), and no checkSupabaseProject() call, unlike every sibling backfill (backfill-smart-index, classify-documents, backfill-text-normalization all require --confirm --write; reindex.ts calls checkSupabaseProject and confirm()).
- **Trigger:** A session with a populated .env.local (loadEnvConfig at line 6 reads it silently) runs `npm run enrich:documents` with no arguments, e.g. to 'see what it does'.
- **Impact:** Up to 25 documents belonging to every owner on whichever Supabase project .env points at (production ref sjrfecxgysukkwxsowpy if that is configured) get their metadata, image searchable flags and skip_reason rewritten, and OpenAI is billed — all without the explicit confirmation AGENTS.md's provider boundary requires. Cross-owner writes by a service-role script contradict the tenancy defence-in-depth posture.
- **Proof:** not executed — traced. scripts/enrich-documents.ts:30-31 `allOwners: !process.env.RAG_EVAL_OWNER_EMAIL && !process.env.RAG_EVAL_OWNER_ID && !process.env.LOCAL_NO_AUTH_OWNER_ID`; grep for `--dry-run|--confirm|--write|checkSupabaseProject|confirm(` in the file returns nothing; rg of `.update(` returns 8 write sites. Comparison table of guard tokens per provider script produced by the lane's rg sweep (backfill-*.ts all carry --confirm --write; enrich-documents.ts carries none).
- **Fix sketch:** Make allOwners opt-in only via --all-owners; add --dry-run default (or require --write); call checkSupabaseProject() before creating the admin client; document the command in docs/scripts-index.md and docs/codebase-index.md with its guard semantics.
- **Caution:** Provider-backed: never run to reproduce without owner approval.
- **Skeptic (CONFIRMED):** Line-level claims all hold. No guard exists: no test, no confirm helper, no project-ref check (requireServerEnv validates env shape only; the mismatch check lives in isDemoMode/production-readiness, not on this path). Mitigations the finder under-states: without --include-current the script only touches documents that `needsEnrichmentBackfill` (:546-556) says lack current enrichment, so the writes are additive backfill rather than a rewrite of good data, and OpenAI spend is bounded by limit 25 at CONCURRENCY 1. Trigger needs a populated .env.local (service-role key), which per the re-grade rule argues for a downgrade, but the repo's own convention for write/backfill scripts (siblings all gate on --confirm/--write, process-hardening.md:730 says defaulting an owner on a write script is dangerous) and the AGENTS.md provider boundary treating script-embedded provider calls as confirmation-required make this a named defence-in-depth gap; Medium stands. Fix sketch (opt-in --all-owners, --write gate, checkSupabaseProjectConfig) touches no RAG-ranking, ledger, gate-manifest or migration surface.
