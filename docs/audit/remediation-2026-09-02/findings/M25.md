### M25 · Daily staging cross-tenant isolation harness has no failure reporting path

`.github/workflows/staging-tenancy.yml:3-6 (schedule daily 18:30 UTC, no issue/notify step); .github/workflows/notify-ci-failure.yml:16-25 (watched workflow list omits `Staging tenancy isolation`)` · process · **CONFIRMED** · safe_cleanup=false · lane L16 (L16-F3)

- **Defect:** notify-ci-failure.yml is the repo's declared solo-maintainer safety net and enumerates nine workflows by name; `Staging tenancy isolation` is not one of them, and the workflow itself only uploads an evidence artifact (`if: always()`), never opens or updates an issue. Every other scheduled provider-backed monitor (Eval Canary, Ingestion Autopilot, Live drift, Ops Digest) either is in the notify list or self-reports to an issue.
- **Trigger:** The daily run fails — a real cross-tenant leak on staging, or a rotated/absent CROSS_TENANT_* secret (scripts/test-cross-tenant-staging.ts:91-92 throws on a missing variable).
- **Impact:** An owner-boundary regression detector can fail every day without a chat alert or an issue; the only signal is GitHub's default failure e-mail to the last committer of the workflow file, which the repo's own notify design treats as insufficient. Confidence is medium because the schedule's current health and secret configuration cannot be read offline.
- **Proof:** grep '^name:' .github/workflows/staging-tenancy.yml → `Staging tenancy isolation`; notify-ci-failure.yml:16-25 lists CI, SAST, Secret Scan, PR Policy, Live domain monitor, Live drift check, Eval Canary, Ingestion Autopilot, Docker image build (all nine names verified against `name:` lines); staging-tenancy.yml has no `issues: write` and no github-script step. Traced, not executed.
- **Fix sketch:** Add `- Staging tenancy isolation` to the `workflows:` list in notify-ci-failure.yml (the head_repository/branch guard already admits scheduled runs), or add a failure-only rolling-issue step mirroring eval-canary.yml:203.
- **Skeptic (CONFIRMED):** Traced from workflow text: a daily owner-boundary detector whose failure produces neither chat alert nor issue, while the repo's own webhooks doc says the notifier covers 'the scheduled monitors'. Health of the schedule cannot be read offline, but the gap exists regardless of current state. Privacy-adjacent monitoring gap on a solo-maintainer repo justifies Medium; not High because no data is exposed by the gap itself. Fix (add one line to the `workflows:` list) is safe — the head_repository/branch guard at :43-47 already admits scheduled runs on main.
