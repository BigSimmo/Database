**L117 · Docling lab legacy runner reads table row/column counts from the wrong keys, so `rows`/`cols` are always null in the raw record** — `eval/docling/harness/run-legacy.ts:33-40` · quality/simplification · CONFIRMED · safe_cleanup=true · L05-F6

`tableFromImage` takes `metadata.table_rows` / `metadata.table_columns` and keeps them only `typeof === "number"`. The extractor emits those keys as arrays (extract_pdf_assets.py: `table_rows` is a list of row lists, `table_columns` a list of header cells) and puts the counts in `row_count` / `column_count`, so both fields are always null. `score.py` scores table cells from `markdown` only (markdown_to_cells), so Gate B numbers are unaffected; the fields are dead. **Impact:** No measurement impact; misleading raw output for anyone inspecting out/raw. **Fix:** Read `row_count` / `column_count` instead. **Skeptic:** Key mismatch is unambiguous; `rows`/`cols` are always null in the raw legacy record. Scoring is unaffected (markdown path), so no Gate B measurement impact. Low quality issue in the lab harness; the one-line fix (read row_count/column_count) touches nothing load-bearing.
