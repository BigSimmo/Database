**L73 · docs/database-drift-detection.md carries three statements that are now false about what gates on drift and what search_schema_health monitors** — `docs/database-drift-detection.md:72-77, :76, :227-228` · docs-drift · CONFIRMED · safe_cleanup=true · L06-F4

(1) :72-74 says check:drift 'runs only from live-drift.yml — workflow_dispatch plus a weekly Sunday 18:30 UTC cron — and blocks no PR or release … nothing forces a run between the drift appearing and runtime noticing'; live-drift.yml:11-17 now also runs on every push to main touching supabase/migrations/** or schema.sql, and AGENTS.md names that run the post-merge schema-application gate. (2) :76 says 'supabase/drift-allowlist.json is empty'; it holds 20 migration_history entries (index category is still empty). (3) :227-228 says search_schema_health monitors '22 names in the latest definer, … **Impact:** Under-states the post-merge protection and mis-states the monitoring ratchet the file itself introduced; a reader could conclude a merged migration has no automatic verification. **Fix:** Update the three sentences: add the push-to-main trigger, say 'the index categories of the allowlist are empty', and change to '30 names in the latest definer, 20260819100300'. **Skeptic:** All three statements verified false. The drift concerns a gate but errs in the conservative direction (under-states protection), so Low stands. Fix must say 'object categories of the allowlist are empty; 20 migration_history entries', not 'empty'.
