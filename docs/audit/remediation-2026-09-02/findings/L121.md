**L121 · The Review state page's data is excluded from the staleness gate by design, and nothing on the page tells the reader it can lag the review corpus** — `scripts/check-repo-awareness-snapshot.ts:41; src/components/developer-area/hub/review-state-page-content.tsx (scope paragraph)` · quality/simplification · CONFIRMED · safe_cleanup=false · L10-F8

`COMPARED_CONTENT_KEYS = ["routes", "documentation", "test_health"]` deliberately omits `review_state`, and `REVISION_INPUTS` omits the review corpus, so the committed `review_state` can fall behind `docs/branch-review-records/` with no gate and with a freshness stamp that cannot move for it. The page copy says 'This is the repository's own review history' without saying it is refreshed only when someone runs `npm run snapshot:repo-awareness`. Today it is in step (snapshot 2,665 records = 880 + 1,206 + 579 from check:branch-review-ledger). **Impact:** Silent under-reporting on a reference page; low because the canonical record is the ledger, not this page. **Fix:** Add one sentence to the page: 'Review records are refreshed only when the snapshot is regenerated (`npm run docs:update`); the stamp above does not move for them.' Optionally compare a records-count-only key in the gate, which cannot conflict on concurrent appends. **Skeptic:** A deliberate consequence of the #EFETZT fix (c0ee4ddd5) that the page copy does not disclose; not the same defect as #EFETZT (which was staleness/conflicts of the gate), so not a duplicate. Reference page can silently lag; canonical record is the ledger: Low. One-sentence copy fix; the optional count-only gate key would need the same care about …
