**L120 · Three ward exports are used only inside their own file and are pinned by no plan, spec or test — export-only cleanup candidates; five other knip hits are pinned and must stay** — `src/components/ward-management/discharges/discharge-board.tsx:73 (groupDischarges); search/patient-search.tsx:129 (ResultsSection); use-ward-sidebar-collapsed.t` · quality/simplification · CONFIRMED · safe_cleanup=true · L09-F7

knip lists 8 unused ward function/const exports and 9 unused type exports. For each I counted importers in src, mentions in tests, and mentions in plans/specs. groupDischarges, ResultsSection and readWardSidebarCollapsedPreference have 2 uses each inside their own module and zero mentions anywhere else, so dropping the `export` keyword changes no behaviour. unitSiteCode (2 plans), movementsByStage (3 plans; named as alive in check-dead-code-candidate.mjs:7), WardModeNavigation (2 plans, 2 tests), isMoreRestrictiveThanRequired and MORE_RESTRICTIVE_NOTE (1 test each) are pinned and are not … **Impact:** Marginal; keeps the knip exports report from masking a real orphan later. **Fix:** Remove `export` from the three declarations (a `check:knip:exports` re-run is the proof); leave the other five until L09-F1's plan bookkeeping is fixed and `check:dead-code-candidate --symbol` can be consulted honestly. **Skeptic:** The three export-only candidates reproduce exactly (two intra-file uses, no external mention). Note the gate still refuses groupDischarges on the 30-day age rule alone, which matters only for deletion; dropping the export keyword while keeping the declaration is not a deletion and changes no behaviour. The finder correctly keeps the five pinned …
