**L56 · Superpowers plan checkboxes are never ticked (2,372 open vs 11 done), so the dead-code gate's 'plan with unchecked tasks' refusal is permanent for every symbol any plan ever named, and cannot tell finished or abandoned plans from in-flight ones** — `docs/superpowers/plans/*.md (50 files with checkboxes); scripts/check-dead-code-candidate.mjs:208-221, 323` · process · CONFIRMED · safe_cleanup=false · L18-F3

Across docs/superpowers, 50 plan/spec files carry 2,372 unchecked `- [ ]` tasks and only 11 checked ones; every plan with checkboxes reports 0 done. That includes plans whose work has demonstrably landed — 2026-08-22-mode-aware-clinical-ask-implementation.md (98 open) while src/lib/clinical-ask/ and five clinical-ask components exist; 2026-08-20-care-plan-implementation.md (211 open) while src/app/mockups/care-plan/** exists — and plans that were superseded — 2026-08-14-ward-management-mockups.md (35 open; all four target paths it names are absent) and … **Impact:** Dead-code triage is blocked for symbols in completed plans (the gate can never say 'this plan finished'), while abandoned plans keep unbuilt or renamed scaffolding alive indefinitely; readers cannot tell from the plan whether the work exists. The gate's plan signal has degraded to 'named in any … **Fix:** Owner decision, not a code change: either (a) adopt a 'Status: complete/superseded' front-matter line that planContractHits() honours so finished plans stop refusing, moving superseded plans (2026-08-14 ward-management-mockups, 2026-07-04 rate-limits) to docs/archive; or (b) tick boxes when tasks … **Skeptic:** Numbers reproduce and the code path is as described: the refusal never expires because no plan is ever ticked and no status marker is honoured. However, the gate is deliberately fail-closed after the #2204 incident, so the 'defect' is plan hygiene and an owner decision, not a broken control — Low process, not Medium. Caution on fix_sketch (a): …
