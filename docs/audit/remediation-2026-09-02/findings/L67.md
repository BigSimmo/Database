**L67 · No unit tests for four of the five custom ESLint rules, and no service-worker test for /forms-pdf/\*.pdf** — `eslint-rules/{require-z-index-ladder,no-hardcoded-hex,require-lucide-icon-aria,restrict-suppress-hydration-warning}.mjs; tests/pwa-service-worker.test.ts:533-53` · test-coverage · CONFIRMED · safe_cleanup=false · L13-F9

Only require-button-wiring has a test file (tests/require-button-wiring.test.ts). The worker test's denylist table covers /api/search, a private /documents/*.pdf, Range and Vary cases, but not the 51 same-origin `/forms-pdf/*.pdf` statutory forms (50 password-gated per check:forms-pdf-manifest), which are the largest sensitive static path on the origin. **Impact:** Regressions like L13-F3 have no offline detector; the forms-PDF network-only behaviour is currently correct by trace (neither isImmutableNextAsset nor isPublicPwaAsset matches `/forms-pdf/`) but unpinned. **Fix:** Add RuleTester suites for the four rules and one `{ label: 'statutory form PDF', url: '/forms-pdf/form-1a.pdf' }` denylist row. **Skeptic:** Coverage gap verified: four of five custom rules have no RuleTester suite (L13-F3's regex gap is the live example), and the worker denylist table never exercises the statutory-forms path even though the behaviour is currently correct by trace. Low: nothing is broken today; the value is pinning. Adding tests is additive and cannot lower a …
