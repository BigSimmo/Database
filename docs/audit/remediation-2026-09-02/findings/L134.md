**L134 · Process-local answer cache evicts by insertion order and overwrites keep their old position, so a refreshed hot answer is evicted first** — `src/lib/rag/rag-cache.ts:264-275` · quality/simplification · CONFIRMED · safe_cleanup=false · L22-F7

`answerCache.set(key, …)` without a preceding `delete` leaves a re-cached key at its original Map position, and `getCachedAnswer` never bumps recency; eviction `while (size > RAG_ANSWER_CACHE_SIZE)` therefore drops the oldest-inserted entry, not the least-recently used. `owner-catalogue-cache.ts:48-49,61` and `signed-url-cache.ts:41-42,50` implement the delete-then-set LRU bump correctly for the same shape, and `bounded-ttl-cache.ts:25` does too — this is the one cache in the lane that does not. **Impact:** Lower hit-rate than the cache size suggests on a ward-round pattern (the same handful of questions repeated); `cacheMetricsSnapshot` would under-report effectiveness. No correctness impact. **Fix:** Route answerCache through `writeBoundedExpiringCacheEntry` from bounded-ttl-cache.ts (already imported at :4) or add the delete-before-set and a read-side bump. RAG impact: no retrieval behaviour change. **Skeptic:** FIFO-not-LRU is confirmed: hits never bump recency, so a hot repeated question ages out in insertion order. The 'overwrites keep their old position' half is largely unreachable — getCachedAnswer deletes the key on expiry or version mismatch before a miss (rag-cache.ts:208-215), so a re-set normally appends at the end; only a concurrent duplicate …
