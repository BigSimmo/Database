**L86 · public/llms.txt still calls the product 'Clinical Guide' and a browser test pins the stale name** — `public/llms.txt:1,3; tests/ui-smoke.spec.ts:1478` · docs-drift · CONFIRMED · safe_cleanup=false · L13-F4

The agent-facing product description was last edited 2026-07-07 (a860c22e5) and predates the 2026-08-28 PsychSift rename (a971decfc). tests/brand-copy.test.ts only scans src/, so public/ escaped the brand gate, and ui-smoke asserts `toContain("Clinical Guide")`, so correcting the file would fail CI until the test moves with it. **Impact:** A public statement on the clinical origin describes a product name that no longer exists, undermining the single-source brand governance the repo pays for; the pinned test converts a fix into a two-file change that a contributor may not expect. **Fix:** Rename in llms.txt (keep the 'Clinical Guide' wording only where it means the phone drawer, per brand.ts:65), update the ui-smoke assertion to the brand name, and add public/llms.txt to the brand-copy test's scanned files. **Skeptic:** All quoted lines match the claim; the brand gate is narrower than the finder implies (it polices retyped taglines, not the name) but the conclusion stands: nothing scans public/llms.txt and a Playwright assertion pins the stale name. Downgraded to Low per the docs-drift rule — the false claim is a product name, not safety/privacy/gate. Fix is a … **Merged at Stage 5:** L106 (L23-F2) — Same stale llms.txt brand name and the same ui-smoke pin; L23-F2 adds the three route claims the proxy contradicts.
