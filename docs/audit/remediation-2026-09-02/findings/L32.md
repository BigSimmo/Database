**L32 · /api/ingestion/jobs, the endpoint the hub polls, is administrator-gated but has no rate limit unlike its sibling admin routes** — `src/app/api/ingestion/jobs/route.ts:43-94; src/components/developer-area/hub/ingestion-panel.tsx:291-297` · security · CONFIRMED · safe_cleanup=false · L10-F9

The route requires `requireAuthenticatedUser(request, supabase, { administrator: true })` with a fresh Auth read (auth.ts:256-269), and both queries are scoped by `.eq("documents.owner_id", user.id)` on an `!inner` join, so auth and owner scoping are correct. It does not call `consumeApiRateLimit`, which ten other API routes (including `/api/clinical-quality` and `/api/ingestion/quality`) do. The hub polls at the server-supplied `pollAfterMs` (5,000 ms) only while `hasActiveJobs` is true; `ClinicalDashboard.tsx:995` polls the same endpoint. Two `count: "exact"` queries per poll. **Impact:** Bounded database load from an already-privileged caller; no data exposure. Recorded because the lane probe asked and because the sibling routes set the expectation. **Fix:** Add the same `consumeApiRateLimit({ ownerId: user.id, ... })` guard with a budget comfortably above 2 requests / 5 s, and have the panel honour `Retry-After` by treating 429 as a transient fetch-error rather than an auth failure. **Skeptic:** Auth and owner scoping are correct; only the limiter that sibling admin routes carry is missing. Caller must already hold the administrator claim, so the re-grade rule caps it at Low. Fix is additive (same helper the siblings use); the panel must treat 429 as transient, as the sketch notes.
