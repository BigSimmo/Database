**L50 · CARING_CONTACTS_DATABASE_URL — the one variable that switches the workspace from in-memory to Postgres — is documented nowhere operators look** — `src/lib/caring-contacts-server/config.ts:18,34-42; .env.example (no entry); src/lib/env.ts (no entry); docs/deployment-architecture.md (no mention); scripts/che` · process · CONFIRMED · safe_cleanup=false · L07-F8

The variable appears only in caring-contacts/run-db-tests.mjs, ci.yml, plan documents and Phase 2A archives. `check:env-parity` (passed, per fast-gates-summary) cannot report it because it is not in .env.example or env.ts. Nothing states which Railway service, if any, should carry it, nor that the login role it names must be a member of `caring_contacts_app` (0001:54 grants membership only to the migration's current_user), without which every `set local role` at postgres-repository.ts:469 fails. **Impact:** Misconfiguration fails closed (500s, or the in-memory demo store), so this is hygiene rather than exposure; but the fail-closed behaviour is undocumented too. **Fix:** Add a commented-out entry to .env.example, one paragraph in docs/deployment-architecture.md (which service, role membership requirement, the two separation guards), and let check:env-parity see it. **Skeptic:** All claims reproduce: the switch variable is undocumented in .env.example, env.ts and deployment-architecture.md, is invisible to check:env-parity by construction, and the role-membership prerequisite for a non-migration login role is stated nowhere operator-facing. The unset case fails closed to in-memory (config.ts:40-42), so no exposure. Not in …
