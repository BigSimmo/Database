{
  "acceptanceCriterion": "Everything committed on the integration branch after 89d7f99ec is sound, decided by a party that did not build it and did not advise on it. Five claims, each decided separately: (1) the steady-state transition is legitimate - all three receipts share one sourceSha, only files under docs/ward-flow/control/evidence/ changed between that sourceSha and the activationSnapshot, the steady-state edit to system-state.json lands strictly after the snapshot, and the recorded runner receipt's inputHash equals committedTreeInputSignature of the snapshot with fileCount 5962; (2) the recovery bundle is real - git bundle verify passes, it advertises the sourceSha at its recorded ref, and cloning it into a NEWLY CREATED empty directory of your own choosing restores that HEAD with both the sourceSha and the integrationBase objects present; (3) the current-truth inventory regenerates byte-identically at its sourceSha, and its disposition manifest classifies every id exactly once in inventory order with zero unclassified and every supersession chain terminating at a canonical source; (4) the community hub associates a person with a team ONLY via a team named on their referral, never via home region - proven by constructing two admissions sharing a homeRegion whose referrals name different teams and confirming they land on different pages; (5) the re-anchor fix is complete - every Instant-typed field declared on Admission in ward-admissions.ts, including the one nested inside followUp, is present in INSTANT_FIELDS in ward-reanchor.ts, and shifting the real seed by a non-zero offset moves every one of them.",
  "criterionId": "WF-VERIFY-20260901-001",
  "falsifier": "Any of: a non-evidence path inside the transition window; the three receipts disagreeing on sourceSha; the steady-state edit landing inside the window rather than after it; a runner receipt whose inputHash or fileCount differs from the committed tree at the activation snapshot; the bundle failing verify, advertising the wrong ref, or restoring to the wrong HEAD; a regenerated inventory differing from the committed one by a single byte; any source unclassified, duplicated, or out of inventory order; a supersession chain that cycles or ends anywhere but a canonical source; an admission being placed on a team page by anything other than its referral, or two same-region admissions with different referral teams landing on the same page; or any Admission Instant field absent from INSTANT_FIELDS or measurably unshifted at a non-zero offset.",
  "focusedCheck": "Git plumbing, hashing, a clone into a directory you create yourself, one regeneration of the inventory via buildExpectedSourceInventory, and a direct measurement of the seed before and after shiftInstants. Run npx vitest run tests/ward-community-hub.test.ts tests/ward-community-hub.dom.test.tsx tests/ward-reanchor.test.ts for claim 4 and 5's existing coverage, but do not treat a passing suite as the answer - the suites were written by the party being checked. No provider, no push, no full-suite gate, and no write anywhere inside the repository.",
  "kind": "ward-flow-verification-criterion",
  "notes": "The target SHA is pinned by the lease rather than named here, so this record cannot go stale against the commit it is attached to. TWO WARNINGS FROM THE PREDECESSOR, both from its own retirement handover. First: it advised Ward Lead on this transition's sourceSha and ordering, so its independence on claim 1 was compromised and it said so - you did not advise, which is the whole reason claims 1 to 3 are here rather than closed. Second: it recorded that nothing committed after 89d7f99ec had been independently verified at all. That is still true when you start, and it is the gap you exist to close. Where a check depends on a number Ward Lead has stated, recompute it; treat every figure in this record as a claim to falsify rather than a target to reproduce.",
  "schemaVersion": 1
}
