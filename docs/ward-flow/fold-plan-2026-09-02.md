# The fold plan — 2026-09-02 evening

**Written by Ward Lead so the sequence survives a compaction. Measured at master `bea964c3b`, with a
control: `git merge-tree --write-tree claude/Wardquestions HEAD` exits 1 (conflicts detected, so the
instrument works). All three builder branches exit 0 and each yields a tree distinct from HEAD's.**

```
claude/ward-builder-two              exit 0   8 ahead / 19 behind
claude/ward-builder-community-route  exit 0   9 ahead /  9 behind
claude/ward-builder-three            exit 0  19 ahead / 11 behind
```

## ⚠️ THE ONE RULE THAT MUST NOT BEND

**`fcb8af1da` — the engine refusal — does not fold until `ward-screen.tsx` can BOTH show a refusal AND
offer the reasoned way through.** Today that screen is silently permissive; with the engine alone it is
**silently broken**, and every gate stays green through it.

⚠️ **AND A HOLD ON A COMMIT IS A HOLD ON ITS WHOLE BRANCH.** A merge takes history, not a selection, so
`claude/ward-builder-three` cannot be partially folded — its unrelated work (the form-blind scan, dark
mode, both contradictory comments, its register) is held with the engine change. **Nobody saw that
coming; the lesson is that work known to need someone else's half should start on its own branch.**

## The order

| #   | What                                                                                                                                                                         | Whose                       | State                                                       |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------- | ----------------------------------------------------------- |
| 1   | Browser-suite repairs                                                                                                                                                        | Ward Builder One            | 2 of 6 fixed, 3 diagnosed, 1 file re-proved. **Not green.** |
| 2   | Fixture, registers, both-reasons, engine/match findings                                                                                                                      | Ward Builder Two            | ready                                                       |
| 3   | **B1** — `ward-screen.tsx` renders the refusal                                                                                                                               | Ward Lead                   | in flight                                                   |
| 4   | **Event field** — `overrideReason?` on `ACCEPT_REFERRAL`                                                                                                                     | Ward Builder Three          | asked; it may refuse                                        |
| 5   | **B2** — the reasoned override control on `ward-screen.tsx`                                                                                                                  | Ward Builder Three, after 3 | queued                                                      |
| 6   | **Item 3** — the accept control on `referral-match.tsx`                                                                                                                      | Ward Builder Two            | building the no-new-field parts                             |
| 7   | **Item 2** — `ACCEPT_REFERRAL` reason path in the reducer                                                                                                                    | Ward Builder Three          | queued                                                      |
| 8   | **`fcb8af1da` + the rest of Three's branch**                                                                                                                                 | Ward Builder Three          | **HELD until 3 and 5**                                      |
| 9   | **Item 1** — the cohort pre-filter and the display cap                                                                                                                       | Ward Builder Two            | **parked until 6+7**                                        |
| 10  | ⚠️ **`ward-screen-eligibility-warning.dom.test.tsx` turned around** — same patient, same unsuitable ward, now asserting the placement is refused unless a reason is recorded | **Ward Lead**               | **blocked behind 8**                                        |

⚠️ **ITEM 10 IS WARD LEAD'S OWN AND IT WAS MISSING FROM THE FIRST VERSION OF THIS PLAN.** The owner
ruled on it directly — _"Yes to your recommendation"_ — and it is the test whose refutation started the
whole engine thread. **It cannot be written until `fcb8af1da` folds, because it asserts a refusal that
does not happen yet.** The integrator's own outstanding work is the easiest to lose from a plan the
integrator writes.

⚠️ **PAIRS THAT MUST NOT SHIP APART:** 3+5 (a screen that shows a refusal but cannot answer it is half a
feature), and 6+7 (a button without its engine lies to the person pressing it; an engine without the
button is the silent-refusal defect on a second screen).

⚠️ **AND 9 IS PARKED ON PURPOSE, NOT FORGOTTEN.** Unfiltering the shortlist before the front door can
accept a reasoned referral offers a coordinator wards that will still be refused — **a worse screen than
the one we have now.**

## ⚠️ B2's SHAPE — RULED: REACTIVE, NOT PROACTIVE

**The reason control appears only AFTER a refusal, growing out of the alert B1 already renders. It does
NOT sit on the row waiting to be used.**

**Ward Builder Three raised it as a design decision nobody had taken and declined to build until it was
ruled — "not reversible cheaply once a test pins it", which is the right test for when to stop.** ⚠️ **A
design choice with a passing test attached stops being a choice.**

**The deciding argument is clinical and it is Ward Builder Three's:** _the ward sees the specific gate it
failed BEFORE choosing a reason, which is the right order for a clinical decision._ ⚠️ **Proactive gets
the order backwards — it asks for a justification before the person knows what they are justifying, and
a reason dropdown offered before any refusal reads as "pick one of these to proceed". That is not a
control, it is a suggestion, and the thing it suggests is overriding.**

**Second argument, as strong and less obvious: nothing appears until a refusal exists, so THE ORDINARY
CASE IS UNTOUCHED.** ⚠️ **A design that adds a clinical-override affordance to every row in order to
serve the rare row has made the rare case cheap** — and cheapness is the wrong property for this.

**And reusing B1's alert rather than adding a second surface matters beyond tidiness: the refusal and the
way through are ONE THING to the person reading them.** Splitting them across two surfaces is exactly how
_"the explanation is there, the button is not"_ happened on the front door.

**WORDING: ships behind placeholders, and the fold does not wait for it.** ⚠️ **But the placeholders must
be UNMISTAKABLY placeholder in the code, not merely neutral** — using the `⚠️ PLACEHOLDER VALUES. THE
OWNER HAS NOT CHOSEN THESE.` pattern `ward-change-reasons.ts` already carries, whose own comment says it
best: _a chosen value and a provisional value look identical in code; the difference is whether anybody
can find out._

## ✅ THE FOLD GATE IS NOW MECHANICAL — `0b603f4d1`. Stop maintaining the list below by hand.

**The set of overridable events is derived from the event union BY AST, so it grows on its own.** Two
guards, deliberately separate:

- **COMPLIANCE** — every surface supplies the reason field, or is allowlisted with a reason and a
  removal condition.
- **COVERAGE** — every `dispatch` with a non-literal type is named. ⚠️ **This half is Ward Verifier's
  and it is the one that generalises.**

**The allowlist IS the gate now:**

```
ward/ward-screen.tsx           ACCEPT_IN_PRINCIPLE, PULL_PATIENT    <- B2, in progress
morning/morning-tour.tsx       REFER_TO_UNITS, ACCEPT_IN_PRINCIPLE  <- paused
referrals/referral-match.tsx   ACCEPT_REFERRAL                      <- Ward Builder Two
coordinator/shortlist-panel    REFER_TO_UNITS                       <- correct, not allowlisted
```

### ⚠️ CORRECTION — I WROTE "REMOVING AN ALLOWLIST ENTRY IS THE MECHANICAL PROOF THAT A SURFACE IS DONE." IT IS NOT.

**The compliance check groups by `(file, eventType)` and passes if ANY construction site in that file
for that event carries the field.** ⚠️ **That grouping is CORRECT and deliberate** — flagging
`shortlist-panel.tsx`'s ordinary refer, which rightly carries no reason, would be a false positive **on
the one surface already doing this properly.**

### ⚠️⚠️ AND THE CORRECTION ABOVE WAS ITSELF WRONG. THE ORIGINAL CLAIM STANDS FOR `ward-screen.tsx`.

**I wrote that `ward-screen.tsx` has TEN `PULL_PATIENT` sites and THIRTEEN `ACCEPT_IN_PRINCIPLE` sites.**
⚠️ **THOSE ARE MENTION COUNTS. Measured, with a control:**

```
ward-screen.tsx   PULL_PATIENT         bare name 10   construction ( type: "…" )  1
                  ACCEPT_IN_PRINCIPLE  bare name 13   construction                1
CONTROL  shortlist-panel.tsx  REFER_TO_UNITS  bare name  5   construction         2
```

**The other nine are a doc comment, a label map, four prose comments, an `attempted ===` comparison and
a testid. The AST scan agrees independently: exactly one construction site per event type, at `:1233`
and `:1382`. And the control discriminates — it finds two real sites where two exist.**

⚠️ **WITH ONE SITE PER EVENT TYPE, "ANY SITE CARRIES THE FIELD" AND "ALL SITES CARRY THE FIELD" ARE THE
SAME STATEMENT. So removing `ward-screen.tsx` from the allowlist DOES prove that surface is done, today.**
The `(file, eventType)` grouping is weaker than it reads **only where a file has several sites for one
event** — true of `shortlist-panel.tsx`, which is exactly why the grouping is right there, and **not true
of `ward-screen.tsx` at all.**

**Ward Verifier's structural point survives as a FUTURE fragility, not a live weakness:** add a second
pull button and the guard would go green on one of them. **Ward Builder Three is pinning the per-surface
site counts so that goes red the day it happens.**

⚠️ **THE SHAPE OF MY ERROR IS WORTH MORE THAN THE CORRECTION: I counted mentions and treated them as
things — INSIDE the correction where I was convicting myself for promoting a guard past its stated
limit.** Ward Verifier passed me the same number and I did not check it. **Two careful chats, both
counting a name. The fifth face of tonight's one disease, and the first to appear inside an apology for
a different instance of itself.**

**THE FOLD GATE STAYS "entry removed AND somebody opened the screen" — but for the CLINICAL reason, not
because the guard is too weak.** A human looking at a clinical control is right anyway.

### ⚠️ AND THE HUMAN HALF OF THAT GATE HAS A TRAP. READ THIS BEFORE YOU LOOK AT THE WARD SCREEN.

⚠️ **THE WARD SCREEN ALREADY RENDERS A REASON DROPDOWN, AND IT IS NOT THE OVERRIDE CONTROL.** Measured at
master:

```
ward/ward-screen.tsx    OVERRIDE_REASONS 0    overrideReason 0    DECLINE_REASONS 2 (import :41, render :1291)
```

**`DECLINE_REASONS` has SEVEN entries and is for DECLINING A REFERRAL. `OVERRIDE_REASONS` has FIVE and is for
OVERRIDING A PLACEMENT GATE. Different list, different event, different purpose.**

⚠️ **SO A PERSON CHECKING "DOES THE WARD SCREEN HAVE A REASON CONTROL YET" — BY LOOKING, OR BY GREPPING FOR A
REASON LIST — GETS **YES**, AND THE ANSWER TO THE QUESTION THEY MEANT IS **NO**.** **B2 has not landed.**

**For once the MECHANICAL half is the safe one:** the guard keys on `overrideReason` inside an
`ACCEPT_IN_PRINCIPLE`/`PULL_PATIENT` construction site, which the decline control does not touch. ⚠️ **The
guard is right and THE EYE is the thing at risk** — which is the exact inverse of every other hazard in this
document, and it lands squarely on the human half of the gate I just strengthened.

**Say it in one line to whoever does the look: the ward screen's existing decline picker is NOT the override
control. `DECLINE_REASONS` ≠ `OVERRIDE_REASONS`.**

**With the header's own admitted gaps (dead code, a disabled flag, an unmounted branch), removing an
entry actually proves: _at one construction site the scan could see, in that file, for that event, an
`overrideReason` property was present in the object literal._ That is a long way from "a nurse can
record a reason."**

⚠️ **THE GUARD'S HEADER ALREADY SAYS ALL OF THIS** — it names the proxy, gives both failure directions,
and says **DO NOT CONCLUDE FROM GREEN** in those words. **I promoted it past its own stated limit, which
is exactly the failure that header was written to prevent — committed by the person RELYING on the guard
rather than the person who built it. That is how a stated limit gets lost.**

**THE FOLD GATE THEREFORE READS: the allowlist entry is removed AND somebody opened the screen.** For a
clinical control that human look was right anyway; **the guard narrows what needs looking at, it does not
replace the looking.** The four-surface table below is history as a MAINTAINED LIST — it is still the
record of how the gate was derived.

### ⚠️ And the mutation that convicted every count-based guard, including mine

**Terminating the event union early — syntactically valid, nothing else complains — leaves 3 overridable
members. The "at least 3" count guard PASSED.** The parse had silently stopped following the union and
**every count-based assertion agreed everything was fine.** Only the **named** `ACCEPT_REFERRAL` pin went
red.

⚠️ **A COUNT TELLS YOU THE PARSE RETURNED SOMETHING. A NAMED MEMBER TELLS YOU IT RETURNED THE RIGHT
THING.** Demonstrated rather than argued — and it is the same compliance-versus-coverage split one level
down, inside the guard's own assertions.

**Disclosed residual, not hidden:** the pin catches `ACCEPT_REFERRAL` being dropped, not the other
three. **It protects against a FIFTH member, not against the existing three being removed.**

## ⚠️ FOLD CONDITION added 2026-09-02 late — the engine speaks one sentence to every caller

**Before `fcb8af1da` folds: for every surface WIRED TO DISPATCH an event whose refusal can carry the
string — WHETHER OR NOT THAT SURFACE IS CURRENTLY RENDERED — a reason control must exist on it.**

⚠️ **The "whether or not it is rendered" clause is one clause long and it is the one that catches the
third surface.** My first wording was _"every surface that can receive the refusal string"_, which
silently excludes anything paused — **and a paused surface is one whose defect is SCHEDULED, not
absent.**

**THE COMPUTED CHECKLIST — four surfaces, measured at `claude/ward-builder-three`, construction sites
only, with a control (`return` present in every file, so the search reads them all):**

| Surface                           | `OVERRIDE_REASONS` | `overrideReason` | dispatches                              | can answer?             |
| --------------------------------- | ------------------ | ---------------- | --------------------------------------- | ----------------------- |
| `coordinator/shortlist-panel.tsx` | **5**              | **5**            | `REFER_TO_UNITS`                        | ✅ **yes**              |
| `ward/ward-screen.tsx`            | 0                  | 0                | `ACCEPT_IN_PRINCIPLE`, `PULL_PATIENT`   | ❌ B2 in flight         |
| `morning/morning-tour.tsx`        | 0                  | 0                | `REFER_TO_UNITS`, `ACCEPT_IN_PRINCIPLE` | ❌ paused but **wired** |
| `referrals/referral-match.tsx`    | 0                  | 0                | `ACCEPT_REFERRAL` (`:202`, sole site)   | ❌ **item 6**           |

⚠️ **THREE OF FOUR CANNOT ANSWER. Only the coordinator's shortlist can.**

### ✅ CONFIRMED BY A THIRD, INDEPENDENT COMPUTATION — same four, different method

**Because this list was derived wrongly twice, a third pass was run from a deliberately different
starting point** — from **every `reject(...)` call in the reducer** (~100 sites) rather than from the two
named refusal functions, and with a whole-`src/` literal grep for `type: "<EVENT>"` rather than the bare
event names.

**It found the same four files, the same event attribution, and the same can-answer verdicts.** ⚠️ **And
it did NOT fall into the trap that caught the second attempt** — `ACCEPT_REFERRAL` reaches its refusal
only through `referralAcceptanceRefusal`, so starting from `eligibilityRefusal`'s call sites misses it;
starting from all `reject()` calls does not.

**It also separated the 11 literal hits from the 23 bare-name hits:** four are type-declaration members
in `ward-flow-events.ts`, the rest are reducer `case` labels, comments, and a role-permission **map key**
(`REFER_TO_UNITS: ["coordinator"]`) — none of which construct anything. **Controls: the discriminator
correctly excluded a known comment and included a known construction site; `return` counts were non-zero
in all four reported files, proving each was actually read.**

### ⚠️ THE SHARED BLIND SPOT — CHECKED, AND IT IS NOT EMPTY

**All three computations are static searches for a literal `type: "EVENT"`.** An event built dynamically
is invisible to every one of them. **I recorded that as absent-and-unverified rather than closing it by
assumption. Ward Verifier then checked it, and the pattern EXISTS:**

```
ward-management-console.tsx:480
  type: patient.flaggedUrgent ? "CLEAR_MOVEMENT_URGENT_FLAG" : "FLAG_MOVEMENT_URGENT",
```

**A real `dispatch()` inside a button's `onClick`. A COMPUTED event type, invisible to every method we
own.** Confirmed independently: a grep for a non-literal `type:` across all of `ward-management` returns
**exactly this one site.**

⚠️ **BENIGN TODAY — neither of those events is override-gated, so the four-surface table stands
unchanged.** But _"no such construction is known to exist"_ is now false, **and the shape is IDIOMATIC
here rather than merely possible. The next one could be over an override-gated event, and every method
we own would pass it green.**

**The rest of the sweep, so the gap is closed rather than narrowed:** three `dispatch(` sites take a
non-literal argument, all in `morning-tour.tsx` — **harmless, because those events are built as literals
elsewhere in the same file, so the literal search still sees that surface. Indirection alone does not
hide one.** One spread-near-a-type hit was a false positive (an array spread of unit ids).

### ✅ COVERAGE IS SRC-WIDE, NOT WARD-MANAGEMENT-WIDE — and the control is what makes the zeros real

**Ward Verifier then found a residual in its own sweep: it had searched for computed event types only
inside `ward-management` — the same coverage gap it had just finished naming, applied to its own scope
choice.** Swept all of `src/` and confirmed independently here:

```
dispatch( sites in src/                              118
CONTROL: dispatch sites OUTSIDE ward-management      yes — care-plan/mockups/** (4+ files)
REFER_TO_UNITS       outside ward-management          0 files
ACCEPT_IN_PRINCIPLE  outside ward-management          0 files
PULL_PATIENT         outside ward-management          0 files
ACCEPT_REFERRAL      outside ward-management          0 files
```

⚠️ **THE CONTROL IS THE WHOLE VALUE: there ARE dispatch sites outside `ward-management`, so those zeros
are the probe finding nothing rather than the probe not looking.** **The four-surface table's coverage
can therefore be stated src-wide.**

**One shape remains unexcluded, and it is recorded as a JUDGEMENT rather than an oversight:** an event
name assembled from fragments — `` type: `ACCEPT_${x}` `` — would evade a name search entirely. **Nothing
does that**; the single computed-type site uses two complete string literals in a ternary. ⚠️ **No check
was built for fragment assembly, deliberately: it would be a bizarre way to write a dispatch, and the
closed union plus `EVENT_ROLE` totality make it hard to do by accident.** **Written down so the next
reader inherits a decision, not a hole somebody forgot.**

### ⚠️ AND THE PRINCIPLE, WHICH OUTRANKS THE INSTANCE

**Every anti-vacuity guard we built checks that the search found SOMETHING. None checks that it found
EVERYTHING.**

⚠️ **Our three computations asserted COMPLIANCE — every site found supplies the field. None asserted
COVERAGE — that every site was found. Compliance without coverage is exactly how three careful,
differently-started computations agreed on a list that was missing a surface.**

**A static search that cannot detect its own incompleteness is the same class as a check that cannot
fail.** ⚠️ **"Three agreeing methods that share one assumption are one method with three witnesses" —
and the addition is that the assumption is now TESTABLE, so it need not stay an assumption.** The
coverage assertion — count every `dispatch(` site, count those with a literal `type:`, pin the
difference to a named allowlist — has gone to Ward Builder Three for the derived guard.

⚠️ **AND THE FOURTH IS THE CHANGE'S OWN SURFACE.** `referralAcceptanceRefusal` exists to make the front
door overridable; the only file dispatching `ACCEPT_REFERRAL` is `referral-match.tsx`, and it cannot
record a reason. **The change fixes the front door's policy and leaves the front door unable to use it.**

⚠️ **AND IT ALREADY HAS THE DISPLAY HALF** — `:198` captures `rejections.length`, `:208` bumps
`checkToken`, the same detect-a-new-rejection pattern the ward screen just gained. **So it will show a
refusal saying a recorded reason is the way through and offer no way to record one. Not a future risk:
the finished state of that file the moment the engine folds.** It is also the file already carrying the
accept-control-and-reason-as-two-arms-of-one-ternary defect — **about to acquire a second defect of the
same family, at the same moment, from a different direction.**

### ⚠️ THE SET OF GATED EVENTS IS NOT FIXED, AND THAT IS THE REAL HAZARD

**This gate is defined over "events whose refusal can carry the string", and that set GREW TONIGHT** —
silently, when `ACCEPT_REFERRAL` became override-gated. ⚠️ **Any future change that makes another event
overridable ENLARGES THIS GATE WITHOUT TOUCHING ITS WORDING, and nothing goes red.**

**So the checklist above is not decoration: re-derive it whenever the event set changes.** The list was
computed wrongly twice tonight — once missing the paused surface, once missing the change's own surface
— **both times by careful people deriving it under time pressure from the wrong starting point.**

⚠️ **`morning-tour.tsx` is unrendered by owner instruction, which is why three separate counts missed
it — including two by the chat that found it, after it had read the pause comment itself.** The pause
comment says the tour returns _"when the application it describes exists"_ — **a decision somebody
makes later, on other grounds, without reading this plan.**

**And the breakage would be specific, because the tour's pair is HARDCODED:**
`TOUR_MOVEMENT_ID = "WF-901"`, `TOUR_UNIT_ID = "scgh-adult-open"`. ⚠️ **If that pair fails any
suitability gate under the new engine, the tour's own scripted step is refused, it cannot supply a
reason, and it stops working — on the day it is un-paused, attributed to the un-pausing, months after
the change that caused it, by somebody who never saw this conversation.**

**Being checked now: `eligibility(WF-901, scgh-adult-open)` under the new gate.** Pass and it is noted;
fail and **it is recorded beside the pause, not discovered later.**

**Why it was added.** B1 landed the display half and closed its defect — and **made the screen
articulate about a capability it does not have.** The refusal ends _"Record an override reason to place
anyway"_, and `eligibilityRefusal` emits that clause **only when `overrideReason` is undefined, which
on the ward screen is always.** Measured across four refs with a control:

```
ref                                   OVERRIDE_REASONS  overrideReason  rejections  CONTROL:declin
HEAD (master)                                0               0             10            49
claude/ward-builder-three                    0               0             10            49
claude/ward-builder-two                      0               0              0            48
claude/ward-builder-community-route          0               0              0            48
OVERRIDE_REASONS renders in exactly one file: coordinator/shortlist-panel.tsx
```

⚠️ **AN INSTRUCTION THE INTERFACE CANNOT OBEY IS WORSE THAN THE SILENCE THIS PAIR WAS DECLARED
INSEPARABLE TO PREVENT.** A dead button is a puzzle somebody escalates; _"record an override reason"_
with no control to record one **sends a nurse hunting for something that does not exist, while they are
moving a patient.**

⚠️ **The general failure is NOT "we forgot the ward screen." It is that the engine speaks one sentence
to every caller and only some callers can act on it.** Today the check has one candidate and an obvious
answer. **After B2 it is what stops the identical defect appearing on the third screen nobody thought
about** — and by then nobody will be looking.

**Rejected as an interim, and the reason is recorded so it is not re-proposed:** changing the string to
name the coordinator as the only person who can override is **accurate today and false the moment B2
lands.** ⚠️ **A clinical sentence that has to be un-written costs two edits to the same words.** It
stays available as a fallback if B2 cannot land, decided at fold time, not before.

## ⚠️ WHAT A FOLD COSTS, and it is NOT symmetric

**Observed at `daa034f00`: 4 failures from two green trees.** My first formulation was _"a fold's cost
is the union of both sides' assumptions, and only the merger ever sees both lists."_ ⚠️ **Ward Builder
Three corrected the mechanism and its version is the one to keep:**

> **Three of the four failures were fixtures that were LEGAL under the old engine and became defects
> under the new one. Nobody wrote them wrongly. They encoded a permission the engine used to grant.**

⚠️ **So it is NOT "both sides had latent red". It is that ONE SIDE CHANGED WHAT IS LEGAL, and the other
side's correct-at-the-time work becomes incorrect on contact.**

**Which decides who pays: the side that NARROWED THE RULES owes the repair, and it must not be reported
as the other branch's breakage.** ⚠️ **A fixture that breaks against a new gate was not sloppy — it was
accurate about an engine that no longer exists, and the fact that it broke is the strongest evidence
the gate does anything at all.** That makes it **a credit to the work that wrote it, not a debt against
it** — and the merger is the only person positioned to say so, because only the merger sees both sides.

## ⚠️ Two merge traps waiting in `origin/main`

**1. The Playwright regex silently deletes tests.** One regex names which specs run and **both sides
added to it** — main added `sources` and `therapy-pathways`, this branch `ward-morning` and
`ward-referrals`. Verified by running each branch's actual regex text against the other's filenames:
all four return no match. ⚠️ **Taking either side drops the other's specs out of every project. Not
deleted, not reported — the suite goes green because it ran fewer tests. THE CORRECT RESOLUTION IS THE
UNION OF ALL FOUR TOKENS.** Whether `tests/playwright-project-isolation.test.ts` actually fires on a
dropped token is **unverified** and should be checked before the merge, not after.

**2. `shortlist-panel.tsx` — TAKE BOTH, THEN RENAME BOTH OCCURRENCES.** Main renamed a badge test id to
remove an ambiguity; on our side the old id appears **twice, on spans carrying different classes**, so
`ui-ward-roles.spec.ts:688`'s `toHaveCount(3)` has always been a sum of two kinds. ⚠️ **That is a live
defect today, not a merge hazard — main's rename is the fix, not the cause.** Renaming one occurrence
leaves the ambiguity while looking as though the rename landed.

**Checked with a control:** of the files the next two steps touch, `referral-match.tsx`,
`ward-flow-events.ts` and `ward-flow-reducer.ts` are **untouched by main**; only `shortlist-panel.tsx`
is contested, and it belongs to the parked step.

## What "done" means, and it is not a count reaching zero

⚠️ **Playwright aborts a test at the first failed expect, so a failure list is a snapshot of what fails
FIRST. Repairing it does not shorten the list — it reveals the next layer.** The referrals journey
carries ~33 assertions across two tests, so one early failure can hide ~30.

**So the next full run is a DISCOVERY step, not a victory check.** The suite is clean when **a run stops
revealing new causes**, not when the known list empties. `expect.soft` exists (Playwright 1.62.1,
confirmed with a discriminating control) and would collapse this to one run — **for read-only text
assertions only, never preconditions, and not tonight while three chats are editing the same specs.**

## Every green here has a boundary

`playwright.config.ts`, `tsconfig.typecheck.json`, `package.json` and the lockfile all differ on
`origin/main`. ⚠️ **So every number reported on this branch is a result under THIS branch's runner and
typecheck config — not a prediction about CI.** Say so whenever reporting one.

---

## 🔴 THE FIELD I FOLDED TO UNBLOCK EVERYONE HAS NO CONSUMER, AND IT WEAKENS THE FOLD GATE

**Found by Ward Verifier. Verified here independently before recording, because it is a finding about my own commit and that is exactly the kind I check hardest.**

```
master 36fa21d78                three 95637ba44
overrideReason DECLARED on:
  REFER_TO_UNITS   ✓ (:146)     ✓
  ACCEPT_IN_PRINCIPLE  —        ✓
  PULL_PATIENT         —        ✓
  ACCEPT_REFERRAL  ✓ (:623)     ✓

master's reducer, overrideReason: 4 hits — ALL FOUR inside case "REFER_TO_UNITS" (:755–:777)
CONTROL, same file:  PULL_PATIENT → 12 hits     <- the file is being read
```

⚠️ **SO `ACCEPT_REFERRAL` DECLARES AN OVERRIDE REASON ON MASTER AND NOTHING ANYWHERE READS IT.** A field with no producer's mirror image — **a field with no consumer, shipped by me, at `6ed61434b`, in the commit whose whole purpose was to unblock somebody else.**

### The merge consequence, and it is the dangerous half

**Both trees added the field to `ACCEPT_REFERRAL` INDEPENDENTLY, with different doc comments at the same insertion point.** That is a duplicate INTENT, which is why it presents as a textual conflict.

⚠️ **THE DERIVED SET IS 4 WHICHEVER SIDE OF THAT HUNK WINS, BECAUSE BOTH DECLARE THE FIELD. `EXPECTED_OVERRIDABLE_EVENTS` WILL NOT GO RED ON THE MERGE.** **The strongest automated gate we have is silent by construction on this exact resolution.** Better known than discovered.

**TAKE THREE'S VERSION, AND TAKE IT FOR THE STATED REASON: it is the one with a consumer.** Not because it is longer, newer, or tidier. Mine is a strict subset with nothing behind it.

### ⚠️ AND THE PART THAT OUTLIVES THE FOLD — the guard's premise is falsifiable and master falsifies it

**The guard's premise:** _an event whose union member declares `overrideReason` is an event whose refusal can be answered by a recorded reason._ **Its entire scope is derived from that.**

⚠️ **MASTER PROVES THE PREMISE CAN BE FALSE.** The declaration and the engine support drift apart, and master is drifted right now.

**This is the intersection-type finding arriving from the other direction.** Earlier: a declaration is not load-bearing for the REDUCER, because the refusal functions take `WardFlowEvent & { overrideReason?: … }`, so REMOVING a declaration produces no type error. Now: a declaration can exist with NO reducer code at all and nothing notices. ⚠️ **Both halves of one gap — declaration and behaviour are joined by convention only.**

**The failure DIRECTION is safe:** the guard demands a control for a capability the engine lacks, which generates work rather than risk. **But the set is one of DECLARED INTENTIONS, not ENGINE CAPABILITIES, and the guard's own header does not say so.** ⚠️ **One-line header correction, owed before anybody quotes that set as "the events the engine will let you override".** Ward Verifier is sending it to Ward Builder Three, whose files both are.

### ⚠️ CORRECTION, SAME HOUR — HALF OF WHAT I JUST RECORDED AS A DISCOVERY WAS ALREADY WRITTEN IN THE CODE

**I recorded "a field with no consumer, shipped by me" as a finding. `ward-flow-events.ts:604` had already said it, at length, before Ward Verifier or I looked:**

> ⚠️ **NOTHING ON THIS BRANCH READS THIS FIELD.** Setting it changes no behaviour […] ⚠️ **SO THIS IS DELIBERATELY THE "FIELD WITH NO PRODUCER" SHAPE THIS PROJECT HAS SPENT THE DAY REPORTING AS INVISIBLE TO EVERY GATE** — recorded in the code rather than left to be discovered, because the thing that makes that shape dangerous is that nobody wrote it down. **The window is bounded by another chat's work in flight, not by anyone's intention to remember.**

⚠️ **SO IT IS A DISCLOSED, BOUNDED, DELIBERATE GAP — NOT AN UNDISCOVERED DEFECT.** The comment even names its own closing condition and tells a future reader that finding it still open means something went wrong. **That is the control working exactly as designed, and reporting it as a discovery takes credit the comment already earned.**

⚠️ **WHY I MISSED IT IS THE PART WORTH KEEPING. I was writing a self-critical entry about my own commit, and a finding that confirms the lesson you are already learning gets checked last.** The commit message read well. I did not open the file the finding was about. **Third instance tonight of accepting a claim because it flattered the theme.**

**WHAT SURVIVES, AND IT IS THE HALF THAT MATTERS:** the code's comment discloses that THIS FIELD is inert. It says nothing about the GUARD, and the guard's premise — _a declared field means an engine that honours it_ — is genuinely falsified by this, genuinely undocumented, and genuinely outlives the fold. ⚠️ **Ward Verifier's finding is intact; my framing of it was not.** The header correction is still owed.

**And the merge instruction is unchanged: take Three's `ACCEPT_REFERRAL` variant, because it is the one with a consumer, and know that the fold gate is green either way.**

---

# ⚠️ THE HOLD IS DISCHARGEABLE. B2 EXISTS. Measured at `2b75abbc2`, not reported.

```
2b75abbc2  feat(ward-flow): the ward can record a reason and proceed
           and cannot when no reason would help

  ward-flow-events.ts                        +23
  ward-flow-reducer.ts                       +27
  ward/ward-screen.tsx                      +112      <- B2
  tests/ward-override-control.dom.test.tsx  +195      (new)
  tests/ward-override-surfaces.test.ts      +218
```

**`ward-screen.tsx` can now BOTH SHOW a refusal (B1, on HEAD at `05897eea3`) AND ANSWER it (B2, on
Three's branch).** ⚠️ **THE ONE RULE THAT MUST NOT BEND IS SATISFIED — not waived.** Folding
Three's branch brings the engine and the control in together, which is the condition the rule was
written to require.

## The fold is now ONE FILE, ONE HUNK

```
CONTROL   claude/Wardquestions vs HEAD          exit 1   (instrument works)
          claude/ward-builder-three vs HEAD     exit 1
          conflicting files: ward-flow-events.ts   -- and nothing else
```

**RESOLUTION, decided in advance and for a stated reason: TAKE THREE'S.** It is the variant with a
consumer; mine is a strict subset that nothing reads.

⚠️ **AND KNOW WHICH GATES SPEAK.** The derived-set assertion is **SILENT** on this hunk — both
sides declare the field, so the set is 4 either way. The declaration-count cross-check is **LOUD** on
the resolution neither Ward Verifier nor I considered: a naive keep-both adds a fifth textual
`overrideReason` declaration while the AST set stays at four, so
`ward-override-surfaces.test.ts:437` fails `expect(4).toBe(5)`. **Three shipped the detector for its
own merge hazard. Both statements recorded, neither replacing the other.**

## ⚠️ MY OFFER TO BUILD B2 WAS STALE IN THE DANGEROUS DIRECTION, AND THREE CAUGHT IT

**I offered to build B2 on the master line on the evidence that `ward-screen.tsx` was not in Three's
footprint. It was true when the inventory ran and false when I sent the message** — Three had
committed twenty minutes earlier. ⚠️ **Had it accepted, we would have had TWO independent
implementations of one control in one file, both substantial, both looking correct — far worse than
the events-file hunk.**

**An inventory is a photograph. I quoted one as a state.**

### And Three's reason for declining is the one I gave it myself

_"I wrote the engine that refuses, so I know what a refusal can and cannot say."_ ⚠️ **It showed
immediately: the control renders ONLY when the refusal carries `OVERRIDE_REASON_REQUIRED` — a
fragment the reducer now EXPORTS, so the screen matches the engine's own fact rather than a copy of
its wording. A refusal no reason can answer — no bed, no specialling staff, a stale count — shows
the refusal and NO control.**

**Offering a reason box against a physical fact promises something no reason can buy. A control built
by somebody who had only READ the refusal would very likely have offered it every time.** _That is
what the gap was._

### ⚠️ AND THE GUARD REJECTED ITS AUTHOR'S FIRST VERSION, WHICH IS THE BEST RESULT OF THE NIGHT

**Three wrote a dispatch with a COMPUTED type — the exact blind spot Ward Verifier had found hours
earlier, written by the author of the guard that catches it, an hour after committing it.**

⚠️ **THE CONSEQUENCE WOULD HAVE BEEN SILENT AND PERFECT: the screen gains the capability, every
static check remains unable to SEE it, and the allowlist entry saying "this screen cannot record a
reason" could NEVER be removed — because nothing could tell it had been fixed.** Rewritten as two
explicit literal dispatches. **The guard shaped the code before it shipped rather than complaining
after, which is the first time one has done that tonight.**

## Fold order and the one precondition

1. ⚠️ **WAIT FOR THREE TO DECLARE ITS BRANCH FINAL.** It has the guard-header correction still to
   write. **Folding a moving branch is how a fold takes half a change.**
2. **Three first** — it carries the conflict and the unblock.
3. **Two second** — textually clean, and Two is running the SEMANTIC proof now: its full ward suite
   against the MERGED tree, not its own tip. **Its 2231 passing tests were measured on an unmerged
   tree and are not fold evidence.**

## Owed at fold time, carried here so it is not lost

- **Ward Verifier's register correction, which it cannot apply** — its checkout is detached and the
  file is on Three's branch. `finding-register-ward-verifier.md:13` becomes _"All thirteen are
  reasoned; four became tested only because another chat ran something."_; `:47` becomes _"Thirteen
  of thirteen reasoned, four of them later tested by others."_ ⚠️ **Line `:46` — _"has been the
  loudest voice for measurement all day and has measured nothing"_ — STAYS. Thirteen reasoned and
  zero measured BY IT is the honest shape, and the correction must not soften it.**
- **`4d289a277`'s title overstates its coverage** (Ward Builder One): titled _"pin what EACH
  eligibility path actually asks"_, it pins ONE — `referralEligibility`'s gate list has no
  exact-array pin anywhere. ⚠️ **NOT a safety hole, said as loudly as the finding:
  `referralAcceptanceRefusal` fails CLOSED, so a gate absent from `SUITABILITY_GATES` refuses
  outright before the override reason is read. The gap is "nobody is forced to notice", never "it
  becomes bypassable".**
- **`prior_decline`** is the one `ELIGIBILITY_GATES` member that could join `SUITABILITY_GATES` today
  with nothing going red. Pre-existing, already an owner question in the reducer's own comment.

## 🔴 THE INDEPENDENT MUTATION IS IMPOSSIBLE BEFORE THE FOLD, AND THE REASON IS STRUCTURAL

**Both chats offered a tree and both measured themselves out of it before starting.** Ward Verifier:
400+ commits behind, no `@playwright/test`. Ward Builder One: its `ward-flow-events.ts` has 1
declaration where the guard expects 4, and `ward-override-surfaces.test.ts` is ABSENT.

⚠️ **THE GUARD WOULD GO RED ON ARRIVAL FOR UNINTERESTING REASONS AND THEN RED FOR EVERY MUTATION
— every result looking like the guard working and proving nothing. The contaminated-mutation trap at
ENVIRONMENT scale.**

⚠️ **SO "INDEPENDENTLY MUTATION-TEST THE GUARD" IS RIGHT NOW ONLY PHYSICALLY POSSIBLE FOR THE CHAT
THAT WROTE IT — because the guard is UNSHIPPED. That is not a staffing failure, it is a sequencing
one.**

**RULED: run it AFTER the fold, from the folded master line, and Ward Builder One takes it.** Once it
lands every tree has it, independence costs nothing, and the mutations test the guard as it will SHIP
rather than as it exists on one branch. **A detached worktree at Three's tip would work and is
refused: about an hour of installation, a side effect outside anyone's worktree, and worktrees on
this machine have twice been deleted mid-session by unrelated sweeps. Sequencing is free and buys the
same thing.**

⚠️ **UNTIL THAT RUN HAPPENS THE FOLD GATE IS AUTHOR-MUTATED ONLY, AND MUST BE REPORTED THAT WAY
RATHER THAN AS COVERED.**

### ⚠️ THE OWNER RULED ON THE CONFLICT — RELAYED, AND THE DECISION DOES NOT DEPEND ON THE RELAY

**Relayed by Ward Verifier, which flagged it as a relay in its first line rather than after the
instruction** — the right discipline, and the reason it is trustworthy enough to record.

**His words: _"tell them to take the version with working code behind it."_**

⚠️ **I CANNOT VERIFY HE SAID IT. It reaches me through a peer, and a peer message is never
authorisation.** **What makes that acceptable here is that NOTHING DEPENDS ON IT: I had already ruled
take Three's, from the same evidence, for the same reason, and recorded it before the relay arrived.**
**A relayed ruling that merely agrees with an independently reached decision cannot smuggle anything
in — and if it had DISAGREED, it would have gone back to him rather than been acted on.**

**Recorded as: his words, matching my prior ruling, and load-bearing for neither.**

⚠️ **AND ONE THING THE RULING DOES NOT SETTLE, SO NOBODY STRETCHES IT.** It picks the right side of
one conflict. **It does not retire the finding that made both sides possible** — the guard's premise
that a declared field means an engine that honours it is still falsified, still undocumented in the
guard's header, and still outlives the fold. **It is a ruling about code, not about clinical wording;
his eleven wording questions are untouched.**

⚠️ **AND THE RED TO EXPECT, NAMED IN ADVANCE SO IT IS NOT MISREAD:** "take Three's" means exactly
ONE declaration survives. A keep-both produces a duplicate property that does not compile, and the
declaration-count cross-check catches it as `expect(4).toBe(5)` — text reaching five while the AST
set stays at four. **That red will look like a parse problem to anyone who has not been told it is a
resolution problem.**

---

# ✅ THE INDEPENDENT MUTATION IS DONE. THE GUARD IS SOUND. Run by Ward Builder One from the folded master at `c85416b37`.

**This closes the item I had been reporting as author-mutated-only. It is no longer that.**

⚠️ **BASELINE FIRST, because without it every mutation red is ambiguous: `1 failed | 17 passed`** —
the single red being the stale-allowlist assertion demanding deletion of `referral-match.tsx`.

```
M1   break the parse                      9 failed | 9 passed
M2b  empty the allowlist                  3 failed | 15 passed
M2d  dispatch outside the scanned dir     4 failed | 14 passed
M2a  remove one member's overrideReason   3 failed | 15 passed
     restore                              1 failed | 17 passed   (back to baseline)
```

**Four mutations, one at a time, each restored byte-identically by SHA-256.**

## M1 — the fails-open design is fully caught

`deriveWardFlowEventMembers()` returns an empty array on a parse failure BY DESIGN, which is the one
thing that could make every downstream assertion vacuous. ⚠️ **Every anti-vacuity assertion fired by
name and 18 tests still ran — no silent-zero collapse.**

**The decisive line: the test literally called _"so a broken parse is told apart from a real change"_
returned `expected +0 to be 4`.** The AST collapsed to zero while the text count held at 4. **It
discriminates exactly as its name claims.**

## ⚠️ M2a IS THE FINDING WORTH KEEPING, AND IT VINDICATES THE TWO-GUARD DESIGN

**Removing `PULL_PATIENT`'s field dropped the AST set to 3 — and the text-versus-AST cross-check did
NOT fire, because BOTH collapsed to 3 together.** ⚠️ **That is Ward Builder Three's own _"two things
agreeing prove nothing when both have collapsed"_, reproduced live against its own guard.**

**The snapshot pin is what caught it. Neither guard alone would have; the pair does.** **An argument
for redundancy that is measured rather than asserted.**

## M2d's failure message is the standard the rest should meet

It fires, names the file and line, **and warns against the wrong fix**:

> _"Do NOT narrow this match to `type: \"X\"` to make it pass: that would go green on a computed-type
> dispatch, which is precisely what it exists to catch."_

**A failure message that anticipates the lazy repair is worth more than one that merely reports.**

## ⚠️ AND THE PREDICTION THAT WAS WRONG IS THE MOST INSTRUCTIVE PART

**Ward Builder One predicted the needle cross-check would be RED on arrival at 6-versus-4, because a
doc comment at `ward-flow-events.ts:849`/`:852` contains the literal declaration twice while
explaining the intersection-type trap.**

**It passed. Three had already fixed it inside `95637ba44..8dcb7ba2d`** — the count is now
`.split(...).filter((line) => line.trim().startsWith(needle))`, so a comment line beginning `*`
cannot count.

⚠️ **HAD THE DELTA RE-READ BEEN SKIPPED, A REPAIRED GUARD WOULD HAVE BEEN REPORTED AS BROKEN.**
**One flagged its own review's shelf life — reviewed at `95637ba44`, folded at `8dcb7ba2d` — and
asked whether the delta touched the file its findings were written against. It did. That question is
the only reason the false report did not happen.**

**And the needle itself had been a proxy one layer finer than the one everyone was warning about: it
is not enough to count the declaration FORM rather than the bare name, because a comment can contain
the declaration form too.**

## ✅ CHECK 2 — THE ED SCREEN, OBSERVED RATHER THAN DERIVED

**What the screen shows: exactly FOUR `<option>` elements** — "Choose a reason", "Belongs to another
service", "Referred elsewhere", "Another reason — needs follow-up", in that order.

⚠️ **UNIT STATED, as instructed: four OPTIONS, being three derived reasons plus the placeholder —
not four reasons.** The assertion is sound because `toHaveText(array)` pins element count, order and
text together, so it cannot pass on three or five.

**Then, and only then, the assertion: `1 passed (4.2s)`, exit 0, chromium.** **Ward Builder Two's
REASONED line was right on every dimension and is now observed.**

## 🔴 WHAT IS STILL OPEN — ONE ITEM, AND IT IS THE HUMAN HALF

**The fold gate was: the allowlist entry removed AND somebody opened the screen.** ✅ The first is
done and mechanically proven. ⚠️ **The second is NOT. Nobody has seen the override control on
screen in the state where it appears** — Ward Builder Three drove the coordinator side live and the
trail ended at the navigation boundary. **That is the last open item on this fold and it is not a
formality: every count supporting it is a mention count.**

---

# ✅✅ THE FOLD GATE IS CLOSED. ALL FOUR ITEMS. `4d630f340`.

**The last one — _somebody opened the screen_ — was closed by Ward Builder Three driving the whole
flow live, clicks only, no URL bar, no reload:**

```
coordinator -> override recorded, register shows 1
click "All wards"      -> /mockups/ward-flow/wards          23 ward links present
click SCGH Adult Open  -> /mockups/ward-flow/ward/scgh-adult-open
                          incoming: ward-incoming-WF-009    ✅ STATE SURVIVED
press Accept           -> REFUSED, naming the gate: "failed gate security: Open ward does not
                          meet a secure requirement. This placement needs a recorded override reason."
                       -> the reason control APPEARED, five radios
submit before choosing -> disabled
choose a reason        -> enabled
submit                 -> "Accepted, awaiting bed", no new refusal
                       ⚠️ AND THE WARNING REMAINS ON THE ACCEPTED ROW — overridden, not erased
                          "OVERRIDES RECORDED AGAINST SCGH ADULT OPEN — WF-009 18:05,
                           Decided by Flow coordinator, Clinical urgency outweighs the mismatch"
```

⚠️ **THAT IS THE FIRST TIME ANYBODY HAS LOOKED AT THE CONTROL IN THE STATE IT EXISTS FOR.** Every
count supporting this fold until now was a MENTION count. **The warning surviving on the accepted row
is the detail worth keeping: an override that erased the reason for itself would be the record
forgetting why it was made.**

## ⚠️ THE "NO ROUTE" FINDING WAS THE UNIT TRAP, AND IT WAS WRONG IN THE ALARMING DIRECTION

**Three counted ward links in the COORDINATOR PAGE'S DOM, got 1, and concluded no in-app route
existed — that nobody could watch a referral leave one screen and arrive at another.** **What it
counted was the page's own content. The route lives in the RAIL, which is chrome on every page.**

⚠️ **And it is worse than "elsewhere in the DOM": the role switcher's ward links are inside a
COLLAPSED MENU and are not in the DOM at all until it opens. A link count cannot see a route that
does not exist yet.** Its candidates are derived from the focused movement's `referredUnitIds` — **so
the very action Three had just performed is what populates them, and it measured without opening it.**

**WHAT SURVIVES, AND IT IS A REAL DEFECT WORTH FIXING: the one route is unsignposted at exactly the
moment it becomes useful.** **A label problem, not an architecture one, and cheap.**

## 🔴 AND MY OWN SLIP, IN THE SAME TURN I WROTE UP SOMEBODY ELSE'S

**I verified `57ede2c34` — its stat, its zero caller count, that it was the branch tip — and then
merged the BRANCH, which had moved. So I folded `a3886747e` without looking at it.**

⚠️ **I caught it only because the merge reported 271 lines where I had verified 245.** **It turned out
to be exactly the fix that was needed, which means I got lucky rather than careful.**

**Recorded because I had written up Ward Builder One's shelf-life caveat twenty minutes earlier and
praised it for flagging that its review described a tree no longer being folded. Observations expire,
and mine expired inside a single turn.** **Verifying a COMMIT and merging a BRANCH are two different
acts, and nothing about the first constrains the second.**

**What came in with it is a genuine defect Ward Builder Two found by standing where the caller
stands: `nearPatients(["Marcus", "Halowin"])` returned NOTHING — right first name, misspelt surname,
the commonest duplicate there is.** ⚠️ **An "already found" guard excluded the patient because
"marcus" exactly matched a real given name. Its premise — an exact hit means the search already
showed them — was TRUE of the search screen with one term and FALSE of the add screen with two,
because the search that brought the clinician there used only one of them.**

**Three had flagged that exact line as unpinned and predicted it would bite when somebody seeded a
patient whose given name near-missed another's surname. The real trigger needed no new fixture: it
needed a caller with two terms, which is the caller it was built for.** **Right line, wrong trigger,
and it said so in the commit rather than quietly fixing it.**

**Verified after the fold: 46 passed across the three affected suites, typecheck exit 0.**

---

## ✅ A SECOND INDEPENDENT MUTATION RUN, BY A DIFFERENT HAND, AND IT AGREES BY A DIFFERENT ROUTE

**Ward Verifier, three mutations on the folded master from a moved checkout, designing none of the
assertions that caught the first two.**

```
BASELINE  stale referral-match entry deleted locally  ->  18 passed, exit 0
M1  ward-screen PULL_PATIENT loses overrideReason     ->  1 red: compliance guard, BY NAME
M2  union truncated after PULL_PATIENT, still parses  ->  4 red BY NAME
    ⚠️ AND THE COUNT GUARDS PASSED — "> 0" and ">= 3" are both satisfied by three members
M3  morning-page imports MorningTour (the un-pause)   ->  1 red: zero-import assertion, BY NAME
RESTORED  four files byte-identical, hashes proven
```

### ⚠️ TWO HANDS, TWO DIFFERENT MUTATIONS, ONE CONCLUSION — which is worth more than either

**Ward Builder One's M2a: removing one member's field collapsed AST and text TOGETHER, so the
cross-check stayed silent.** **Ward Verifier's M2: a truncation that still PARSES leaves three
members, and _"at least three"_ waves it through.**

⚠️ **The same blindness reached from opposite sides. The count guards are decorative; the NAMED pins
are what work.** **Neither run alone establishes that. Both do.**

**And the cross-check gave the right DIAGNOSIS on M2, not merely a red: AST 3 against 4 textual
declarations is a DISAGREEMENT, which is the signature of a broken parse rather than a real change.**
**The taxonomy worked on a real case rather than a constructed one.**

**M3 tested a mechanism nobody had ever tested** — the zero-import assertion, on the un-pause event
it was written for. **An entry with a correct written condition and no trigger now has a proven one.**

**The baseline independently confirms the stale-entry deletion at `597481bdf`: 18/18 with it gone, so
nothing else was wrong with that surface.**

### The declared limits, kept as declared

- ⚠️ **M1's EVENT-LEVEL DISCRIMINATION IS UNTESTED.** Its own failure message was filtered out of
  the run's output, so whether the compliance guard names `PULL_PATIENT` or only the file is unknown.
  **Owed, low priority: a vague message is a worse diagnosis, not a missed defect.** **Declared by the
  runner rather than found afterwards.**
- **The usual single-file restore command is blocked in that worktree by the protection hook, so
  every mutation was reversed by hand and proved by hash.** ⚠️ **Slower and strictly better: a
  restore command that silently no-ops leaves the mutation in place and reports success.**

### ⚠️ AND A RELAY FAILURE OF MINE, CAUGHT BY THE CHAT THAT PREDICTED IT

**Ward Verifier's report closed with the human look _"still owed and still unassigned"_. It had been
done an hour earlier and I had not told it.** **It had armed its own watch precisely because _"a
result was checked tonight and never reached me"_ — and then the next result did not reach it
either.**

**Two chats, one integrator, and the integrator is the single point of failure for every fact that
has to cross between them.** **Its watch was the mitigation and this is the second time it earned it.**

### ✅ THE M1 GAP IS CLOSED, POSITIVELY — strike the "owed, low priority" line above

**Re-run with the message captured:**

```
AssertionError: src/components/ward-management/ward/ward-screen.tsx dispatches PULL_PATIENT
at line(s) 383, 1492 and never builds an overrideReason field for it
```

⚠️ **It names the EVENT, the FILE and BOTH LINE NUMBERS — and it stayed SILENT about
`ACCEPT_IN_PRINCIPLE`, which still carries the field.** **So the discrimination holds in both
directions: it names the broken event and does not smear the intact one.** `ward-screen.tsx`
restored byte-identical, hash proven.

### ⚠️ AND THE RUNNER WALKED INTO THE TRAP IT OPENED THE NIGHT BY NAMING

**Its first act tonight was to say: with one test already red, a mutation breaking a DIFFERENT test
still reads "1 failed" and you will misread it — so establish a green baseline first.**

**It then restored the stale allowlist entry, mutated on top of it, and got "Failed Tests 2".** ⚠️
**It had rebuilt the contaminated baseline by hand, one message after explaining why it must not
exist.**

**IT COST NOTHING, AND THE REASON IT COST NOTHING IS THE ONLY PART WORTH KEEPING: the number was
WRONG-LOOKING. It expected one and got two, which forced a second look.**

⚠️ **HAD THE STALE ENTRY PRODUCED NO RED, OR HAD IT EXPECTED TWO, THE PAIR WOULD HAVE READ AS ONE
RESULT AND NOTHING WOULD HAVE FLAGGED IT.** **So the transferable rule is not "remember the
baseline" — everybody remembers, including the person who wrote the warning an hour earlier. It is:**

> **PREDICT THE FAILURE COUNT BEFORE THE RUN. A count you did not predict is the only thing that
> makes a contaminated run visible.**

**A baseline you forgot to take is invisible; a number that disagrees with a prediction is not.**

---

# 🔴 A GUARD THAT ONLY RUNS AT PUSH, ON A BRANCH THAT NEVER PUSHES, IS A GUARD THAT NEVER RUNS

**Found at the first push attempt, 2026-09-03, after 986 commits.**

**`npm run lint` fails: `11 problems (2 errors, 9 warnings)`, and the gate runs `--max-warnings 0`
so every warning is a failure too.**

⚠️ **THE PRE-COMMIT HOOK DOES NOT RUN LINT. ONLY THE PRE-PUSH GUARD DOES. This branch has never been
pushed — so lint has never once gated it.** **These are not tonight's regression. They are the entire
branch's accumulated lint debt, surfacing the first time a gate could see it.**

### ⚠️ THE SHAPE IS THIS PROJECT'S OWN DEFECT CLASS WITH THE TRIGGER MISSING RATHER THAN THE ASSERTION

**Every other entry in these registers is about an assertion too WEAK to fail. This one is an
assertion that is perfectly strong and NEVER EVALUATED.**

⚠️ **AND THE THING THAT DISABLED IT WAS A SAFETY RULE. "Ward Flow is never pushed" exists to protect
the work — and it silently switched off a safety check for 986 commits.** **Nobody chose that and
nothing reported it.**

**It also predicts its own scale, which is how you can tell it is the explanation rather than a tidy
story: eleven problems across 986 ungated commits is roughly what "never once gated" should look
like. A regression from tonight would not have that shape.**

## A SEPARATE FAILURE THAT SURFACED IN THE SAME MESSAGE — recorded apart, because they are different

**The push guard reported _"lint failed"_ and eslint had never started.** Measured by Ward Verifier
against `guard-push.mjs`:

```
changed files vs origin/main : 784
argument-string length       : 56,570 bytes
Windows CreateProcess limit  : 32,767 characters
eslint policy files touched  : none
```

⚠️ **The operating system refused to launch the process, and `runStaticCheck`'s catch block sorts
that into the same bin as eslint finding real defects — printing the same words.** **"Never ran" and
"ran and found problems" are OPPOSITE STATES, and the message picks the alarming one.**

**A shared-repository defect rather than a Ward Flow one. Recorded, not fixed.**

⚠️ **AND IT NEARLY COST THE FINDING ABOVE: "the command line is too long" reads exactly like a
Windows quirk to be skipped past. Running lint directly instead of assuming is the only reason the
eleven real problems were found.** **Two failures in one message, one masking the other, and the
mask was the more plausible-looking of the two.**

## ⚠️ ONE WARNING WITH A TEMPTING WRONG FIX — `WORLD_FACT_GATES`

**An unused-variable warning invites exactly one lazy fix: find a use for it.** ⚠️ **Here that fix
would re-assert a grouping THE OWNER OVERTURNED THE DAY BEFORE** — the orphaned list still classifies
`capacity_freshness` as a world fact, and he moved it into `SUITABILITY_GATES` on 2026-09-02.

**Delete it. The check it belonged to was replaced deliberately by named pins that are present and
working; only the variable was orphaned. The file's own comment is the evidence.**
