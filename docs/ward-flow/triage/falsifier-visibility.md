# Falsifier instrument-visibility classification — the 24 triage findings

Read at integration branch `codex/task-ward-flow-live-state-20260831`, commit
**`b42b7e92268f5cda7016096bb1bbcab3f7c84a0c`** (2026-09-02 13:09:35 +0800), via
`git show b42b7e9:<path>`. The working tree is a separate, older checkout and was not used as
evidence.

The three source documents (`wf-build2-006-batch-a.md`, `-b.md`, `-c.md`) were themselves read
against `claude/ward-builder-two` HEAD `5c1dc6080`, an **ancestor** of `b42b7e9`
(`git merge-base --is-ancestor 5c1dc6080 b42b7e9` confirms it; 180 commits separate the two). So
every finding below carries two facts: (1) which instrument, if any, could ever detect its stated
falsifier, and (2) whether the cited code still reads the same way 180 commits later.

## What this file does NOT do

No mutation was run. No test, build, or Playwright run was executed, per the task's read-only
constraint. Every classification below is reasoning about instrument _mechanism_ — what a value
assertion, a rendering assertion, or `tsc`'s type checker can structurally observe — not a
measured result.

## Method actually used (stated so the coverage figure means something)

`git diff --stat 5c1dc6080 b42b7e9 -- src/components/ward-management/ tests/` was run once,
covering every file any of the 24 findings cite. It lists 54 changed files. Any finding whose cited
files are **not** in that list is proven byte-identical between the triage docs' read-point and the
integration branch — the docs' own quoted code still describes the live file, with no re-read
needed to establish that fact (the diff-stat itself is the proof). For findings whose cited files
**were** in the changed list, the actual integration-branch content was opened directly (`git show
b42b7e9:<path>`, targeted `grep -n`, and full `git diff` review) rather than trusted from the older
docs. Files opened directly this way: `ward-eligibility.ts`, `ward-eligibility.test.ts`,
`ward-flow-reducer.ts`, `ward-withdrawal-reason-privacy.test.ts`,
`tests/ward-screen-fd23-leaks.dom.test.tsx`, `ward/ward-screen.tsx`, `ward-derivations.ts`,
`ward-sites.ts`.

## The one staleness correction that matters

**Finding 2.5 is STALE-CLOSED at `b42b7e9`, contradicting its CURRENT verdict in batch A.**
`tests/ward-withdrawal-reason-privacy.test.ts` and `tests/ward-screen-fd23-leaks.dom.test.tsx` were
both rewritten on 2026-09-02 (each carries a dated comment: _"THIS LOOP READ ONLY `allUnits()`
UNTIL 2026-09-02"_) to check a withdrawal label against **site names, site codes, and emergency-
department names** as well as unit names, via a new `forbiddenPlaceNames()`/`namesRealPlace()`
word-boundary detector, complete with its own proved-before-trusted positive/negative controls.
`"Fremantle Hospital"` — the exact site name finding 2.5's proposed falsifier interpolates into a
new withdrawal label — is confirmed present in `wardSites` (`ward-sites.ts:303`). The falsifier
text `"Withdrawn — a bed was confirmed at Fremantle Hospital."` would now trip
`expect.soft(namesRealPlace(label, "Fremantle Hospital")).toBe(false)` in both files. The gap 2.5
named is real history, not current fact.

Every other finding's cited files were confirmed present, byte-identical in the cited logic, at
shifted line numbers only (consistent with unrelated insertions elsewhere in the same files — a new
`forensic` gate mirrored into `eligibility()`, and a new specialling-capacity refusal block in the
reducer, neither of which touches any cited branch). No other finding's classification or CURRENT
status changes.

## Classification table

| #   | ID                                                                                | Falsifier (one line)                                                                                                         | Class                                              | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --- | --------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 1.5                                                                               | Empty `suburbUnknownLabels.not_known` string                                                                                 | VITEST                                             | The rendered `<option>` markup is a DOM value a test assertion reads directly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2   | 2.5                                                                               | Interpolate a new withdrawal label naming "Fremantle Hospital"                                                               | **STALE-CLOSED at `b42b7e9`** (was VITEST-class)   | Site/ED-name checking was added to the FD-23 guard on 2026-09-02, after the triage doc's read-point; the falsifier now trips it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 3   | 5.1                                                                               | Swap `referralEligibility`'s purpose filter for a site-code inference                                                        | VITEST                                             | Which referrals populate a rendered inbox array is a runtime/DOM value; a sibling DOM test (`ward-ed-psychiatry-hub.dom.test.tsx`) already observes it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 4   | 6.1                                                                               | (already fixed — sex-designation gate now wired into the movement path)                                                      | STALE-CLOSED                                       | Confirmed still fixed at `b42b7e9`; a further, additive `forensic` gate was mirrored into the same function since, consistent with the fix holding.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 5   | 6.2                                                                               | Exempt `capacity_freshness` from the `eligible` combining boolean                                                            | VITEST                                             | `verdict.eligible` is a computed boolean a test can assert directly; only the combining formula changes, individual `gate.pass` values (which the new `eligibilityWarning()` render path filters on) are untouched either way.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 6   | 6.8                                                                               | Shallow-spread instead of `structuredClone` when seeding referrals                                                           | VITEST                                             | Object-reference aliasing is a real JS runtime semantic (mutate-through-reference is `.toBe`/`.not.toBe`-observable); no test currently probes it, but the property is not structurally invisible to vitest.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 7   | 6.9                                                                               | Let a rejected `RECEIVE_REFERRAL` fall through instead of returning                                                          | VITEST                                             | The resulting `state.referrals`/`state.rejections` arrays after a dispatch are runtime values read directly by assertions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 8   | 7.3                                                                               | Exempt `forensic` from `referralEligibility`'s combining boolean                                                             | VITEST                                             | `verdict.eligible` gates an actual Accept/Reject render path, observed by both a DOM test and a Playwright journey.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 9   | 7.4                                                                               | Add a helper module that **re-exports** (not imports) the bed-release model, imported normally by a D15 entry point          | **NEITHER**                                        | The property is "does file X's transitive import graph reach module Y" — a static source-structure fact with zero runtime manifestation (the re-exported symbol is never executed, rendered, or read back); `tsc` raises no error on a re-export either, so neither instrument's mechanism touches it. This is the direct instance of the task's fact #3 (a re-export defeating both suites at once).                                                                                                                                                                                                                                                                                                                                                                 |
| 10  | 8.6                                                                               | Hardcode `transportNeeded: false` behind a same-substring trailing comment                                                   | VITEST                                             | The created referral's `transportNeeded` field is a plain runtime boolean asserted by value.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 11  | 8.8                                                                               | Grant `coordinator` the `CLEAR_BED_RELEASE_BLOCK` role                                                                       | VITEST                                             | Rejection count/content after a dispatched event, and the literal permissions table pin (`ward-event-permissions.test.ts`) that actually catches it, are both runtime values.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 12  | 8.2 (contracts, parallel cap)                                                     | Delete the `PARALLEL_REFERRAL_CAP` check in `REFER_TO_UNITS`                                                                 | VITEST                                             | Array lengths (`rejections`, `referredUnitIds`) after a reducer dispatch are runtime values; a differently-shaped dispatch (`ward-flow-reducer.test.ts`) already exercises the cap.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 13  | 8.1 (contracts, PII probe)                                                        | (no new edit — confirms the sweep's own correction)                                                                          | VITEST                                             | The compensating guard (`ward-withdrawal-reason-privacy.test.ts`) checks `.reason` membership in a fixed runtime enum — a value comparison.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 14  | 8.1 (legal-figure-guard, case-sensitivity)                                        | —                                                                                                                            | **STALE** (mechanism rewritten)                    | The file's "BROADENED 2026-08-24" rewrite replaced the described capitalised-variant extraction with a direct lowercase substring match; the described gap can no longer be reproduced as written.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 14b | 8.1 (legal-figure-guard, JSX-text blindness)                                      | Render absence via a new JSX text child instead of a template-literal string                                                 | VITEST                                             | Rendered text is fundamentally a DOM/rendering value; the shared literal-extractor (`ast-string-literals.ts`) is simply currently blind to `ts.isJsxText`, not structurally incapable of ever being.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 15  | 8.2 (legal-figure-guard, "form required" substring)                               | Reword officer-screen fallback text to insert a word between "form" and "required"                                           | VITEST                                             | Rendered fallback string content, compared by substring — a text/rendering value.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 16  | 8.4 (change-reasons label)                                                        | Reword `patient_no_longer_coming`'s label to duplicate `ward_withdrew_the_bed`'s wording                                     | VITEST                                             | Label strings are compared for truthiness/equality directly — plain runtime string values (partially guarded: two of thirteen reason codes have independent pins elsewhere).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 17  | 8.5                                                                               | Swap the first two entries of `URGENT_MARK_REASONS`                                                                          | VITEST                                             | Array element order is a runtime value (`toEqual([...])` would pin it); simply nothing currently asserts order for this one list.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 18  | 9.6                                                                               | Append a new top-level CSS rule after the `@media print` block's real closing brace, without touching any rule inside it     | **NEITHER**                                        | The guard slices raw file text from a string index to end-of-file rather than parsing real block boundaries or checking actual computed style under print-media emulation; no Playwright/print-emulation test exists, so the real "does print CSS apply correctly" rendering fact is outside both vitest's text-slice proxy and `tsc` today.                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 19  | 11.5                                                                              | `Math.max` → `Math.min` in `referralDecidedAt`                                                                               | VITEST                                             | The function's return value is a runtime number, directly assertable; unguarded only because every seeded referral has at most one decided destination, not because the property is instrument-invisible.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 20  | 12.7                                                                              | Reword `notInDepartment` clock term to assert the opposite meaning                                                           | VITEST                                             | The term is a returned/rendered string constant — a plain runtime value, currently pinned only negatively (no digit/no "arriv"/non-empty), not for exact text.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 21  | 13.5                                                                              | Three sub-edits: dynamic `import()`, rest-element `...flow` destructuring, a union-typed (`Referral \| undefined`) parameter | **NEITHER**                                        | All three defeat a regex/string scan over raw source text approximating import shape, destructuring bindings, or type-annotation shape; none changes a value this file's own guard test observes, and TypeScript permits all three constructs without a compiler error, so `tsc` doesn't catch them either. (Caveat: sub-edit (b) does route real referral data into a nominally ward-only module; if that data were ever rendered, a DOM leak test could in principle observe _that_ — but the cited guard itself, and its stated property, is purely structural.)                                                                                                                                                                                                   |
| 22  | 13.7                                                                              | Blank out `WARD_FLOW_ROLE_LABELS.officer`                                                                                    | VITEST                                             | Role-label strings are the object of `toBeTruthy()`/literal `toBe()` checks elsewhere in the suite — plain runtime string values, unevenly pinned (3 of 6 roles have independent literal pins; `officer` does not).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 23  | 14.4                                                                              | Two edits: derive `homeRegion` from suburb name instead of taking the event field; empty `no_fixed_abode`'s label            | VITEST                                             | A reducer-computed field value and a rendered/returned label string are both runtime values (half A's guard is structurally blind because it reads a static fixture that never goes through the reducer — a real gap, but still a value-shaped property).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 24  | 14-cluster (`ward-referral-visibility.test.ts`, ward-scoped projection field set) | Four sub-facets: §14.1 (closed), §14.2 (live pin), §14.2's "fully-populated" sibling, §14.2b (`suburb`)                      | **MIXED — TSC + VITEST + NEITHER on one property** | §14.1: the field set is enforced by a `Required<WardScopedReferral>` **type** annotation per the file's own doc comment → **TSC**. §14.2 (line 469, unchanged file): `Object.keys(projection!).sort()` compared against real production output → **VITEST**, a genuine live runtime pin. Its sibling ("fully-populated projection" test): compares a hand-built literal, so its only real teeth are the same `Required<...>` type annotation, which `vitest` never checks (no `typecheck` block in `vitest.config.mts`) → **TSC only**. §14.2b: `suburb` sits on `Referral` but on **neither** projection type, so `tsc` has no contract to enforce, and no runtime assertion checks for its absence either — an explicitly self-documented double gap → **NEITHER**. |

## Summary counts

- **VITEST**: 19 of 24 (rows 1, 3, 5–8, 10–13, 14b, 15–17, 19, 20, 22, 23 — plus row 2's _pre-fix_ falsifier was VITEST-class, now moot).
- **NEITHER**: 3 of 24 cleanly (rows 9, 18, 21), **plus one facet** of the mixed row 24 (§14.2b, the `suburb` gap) — so 4 of 24 findings have a NEITHER component; 3 are NEITHER outright.
- **TSC**: 0 of 24 cleanly, but **two facets** of the mixed row 24 (§14.1 and §14.2's sibling test) are TSC-only.
- **STALE / STALE-CLOSED**: 2 of 24 outright (rows 2, 4), plus one further facet (row 14, part a) inside an otherwise-live finding.
- **MIXED (spans more than one bucket on a single named property)**: 1 of 24 (row 24) — the clearest single illustration of all three of the task's opening instrument facts on one property.

**Answer to the summary question:** counting only findings that are cleanly NEITHER, **3 of the 24**
(7.4, 9.6, 13.5) were never decidable by a vitest mutation run or a `tsc` type check, however either
was run — for these, a clean mutation result would have been actively misleading, exactly as the
brief warned. Counting the `suburb` facet of the mixed row 24 as a fourth partial instance, **4 of 24
findings touch a NEITHER-class gap.**

## What this check did not cover

- No test, build, or Playwright run was executed — every verdict above is mechanism reasoning, not
  a measured mutation result. That was the task's explicit read-only constraint, not an oversight.
- Full-file re-reads at `b42b7e9` were done for 8 files (listed under Method above), covering 12 of
  the 24 findings directly. The remaining 12 findings' CURRENT status rests on the single
  `git diff --stat` proof of byte-identical files plus the triage docs' own verbatim-quoted code —
  not a fresh independent read of every cited line at the integration SHA.
- `docs/ward-flow/triage/reexport-blindness-sweep.md` and `d15-live-check.md`, both present in the
  working tree but not cited by the task or present on the integration branch, were not opened —
  out of scope for this task.
- The classification of borderline cases (9.6's CSS block-boundary check; 13.5(b)'s rest-element
  destructuring, which does route real data even though its cited guard is structural) required a
  judgment call about whether "a value/behaviour/rendering change a test assertion can observe"
  means the property has _any_ runtime manifestation at all, versus whether _this specific_ guard
  observes one. Both are argued in the "why" column; a reader who draws that line differently could
  move 9.6 to VITEST or part of 13.5 to UNCLEAR.
