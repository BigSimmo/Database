# Everything outstanding — Ward Lead's full list, 2026-09-02

**Compiled from this chat's whole memory at master `e38adb2f8`.** Sixty-one items. Nothing here is
merged-and-done; this is only what remains.

---

## A. DECISIONS ONLY THE OWNER CAN MAKE — 27

### The one that outranks everything

**A1.** ⚠️ **Should the engine refuse a placement nobody explicitly overrode?** Today it refuses
nothing. `REFER_TO_UNITS`, `ACCEPT_IN_PRINCIPLE` and `PULL_PATIENT` check no eligibility at all — the
reducer's single check sits on a different model that never places anyone. Proven by driving the real
reducer: a detained, involuntary adult male into the forensic bed, **zero rejections at every step**.

### Privacy — one question wearing five hats

**A2.** ⚠️ **Should the app have any notion of who is looking?** There is none anywhere. **Answering
this settles A3–A6.**
**A3.** Should the sidebar stop naming other wards a patient was referred to?
**A4.** Should the patient workspace stop showing how many wards were tried and which accepted?
**A5.** ⚠️ **Should a coordinator see a patient's suburb?** Raised independently by three chats. It is
in **neither projection's type, so no gate can catch it either way.**
**A6.** ⚠️ **Cross-page inference across 65 community team pages.** Anyone who opens two learns a
person was referred to both, without the software displaying it. **Not answerable by any search over
source.** Moot only while every seeded referral is single-destination — and the fixture changed three
times in one day.

### Clinical

**A7.** Should a shortlist show beds a ward **says** it can staff, or beds **physically empty**? The
two halves disagree; a ruling of 2026-09-01 already chose one way for a different action. Measured
impact today: zero.
**A8.** ⚠️ **Should the ED referral form stop pre-selecting "no one-to-one nursing"?** Five sibling
fields were fixed; this one was left while ruling 1 was open. **Ruling 1 has landed, so it now feeds a
gate rather than a display.**
**A9.** ⚠️ **Four controls pre-select the data-provenance reason.** A clinician correcting a mistyped
legal status who never touches the control **records the correction as a fresh report from the
treating team.** Audit trail, not liberty.
**A10.** Can an ED ever **accept** a referral? Nothing produces that state, so one branch of the new
wording has no reachable input and no test.
**A11.** Two ED browser journeys have **never once passed**. Worth a Playwright window, or drop them?

### Wording

**A12.** Is **"Ward nurse in charge"** the same role as "Ward manager"? A fourth spelling, outside
ruling 5; changing it means rewriting a search test.
**A13.** The community hub reads _"Expected discharge was 1 week ago"_ — past tense in place of a word
an existing test bans page-wide. Implementer's choice.
**A14.** ⚠️ **Should the referral board say what an ED referral was FOR?** Verified: the label function
receives the whole destination including purpose and returns the kind alone, so the board reads
_"Also refused — Emergency department: No suitable bed"_ where no bed was requested.
**A15.** The "recently answered" list is **uncapped**, so "recently" decays with use. How many rows?

### Data and demo

**A16.** ⚠️ **The seed cannot exercise ruling 6 at all** — all ten seeded referrals go to one
destination, hand-counted. The feature is proved in tests and **cannot be seen on screen.** Add a
two-armed referral to the seed?
**A17.** `--clinical-border-subtle` **has no analog anywhere in the repository.** Its value is an owner
decision; nobody invented one.

### Process and record-keeping

**A18.** ⚠️ **The DOM sweep lost 53 of its 61 findings** — they exist nowhere. Recover by re-running,
or retire the document? (One chat recommends retire-and-re-run.)
**A19.** Are the **131 findings worth triaging at all**, given the structural bias in C3?
**A20.** How should **"already fixed before it was raised"** be counted? Not a false positive, but the
hit rate depends on the answer.
**A21.** Does the **claims register cover every figure**, or not? Figure 3 shipped without an entry;
its siblings have them.
**A22.** ⚠️ **Who owns `tests/ward-screen-fd23-leaks.dom.test.tsx`?** **Asked eight times and never
answered.** It sits in nobody's declared set, so nobody may safely edit it. Same `allUnits()`-only
blind spot at line 214 that was closed elsewhere at `64b4c1388`; the shape is already worked out.
**A23.** Was the **double-mount ever observed on a real page**, or only under `mockups/`? Decides
whether that fix was a fix or a relocation.
**A24.** May a Ward Flow branch change `src/app/**` at all?
**A25.** May five production routes lose the inherited loading skeleton? ⚠️ **Never re-checked since
first raised.**
**A26.** ⚠️ **May the four leftover files be deleted?** Including `tests/scratch_debug_elig.test.ts`,
now **on the master line**, whose entire body asserts that true is true.
**A27.** May the scratch branches be swept? At least nine exist (`trial-merge-*`, `trial-b1-*`,
`trial-b2b3-*`, `check-master-*`, `verify-*`). **None is checked out anywhere** — verified.

---

## B. TECHNICAL DEFECTS STILL PRESENT — 8

**B1.** ⚠️ **Undeclared CSS custom properties.** Six names, ten references, no fallback, never
declared, in `board.module.css` and `ward-management.module.css`. **They are wrong names, not missing
values** — every one is a lone absentee from a family that exists, and nine of ten have a real
already-declared token they should use. **Consequence:** an "Away" button and a "Leaving" selector,
both production tap targets, lose background, border and the browser's own control chrome. **Two
controls that stop looking like controls.** Text survives (colour is inherited). ⚠️ **Nobody has
opened the board to look.**
**B2.** ⚠️ **Two documents name live branches without saying so.** `ward-flow-coordination-rules.md`
and `ward-flow-fold-manifest-2026-08-31.md` point at `claude/Wardquestions` and `claude/Ward-design`.
**Both verified unmerged, so both are live** — the fix is to say "live", not "merged".
`Wardquestions` holds the orchestrator handover and ten ward documents **that exist nowhere else.**
**B3.** **Unbounded recursive delete** in `tests/ward-flow-chat-control.test.ts` with no retry guard.
The retry-bounded shape already exists earlier in the same file.
**B4.** ⚠️ **A double cast hiding a worse one.** `tests/ward-release-band-day-boundary.test.ts:34`
uses `as unknown as BedRelease`, which suppresses both excess- and missing-property checking. Its
fixture has **three fields the type does not have and is missing six it requires** — including
`blocker`, **the field the blocked-discharges figure counts.**
**B5.** ⚠️ **A property with no backstop anywhere.** The thing `ward-referral-matching.test.ts` exists
to protect — that referral matching never reads the unvalidated bed-release model — **has no other
guard in the suite**, and the edit that disables it is an ordinary re-export refactor, not a
contrived one. The file's own scanner is regex-based and cannot see through a re-export.
**B6.** **`ward-scenarios.test.ts` drift, live and widening.** Prose says 41 open movements and 342
pairs; the assertion says 43 and 325; the failure message says 41. **The pinned figure was corrected
three times and the prose was never re-measured** — inside a file whose own comment instructs readers
to re-measure rather than adjust.
**B7.** ⚠️ **A test asserting `true` is `true` ships on the master line** (`scratch_debug_elig`). Left
because the protection hook refuses removal and the override needs approval.
**B8.** **`tests/ward-screen-fd23-leaks.dom.test.tsx`** carries the same blind spot closed elsewhere,
and is unowned — see A22.

---

## C. THINGS BELIEVED BUT NOT VERIFIED — 13

**C1.** **126 findings, no mutation run for any of them.** Six candidate guards found by static search
only. Leads, not verdicts.
**C2.** **24 triage findings, none mutation-observed** — all reached by reading, at a commit now
behind.
**C3.** ⚠️ **The structural-bias claim** — that the 131 skew toward mis-attribution because the `.ts`
sweep read neither the DOM nor the Playwright family. Its own author calls it _"the most consequential
thing I believe and the least tested."_ Rests on six instances and two retractions.
**C4.** ⚠️ **53 of 61 DOM findings have no record at all** and cannot be re-checked by anyone.
**C5.** ⚠️ **A sweep of 14 files returned four negatives of undetermined method** — its transcript was
0 bytes, and the wrong method produces an identical report. **Do not treat those files as clean.**
**C6.** ⚠️ **`BedRelease.waitingOn` never read back**, and **`dischargeConfirmedAt` has no runtime
writer** — unverified leads from an audit whose parent died before reconciling its children. **Two
other findings from that run were false and withdrawn.**
**C7.** **One mutation remains inconclusive** — emptying an exception map broke the file's parse and
the runner reported "no tests", the fork-failure shape rather than a negative. **That assertion has
never been proved.**
**C8.** **Two ED browser journeys fixed and never re-run.** Both failed their first ever run.
**C9.** ⚠️ **Three of four mutation proofs from one branch overstate.** One commit's _"every pin
fired"_ is false — roughly **46 of 63** assertions were never exercised, **and that is a floor.**
**C10.** ⚠️ **The engine-enforcement finding has never been independently verified by anyone.** Ward
Verifier offered to check it, said it outranks everything it raised itself, and has not yet reported.
**C11.** **A pre-selected-default sweep pattern-matched ~106 of 115 controls.** A pattern scan is not
a sweep, so those 106 are **unswept**.
**C12.** **Eight rulings lived only in a git-ignored ledger.** Their substance was rescued into a
report; the ledger itself was not.
**C13.** **Whether the location was the cause of this chat's isolation** — never proved, and the test
was never completed.

---

## D. ENVIRONMENT AND PROCESS — 6

**D1.** ⚠️ **The shell lost its own commands mid-session** — `ls`, `grep`, `head`, `sleep`, `wc`,
`python`, `cmd`, and even `command -v`. Partial recovery later. **A missing command reports "not
found" and a non-zero exit, which looks exactly like a failing check.**
**D2.** ⚠️ **Git could not spawn its own pre-commit hook.** Several commits used `--no-verify` and say
so; **those gates are UNRUN, not passed.**
**D3.** **The protection hook has three known false positives:** it blocks scratch-file removal, it
blocked removing a symlink that is not a worktree, and **it blocked a command that merely wrote a
warning quoting the removal command as text.**
**D4.** ⚠️ **A restore was content-identical, hash-different, and `git diff` called it clean** — a
Python text-mode write had converted 2,721 line endings. **Verify a restore by hash, never by diff.**
**D5.** **Ten or more sessions run concurrently**, matching a known pattern of machine starvation.
**D6.** ⚠️ **`--reporter=basic` does not exist in this vitest.** It dies at startup, runs nothing, and
reports no failures. **Two chats independently hit this same flag on the same night**, and one says
plainly it would have reported three failures as fixed had it trusted the exit code.

---

## E. COORDINATION — 4

**E1.** ⚠️ **This chat could not receive a single message, in either direction, for its entire life**,
while the other four messaged each other normally. Cause never established.
**E2.** ⚠️ **There is no Ward Lead chat running.** The main line has no integrator.
**E3.** **Ward Verifier's reports exist only in chat**, never in git — it has no branch and correctly
writes no files. **Its closing report has not yet arrived.**
**E4.** **One handover contains two errors its author deliberately did not amend**, on the grounds
that amending a delivered document to hide an error is worse than leaving the correction beside it.
Both are corrected in later reports.

---

## F. MY OWN ERRORS — 19, listed in full in `ward-lead-12-hour-review.md`

The four that still matter:

**F1.** ⚠️ **I told the owner no patient could be placed in a forensic bed. They could.** And **both**
of my own attempts to verify it failed silently, **both towards the comfortable answer.**
**F2.** **Every factual error I made ran in that same direction** — the forensic reassurance, a
retraction of a true finding, and diagnosing a messaging fault as general when it was mine alone.
**F3.** **I broke my own rule** about two workers in one folder, having written that rule to four
chats the same evening.
**F4.** **My verdict on the unsafe merge was correct and arrived too late to be used**, because I
could not reach anyone. That is the whole cost of E1 in one line.
