| 2026-09-02 | claude/caring-contacts-vocabulary-tmnc89 | c4ead393eef53bad18ce122da7164715b0b4878d | caring-contacts prohibited-language scan | Reviewed by the clinical-governance-reviewer subagent before the push; no blocker. Verdict: no guard is weaker than origin/main at any point in the diff, and tests/helpers/caring-contacts-prohibited-language.ts is documentation-only (its non-comment content hashes identically on both sides). The reviewer replicated both scanners across all 109 files in the five roots and ran 13 adversarial fixtures against the raw-prose refactor, confirming it coverage-neutral: the only differences are removed duplicate reports, and no input was found where the old scan catches and the new one misses. Three findings were fixed in the diff rather than deferred. FIRST and most serious, the definition-site exemption for src/lib/caring-contacts/message-rules.ts was whole-file, which would have put the five sentences a discharged patient reads by SMS -- programmeLine, operatingHours, emergencyDirection, crisisSupportContact and closingStatement -- permanently outside the scan, on the commit whose purpose was to reach unwatched wording; it matters because this vocabulary is wider than PROVISIONAL_MESSAGE_RULES.prohibitedTerms and validateGovernedMessage checks none of the extra terms. It is now computed from the policy, exempting only text that is exactly a declared term. SECOND, its anti-rot test was unfalsifiable by construction (non-emptiness cannot fail there) and now asserts the offence SET. THIRD, the scope comment claimed src/lib/tools-catalog.ts was clean of this vocabulary when it carries bestFor: 'Safe and effective prescribing'; the comment now names that match. Verified by mutation rather than by a green run alone: planted offences in service-state.ts, reach-reporting.ts, message-rules.ts closingStatement, demo-seed.ts and an api/caring-contacts route each failed the scan naming file and exact string, and JSX text containing an apostrophe is still caught. Residual risk recorded and not fixed here: a change WEAKENING a clinical safety guard that lives under tests/ escapes the governance preflight gate, because clinicalRiskPatterns has no pattern for the tests that are themselves the guard. | npx vitest run over all 69 caring-contacts test files: Test Files 69 passed (69), Tests 1476 passed (1476). npm run typecheck: clean, gate-receipts recorded a pass for typecheck:internal (6002 input files). npx eslint on both changed files: clean. npx prettier --check on every changed file: All matched files use Prettier code style. npm run check:outstanding-issues: snapshot in step (75 open, 13 pending), ledger inbox check passed, outstanding-issues guard passed 529 rows. npm run docs:check-links: reproduced the CI failure locally, then passed (4731 repo path references resolve). npm run check:ledger-write-discipline, docs:check-index, docs:check-inventory, docs:check-scripts: all passed. GitHub CI on head c4ead39: all 25 check runs completed with no failures, PR required success. Not run and not required for a tests-only diff with no production module touched: verify:pr-local, verify:cheap, verify:ui, verify:release. Nothing provider-backed was run. |
