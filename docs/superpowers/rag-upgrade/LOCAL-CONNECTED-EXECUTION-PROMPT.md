# New-session local connected execution prompt

Use this only after Cloud P17 and accepted `PROGRAMME.json` are published. Fill the exact remote branch/tip and start with L00 at local high effort.

You are the local handover controller for one declared L00–L10 phase of the Database RAG upgrade. Read root `AGENTS.md`, `local/START-HERE.md`, `local/programme-manifest.json`, `local/execution-order.md`, `local/approval-matrix.md`, `local/connected-execution.md`, the route-evidence/connected/operational schemas and both RAG specifications. Complete only the declared local phase, create its independently reviewed receipt and stop.

For L00, run the repository task-start preflight, fetch current `origin/main` and the exact accepted Cloud programme branch, verify its immutable remote tip and `PROGRAMME.json` commit/hash, and create a fresh isolated local operations worktree from that programme metadata commit. Never use, alter, rebase, merge or cherry-pick `codex/rag-local-build-20260822`; compare it only as quarantined reference evidence. Run setup, runtime, lock parity, package and full programme-receipt checks. Classify current-main overlap without absorbing it. L00 is offline/read-only and has no connected authority.

For L01–L10, start a fresh local session at the exact predecessor receipt commit and select the manifest effort before launch. The prompt does not authorize hosted reads/writes, source browsing, providers, migrations, deployment, reindex, activation, rollback or cleanup. Before an operation, obtain a target-specific, unexpired approval containing service, target, actions, scope, exposure, expected state change, cost ceiling, rollback and stop conditions. Read-only, mutation, deployment, promotion, rollback and cleanup authorities remain separate.

Load/hash all phase skills; prove controller and fresh reviewer route from direct host-emitted Codex metadata in the tracked route-evidence shape; reject fallback or model-authored substitutes. Produce only sanitized tracked evidence and hashes. Raw protected/hosted/provider data remains in approved ignored secure output. Execute every manifest-required operation class under an exact service/target/action-class approval valid at `performedAt`; an accepted GO phase requires every operation to pass. Obtain a fresh reviewer over the exact full phase range with PASS specification and quality verdicts, atomically add the receipt/review/route metadata, validate, push only when separately authorized and stop.

L10 may resume after retention without a permanent watcher. It accepts only with exact zero-eligible or bounded-cleanup proof, empty residual gates and a fresh Sol/xhigh whole-operational review over the immutable PROGRAMME-to-L10 range, then adds only `OPERATIONAL.json` and its exact route/review artifacts in one atomic commit. Never edit accepted Cloud or local receipts or `PROGRAMME.json`.
