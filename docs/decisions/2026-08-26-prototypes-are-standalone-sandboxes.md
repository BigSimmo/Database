# Decision: Each prototype is its own sandbox

- **Status:** decided 2026-08-26
- **Source:** `src/app/mockups/mockups-layout-client.tsx`, find "The product owner's instruction is that each prototype is"

Each prototype is its own sandbox that interacts only through the developer page and otherwise works as a standalone app, so it does not render inside the main application's shell.

If this summary and the source ever differ, the source wins.
