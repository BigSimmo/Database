# Caring Contacts — hazard log

> **DRAFT — requires clinical sign-off by the owner before any real-patient use.**
>
> This document is a starting point assembled from what this repository already records. It is
> **not clinical authority**, it is not a risk assessment anybody has performed, and nothing in it
> constitutes approval of a pilot. No row below has been reviewed, accepted, or signed off by a
> clinician. Until the owner signs it, treat every "residual risk" cell as an unreviewed assertion
> by whoever drafted the row, not as an accepted level of risk.

**Status:** first draft, 2026-09-04. Unreviewed.
**Scope:** the Caring Contacts workspace only — `src/lib/caring-contacts/**`,
`src/lib/caring-contacts-server/**`, `src/components/caring-contacts/**` and
`src/app/caring-contacts/**`. It does **not** cover the PsychSift answer pipeline; that has its own
register in [`docs/clinical-hazard-analysis.md`](../clinical-hazard-analysis.md) and
[`docs/clinical-hazard-controls.json`](../clinical-hazard-controls.json), and the two must not be
read as one document.

## Why this file was written from scratch

Outstanding-issue row `#1S81R8` cites `docs/caring-contacts/hazard-log.md` as an existing record of
hazards H-00, H-04 and H-05, and `#TDKW4W` cites hazard H-44. **No such file has ever existed in
this repository.** `git log --all --name-only` across the full fetched history returns no path
matching `hazard-log`, and the recovery command in
[`phase-1-handoff.md`](phase-1-handoff.md) named a commit that is not a valid object. The
2026-09-02 repository audit records this as finding M6
([`docs/audit/full-repository-audit-2026-09-02.md`](../audit/full-repository-audit-2026-09-02.md),
§M6) and independently confirmed it.

So this is a **reconstruction, not a recovery**. Two consequences the reader must hold onto:

1. **The original wording of H-00, H-04, H-05 and H-44 is lost.** What appears below for those four
   rows is drawn from the ledger rows' own detail cells, which are quoted rather than paraphrased
   wherever the ledger is specific. If the original log said more, this document does not know it.
2. **The original numbering is unrecoverable.** H-01 to H-03, H-06 to H-43 and anything above H-44
   are not in this repository in any form. Those identifiers are therefore **retired and never
   reused here**: hazards newly recorded from the code carry an `H-C##` ("control-derived")
   identifier so that no future reader can mistake a 2026-09-04 reconstruction for a row from the
   lost 2026-08-19 log.

## How to read the columns

- **Existing control** cites the exact `file:line` of the code that enforces the row, and names the
  offline test that proves it runs. Every line number in this document was read at commit time; if
  a citation has drifted, the citation is wrong, not the control.
- **Residual risk** is what the cited control does _not_ cover. It is drafted, not agreed.
- **Status** is one of `UNMITIGATED` (no control exists), `Controlled — unreviewed` (a control
  exists in code and is tested offline, but no clinician has confirmed the control is the right
  one), or `Partial` (a control exists and a known gap is recorded beside it).
- A `Controlled — unreviewed` status is a statement about **code**, not about clinical adequacy.
  Static proof that a rule executes says nothing about whether the rule is correct for a
  suicide-aftercare service. Only the owner can say that, and he has not.

## Part A — the hazards with no control at all

These four block a real-patient pilot. Nothing in the code addresses any of them, and no code
change can: they are decisions and reviews that people have to perform.

| id       | hazard                                                                                  | cause                                                                                                                                                                     | harm                                                                                                                                                                              | existing control | residual risk                                                                                                                                                                                 | owner                                 | status          |
| -------- | --------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- | --------------- |
| **H-00** | No named clinical safety officer, so nobody owns clinical risk for this service         | The programme was built as a prototype. No governance role has been filled, and the code's own role vocabulary names seats nobody occupies                                | Every hazard below is unowned. An incident has no named person to escalate to; a clinical judgement about wording, cadence or an adverse event has no author                      | **None**         | Total. The whole of this document is unowned until this row closes, including the statuses in Part B                                                                                          | Josh (owner)                          | **UNMITIGATED** |
| **H-04** | The patient-visible message set has never been read by anyone with lived experience     | No lived-experience review has been convened. Every patient-visible string in the repository was drafted by an implementer or authored by the owner                       | A discharged patient in a suicide-aftercare programme receives wording nobody with lived experience has judged; the wording may be harmful in ways only that review would surface | **None**         | Total for wording. The `PROVISIONAL` markers and the approval-gate constant record the absence honestly, but recording an absence is not a review                                             | Josh (owner) to convene               | **UNMITIGATED** |
| **H-05** | No Aboriginal cultural safety review, in a Western Australian suicide-aftercare service | No Aboriginal health governance body has been approached. The programme holds a small-cell disclosure threshold but no cultural safety review at all                      | Culturally unsafe contact, wording or reporting in a population where suicide aftercare carries specific and well-documented cultural safety requirements                         | **None**         | Total. `reach-reporting-governance.ts` limits what may be _published_ about Aboriginal and Torres Strait Islander patients; it reviews nothing                                                | Aboriginal health governance          | **UNMITIGATED** |
| **H-44** | Hospital referral feed feasibility is unconfirmed and is the largest programme risk     | Every screen, rule and table assumes a structured referral arrives from a WA hospital system and a structured outcome is written back. No document names an actual system | The service cannot start, or starts on manual entry nobody has designed for; the whole build rests on an integration nobody has confirmed is possible                             | **None**         | Total. The provider-neutral referral interface insulates the _build_, not the service. the referral-feasibility document cited by `#TDKW4W` (under docs/caring-contacts/) also does not exist | Josh (owner) to open the conversation | **UNMITIGATED** |

### What "UNMITIGATED" means here

None of the four blocks the synthetic build, and none is a defect in the code. Every one blocks a
pilot with a real patient. H-04 is the one that is ready to run today: the facilitation material is
[`message-review-pack.md`](message-review-pack.md) in this directory — itself a first draft, and
also unreviewed.

## Part B — the hazards the code actually enforces

Every row cites the control that exists **today**, at the line it exists on. This is offline,
static evidence. It establishes that a rule is written and that a test exercises it; it does not
establish that the rule is clinically right, and it does not establish that anything is safe.

### B1 — Death, withdrawal and plan lifecycle

| id        | hazard                                                                     | cause                                                                              | harm                                                                                           | existing control                                                                                                                                                             | residual risk                                                                                                                       | owner           | status                  |
| --------- | -------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | --------------- | ----------------------- |
| **H-C01** | A caring contact is delivered after the recipient has died                 | A death is recorded in the hospital system after the plan's calendar was fixed     | A bereaved family receives an automated "thinking of you" message addressed to the dead person | `src/lib/caring-contacts/hospital-events.ts:93` (cancels the plan and every unsent contact), dispatched at `:156`; proved by `tests/caring-contacts-hospital-events.test.ts` | Depends entirely on the death reaching this system. Nothing here detects a death nobody reported — that is H-44's feed, unconfirmed | H-00 (unfilled) | Controlled — unreviewed |
| **H-C02** | A death timestamp that cannot be parsed makes the cancellation fail open   | An upstream feed sends a malformed or absent instant                               | As H-C01                                                                                       | `hospital-events.ts:97` — an unusable instant raises an exception for review but does **not** stop the cancellation                                                          | The exception has no defined human workflow attached to it in this repository                                                       | H-00 (unfilled) | Controlled — unreviewed |
| **H-C03** | A correction to a recorded death puts messages back in flight              | A data-entry error is fixed, and the fix is treated as "resume"                    | Messages restart off the back of a data fix, with nobody having decided that they should       | `hospital-events.ts:113` — refuses unless the plan is cancelled, raises a governance incident, and explicitly never resumes the episode                                      | The incident requires a human to act on it, and no on-call rota exists (H-00)                                                       | H-00 (unfilled) | Controlled — unreviewed |
| **H-C04** | A third party ends a service the patient chose                             | A relative or clinician contacts the service asking for it to stop                 | A patient loses an aftercare contact they consented to, on somebody else's say-so              | `hospital-events.ts:176` — a third-party withdrawal is refused before the plan state is even consulted; a third party may only _pause_ (`:127`)                              | Nothing prevents a third party from persuading the patient's own clinician to withdraw on the patient's behalf                      | H-00 (unfilled) | Controlled — unreviewed |
| **H-C05** | A readmission or a changed mobile silently resumes or rebases the calendar | An event arrives and the system "helpfully" recomputes                             | Contacts arrive on the wrong days, or resume without a human decision                          | `hospital-events.ts:127` — pauses only, raises a contact-changed exception on every mobile change, and rebases no date                                                       | Requires a human to unpause; no reminder mechanism exists                                                                           | H-00 (unfilled) | Controlled — unreviewed |
| **H-C06** | An urgent-safety pathway retirement leaves running plans sending           | A version is retired for a safety reason while plans are live against its snapshot | Contacts keep going out under wording or cadence that was withdrawn for safety                 | `src/lib/caring-contacts/pathway-versions.ts:247` — only an `urgentSafety` retirement pauses future contacts; proved by `tests/caring-contacts-pathway-versions.test.ts`     | Whether a given retirement _is_ urgent is a human judgement with no criteria written down                                           | H-00 (unfilled) | Partial                 |

### B2 — What a message says

The five rows below sit behind one function, `validateGovernedMessage`
(`src/lib/caring-contacts/message-policy.ts:152`). **Read the note under this table before relying
on any of them.**

| id        | hazard                                                                         | cause                                                                                    | harm                                                                                                          | existing control                                                                                                                                                                                                                                                 | residual risk                                                                                                     | owner           | status                  |
| --------- | ------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | --------------- | ----------------------- |
| **H-C07** | An empty or unwritten message is sent as a caring contact                      | A sender resolves no body and sends the empty string, or skips the contact silently      | A person in aftercare receives a blank message, or receives nothing while the record says they were contacted | `message-policy.ts:175` refuses any unauthored body for every message type; `:280` defines "authored"                                                                                                                                                            | A sender that never calls the chokepoint meets no refusal — see the note below                                    | H-00 (unfilled) | Partial                 |
| **H-C08** | An implementer drafts patient-visible wording for a suicide-prevention message | A closing message is due and no closing wording has ever been authored                   | Unreviewed clinical copy reaches a discharged patient at the moment a service ends                            | `message-policy.ts:317` refuses rather than drafting; `src/lib/caring-contacts/message-copy.ts:151` does the same for a missing preferred name                                                                                                                   | The refusal is the whole control. The wording still has to be written, by the review H-04 names                   | Josh (owner)    | Partial                 |
| **H-C09** | A message carries commercial or risk-scoring vocabulary                        | Wording drifts toward CRM language ("campaign", "lead", "high risk", "engagement score") | A person in suicide aftercare is addressed as a marketing target or told they are "high risk"                 | `message-policy.ts:191` over the frozen list at `src/lib/caring-contacts/message-rules.ts:172`, with the whole-word `lead` pattern at `:167`; parity with the clinician-facing screen list is pinned by `tests/caring-contacts-interface-vocabulary.test.ts:867` | A denylist catches known-bad words, never bad wording in general. Only the H-04 review addresses the general case | Josh (owner)    | Partial                 |
| **H-C10** | A message omits how to get help                                                | A first or closing message is composed without the support block                         | Someone reads a message from a mental-health service that tells them nothing about where to turn              | `message-policy.ts:208` (first message: programme line, hours, `000`, crisis contact) and `:219` (closing: final-message statement plus support)                                                                                                                 | The required fragments are `PROVISIONAL` content nobody clinical has approved                                     | Josh (owner)    | Partial                 |
| **H-C11** | A message invites a reply on a channel nobody reads                            | A question mark, or "let us know", in a one-way message                                  | A distressed person replies expecting to be heard, and is not                                                 | `message-policy.ts:233` refuses any `?`                                                                                                                                                                                                                          | Only a question mark is detected. "Text back if you need us" contains none and would pass                         | Josh (owner)    | Partial                 |
| **H-C12** | The patient's own mobile number is echoed into the message body                | A substitution error or a template mistake                                               | A patient receives their own number back from a service, an alarming and identifying artefact                 | `message-policy.ts:229`                                                                                                                                                                                                                                          | Only checks the number the caller supplies; a stale or second number is not compared                              | H-00 (unfilled) | Controlled — unreviewed |

> **The control in B2 is not wired to anything, because there is nothing to wire it to.**
> `validateGovernedMessage` is called from `message-policy.ts` itself, from
> `tests/caring-contacts-message-policy.test.ts`, `tests/caring-contacts-message-copy.test.ts` and
> `tests/caring-contacts-interface-vocabulary.test.ts` — and from **no production code path**
> (`grep -rn "validateGovernedMessage" src worker scripts`). This is not a defect: there is no
> telephony provider and no sender in this repository, and no message has ever been sent to any
> number, real or test. It matters for the pilot because it means **every B2 row is proof about a
> function, not proof about a delivered message.** When a sender is built, the sender calling this
> chokepoint on every path is itself a hazard that will need its own row and its own control.

### B3 — Real versus fictional contact details

| id        | hazard                                                                        | cause                                                                                         | harm                                                                                        | existing control                                                                                                                                                                                                                                                                        | residual risk                                                                                                                  | owner           | status  |
| --------- | ----------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | --------------- | ------- |
| **H-C13** | A message reaches a patient carrying a number that connects to nobody         | Every specimen message names this prototype's reserved fictional staffed line                 | A person in crisis dials a number from a mental-health service and reaches dead air         | `message-policy.ts:204` refuses by default on the marker pattern at `message-rules.ts:122`; the only way past it is an explicit acknowledgement flag at the call site                                                                                                                   | The acknowledgement flag exists and is greppable, but nothing stops a future caller passing it in production                   | H-00 (unfilled) | Partial |
| **H-C14** | A working crisis number is marked as fake, or the crisis sentence is reworded | An implementer "tidies" the crisis-support sentence or files it among the fictional numbers   | The one sentence that must survive is deleted, reworded, or suppressed as synthetic         | `message-rules.ts:117` holds the owner-authorised sentence as the single source, interpolated into both patient-visible messages; `src/lib/caring-contacts/synthetic-contacts.ts:1` forbids adding it to the fictional list; proved by `tests/caring-contacts-message-copy.test.ts:323` | The numbers in that sentence (Lifeline, 13YARN) are **not** aged by any check in this repository — see "Unverified" below      | Josh (owner)    | Partial |
| **H-C15** | A message is silently split into three or more SMS segments, or truncated     | Wording grows, or a long preferred name is substituted                                        | A safety message arrives cut in half, or the half with the crisis number never arrives      | `message-policy.ts:186` (two-segment ceiling); `message-copy.ts:105` derives the preferred-name cap from the wording rather than hard-coding it                                                                                                                                         | The ceiling is only evaluated when the text is GSM-7 encodable — see H-C16                                                     | H-00 (unfilled) | Partial |
| **H-C16** | A non-GSM-7 name silently disables the segment ceiling                        | A preferred name such as `Zoë` makes the whole message unencodable, and `gsm7.valid` is false | The length check at `message-policy.ts:186` never runs, and nothing else is checking length | `message-copy.ts:156` refuses the name outright and names the offending characters, sending the clinician back to ask the person how to spell it                                                                                                                                        | The refusal is in the _name_ path only. A non-GSM-7 character introduced anywhere else in a message still bypasses the ceiling | H-00 (unfilled) | Partial |

### B4 — Dispatch state and service-wide stop

| id        | hazard                                                                | cause                                                                                               | harm                                                                                    | existing control                                                                                                                                                                                                                                              | residual risk                                                                                          | owner           | status                  |
| --------- | --------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | --------------- | ----------------------- |
| **H-C17** | A contact goes out outside the approved hours                         | A retry, a queue backlog, or a timezone error pushes a send into the night                          | An unsolicited message about suicide aftercare arrives at 3 am                          | `src/lib/caring-contacts/schedule.ts:152` publishes the 09:00–18:00 AWST window; `:140` throws at module load if any send hour is moved outside it; `:158` is the predicate every send-time decision uses; proved by `tests/caring-contacts-schedule.test.ts` | AWST is hard-coded. A patient outside Western Australia would receive contacts at the wrong local time | H-00 (unfilled) | Partial                 |
| **H-C18** | A retry sends late rather than not at all                             | Three attempts 45 minutes apart from a 17:00 contact would reach 18:30                              | As H-C17                                                                                | `src/lib/caring-contacts/service-rules.ts:41` sets the policy so the window wins over the retry count, deliberately                                                                                                                                           | The interaction is documented and tested, but no production sender exercises it (see the B2 note)      | H-00 (unfilled) | Controlled — unreviewed |
| **H-C19** | A message is dispatched against an ended, cancelled or withdrawn plan | A queued job outlives the plan state change                                                         | A withdrawn patient — or a bereaved family — receives a contact after the service ended | `message-policy.ts:251` (terminal contact) and `:255` (terminal plan); store-side, `src/lib/caring-contacts/in-memory-repository.ts:481` and `src/lib/caring-contacts/db/postgres-repository.ts:827` refuse a status write on a non-active plan               | Defence in depth on two layers, one of which (the chokepoint) has no production caller                 | H-00 (unfilled) | Partial                 |
| **H-C20** | A draft or paused plan sends anyway                                   | A plan that has not started, or is held, is treated as merely "not ended"                           | Contacts begin before anybody activated the plan, or resume during a hold               | `message-policy.ts:260`, added under `#PAMATF` because a draft or paused plan previously passed this check                                                                                                                                                    | As H-C19                                                                                               | H-00 (unfilled) | Partial                 |
| **H-C21** | A confirmed incident does not stop sending                            | Wrong recipient, duplicate send, unauthorised content, privacy incident, or loss of audit integrity | The failure that triggered the incident keeps happening while it is being investigated  | `src/lib/caring-contacts/service-state.ts:251` is the single question every dispatch path asks; wired at `in-memory-repository.ts:351` and `db/postgres-repository.ts:671`; proved by `tests/caring-contacts-service-state.test.ts`                           | Somebody has to notice and record the stop. No automatic detector exists for any of the five reasons   | H-00 (unfilled) | Partial                 |
| **H-C22** | One person restarts a stopped service alone                           | The same individual supplies each required approval under a different role                          | A service stopped for a clinical or privacy incident resumes on one person's judgement  | `service-state.ts:149` requires three roles; `:220` refuses both a repeated role and a repeated actor                                                                                                                                                         | The roles are seats nobody occupies until H-00 closes, so today one person is all there is             | Josh (owner)    | Partial                 |
| **H-C23** | The incident banner leaks patient detail onto every screen            | The free-text incident note is interpolated into the banner                                         | A patient's name, number or clinical note is rendered service-wide                      | `service-state.ts:290` takes a narrowed fact type that structurally omits the note; the narrowing is at `:89`                                                                                                                                                 | A future edit could widen the parameter; only the type stops it                                        | H-00 (unfilled) | Controlled — unreviewed |

### B5 — Approval, access and the record

| id        | hazard                                                                           | cause                                                                                          | harm                                                                                                      | existing control                                                                                                                                                                                                                       | residual risk                                                                                                                                                                                                                   | owner           | status                  |
| --------- | -------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ----------------------- |
| **H-C24** | An author approves their own pathway version                                     | One person drafts the cadence and wording and signs it off                                     | Clinical content ships with no independent check at all                                                   | `src/lib/caring-contacts/permissions.ts:326`, applied at `pathway-versions.ts:196`; proved by `tests/caring-contacts-permissions.test.ts`                                                                                              | Two approvals from two people is a process control, not a competence check                                                                                                                                                      | H-00 (unfilled) | Controlled — unreviewed |
| **H-C25** | One person supplies both required pathway approvals by changing role             | Role-switching in a single-operator prototype                                                  | The dual-approval record is real in form and empty in substance                                           | `pathway-versions.ts:199` refuses a repeated role and a repeated actor                                                                                                                                                                 | As H-C22: the seats are unfilled                                                                                                                                                                                                | Josh (owner)    | Partial                 |
| **H-C26** | A pathway version's approval is read as approval of the message wording          | The approval seats render next to the wording on screen                                        | A clinician believes a discharged patient's message has been signed off when it has not                   | `message-copy.ts:240` holds the status sentence beside the words it describes, so no screen can restate it and go stale                                                                                                                | Depends on every screen reading the constant rather than writing its own sentence                                                                                                                                               | Josh (owner)    | Controlled — unreviewed |
| **H-C27** | A clinician reads another team's patients                                        | A URL is guessed, or an API is called directly                                                 | Unauthorised access to identifiable mental-health records                                                 | `permissions.ts:301` denies cross-team before any role is consulted; enforced at the API boundary in `src/lib/caring-contacts-server/handler.ts:328`                                                                                   | Team membership is asserted by the session, and this prototype's session model is not the eventual one                                                                                                                          | H-00 (unfilled) | Controlled — unreviewed |
| **H-C28** | Patient data leaks into the audit trail                                          | A field carrying a name, note or mobile is added to an audit event                             | The trail intended to prove good behaviour becomes a second copy of the record it audits                  | `src/lib/caring-contacts/audit.ts:83` scans every field of every event: forbidden field names (`:50`) and an Australian mobile-number pattern (`:43`), and throws                                                                      | A denylist of field names and one number format. A free-text field with a new name would pass unless its name is on the list                                                                                                    | H-00 (unfilled) | Partial                 |
| **H-C29** | A search term or a patient's name is recorded as an audited object id            | A read is audited with whatever string the caller had to hand                                  | The audit trail itself becomes a searchable index of what was typed about whom                            | `src/lib/caring-contacts/access-audit.ts:130` constrains the id shape; `:158` validates before anything is constructed; proved by `tests/caring-contacts-access-audit.test.ts`                                                         | —                                                                                                                                                                                                                               | H-00 (unfilled) | Controlled — unreviewed |
| **H-C30** | A refused write leaves no trace                                                  | The API boundary denies a mutation before any store runs, so the store's own audit never fires | An attempted unauthorised action is invisible afterwards                                                  | `access-audit.ts:33` carries a `mutation` access kind for exactly this case (Ruling 45), used by the boundary                                                                                                                          | Only covers refusals at the boundary. A refusal further out has no record                                                                                                                                                       | H-00 (unfilled) | Controlled — unreviewed |
| **H-C31** | Identifying detail is kept indefinitely                                          | Nothing ages a completed episode out                                                           | A suicide-aftercare record naming a patient persists with no end date                                     | `src/lib/caring-contacts/retention.ts:31` (7 years), `:62` (due only after a terminal state, in AWST days), `:121` and `:148` de-identify by construction rather than by deletion; proved by `tests/caring-contacts-retention.test.ts` | **Seven years is a working assumption, not a legal determination** — `phase-1-handoff.md` says so. Nothing schedules the clearance                                                                                              | Josh (owner)    | Partial                 |
| **H-C32** | A coordinator's attestation is read as the patient's consent                     | The record says "patient agreement confirmed"                                                  | The service believes it holds consent it does not hold; the actual agreement lives in the hospital record | `src/lib/caring-contacts/assurances.ts:45` names the act rather than a patient state, `:122` keeps every sentence in reported speech, and `:92` stops compiling if a free-text fourth field is added                                   | The distinction is carried by wording and a type guard. A screen that paraphrases it loses it                                                                                                                                   | Josh (owner)    | Partial                 |
| **H-C33** | Training data mixes with live data                                               | A cross-workspace query or join                                                                | Synthetic contacts are treated as real, or a real patient's data appears in a training sandbox            | `src/lib/caring-contacts/training.ts:53` returns true only when both workspaces are live; proved by `tests/caring-contacts-training.test.ts`                                                                                           | It is a predicate future code is _expected_ to consult; nothing forces a new query to ask                                                                                                                                       | H-00 (unfilled) | Partial                 |
| **H-C34** | A published figure re-identifies an Aboriginal or Torres Strait Islander patient | A small cell in a reach report                                                                 | Re-identification in a small population, in a service where that is a specific and serious harm           | `src/lib/caring-contacts/reach-reporting-governance.ts:62` holds a threshold of 5 with its author, date, basis and what it does **not** rest on; proved by `tests/caring-contacts-reporting.test.ts`                                   | The owner recorded on 2026-08-26 that one commit can move both the number and its pin, and **accepted that gap** for a prototype holding no real data. That acceptance does not survive a real-patient pilot. Connected to H-05 | Josh (owner)    | Partial                 |

## What this document does not do

- **It does not assess risk.** There is no likelihood, no severity, no risk matrix and no acceptance
  decision anywhere in it, because nobody qualified has made one.
- **It does not certify any control.** "Controlled — unreviewed" means a test passes offline.
- **It does not cover the referral feed, the telephony provider, hosting, authentication at scale,
  or anything the prototype does not contain.** Those are outside what the repository can evidence.
- **It is not a substitute for H-00.** A hazard log with no named clinical safety officer is a
  document with no owner.

## Claims in this document that could not be verified from the repository

Listed separately because the owner is being asked to sign this off, and an unverified claim in a
signed document is worse than an absent one.

1. **The original text of H-00, H-04, H-05 and H-44.** Reconstructed from the ledger detail cells
   in `docs/outstanding-issues.md` (`#1S81R8`, `#TDKW4W`). The original log is not in this
   repository and its full wording is unknown.
2. **The original hazard numbering.** Whether H-01, H-02, H-03 and the range up to H-44 named real
   hazards, and what they were, cannot be established here.
3. **The Lifeline and 13YARN numbers in the crisis-support sentence** (`message-rules.ts:117`).
   The owner authorised the sentence in writing on 2026-08-27 according to the comment above the
   constant; **no source or verification date for the numbers themselves is recorded anywhere in
   the repository, and no check ages them.** To verify — no network access in this session. This is
   the same class of gap the 2026-09-02 audit raised for the care-plan crisis lines; see
   [`docs/care-plan/crisis-lines-verification.md`](../care-plan/crisis-lines-verification.md).
4. **That the seven-year retention period is lawful or appropriate.** It is described in
   `phase-1-handoff.md` as "the working assumption the schema is built around". No legal basis is
   cited anywhere in the repository.
5. **That a small-cell threshold of 5 is adequate.** `reach-reporting-governance.ts:66` states on
   its own account that it rests on "common practice … by analogy" and on no calculation over this
   programme's data.
6. **That the reserved fictional number block is correct at its upper bound.**
   `docs/care-plan/cloud-session.md:258-259` records that the implementer did not verify where the
   reserved block ends.
7. **That every hazard in this workspace is listed.** Part B was assembled by reading the modules
   under `src/lib/caring-contacts/`. It is a survey, not an exhaustive derivation, and the screens,
   API routes and database layer were not swept the same way.
8. **Whether any control is the _right_ control.** Nothing in the repository can answer that. That
   is what sign-off is for.

## Related records

- [`message-review-pack.md`](message-review-pack.md) — the facilitation pack for H-04.
- [`phase-1-handoff.md`](phase-1-handoff.md) — Phase 1 record and the owner's open decisions.
- [`PROGRESS-LEDGER.md`](PROGRESS-LEDGER.md) — programme history and where the external handoff
  bundles live.
- [`docs/clinical-governance.md`](../clinical-governance.md) — repository-wide clinical governance
  checklist (a different system; read the scope note at the top of this file).
