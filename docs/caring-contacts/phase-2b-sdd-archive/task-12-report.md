# Task 12 — the schedule read

Group 2's read: what a team's caring-contact plans hold on a given AWST day, and what each day's
three sending windows contain. No screen, no overlay, and no change to `listSendableContacts`,
`schedule.ts`'s window mapping or `contactSendability()`.

## Commits

| SHA         | What                                                                        |
| ----------- | --------------------------------------------------------------------------- |
| `0eab7c4b7` | The derivation in the sealed domain, and the window inverse beside its map. |
| `161733566` | The HTTP boundary, the new access object type, and the union/enum pin.      |
| `90bac3773` | One added assertion: a sent contact carries no reason for not sending.      |
| `b11e67518` | This report.                                                                |
| `6b75dcf3b` | The `test:cc-guards` gate row added to this report.                         |

The fix round that follows the review has its own commits, listed under "Fix round" below.

## What was built

| File                                                | What it is                                                                         |
| --------------------------------------------------- | ---------------------------------------------------------------------------------- |
| `src/lib/caring-contacts/schedule-view.ts`          | NEW. `buildScheduleRange(plans, from, to)` and the two classifications it adds.    |
| `src/lib/caring-contacts/schedule.ts`               | EDIT. One addition: `sendingPreferenceAt`, derived from `SEND_HOUR_BY_PREFERENCE`. |
| `src/lib/caring-contacts/access-audit.ts`           | EDIT. One new `AccessedObjectType` member, `contactSchedule`.                      |
| `src/app/api/caring-contacts/schedule/route.ts`     | NEW. `GET`, through `readHandler`.                                                 |
| `src/app/api/caring-contacts/access-trail/route.ts` | EDIT. Its hand-copied `z.enum` kept in step, plus a note saying it is a hand copy. |
| `tests/caring-contacts-schedule-view.test.ts`       | NEW. The derivation.                                                               |
| `tests/caring-contacts-schedule-route.test.ts`      | NEW. The boundary, and the pin holding that `z.enum` to the union.                 |
| `package.json`                                      | EDIT. Both suites added to `test:cc-guards`.                                       |
| `docs/site-map.md`                                  | Regenerated by `npm run sitemap:update` for the new API route.                     |

## Ruling [124] — no repository method

`buildScheduleRange` takes `readonly PlanRecord[]` — whatever `listPlans` released for the actor —
and does the rest itself. It calls no store. `listSendableContacts` is neither called nor changed.

**Where the derivation lives, and why.** The sealed domain, `src/lib/caring-contacts/`. Three of the
four rules it composes are already owned there — the window mapping (`./schedule`), the sendability
classification (`./model`), the plan lifecycle — and the fourth, that a plan's own state holds
sending, is a rule about the domain rather than about any screen. There will also be more than one
reader: this HTTP route publishes it, and Task 13's Server Component render will read the store
directly rather than call the service over its own network, exactly as `patients/page.tsx` does
beside `plans/route.ts`. Both readers must get the same answer, and the only way to guarantee that is
for there to be one answer. The argument is written into the module header.

## What the read answers

`buildScheduleRange(plans, fromCalendarDay, toCalendarDay)` returns
`{ ok: true; view } | { ok: false; reason }`. The view holds every day of the inclusive range, in
order, **including the days holding nothing** — the seven-day strip and the selected day come from
one call. A single day is the range `(day, day)`; there is deliberately no second entry point,
because two entry points would be two places for the grouping to be applied slightly differently.

Each `ScheduleDay` holds:

- **`windows`** — always all three approved windows, in the order they occur in a day, taken from
  `SENDING_PREFERENCE_OPTIONS` so neither the order nor the wording is restated here.
- **`outsideApprovedWindows`** — see "What Task 13 has to render that the mockup does not show".
- **`exceptions`** — the named-exceptions panel's contents.
- **`counts`** and **`disposition`**.

Each `ScheduleEntry` carries the plan, patient and contact identifiers, the cadence label, the
message type, the AWST day and instant it sends at, and four derived facts:

| Field              | What it says                                                                     |
| ------------------ | -------------------------------------------------------------------------------- |
| `sendability`      | `contactSendability(state)`, unmodified — the contact's own answer.              |
| `planHold`         | What the PLAN's state says: `planNotStarted`, `planPaused`, `planEnded`, null.   |
| `isDue`            | Still to send AND not held. The only entries the service will actually send.     |
| `notSendingReason` | `absorbedByFirstContact`, `suppressed`, `cancelled` or `missed`; otherwise null. |

## The plan-state gate

The filed defect on `listSendableContacts` is that it has none. This read has one, and it is not a
workaround for that surface — it is the rule stated where a schedule needs it. Neither `pausePlan`
nor `activatePlan` touches a contact, so a draft plan's contacts and a paused plan's contacts both
sit in `scheduled`. A read that asked only the contact would announce a plan nobody has started, and
a plan a coordinator deliberately paused, as work the service is about to do.

`planSendingHold` is an exhaustive switch over `PlanState` rather than a list of held states, so a
seventh plan state cannot default into "this plan is sending".

## Counts are derived

`ScheduleCounts` is `StoredContactSummary & { due, held, needsReview }`. The sent / still-to-send /
never-will arithmetic is `summariseStoredContacts`'s, unchanged and not re-implemented; what this
read adds is the split of `stillToSend` into the part the service will send and the part its own plan
is holding. The invariant is stated on the type and asserted in the suite:
**`due + held === stillToSend`**.

Nothing in the code or in this report states how many contacts a plan has. The absorbed-contact case
is asserted by deriving both sides from the plan under test: the entries carrying
`absorbedByFirstContact` must equal the planned entries the planner marked suppressed, and the due
entries must equal the day's entries less those.

## The empty-state distinction, one layer down

`ScheduleDayDisposition` has three values, and the first two are the ones that matter:

- `noContactsPlanned` — no contact falls on this day at all.
- `nothingDue` — contacts fall on this day and none of them is due.
- `contactsDue` — at least one is.

A day whose every contact was cancelled by a withdrawal reports `nothingDue` with
`counts.willNotBeSent` equal to `counts.total` and each entry naming `cancelled`; a day that was
never given a contact reports `noContactsPlanned` with a total of zero. An absorbed first contact
reports `absorbedByFirstContact`, which is the plan working as designed, and is kept apart from a
cancellation, which means something stopped it. All three are asserted.

## Ruling [125] — the trail

The read is recorded as `{ search, contactSchedule, "<from>:<to>" }`. Its own `AccessedObjectType`
member rather than the caseload's `plan`: recording it as `plan` would be defensible by provenance
and wrong by meaning, and with no `objectId` filter on the trail's query surface a shared member
would make a caseload read and a schedule read one undifferentiated stream. The `objectId` is the two
days that were read — identifier-shaped by construction, because the schema accepted only two AWST
calendar days before either could reach it.

`src/app/api/caring-contacts/access-trail/route.ts`'s `z.enum` is a hand copy of that union and
nothing in the build compared the two. `tests/caring-contacts-schedule-route.test.ts` now pins them
as the same set, in the same order, by parsing both files; every step of that parse throws rather
than comparing two empty lists. That closes the mechanism the filed issue describes, as a side effect
of this task — flagged rather than claimed, because the issue is not mine to close and a maintainer
should decide whether this pin is where the check belongs.

## What Task 13 gets

A `ScheduleRangeView` per call, either from `GET /api/caring-contacts/schedule?from=&to=` or — the
shape the other workspace screens use — by calling `buildScheduleRange` on an `auditedRead` of
`listPlans` inside the Server Component, with the same access identity the route records. It gets,
per day: the three windows with their approved wording and send times; a fourth group for contacts
moved off an approved send time; the named exceptions; the counts; and the disposition. Every entry
carries its own state, its plan's hold, whether it is due, and why it is not.

It releases **no patient identity**: `PlanRecord` carries none, so a row is keyed by the synthetic
`patientId` and a screen wanting names reads `listPatientNames`, which is audited in its own right.
That is asserted at both layers.

### What Task 13 has to render that the mockup does not show

**A contact can be scheduled at a time that is not one of the three approved send times.**
`moveContactWithinDay` accepts any hour and minute inside the approved 09:00–18:00 window and both
stores persist it, so 11:30 is an ordinary stored state. Nothing in this domain says which window a
moved contact then belongs to, and inventing a band (09:00–11:59 is "morning") would be a second copy
of the window mapping and a rule nobody approved. So `sendingPreferenceAt` matches the approved hour
**and** minute, and a contact matching neither is reported in `outsideApprovedWindows` rather than
filed under a window it has left. The approved design shows three window columns and no fourth group.
**This is a difference between the design and the types, recorded rather than resolved**; the owner
should say what that group is called on screen.

**A held contact is not an exception.** A draft plan whose first contact day has arrived, and a
paused plan's contacts, are reported as `held` and are not put in the named-exceptions panel. That is
a judgement, not a derivation: the approved design's exception panel is about transport, and adding
an operational category to it is a product decision. Task 13 has `counts.held` and each entry's
`planHold` if the owner wants them surfaced.

**Named exceptions** are the four provider outcomes that are not a delivery, plus `missed` — five
contact states, classified by an exhaustive switch so a new state cannot default into "not an
exception". They are taken OUT of their sending window rather than listed twice, because the design
keeps named exceptions separate from the routine window lists and a screen rendering both would
otherwise count one patient as two. The invariant is asserted: **every entry a day holds appears in
exactly one group.**

## Verification

### Boundary cases, written first

**The 00:00 case is the timezone guard. The 17:00 case is not, and this section originally let a
reader think otherwise** — corrected in the fix round below rather than left to be inherited.

- **00:00 AWST — the timezone guard.** 2026-09-01 00:00 AWST is 2026-08-31 16:00 UTC. Unreachable
  through `buildApprovedSchedule` — the approved window starts at 09:00 — so this record is assembled
  by hand and labelled defensive, with `planned.calendarDay` deliberately set to the UTC date. **All
  three answers are therefore available to the code and only one is right**: it must place the
  contact on the AWST day of its own `sendAt`, never on the UTC date and never on the calendar day
  recorded beside it. It is the one case here that fails under a UTC-date bug AND under a
  trust-the-recorded-day bug. M2 is the mutation that exercises it.
- **17:00 AWST on the last day of a month — month-end enumeration, not the timezone guard.** 17:00
  AWST is 09:00Z on the same date and `planned.calendarDay` agrees with both, so every candidate
  derivation returns `2026-08-31` and the case fails under no plausible off-by-one. What it is
  actually worth is the month-end enumeration and its second assertion, that the following day holds
  nothing — a real distinction, since a day holding nothing and a day that was never asked about are
  different results. Kept for that, described as that.
- **A range across a month end.** Every day is enumerated, including the empty ones.

### Mutation ledger

Both suites were run for each attempt: `tests/caring-contacts-schedule-view.test.ts` and
`tests/caring-contacts-schedule-route.test.ts`. Presence was checked in process on the bytes on disk,
never through a shell. `git status --porcelain` was verified empty before each mutation and after
each restore; it was empty every time.

| #   | Mutation                                                                  | Predicted                                        | Result | The line the suite printed                                                                              |
| --- | ------------------------------------------------------------------------- | ------------------------------------------------ | ------ | ------------------------------------------------------------------------------------------------------- |
| M1  | `sendingPreferenceAt` drops the minute check                              | 10:30 answers `morning` where null was expected  | RED    | `expected 'morning' to be null` — `Tests 1 failed / 33 passed (34)`                                     |
| M2  | Bucket by `planned.calendarDay` instead of `awstCalendarDay(sendAt)`      | the midnight contact lands on 2026-08-31         | RED    | `expected 'contactsDue' to be 'noContactsPlanned'` — `1 failed / 33 passed (34)`                        |
| M3  | `isDue` ignores `planHold`                                                | a draft plan's contact becomes due               | RED    | `expected [ [ 'stillToSend', …(2) ] ] to deeply equal […]` — `3 failed / 31 passed (34)`                |
| M4  | `notSendingReasonFor` drops the absorbed branch                           | the absorbed entry reports `suppressed`          | RED    | `expected [] to have a length of 1 but got +0` — `1 failed / 33 passed (34)`                            |
| M5  | `needsOperationalReview` returns false for `missed`                       | `missed` disappears from the named states        | RED    | `expected [ 'notDelivered', …(3) ] to deeply equal [ 'notDelivered', …(4) ]` — `1 failed / 33 passed`   |
| M6  | `planSendingHold("paused")` returns null                                  | a paused plan holds nothing                      | RED    | `expected [ [ 'draft', 'planNotStarted' ], …(5) ] to deeply equal […]` — `2 failed / 32 passed (34)`    |
| M7  | `dispositionOf` tests `due === 0` instead of `total === 0`                | a stopped day reads as an empty day              | RED    | `expected 'noContactsPlanned' to be 'nothingDue'` — `4 failed / 30 passed (34)`                         |
| M8  | Windows keep the exception entries                                        | one patient appears in a window and in the panel | RED    | `expected 2 to be 3` (the duplicate-id check) — `3 failed / 31 passed (34)`                             |
| M9  | `counts.due` ignores `planHold`                                           | a held contact is counted as due                 | RED    | `expected 1 to be +0` — `3 failed / 31 passed (34)`                                                     |
| M10 | `SCHEDULE_RANGE_MAX_DAYS` 31 → 30                                         | the longest accepted range is refused            | RED    | `expected 400 to be 200` — `2 failed / 32 passed (34)`                                                  |
| M11 | `span < 0` → `span < -1`                                                  | a range inverted by one day is answered          | RED    | `expected [ …(2) ] to deeply equal [ …(2) ]` — `2 failed / 32 passed (34)`                              |
| M12 | `ordered` stops sorting                                                   | the reversed input keeps its arrival order       | RED    | `expected [ 'SYN-PLAN-020', 'SYN-PLAN-019' ] to deeply equal [ 'SYN-PLAN-019', 'SYN-PLAN-020' ]`        |
| M13 | The day's counts are taken over the routine entries only                  | the totals omit the exception                    | RED    | `expected [ …(2) ] to have a length of 1 but got 2` — `3 failed / 31 passed (34)`                       |
| M14 | The route records `plan` instead of `contactSchedule`                     | the trail cannot tell the two reads apart        | RED    | `expected [ { …(7) } ] to deeply equal [ ObjectContaining{…} ]` — `1 failed / 33 passed (34)`           |
| M15 | The route drops its range pre-check                                       | an inverted range answers 500 rather than 400    | RED    | `expected [ …(2) ] to deeply equal [ …(2) ]` — `1 failed / 33 passed (34)`                              |
| M16 | `contactSchedule` removed from the access-trail `z.enum`                  | the enum and the union stop being the same set   | RED    | `expected [ 'plan', 'contact', 'episode', …(8) ] to deeply equal […(9) ]` — `1 failed / 33 passed (34)` |
| M17 | `notSendingReasonFor` drops its `willNotBeSent` guard                     | GREEN expected — over-sensitivity control        | GREEN  | `Tests 34 passed (34)`                                                                                  |
| M18 | `buildScheduleRange` drops the out-of-range filter on the collecting pass | GREEN expected — over-sensitivity control        | GREEN  | `Tests 34 passed (34)`                                                                                  |

**Where a prediction was not exactly what fired.** M2, M6 and M8 went red on an assertion earlier in
the same case than the one predicted — the midnight case's first assertion rather than its second,
the `planSendingHold` table rather than the paused-plan case, and the duplicate-identifier check
rather than the length check. Each is the same defect caught by a sibling assertion, and each is
stated here rather than smoothed over.

**The two greens, honestly.** M17 and M18 were predicted green and are recorded as controls, not as
coverage.

- **M17.** The `willNotBeSent` guard in `notSendingReasonFor` is **unfalsifiable by any reachable
  state**: no `ContactState` is both sendable and one of the states the switch beneath it names, so
  removing the guard changes no asserted value. It is a defensive statement of intent and this report
  claims nothing more for it. A mutation that leaves every asserted value unchanged proves nothing,
  however the suite behaves.
- **M18.** The out-of-range filter on the collecting pass is a memory and work bound, not a
  correctness one: days outside the range are collected into a map that is then read only for the
  days requested. Removing it is invisible to the suite, correctly.

### Gates

| Gate                                                    | Evidence                                                                                                                                    |
| ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| Task suites, during iteration                           | `Tests  24 passed (24)` then `Tests  10 passed (10)`, and `Tests 34 passed (34)` for the pair on every restored tree in the mutation rounds |
| `npm run test:cc-guards` (`GATE_RECEIPTS=refresh`)      | `Test Files  20 passed (20)` and `Tests  425 passed (425)`                                                                                  |
| `npm run test` (full, `GATE_RECEIPTS=refresh`)          | `Test Files  838 passed / 3 skipped (841)` and `Tests  10228 passed / 74 skipped (10302)`                                                   |
| `npm run typecheck`                                     | exit 0, no diagnostics emitted                                                                                                              |
| `npm run lint` (whole tree, ESLint cache deleted first) | exit 0, no findings printed                                                                                                                 |
| ESLint over the changed files, uncached, JSON formatter | `files linted: 7` and `errors: 0 warnings: 0`                                                                                               |

`typecheck` and `lint` print nothing on success, so those two rows are an exit code and an absence of
diagnostics rather than a summary line. The JSON row is there because it is the one affirmative
number available for lint; there is no equivalent for `tsc`, and this report does not pretend
otherwise.

**`tests/ui-caring-contacts-workspace.spec.ts` is not touched by this change, and I do not believe
the browser gate needs to run for it.** No page route, component, chrome or style is added or
changed; the new route is an API route, which that spec never visits and which the reachability gate
does not govern. The screen this read feeds is Task 13's, and that is where the browser gate belongs.

**The full-suite run predates the policy that now forbids it.** The controller's
`67df71618` reserved `npm run test` for the merge point, precisely because the exclusive lease was
being starved; that commit landed on this branch after my run had already completed. The row is left
in because the evidence is real, not because the run should be repeated. `test:cc-guards` is the row
that matters from here.

### The shared machine

Every heavy run queued behind other worktrees' leases; retries were on the refusal, never on a break.
Two operational notes, because both cost real time and one was my own defect:

1. My first gate wrapper retried on any output mentioning the lock **directory path** — which a run
   that merely WAITED for the lease also prints. It therefore discarded good runs and retried for
   ever. The corrected wrapper keys on the coordinator's own `acquireHeavyRunLock` /
   `ADMISSION_BUSY` markers. This is the "checks that cannot fail" shape pointed the other way: a
   wrapper that cannot succeed.
2. Killing that wrapper left its `vitest` child alive and stalled on a dead pipe, holding the
   exclusive lease at zero CPU. It was provably mine — the lease record named this worktree — and
   terminating it released the lease cleanly. Wrappers here should write to a **file**, never to a
   pipe a supervisor may close.

## Concerns

1. **The fourth group is a shape the approved design does not have.** `outsideApprovedWindows` exists
   because `moveContactWithinDay` makes it reachable. The owner has not seen it and it has no
   wording.
2. **"Named exception" is my definition.** Five contact states, derived by exhaustive switch so
   nothing can default in, but the choice of those five — in particular including `missed` — is a
   judgement about what a coordinator must act on, not a rule the domain already held.
3. **Held plans are excluded from that panel by the same kind of judgement.** A draft plan whose
   first contact day has passed may well be exactly what a coordinator needs to see.
4. **The range maximum is a bound I chose.** A month, so a month view is possible; the reason it
   exists at all is that the range arrives from a query string.
5. **`package.json` is a shared file.** Both suites were added to `test:cc-guards`, which is the line
   the other implementers are most likely to have edited too. A one-line conflict for the controller.
6. **In production a malformed query answers 400 while a well-formed one answers 404**, because the
   query is parsed before `readHandler`'s demo gate. That is the access-trail route's existing shape,
   followed deliberately rather than invented; it is a small existence signal and it is stated here
   so the choice is visible rather than inherited silently.
7. **M17 is unfalsifiable.** Recorded above rather than described as covered.

---

# Fix round — after review

Task 12 passed review. Two Important findings and two corrections to this report remained. A fresh
implementer took them; the original session was gone, so the work below was done from this report,
the brief and the tree rather than from any memory of building it.

## Commits

| SHA         | What                                                                                        |
| ----------- | ------------------------------------------------------------------------------------------- |
| `7d2ec90df` | The two missing assertions: the message kind and the plan state each entry carries.         |
| `f2a111737` | The false count in `notSendingReasonFor`'s defensive branch, restated as its invariant.     |
| `a4550fc80` | Formatting repair — two Task 12 files had been committed unformatted (an unlisted finding). |

## Important 1 — two fields reached the wire unasserted

`messageType` and `planState` were mapped onto `ScheduleEntry` in `entryFor` and read by nothing.
Neither test file referenced `entry.messageType` or `entry.planState`, so both mappings were
type-compatible with a constant and no mutation in the ledger above targeted either — consistent
with there being no assertion to falsify. **This report did not disclose the gap**, which is the
worse half of it: a limitation nobody wrote down is not a limitation anybody weighed.

`messageType` is the field that matters most here. It is what tells a **closing message** apart from
a caring contact — a distinct kind, not one more contact — and the brief named that distinction
explicitly.

Two cases were added to `tests/caring-contacts-schedule-view.test.ts`, in a new block,
`buildScheduleRange — what an entry carries through from the planner and the plan`:

- **The message kind.** Each of a seeded plan's contacts is looked up on the AWST day it actually
  sends on — a plan runs for months and no single accepted range spans one — and the kinds reported
  are compared, contact by contact, against the kinds the planner stamped. Derived from the plan,
  never typed out. Two supporting lines: a labelled **fixture precondition** that the plan holds more
  than one kind, which is what stops the comparison being satisfiable by a constant, and a named
  assertion that the plan's last contact is the closing message and the read says so.
- **The plan state.** An active plan and a paused plan sit on the same day. Each entry's reported
  `planState` is compared against its own plan record's state, plan by plan so neither list's order
  is asserted, plus a labelled fixture precondition that the two states differ.

The plan-state case is the one worth understanding. `planHold` is derived from `record.plan.state`
**separately**, so it stayed correct under a hardcoded `planState` — the field went unnoticed
precisely because its more visible neighbour kept working. That is why the assertion compares the
raw state and not the hold.

### Mutation ledger, continued

Numbering continues from the round above. Both suites ran for each attempt via `test:cc-guards`.
Presence was checked in process on the bytes on disk, never through a shell. `git diff --quiet` was
asserted clean before each mutation and again after each restore, and was clean every time.

| #   | Mutation                                                    | Predicted                                                                                                                                                                           | Result | The line the suite printed                                                                                                                     |
| --- | ----------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| M19 | `entryFor` maps `messageType: "first"` instead of the stamp | The message-kind case's first assertion fails; the plan holds three kinds, so a constant cannot satisfy it. One test — no other case reads the field.                               | RED    | `expected [ [ …(2) ], [ …(2) ], [ …(2) ], …(7) ] to deeply equal [ [ …(2) ], [ …(2) ], [ …(2) ], …(7) ]` — `Tests 1 failed / 426 passed (427)` |
| M20 | `entryFor` maps `planState: "active"` instead of the state  | The plan-state case's first assertion fails on the paused plan. The draft and paused **hold** cases stay green because `planHold` is derived separately — which is the whole point. | RED    | `expected [ [ 'SYN-PLAN-017', 'active' ], …(1) ] to deeply equal [ [ 'SYN-PLAN-017', 'active' ], …(1) ]` — `Tests 1 failed / 426 passed (427)` |

Both predictions were exact — the failing case, the failing assertion within it, and the count. No
unexpected number appeared in either message. M20's two sides printing an identical visible prefix is
the expected shape rather than a suspicious one: the first pair is the genuinely active plan, which
is right even under the mutation, and the elided second pair is the paused one, which is not.

## Important 2 — a false count inside the comment that explains a defensive branch

`schedule-view.ts`'s `notSendingReasonFor` ended with:

> `contactSendability` classifies exactly these four as `willNotBeSent`; if it ever classifies a
> fifth, saying nothing is better than naming the wrong reason.

It classifies **three** (`missed`, `suppressed`, `cancelled`), and the switch directly above the
comment names three. `absorbedByFirstContact` is not a `ContactState`, so no reading makes the
sentence true. This is the named failure mode of the standing discipline's writing rule — a count
restated in prose about something **elsewhere** — landing inside the very comment written to explain
a defensive branch, which is the same collision that rule already records having happened once.

**Not fixed by changing "four" to "three".** The sentence now states the invariant the branch rests
on, so adding a state cannot make it false:

> Unreachable while every state `contactSendability` answers `willNotBeSent` for is named above. If
> one is ever added there and not here, saying nothing is better than naming the wrong reason, so
> this returns null rather than guessing.

The same error was repeated in this report's M17 note ("one of the four the switch beneath it
names") and is corrected there in place, to "one of the states the switch beneath it names".

Nothing else in the file or the report was changed. In particular the `ScheduleNotSendingReason`
doc's "kept apart from the other three" is a count sitting **directly on top of the union it
counts**, which the discipline permits and which is correct.

## The two report corrections

- **The commits table listed three SHAs where the range held five.** `b11e67518` and `6b75dcf3b`
  are now in it.
- **The 17:00 case was not the timezone guard, and the section let a reader think it was.**
  Corrected in place above. 17:00 AWST is 09:00Z on the same date and `planned.calendarDay` agrees,
  so every candidate derivation returns `2026-08-31`: the case fails under no plausible off-by-one.
  Its real value is the month-end enumeration plus the "next day holds nothing" assertion, which is
  worth keeping and is now described as that. **The 00:00 case is the timezone guard** — hand-built
  so all three answers are available, and the only case here that fails under both a UTC-date bug and
  a trust-the-recorded-day bug. This was not a defect in the original work: the brief prescribed the
  17:00 case. It was a claim that should not have been inherited.

## An unlisted finding: two Task 12 files were committed unformatted

Not in the review's list; found while committing. `prettier --check` fails on
`src/lib/caring-contacts/schedule-view.ts` and `tests/caring-contacts-schedule-route.test.ts`, while
`model.ts`, `schedule.ts`, `access-audit.ts`, the API route and the view test all pass — so it is
those two new files specifically, not a misconfigured invocation.

The original gate table is not wrong about this; it never claimed format. That is the trap:
**formatting is in none of `test`, `typecheck` or `lint`**, so every gate the task ran could pass on
an unformatted file, and the changed-file CI check and the pre-push guard are where it would have
surfaced. Repaired in `a4550fc80`: three hunks, all line breaks and trailing commas, no token
reordered and no line of behaviour touched.

I fixed rather than only reported it, because it would otherwise block this branch's own handoff and
because it is whitespace. Flagging it so the controller knows a formatting commit sits inside a task
whose diff is otherwise under mutation review — and because if two of this task's new files were
never formatted, the sibling tasks in the other worktrees are worth the same one-line check.

## Gates

| Gate                                                             | Evidence                                                                         |
| ---------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| `npm run test:cc-guards` (`GATE_RECEIPTS=refresh`), new tests in | `Test Files  20 passed (20)` and `Tests  427 passed (427)`                       |
| `npm run test:cc-guards`, M19 applied                            | `Test Files  1 failed / 19 passed (20)` and `Tests  1 failed / 426 passed (427)` |
| `npm run test:cc-guards`, M20 applied                            | `Test Files  1 failed / 19 passed (20)` and `Tests  1 failed / 426 passed (427)` |
| `prettier --check`, the five Task 12 source and test files       | `All matched files use Prettier code style!` after `a4550fc80`                   |
| `npm run typecheck` (`GATE_RECEIPTS=refresh`)                    | exit 0, no diagnostics emitted                                                   |
| `npx eslint --no-cache`, the three files this round changed      | `files linted: 3`, `errors: 0 warnings: 0` (JSON formatter)                      |

The ESLint cache directory was deleted before that run and `--no-cache` passed, because the per-file
cache is exactly what hides a failure caused by a different file's change. `typecheck` prints nothing
on success, so its row is an exit code and an absence of diagnostics rather than a summary line; the
lint row carries a JSON count because that is the one affirmative number available, and there is no
equivalent for `tsc`.

The full `npm run test` was **not** run: the controller owns it at the merge point, and three
concurrent worktrees starving the exclusive lease is what that policy exists to stop.
`tests/ui-caring-contacts-workspace.spec.ts` is still untouched by this branch and the browser gate
is still not needed for it — this round adds two unit assertions, one comment and three whitespace
hunks.

## Concerns from the fix round

1. **The formatting finding is the one that generalises.** Two of five new files in a reviewed,
   mutation-tested task had never been through Prettier, and no gate the task ran could have said so.
   Worth one `prettier --check` across the other Group 2 worktrees before the controller merges.
2. **`entryFor`'s remaining fields are asserted, but I checked that by reading the tests, not by
   mutating.** `patientId`, `cadenceLabel`, `sendAt` and `state` are each read by at least one
   existing case, and `planId` and `contactId` by many. I did not mutate them, so that is a reading
   of the suite rather than a proof about it, and I would rather say so than imply the field-mapping
   line is now fully covered.
3. **The two fixture preconditions I added are labelled, not mutated.** They assert properties of the
   seeded plan rather than of the code, and cannot go red under any mutation of `schedule-view.ts`.
   They are in the suite so that a later fixture change cannot quietly make the assertion above each
   of them vacuous. Recorded as what they are.
4. **M17's note is now count-free, but M17 itself is still unfalsifiable.** The original concern
   stands unchanged; the correction was to the sentence, not to the coverage.
