# Task 13 — the Schedule screen

Group 2's screen: what this team's caring-contact plans put on one AWST day, in the three approved
sending windows, with the contacts that sit at no approved send time, the named exceptions, and the
plans that are holding their own contacts. It renders Task 12's read and derives no schedule rule of
its own.

## Commits

| SHA         | What                                                                                           |
| ----------- | ---------------------------------------------------------------------------------------------- |
| `a0959de6d` | The screen, the page, the lit nav entry, the shared contact vocabulary, the day-offset helper. |
| `bb2434abe` | The two suites, the clock cases, the browser proof block, the `test:cc-guards` entries.        |
| `d6ddd0fc2` | The route registered in the four places the build checks, and two test corrections.            |
| `d8f6a62a5` | Two assertions that could not have failed, replaced with two that can.                         |
| `df307c3d7` | The row's own send time asserted, found by mutation M6.                                        |
| `eae72a818` | Formatting for the three files Prettier had not seen.                                          |

This report's own commit is not in that table, since it cannot name its own hash before it exists;
the branch log is the authority. Every SHA above was checked to still resolve
(`git cat-file -e <sha>^{commit}`) after the last commit of this task.

## What was built

| File                                                             | What it is                                                                          |
| ---------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| `src/components/caring-contacts/workspace/schedule-screen.tsx`   | NEW. `ScheduleScreen`, `parseScheduleDay`, `scheduleDayLabel`, the strip constants. |
| `src/app/caring-contacts/schedule/page.tsx`                      | NEW. The Server Component page: the audited reads, the day, the strip's range.      |
| `src/components/caring-contacts/workspace/contact-vocabulary.ts` | NEW. `MESSAGE_TYPE_LABELS` and `CONTACT_STATE_LABELS`, moved out of one screen.     |
| `src/components/caring-contacts/workspace/patient-overview.tsx`  | EDIT. Imports those two maps instead of declaring them. No rendered text changes.   |
| `src/components/caring-contacts/workspace/shell.tsx`             | EDIT. `href: CARING_CONTACTS_ROUTES.schedule` on the Schedule destination.          |
| `src/lib/caring-contacts-routes.ts`                              | EDIT. `CARING_CONTACTS_SCHEDULE_DAY_QUERY_PARAM` and `scheduleDayRoute`.            |
| `src/lib/caring-contacts/clock.ts`                               | EDIT. `awstCalendarDayOffset` — see "The one domain addition" below.                |
| `tests/caring-contacts-schedule-screen.dom.test.tsx`             | NEW. The screen, against real `buildScheduleRange` output.                          |
| `tests/caring-contacts-schedule-page.dom.test.tsx`               | NEW. The page's reads, its access identity and its fail-closed branches.            |
| `tests/caring-contacts-clock.test.ts`                            | EDIT. Four cases for the day-offset helper.                                         |
| `tests/caring-contacts-workspace-shell.dom.test.tsx`             | EDIT. Schedule is a link now, on the rail and on the phone bar.                     |
| `tests/ui-caring-contacts-workspace.spec.ts`                     | EDIT. `SCHEDULE_SCREEN` plus its seven-test browser block. **I did not run this.**  |
| `tests/design-system-adoption.test.ts`                           | EDIT. The production-route census moves by one.                                     |
| `docs/design-system/adoption-contract.json` + manifest           | EDIT. The route declared, and the manifest regenerated by its own script.           |
| `docs/site-map.md`                                               | Regenerated by `npm run sitemap:update`.                                            |
| `docs/codebase-index.md`                                         | EDIT. The route row, and what the screen must not let collapse.                     |
| `package.json`                                                   | EDIT. Three suites added to `test:cc-guards`.                                       |

## The route checklist, all five steps

The page; the `href` in the same change (Ruling 89); `npm run sitemap:update`; a `docs/codebase-index.md`
entry; and reachability. Reachability needed no new assertion: `tests/route-reachability.test.ts`
parses `href: CARING_CONTACTS_ROUTES.*` out of the shell's frozen destination table, so lighting the
entry is what makes the route reachable to that gate — and `tests/caring-contacts-workspace-shell.dom.test.tsx`
independently pins that the entry renders as a real link, which is what stops the parser vouching for
a dead row.

Two further registrations the checklist does not name and the build does check, both found by running
the gate rather than by reading it: the design-system **adoption contract** lists the workspace
surface's routes explicitly, and `tests/design-system-adoption.test.ts` carries a **census** of every
production page route. A new route fails both until it is declared.

## What the screen says, and why it is derived rather than written

**Everything comes off the `ScheduleRangeView`.** The three windows carry the wording and the send
time `SENDING_PREFERENCE_OPTIONS` publishes; the grouping, the holds, the exceptions, the counts and
the disposition are Task 12's. This file chooses words and layout. The one thing it formats itself is
the AWST wall-clock time of a contact that sends at **no** approved time — the three windows publish
their own `sendTime` and the screen renders those.

**One read feeds the whole screen.** `buildScheduleRange` is asked for the seven-day strip, and the
day open beneath it is one of the days that call returned. There is deliberately no second call for
"the selected day": a strip that disagreed with the day under it is exactly the quiet inconsistency a
schedule cannot afford, and Task 12 built the range entry point so that it could not happen.

### Ruling [126] — named by the time, never as an act

The group is headed **"Not at an approved send time"** and each row states the time it sends at. It
is not called "moved", it does not invent a fourth window, and it does not hide the contact among the
three. The corrected reasoning is written into the file so it cannot be re-argued from the group's
existence alone: a deliberate move is the only way to REACH an off-window time, but the converse is
false — a morning plan's contact moved to 2:00 pm lands silently inside the afternoon window and
nothing in `PlanRecord` records that a move happened — so "moved" would be true of everything in the
group and would miss every moved contact that landed on an approved hour.

**I could not find an honest way to distinguish a moved contact from an unmoved one, and did not
try to derive one from the window.** `PlanRecord` carries `planned.sendAt` and `planned.calendarDay`
and no history; `rescheduleContact` writes the new instant over the old. The audit trail records the
write, but that is a different read on a different surface and is not what a schedule row has. So the
screen states what it can attest and nothing more. Mutation **M8** pins this: it leaves the heading
and the time intact and only puts the word "moved" into the group's description, and the suite goes
red on that alone.

### The four states that must not collapse

1. **Every contact stopped, against none planned.** A stopped day renders its contacts, its numbers
   and, per contact, why it will not be sent; the statement reads "No contact on this day will be
   sent at all." A day with nothing on it renders an empty state and no numbers at all. M2 and M9
   both falsify this pair.
2. **A held plan is not an exception, and is not a quiet day.** A draft plan whose first contact day
   has arrived is reported by the read as `held`, stays out of the named-exceptions panel — Task 12's
   judgement, rendered rather than re-decided — and is stated **above the day's windows** as its own
   automated state: "Plan not started", with why ("a discharged patient is receiving nothing while
   the plan record looks complete") and what would change it ("starting the plan, from that patient's
   record"). Each held row also names the hold and points at that statement.

   The notice is derived from the **entries**, not from `counts.held`, because the count says how
   many are held and not by what, and a coordinator's next move differs completely between a plan
   nobody started and a plan somebody paused. Only contacts that would otherwise go out are
   considered: `planHold` is reported on a sent contact too, and telling a reader that an ended plan
   is holding a message it already sent would name the wrong reason for the same silence. **That
   guard was unproven until I wrote an assertion for it** — see "What running the mutations changed".

3. **`nothingDue` is three days, and `counts` says which.** With nothing due, `alreadySent`, `held`
   and `willNotBeSent` partition the day exactly, so the statement names whichever parts are present
   and says "every" or "some" from the same numbers. Three seeded days — one delivered, one draft,
   one withdrawn — report the same disposition and produce three different statements. M1 and M2
   falsify it.
4. **An empty day you chose, against an empty stretch of days.** A day with nothing on it inside a
   strip that holds contacts elsewhere gets the `filtered` shape: it names the day, says other days
   above do hold contacts, and offers a link to one of them — a remedy that is a real control
   directly above (Ruling 93). A strip with nothing anywhere in it says something else and offers no
   such link, because sending a clinician back to the strip there would send them hunting through
   days that are all equally empty. M9 falsifies it.

**Where the "filter" in §4.4 lands on this screen, stated because it is a judgement.** The approved
design gives this screen no filter, and I did not add one. The narrowing a coordinator actually
performs here is choosing a day, so that is what the `filtered` kind is used for. If the owner wants
a real filter later — "only what is still to go out" is the obvious one, and `entry.isDue` already
carries it — the empty-state branch it needs is already the right shape.

### Counts are a data readout, never prose

Ruling 94. The day's numbers are a `<dl>` sitting directly above the lists they count; the prose
beside it carries no number at all. The first four rows partition the day; **named exceptions cut
across that partition** — a missed contact will not be sent, and a contact whose transport receipt
never arrived has already been sent, and both are exceptions — so that row is separated from the four
and labelled as the panel it matches rather than as another part of the split.

Nothing on the screen or in this report states how many contacts a plan has.

## The one domain addition, and why it is not on the screen

The day strip has to name the days either side of the one being looked at, which is arithmetic over
an **AWST calendar day**. `schedule-view.ts` already does this privately (`calendarDaysFrom`), and
the brief froze that module. The choice was therefore between writing the arithmetic on the screen —
a second place in the codebase that knows how to step an AWST day, on the surface that tells a
coordinator when a discharged patient hears from the service — or putting it where AWST calendar days
already live.

I put it in `clock.ts` as `awstCalendarDayOffset`, and it steps from **midday** rather than midnight
for the reason `schedule-view.ts` records: midday is the furthest point from either day boundary, so
adding whole days cannot cross one by accident.

**This leaves a duplication I was not allowed to resolve.** `schedule-view.ts`'s `calendarDaysFrom`
is the same arithmetic, and one of the two should go. It is stated in both files and reported here
rather than fixed. **If the controller would rather the helper had not been added at all, reversing
it means putting `middayOf`-style arithmetic on the page, which I think is the worse of the two.**

## What this screen deliberately does NOT read

**No patient name.** The caseload reads `listPatientNames` (Ruling 91) because recognising your own
patients is the whole job of a caseload. A schedule answers a different question — what does the
service send today, and what needs a person — and it is a working view a coordinator returns to
through the day. Folding an identity-bearing read into it would write a `patientName` row into the
access trail every time somebody glanced at a day, which is the opposite of what that row is for: it
exists so "who read patients' names, and when" has a short, meaningful answer. Every row is headed by
the synthetic identifier and links to the patient record, which holds the name and is audited for
reading it.

**This is a difference between the approved design and what is built**, recorded rather than resolved:
the mockup's schedule shows patient names. It is a one-prop change if the owner wants them — the page
would add the read and the capability exactly as the caseload does — and it would then also need the
"names are not shown in this role" notice, which `patients-directory.tsx` currently owns privately and
whose own comment says it should move to `workspace/` on its second use. **I did not read names, so I
did not move it.**

## The access trail

`{ search, contactSchedule, "<from>:<to>" }` — byte for byte the identity
`src/app/api/caring-contacts/schedule/route.ts` records for the same question, even though the store
read underneath is `listPlans`. Recording it as `plan` would be defensible by provenance and wrong by
meaning: the trail's query surface has no `objectId` filter, so a caseload read and a schedule read
sharing a member would become one undifferentiated stream and "which days did this clinician look at"
would stop being answerable. Ruling 46, Ruling [125]. M16 falsifies it.

The service-state read is the usual `{ administrative, serviceState, "service" }`, required on every
screen (Ruling 56) and passed to the shell as a state that was actually read.

## Verification

### Gates

| Gate                                                           | Evidence                                                   |
| -------------------------------------------------------------- | ---------------------------------------------------------- |
| `npm run test:cc-guards` (`GATE_RECEIPTS=refresh`), final tree | `Test Files  23 passed (23)` and `Tests  458 passed (458)` |
| `npm run typecheck` (`GATE_RECEIPTS=refresh`), final tree      | exit 0, zero `error TS` lines emitted                      |
| `npx eslint --no-cache`, the 13 changed TypeScript files       | `files linted: 13`, `errorCount: 0 warningCount: 0` (JSON) |
| `prettier --check`, every file this task changed               | `All matched files use Prettier code style!`               |

The ESLint cache directory was deleted before that run and `--no-cache` passed, because the per-file
cache is what hides a failure caused by a different file's change. `typecheck` prints nothing on
success, so its row is an exit code and an absence of diagnostics rather than a summary line; the lint
row carries a JSON count because that is the one affirmative number available, and there is no
equivalent for `tsc`. Both were run **after** the last edit to the tree, not before it.

`prettier --check` was run because formatting is in none of `test`, `typecheck` or `lint` — Task 12's
round-2 finding — and it caught three files: `docs/codebase-index.md`,
`src/components/caring-contacts/workspace/schedule-screen.tsx` and
`tests/caring-contacts-schedule-screen.dom.test.tsx`. Repaired in `eae72a818`, whitespace only.

The full `npm run test` was **not** run: the controller owns it at the merge point.

### The browser gate — what I think it needs, which I did not run

**`tests/ui-caring-contacts-workspace.spec.ts` is touched, and it had to be.**
`tests/caring-contacts-workspace-screens.test.ts` fails offline on a production workspace route that
`WORKSPACE_SCREENS` does not list, so adding the route without adding the entry goes red — and adding
the entry without writing a block would make the omission visible while proving nothing. So the entry
and a block for it are both there. Seven tests, modelled on the "new plan" block:

- serves an empty schedule as a page rather than a missing resource, and the empty state says which
  of the two empty days it is;
- a full day strip of `SCHEDULE_STRIP_DAYS` links with exactly one `aria-current="page"`, and
  following one changes the URL and the day;
- reachable from the workspace navigation, not only by typing the URL (Ruling 89, end to end);
- 320px: no horizontal overflow, the right width state, and a day in the strip at or above the 48px
  production tap floor — seven days across 320px is the tightest grid in this workspace;
- dark: shell chrome AND this screen's own empty-state surface, border and ink;
- forced colours: the statement in words, its border surviving, and `aria-current` still carrying
  which day is open once the accent border is dropped;
- print.

**I ran none of it.** The isolated Playwright server seeds no plans, so every day in the strip is
empty and the block proves the screen's shell, its statement, its strip and its layout — the populated
day, its windows, its holds and its exceptions are proved in the DOM suite, which can seed. If the
gate is red I would look first at the day-strip click assertion, which is the only navigation in the
block, and at the 320px tap-floor measurement on a seven-column grid.

### The mutation ledger

Both suites ran through `npm run test:cc-guards` (`GATE_RECEIPTS=refresh`) for every attempt.
Presence was checked in process on the bytes on disk, never through a shell. `git status --porcelain`
was asserted empty before each mutation and again after each restore, by the driver, which throws
rather than continuing; it was empty every time.

The driver and its logs live under a `cc-schedule/` directory in the scratchpad and every log's first
lines record the worktree, the suite command and the mutation id — because the scratchpad is shared,
and one implementer's driver was overwritten by another task's at the same path on the day this was
written. A result that is not this task's now says so itself.

| #   | Mutation                                                    | Predicted                                                                  | Result | The line the suite printed                                                                                                              |
| --- | ----------------------------------------------------------- | -------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------- |
| M1  | The held clause returns the already-sent words              | pair 3's held statement stops naming the hold; one test                    | RED    | `expected 'Nothing on this day is waiting to go …' to contain 'Every contact on this day belongs to …'` — `1 failed / 457 passed (458)` |
| M2  | `quietDayClauses` returns `[]`                              | the three quiet days collapse into one sentence; pairs 1 and 3             | RED    | `expected 'Nothing on this day is waiting to go …' to contain 'No contact on this day will be sent a…'` — `2 failed / 456 passed`       |
| M3  | `distinctHoldsOf` drops the still-to-send guard             | a withdrawn plan grows a "Plan ended" notice for messages it never sent    | RED    | `expected <div role="group" …(2)>…(3)</div> to be null` — `1 failed / 457 passed (458)`                                                 |
| M4  | `PLAN_HOLD_LABELS.planNotStarted` becomes "Plan paused"     | pair 2 cannot find the notice at all                                       | RED    | `Unable to find an accessible element with the role "group" and name "Plan not started"` — `1 failed / 457 passed (458)`                |
| M5  | The not-started remedy becomes "Nothing here."              | pair 2's remedy assertion, and only it                                     | RED    | `expected 'Plan not startedWhy: A plan on this d…' to contain 'Starting the plan'` — `1 failed / 457 passed (458)`                      |
| M6  | `awstClockLabel` drops the 12-hour conversion               | the midnight row reads 0:00 and the 5:00 pm row reads 17:00; two tests     | RED    | `expected 'Synthetic patient identifierSYN-PATIE…' to contain 'Sends at: 5:00 pm AWST'` — `2 failed / 456 passed (458)`                 |
| M7  | The outside-window heading becomes "Moved contacts"         | both cases that look the group up by name                                  | RED    | `Unable to find an accessible element with the role "region" and name "Not at an approved send time"` — `2 failed / 456 passed`         |
| M8  | Only the group's DESCRIPTION says "moved"                   | the heading and the time still pass; the "moved" check alone fires         | RED    | `expected 'Contacts due, day by dayWhat this tea…' not to match /\bmoved\b/i` — `1 failed / 457 passed (458)`                           |
| M9  | `EmptyDay` always takes the no-data branch                  | the chosen-day empty state disappears; three cases look for it             | RED    | `Unable to find an accessible element with the role "group" and name "Nothing is scheduled on this day"` — `3 failed / 455 passed`      |
| M10 | The strip renders `counts.due` instead of `counts.total`    | a day of exceptions shows a zero                                           | RED    | `Unable to find an element with the text: 1` — `1 failed / 457 passed (458)`                                                            |
| M11 | The strip's accessible name drops the total                 | the same collapse, in the name a screen reader hears                       | RED    | `expected 'Monday 31 August 2026 (today). 0 cont…' to be 'Monday 31 August 2026 (today). 1 cont…'` — `1 failed / 457 passed (458)`      |
| M12 | `parseScheduleDay` drops `isAwstCalendarDay`                | `2026-02-30` is accepted as a day; the parser case and the page's fallback | RED    | `expected '2026-02-30' to be '2026-08-31'` — `2 failed / 456 passed (458)`                                                              |
| M13 | `awstCalendarDayOffset` steps from midnight, not midday     | GREEN expected — over-sensitivity control, see below                       | GREEN  | `Test Files  23 passed (23)` and `Tests  458 passed (458)`                                                                              |
| M14 | `awstCalendarDayOffset` ignores the offset                  | every day of the strip becomes the same day; clock, screen and page cases  | RED    | `expected '2026-08-31' to be '2026-09-01'` — `11 failed / 447 passed (458)`                                                             |
| M15 | The page's strip start uses `+` instead of `-`              | the day being looked at falls outside the range that was read              | RED    | `expected [ 'service', '2026-09-13:2026-09-19' ] to deeply equal [ 'service', '2026-09-07:2026-09-13' ]` — `7 failed / 451 passed`      |
| M16 | The page records `plan` instead of `contactSchedule`        | the trail cannot tell a caseload read from a schedule read                 | RED    | `expected [] to deeply equal [ 'allowed' ]` — `2 failed / 456 passed (458)`                                                             |
| M17 | The page ADDS a `patientName` read                          | the absence assertion fires, and the trail grows a row                     | RED    | `expected true to be false` — `3 failed / 455 passed (458)`                                                                             |
| M18 | `ContactRow` never renders its automated state              | pair 1 loses "Cancelled" and the exceptions case loses "Missed"            | RED    | `Unable to find an accessible element with the role "group" and name "Cancelled"` — `2 failed / 456 passed (458)`                       |
| M19 | The empty exceptions panel says only "No named exceptions." | pair 2's panel assertion, and only it                                      | RED    | `expected 'Named exceptionsContacts a person has…' to contain 'Nothing on this day needs a decision.'` — `1 failed / 457 passed`        |
| M20 | The not-permitted branch is never taken                     | the role case cannot find its statement                                    | RED    | `Unable to find an accessible element with the role "group" and name "The schedule is not visible in this role"` — `1 failed / 457`     |

**Where a prediction was not what fired.**

- **M6 was predicted to redden two cases and reddened one, and that was a real gap rather than a
  mis-prediction.** The month-end case asserted `"5:00 pm AWST"` against the whole Early-evening
  SECTION — and the section header already carries the window's own published send time, so the
  assertion was satisfied by the header while the row beneath it read "17:00 pm AWST". The row's own
  send time was unasserted. Fixed in `df307c3d7` by scoping the assertion to the row; M6 re-run
  against the corrected tree is the row in the table.
- **M14 reddened eleven tests where I had predicted "several".** I did not commit to a number, which
  is the honest form for a mutation that removes a primitive three layers use; the eleven are the
  three clock cases, the strip order, and seven page cases.
- **M17's presence check printed `false` while the mutation was demonstrably applied.** The driver's
  check is `after.includes(replace) && !after.includes(find)`, and M17's replacement APPENDS to the
  line it matches, so the second half is false by construction. The red proves presence by itself —
  a mutation that never reached disk cannot make its own target assertion fail — and a presence check
  is load-bearing only for a GREEN. Stated rather than quietly ignored, because the check is wrong for
  the whole class of "add a line" mutations and a later reader would trust it.
- **M4 and M5 were refused on their first attempt**, by the run coordinator: `Database focused-test
capacity is full (worktree D:\Worktrees\Database\cc-plan-detail)`. That is neither a pass nor a
  failure. The driver now retries on the coordinator's OWN markers (`acquireHeavyRunLock`, "capacity
  is full", `ADMISSION_BUSY`) and never on the lock directory path, which a run that merely WAITED for
  the lease also prints — Task 12's wrapper defect, avoided rather than rediscovered. Both rows above
  are from runs that produced a summary line.

**The one green, honestly.** **M13** steps the day offset from midnight instead of midday and is
recorded as an over-sensitivity control, not as coverage. Midnight AWST is exactly representable and
adding whole days from it lands on the right AWST date every time, so the mutation changes no asserted
value. The midday choice is a margin against arithmetic error rather than a fix for a reachable bug,
and this report claims nothing more for it.

### Two assertions that could not have failed, found by asking

Both were written by me and both were caught before the ledger, by asking of each assertion whether
it could go red rather than whether it passed:

- The strip's count check read the whole link's text, and `"Mon 31 Aug"` already contains a `"1"` — so
  `toContain("1")` would have passed on a strip showing due-only zeros, which is the exact collapse it
  was written to catch. It now matches an element whose whole normalised text is the number, which is
  what M10 falsifies.
- A stopped day asserted what it showed and not what it must not show. A withdrawn plan carries
  `planEnded` on every contact, so a screen that dropped the still-to-send guard in `distinctHoldsOf`
  would name the plan as the reason for a silence the contacts had already explained, and nothing
  would have gone red. The added assertion is what M3 falsifies.

### Boundaries of the day

- **17:00 AWST on the last day of a month.** The contact renders on the day headed "Monday 31 August
  2026", the row states "Sends at: 5:00 pm AWST", and the next day in the strip is present and holds
  nothing — a day holding nothing and a day that was never asked about being different results.
- **00:00 AWST.** Unreachable through the planner (the approved window starts at 09:00), so the record
  is assembled by hand and labelled defensive, with `planned.calendarDay` deliberately set to the UTC
  date. Every wrong answer is therefore available — the UTC date, the recorded day, "0:00" and
  "12:00 pm" — and the screen must choose none of them: the contact appears on 2026-09-01 at
  "12:00 am AWST", and 2026-08-31 renders as a day with nothing on it.
- **The day-offset helper** is proved across a month end, a leap-year February, a year end, an offset
  of zero, and the exact seven-day span the page asks for.

## Seams left for Task 14

Named, with where each attaches:

1. **`adjust-date-time`.** A contact row (`ContactRow`) has no control on it. The overlay is opened by
   `?overlay=<id>`, and the row already carries `planId`, `contactId` and the contact's own send time,
   which is what a move needs. A control added there must satisfy the button-wiring rule.
2. **`outside-window-warning`.** Belongs to the move, not to this screen: the group headed "Not at an
   approved send time" is contacts that ARE inside the approved 9:00 am–6:00 pm window, so nothing
   here warns. The warning fires when a coordinator asks for a time outside it.
3. **`resolve-failed-delivery`.** The named-exceptions panel renders its rows as content, with no
   control. The approved mockup makes each exception row a button that opens this overlay; that button
   is Task 14's, and the rows are already scoped and identified for it.
4. **Where a day-level control would go**, if the overlays need one: the day's `<dl>` and the plan-hold
   notices sit between the strip and the windows and nothing occupies that band.

Nothing on this screen renders an `UnavailableDestination`, so Task 14 has nothing to undo.

## Concerns

1. **The day-offset helper is a sealed-domain addition my brief did not ask for**, and it leaves
   `schedule-view.ts`'s private `calendarDaysFrom` as a duplicate I was forbidden to collapse. The
   alternative was writing AWST day arithmetic on the screen. I think this is the better of the two,
   but it is the controller's call and it is one file to revert.
2. **No patient names, against an approved design that shows them.** The privacy argument is in "What
   this screen deliberately does NOT read" and I believe it, but it is a product decision I made and
   the owner may want it the other way. Reversing it also pulls in the "names are not shown in this
   role" notice, which currently lives inside `patients-directory.tsx`.
3. **The named-exceptions definition is still Task 12's judgement.** Rendering it surfaced no case its
   five do not cover — every state `needsOperationalReview` refuses is either routine or already
   explained by `notSendingReason` — so I have nothing to add to that concern, but I have not closed
   it either.
4. **`counts.needsReview` cuts across the day's partition and sits in the same readout.** It is
   separated and labelled as the panel it matches, but a reader who adds the six numbers will get more
   than the total. If that reads badly on screen it should move out of the `<dl>`.
5. **The seven-day strip is my choice of span**, centred three days back. The design shows a week with
   previous/next controls; a centred strip needs no week-boundary arithmetic and keeps the day being
   looked at always visible, but it is not what the mockup draws, and there is no "previous week"
   control — moving through the strip is how you travel.
6. **`package.json`'s `test:cc-guards` gained three entries** (the two new suites and the clock suite,
   which I changed). That is the line every other implementer is most likely to have touched.
   `shell.tsx` and `tests/caring-contacts-workspace-shell.dom.test.tsx` are the other shared files.
7. **I did not run the browser gate**, and Chromium evidence for this screen therefore does not exist
   yet. What I think it needs is in "The browser gate" above.
8. **`contact-vocabulary.ts` moves two label maps out of `patient-overview.tsx`.** No rendered text
   changed and that screen's own suite is green, but it is an edit to a Group 1 file made for a Group 2
   screen. The alternative was a second copy of the frozen transport vocabulary, which is the thing
   that must not exist twice.
