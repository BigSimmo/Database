{
  "version": 2,
  "id": "79b8bfd3-66bc-4111-80b7-5df25727c4c3",
  "createdOn": "2026-09-01",
  "action": "update",
  "payload": {
    "id": "#ZBAC9D",
    "pri": "P2",
    "detail": "documents.owner_id is ON DELETE SET NULL (supabase/schema.sql:63) while a null owner independently means 'public corpus', so deleting an auth user would silently republish that user's private documents. RE-VERIFIED against main a633eb6e3 on 2026-09-01, both in code and by an owner-approved read-only production count. CODE DEFECT (real, unchanged): the retrieval path is closed at no layer — public.retrieval_owner_matches resolves the public sentinel to 'row_owner_id is null' with no public_corpus condition, and 'public_corpus' appears ZERO times across rag/rag-candidate-sources.ts, corpus-grounding.ts, deep-memory.ts, document-enrichment.ts, owner-scope.ts, clinical-search.ts and query-privacy.ts. The helper written for exactly this hazard, src/lib/documents/is-public-document.ts (requires owner_id === null AND metadata.public_corpus === true), has NO production caller outside its own module and the barrel re-export; only tests/documents-is-public.test.ts uses it, and :22 pins the exact orphaned-row case. Same dead-contract shape as #A3AHCX. The one genuinely closed path is the REST document read via withOwnerReadScope (src/lib/public-api-access.ts:116). EXPOSURE (measured 2026-09-01, read-only GET against ref sjrfecxgysukkwxsowpy): total documents 2851; owner_id NOT NULL = 0; owner_id IS NULL = 2851; of those, carrying metadata.public_corpus = true = 2851. EXPOSED COUNT = 0. The entire production corpus is deliberately published and no document is privately owned, so there is currently nothing for the mechanism to leak and the trigger (delete an auth user who owns documents) cannot fire. PRIORITY: stays P2, NOT P1 — an earlier request in this same PR raised it to P1 before the count was run and was cancelled. TRIGGER CONDITION, which matters more than the priority: this must be closed BEFORE the first privately-owned document exists in production, because from that moment the hazard is live and silent, and the helper that would catch it is dead code. NEXT, in order: (a) wire is-public-document into the retrieval path, or add the public_corpus predicate to retrieval_owner_matches — a retrieval behaviour change on a protected RAG surface, so RAG flag plus a live eval-canary pair (expected to move zero rows today, since all 2851 documents carry the marker, which makes this an unusually safe canary); (b) in an approved production window, change documents.owner_id to ON DELETE RESTRICT or add an explicit visibility column and align the RPC predicate. The offline proof proposed in the original row (assert no table whose null owner means public declares ON DELETE SET NULL) cannot be added green today — it would fail on documents.owner_id — so it is the guard accompanying (b), not a step preceding it.",
    "source": "repo-wide audit at 3ed1932 (six domain reviewers); re-verified in code and by owner-approved read-only production count, database coordination chat 2026-09-01",
    "baseRowFingerprint": "13eb57d22f4630a32ff989e07c37a577b5fd8eb3f7388c641c31998143ecdfe0"
  }
}
