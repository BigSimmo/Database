# Direct Python dependencies for the worker OCR stack.
# Generate both runtime-specific locks after changing direct dependencies:
# - Python 3.11 production: `npm run generate:worker-python-lock`
# - Python 3.12 Cloud: `npm run generate:worker-python-cloud-lock`

PyMuPDF>=1.24.0
pillow>=10.0.0
pytesseract>=0.3.10
medspacy>=1.1.0
