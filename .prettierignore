node_modules/
.next/
out/
build/
coverage/
test-results/
test-results-codex-remediation/
playwright-report/
sample-documents/
dev-server*.log
package-lock.json
public/demo-documents/
.tmp-visual/
scratch/
.claude/worktrees/
.impeccable/
# Generated by `supabase gen types`; keep the generator's formatting so
# regeneration stays churn-free.
src/lib/supabase/database.types.ts
supabase/drift-manifest.json
# Trimmed Therapy Compass content export (generated data assets); keep minified.
public/therapy-compass-data/
# Hand-edited source catalogue for scripts/build-therapies-index.mjs. Compact
# single-line JSON (~2.5 MB), the same shape the generated full catalogue keeps
# — pretty-printing it turns a modality scrub into a ~17k-line reflow, which is
# the churn `#179` fixed on the generated side. It lives here rather than under
# `public/therapy-compass-data/` because the generator writes every file in that
# directory and must never read back what it wrote (`#180`).
src/data/therapies-source.json
# Generated by `scripts/build-ranking-snapshot.ts` from an eval-canary artifact.
# The generator writes JSON.stringify(…, 2), which always expands arrays, while
# Prettier collapses short ones — so the two can never agree and every
# regeneration would land an unrelated 200-line reflow. Same reasoning as the
# Supabase types above: keep the generator's formatting so the 30-day freshness
# regeneration (#304, tests/ranking-tuning.test.ts) stays churn-free. Structure is
# not lost — `validateRankingSnapshot` and tests/ranking-tuning.test.ts gate the
# snapshot's shape, version and provenance far more strictly than Prettier does.
scripts/fixtures/rag-ranking-candidate-snapshot.v1.json
docs/branch-review-ledger.md
# Append-mostly ledgers whose rows Prettier would column-pad. Padding is why a
# one-row edit rewrites every row and git sees the whole table as a single hunk
# (#133): it conflicted on nearly every `main` advance, and each conflict stops
# all CI on the PR until resolved (#116). Structure is not lost by ignoring them —
# `npm run check:outstanding-issues` and `npm run check:branch-review-ledger` gate
# row shape, ids, the next-id marker and the merge attribute far more strictly than
# Prettier's alignment ever did.
docs/outstanding-issues.md
# Generated by `scripts/generate-branch-review-index.mjs` from
# docs/branch-review-records/. Same reason as the two ledgers above: Prettier
# column-pads markdown tables, so one long cell would repad all 576 rows and turn
# a one-record append into a whole-file diff. The generator emits single-space
# padding so appending a record stays a one-line diff. Structure is not lost —
# tests/branch-review-index.test.ts gates the generator's escaping, ordering and
# per-record coverage far more strictly than Prettier's alignment would.
docs/branch-review-index.md
# Verbatim user-provided prompt inputs retained for cloud review.
docs/prompts/codex-cloud-review/
# Generated by `scripts/generate-outstanding-issues-snapshot.mjs` from
# docs/outstanding-issues.md; keep the generator's formatting so regeneration
# stays churn-free and check:outstanding-issues-snapshot compares like with like.
data/outstanding-issues-snapshot.json
# Generated by `scripts/generate-repo-awareness-snapshot.ts`; keep the generator's
# formatting so regeneration stays churn-free and check:repo-awareness-snapshot
# compares like with like.
data/repo-awareness-snapshot.json
# Vendored third-party skills, copied verbatim from ~/.claude/skills so a Claude Code web container
# gets the same user skills as the workstation (docs/claude-cloud.md). Reformatting ~400 upstream
# files would turn every future refresh of that snapshot into an unreadable diff, and their formatting
# is not this repository's to own. Everything in `.claude/cloud-profile/` outside `skills/` is ours
# and stays formatted.
.claude/cloud-profile/skills/
