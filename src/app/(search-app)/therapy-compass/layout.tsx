import type { ReactNode } from "react";

// Search chrome and TherapyCompassWorkspace are owned by the shared (search-app) layout.
export default function TherapyCompassLayout({ children }: { children: ReactNode }) {
  return children;
}
