import type { ReactNode } from "react";

// Documents search-shell props (composer visibility) are derived in SharedSearchAppShell.
export default function DocumentsLayout({ children }: { children: ReactNode }) {
  return children;
}
