# Temporary connection test

This file exists only to open a throwaway PR so the `.github/workflows/claude.yml`
Claude Code Action can be triggered via an `@claude` comment, to confirm the
`CLAUDE_CODE_OAUTH_TOKEN` repository secret is configured and working.

Safe to delete; this PR will be closed after verification.
