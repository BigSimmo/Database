---
name: db-remediation-coordination-state
description: "Live-drift remediation (#316) coordinator board — all schema phases complete; PGRST106 alignment fix is LIVE on production; staging is one version behind and blocked; D4 UNRESOLVED (assume merge=deploy); then Phase 5 and reconcile; D5 PITR pending"
metadata:
  node_type: memory
  type: project
  originSessionId: 00219de5-5a9b-4124-bbbb-f504c8fdc7a0
  modified: 2026-08-20T00:00:00.000Z
---

Coordination chat worktree `.claude/worktrees/database-coordination-chat-9c8cbd`; transcript `~/.claude/projects/D--Repos-Database--claude-worktrees-database-coordination-chat-9c8cbd/00219de5-….jsonl` (title "DATABASE MASTER"). Board doc `docs/database-remediation-coordination.md` ON MAIN and current. Forensics `docs/audit/live-drift-forensics-2026-08.md` (1952 lines).

State 2026-08-20 (verified against `origin/main` 1dad6b821 + live-drift run `32378402265`):

- **Phases 0, 1, 2, 3, 4, 6 (6.1/6.2/6.3) COMPLETE on production and staging.** Phase 4 = #2151 (`6801932d8`): 20 indexes rebuilt CONCURRENTLY, 2 orphans dropped, guards pushed by real `db push`. Phase 6.2 = #2185 (`c3ff667de`): all 15 no-statements history rows classified `validation` and guarded by six migrations `20260819110000`–`110500`; `supabase/drift-allowlist.json` now 20 entries (15 validation + 5 superseded); 210 migration rows on prod, pending 0.
- **DRIFT IS RESOLVED.** live-drift on `main` 2026-08-20: step `Compare live schema drift` = **success** — "No unexpected schema drift between live and supabase/schema.sql." missing_live 0, unexpected_live 0, function mismatches 0, migration_history 0 unexpected. First clean compare since 2026-07-26.
- **The job still concludes `failure` and #1963 stays open — but NOT for drift.** The next step, `Align migration history for Supabase Preview` (`check-migration-history-alignment.ts`, Phase 0 #1939), fails on PGRST106: `supabase_migrations` is not exposed to PostgREST. It had never run before because the compare step always failed first. Fix = expose the schema read-only, or wire `SUPABASE_ACCESS_TOKEN` and use `supabase migration list`, or add a versions RPC. Queued P2 inbox `cc60253d`.
- **PGRST106 fix is LIVE on production (2026-08-20).** PR #2198 merged as `a341832af`; the window's pre-flight found `20260820120000` ALREADY APPLIED, so `db push` was never run. Verified read-only: `stmt_count 3` (executed statements, not mark-applied — no guard owed), `prosecdef true`, `provolatile s`, `search_path=""`, `proacl postgres=X/postgres | service_role=X/postgres`, function returns `probe ok` / `version_count 211` vs 211 history rows vs 211 local files. The alignment step's `remoteOnly` set is now empty, so live-drift should go fully green on its next run — NOT yet observed; the dispatch was blocked.
- **D4 ANSWERED 2026-08-20: auto-deploy is effectively ON.** `list_branches(sjrfecxgysukkwxsowpy)` returns a live Git branch record — `{"name":"main","is_default":true,"git_branch":"main","project_ref":"sjrfecxgysukkwxsowpy","created_at":"2026-06-27","updated_at":"2026-07-04"}` — i.e. production is still bound to git `main`, and `updated_at` shows nothing changed it on 2026-08-19. That plus §3.7's 34-second apply after #2106's merge and today's unpushed arrival = **merging a migration PR deploys it to production**. Whatever was toggled on 2026-08-19 did not sever this binding. Deploy-then-merge ordering is therefore unnecessary AND unenforceable; plan on merge=deploy.
- **D4 WAS UNRESOLVED — do not trust the 2026-08-19 "OFF".** That test only showed migrations sitting pending WHILE a PR was open; it never tested deploy-on-merge, so it never contradicted §3.7's 34-second apply after #2106's squash-merge. Today's row arrived with no `db push`. `created_by`/`idempotency_key` are NULL on every row since 20260818090000, so provenance is unrecoverable from the DB. **Assume merging a migration PR deploys it to production until the dashboard is re-checked.**
- **Staging regressed by one version and is BLOCKED, not skipped.** 210 rows, latest `20260819110500`, `migration_history_versions()` absent; pending set exactly `['20260820120000']`, zero remote-only. Both `supabase db push` and MCP `execute_sql` were denied by the auto-mode classifier (live-Supabase confirmation rule from PR #2196 the same day). Needs an operator or a non-auto-mode session.
- **Harness note (new 2026-08-20):** in auto mode, read-only `supabase db query --linked --project-ref <ref>` works, but every live-Supabase WRITE and `gh workflow run` is classifier-denied. Plan windows accordingly.
- **PR #2201 open** (docs-only): forensics + board record of this window, plus the owed PR #2198 review-ledger record.
- **PGRST106 fix MERGED to main 2026-08-20 as `a341832af` (PR #2198) — but NOT deployed.** `20260820120000_migration_history_versions_rpc.sql` adds `public.migration_history_versions()` (stable, security definer, `search_path ''`, service_role-only) and `check-migration-history-alignment.ts` now prefers that RPC. All files verified byte-identical on main. **The PR merged before the production window, which is the opposite of the intended order:** D4 is OFF so nothing auto-applied, so `main` is now one migration ahead of production and `check:drift` will report `migration_history_versions` as a missing function until an approved window runs `supabase db push` (plus the Phase 2 method on staging). Additive and read-only — no runtime risk; only the drift signal is affected. The Supabase preview branch applied the migration cleanly, which proves the chain.
- **Owed, local-only:** the review-ledger record commit `bc8af5cc4` on worktree branch `claude/phase-4-index-restoration-b0f4ea` never pushed (the pre-push guard timed out and the remote branch was deleted on merge). Cherry-pick it onto a fresh branch and bundle it with the post-window forensics update.
- **Remaining:** (1) Phase 5 close-out — after-EXPLAIN set, `#231` re-test on healthy latency, `check:production-readiness` (read-only + one provider gate); (2) the PGRST106 fix; (3) one serialized `npm run issues:reconcile` — 3 pending inbox files (`bbac85a9` #Q5JHBJ done, `cc60253d` PGRST106, `a20fc4ce` docling fixtures); (4) Phase 7 deferred debt.
- D4 = OFF (auto-deploy disabled, empirically confirmed). Every migration needs its own approved window + explicit `db push`. Revisit at Phase 5 close only with written rules.
- **D5 pending (owner): PITR is OFF on production** (`pitr_enabled false`, daily physical backups, ~24h RPO). No DATA-mutating window (#022, #036, #191, #057, reindex/backfill) until decided.
- Open PRs 2026-08-20 (#2195, #2194, #2193, #2189, #2188, #2180, #2012, #2011, #2010): **none touches `supabase/**`, check-drift, or search-health** — no collision risk per `#292`.
- Traps: `supabase db query` parses a leading `--` as a flag (use `--file`); Node can't execFile the supabase npm shim on Windows; `db push` needs `--include-all` when versions sort before the remote tip; main checkout `D:\Repos\Database` is linked to STAGING — link a dedicated worktree for production and unlink after; `issues:update` id is POSITIONAL.

**Why:** the "drift" was three mechanisms — mirror gap (RPCs), out-of-band index drops, chain-vs-mirror objects — plus routing that told nobody. All are now closed; what is left is measurement and one broken check step.
**How to apply:** next = fix the PGRST106 alignment step (that alone turns the weekly job green and lets #1963 self-close) → Phase 5 → reconcile → close `#316` → decide D4 revisit + D5. Related: [[rag-programme-coordination-state]].
