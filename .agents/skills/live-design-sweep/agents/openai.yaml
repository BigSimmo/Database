interface:
  display_name: "UI Alias"
  short_description: "Compatibility alias for Database UI verification"
  default_prompt: "Use $live-design-sweep only as a compatibility alias for $ui."
policy:
  allow_implicit_invocation: false
