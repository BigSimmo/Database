interface:
  display_name: "Workflows Alias"
  short_description: "Compatibility alias for the Database skills catalog"
  default_prompt: "Use $workflows only as a compatibility alias for $skills."
policy:
  allow_implicit_invocation: false
