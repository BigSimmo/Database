# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/output/
/test-results/
/playwright-report/
/playwright-cli/
/.playwright-cli/
/playwright/.auth/
/.local/
# Track only the secret-free project MCP template. Host-local Codex files under
# .codex/ stay ignored. Never put OAuth tokens, bearer headers, or env secrets in
# config.toml — credentials belong in the host credential store. CI Gitleaks and
# check:codex-cloud reject secret-bearing keys in the tracked template.
/.codex/*
!/.codex/config.toml
/.agents/*
!/.agents/plugins/
/.agents/plugins/**
!/.agents/plugins/marketplace.json
!/.agents/plugins/api_marketplace.json
!/.agents/skills/
/.agents/skills/*
!/.agents/skills/access/
!/.agents/skills/api/
!/.agents/skills/audit/
!/.agents/skills/catalog.json
!/.agents/skills/clinical-change-proof/
!/.agents/skills/clinical/
!/.agents/skills/data/
!/.agents/skills/database-flightplan/
!/.agents/skills/database-skills.md
!/.agents/skills/dependencies/
!/.agents/skills/deploy/
!/.agents/skills/documents/
!/.agents/skills/drift/
!/.agents/skills/export/
!/.agents/skills/fix/
!/.agents/skills/handover/
!/.agents/skills/health/
!/.agents/skills/incident/
!/.agents/skills/ingest/
!/.agents/skills/ledger/
!/.agents/skills/live-design-sweep/
!/.agents/skills/migrate/
!/.agents/skills/operations/
!/.agents/skills/operator-closeout/
!/.agents/skills/performance/
!/.agents/skills/plan/
!/.agents/skills/privacy/
!/.agents/skills/prompt-perfector/
!/.agents/skills/rag-change-lab/
!/.agents/skills/rag/
!/.agents/skills/rag-cloud-sdd/
!/.agents/skills/recovery/
!/.agents/skills/reindex/
!/.agents/skills/release/
!/.agents/skills/review/
!/.agents/skills/run/
!/.agents/skills/security/
!/.agents/skills/session-lifecycle/
!/.agents/skills/skills/
!/.agents/skills/sources/
!/.agents/skills/task/
!/.agents/skills/test/
!/.agents/skills/ui/
!/.agents/skills/verify-triage-fix/
!/.agents/skills/workflows/
/skills-lock.json

# next.js
/.next/
/.next-playwright/
/out/

# Lighthouse budget reports (scripts/run-lighthouse-budget.mjs). Regenerated per run;
# the committed artefact is the baseline inside lighthouse-budget.json, not the JSON
# reports. Visual baselines under tests/__screenshots__/ ARE tracked deliberately.
/lighthouse/

# production
/build

# misc
.DS_Store
*.pem
*.bak

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
dev-server*.log
worker-*.log
# Codex Cloud CLI diagnostic; may contain account/session metadata.
/error.log
tmp-*.py
test-output.txt
# node compile cache directory when NODE_COMPILE_CACHE=1 is set
/1/

# env files (can opt-in for committing if needed)
.env*
!.env.example

# generated sample uploads
/sample-documents/
/tmp/

# docling lab run output (generated fixtures, raw measurements, reports)
/eval/docling/out/

# python
__pycache__/
.pytest_cache/

# vercel
.vercel

# supabase local CLI state
supabase/.temp/

# typescript
*.tsbuildinfo
next-env.d.ts

# agent/QA artifacts
.codex-screenshots/
/worktrees/
.worktrees/
.claude/worktrees/
/docs/mockups/
# design/UX review scratch dumps (favourites-review, tools-page-review, etc.) — never commit
artifacts/
# …but Ward Flow preservation copies are the exception: chat-control.mjs writes byte copies of
# other checkouts dirty/untracked work here, and durableDirtyArtifactManifest reads them back with
# pathExistsAtRef, so they MUST be committed. Without this negation the capture command prints
# "PRESERVED FILES: N" while git silently tracks none of them — a preservation step that cannot fail.
!docs/ward-flow/control/evidence/artifacts/
# local hook tool cache — machine-local, never commit
.impeccable/
# Local debugging scratch space — never commit (accidentally landed once via 'Save Codex local changes')
scratch/
.qa-smoke/
# Visual-QA capture scratch (scripts + screenshots) — untracked 2026-07-06 after 24 files landed
.tmp-visual/
*.pid
tmp_output.txt

# design-sync (generated/scratch state)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules/

# esbuild output for the worker image (scripts/build-worker.mjs)
dist/
test-output*.txt
docs/archive/staging-tenancy-evidence-*

# Subagent-driven-development coordination scratch (ledger, briefs, review packages)
.superpowers/

# Node compile cache written to a literal directory name.
# NODE_COMPILE_CACHE takes a directory PATH, not a boolean. A machine with it
# set to a bare value like `1` makes every Node tool - eslint, prettier, tsc,
# jiti - write `<value>/v<node>-<arch>-<hash>/` into the current working
# directory. Measured 2026-08-27: ~2,835 files / 20 MB per worktree, in a dozen
# of them. Ignoring it matters beyond tidiness: untracked, it inflates
# scripts/ci-change-scope.mjs with thousands of phantom changed files and forces
# every scope classification to the heaviest route.
# The real fix is to point the variable at a real path outside every repo. This
# rule is defence in depth for machines where that has not been done.
1/


# Scratch diagnostics written while investigating, never part of the suite. One is awaiting the
# owner approval the protected-work hook requires before it can be removed.
tests/zz-tmp-*.test.ts

# Ward Flow chat identity markers — a per-worktree CLAIM about who is working here, written at
# session start and rewritten after a restart. Never committed: it is local, it goes stale the
# moment a chat ends, and a committed one would freeze a dead role into the repository.
.ward-session.json
